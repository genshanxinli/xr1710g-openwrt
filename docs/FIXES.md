# 修复台账（FIXES）——"修复而不是降级"的执行记录

> 每遇问题：记一条。列：问题 → 根因 → 修复（本层补丁/上游） → 上游状态。上游合入后删除本层补丁并标「已上游」。
> 状态：`carried`（本层携带） / `upstream-open`（上游有 PR 待合） / `merged`（上游已合，删除补丁） / `n/a`（不打算上游）。

| # | 项目 | 问题/背景 | 根因 | 本层载体 | 上游状态 | 备注 |
|---|---|---|---|---|---|---|
| F01 | XR1710G 板级支持 | master 无 xr1710g 文件 | #22397 未合入 | **已对 master 重建**（2026-08-17）：`patches/root/9000-xr1710g-common.patch` + `9001-xr1710g-dts.patch` + `9002-xr1710g-uboot.patch`。与 PR 的偏离：① 不做 common-dtsi 重构（w1700k dts 保持上游原样，冲突面最小）；② 网口命名按自用决策（WAN=1G-1，PR 默认 10G-1=wan）；③ uboot 补丁命名 1000-（master 已有 998/999）；④ 未采纳 PR 的 airoha_fan 改动（我们的 `files/etc/init.d/fan` 动态探测覆盖 xr1710g，避免双脚本抢 PWM） | upstream-open（#22397，2026-08-14 最后活动） | 合入即删；PR 更新后 diff 对照，重取源见 fetch-sources.sh --22397 |
| F02 | 6GHz 不可用 | US 默认 regdb 6GHz NO-IR 限制 | regdb 未含设备功率 | `regdb-0510/0520`（world 5GHz 去 NO-IR 已上游自带 `500-world-regd-5GHz.patch`，F20 删重复 0500） | n/a（功率补丁不打算上游） | 530 实验室 SP 默认停用（#DISABLED） |
| F03 | NPU 内存/卸载 | 只在未装 Wi-Fi 板保留 NPU 内存 | 上游 #24593 | 无需携带（master 已合 2026-08-11） | merged | 跟踪：若回移分支需重拾 |
| F04 | 10G PHY 启动/link | 非 Realtek SoC 上 rtl8261 劣化、boot 失败 | #21777/#22564/#23078/#23383 系列 | 依赖：#22397 板级（含 phylink 配置）；kmod-phy-realtek + rtl826x-firmware | 部分 merged；PHY LED #24034 仍 open | 见 F06 |
| F05 | 风扇温控 | 传感器版本差异（NCT7802/NCT7511Y）；上游 airoha_fan 只覆盖 nct7802 | hwmon 动态探测缺失 | `files/etc/init.d/fan`（动态探测+曲线，覆盖 #22391 修复） | n/a（版本差异不适合上游单一脚本） | 社区实测 NCT7802 为主 |
| F06 | 10G/1G LED 行为 | XR1710G LED 由交换芯片驱动、与 W1700K 相反 | 板级差异 | 1G LED 已实现（9001 dts gsw_phy*_led*，MT7530 驱动）；10G RTL826x LED 待 #24034（实验档候选）；#24619（mt7530 LED 内核侧）跟进 | upstream-open | 2026-08-22 实机：1G 口 LED 默认不亮已加配置（F37），10G 口 LED 仍缺 sysfs/DTS（F37 ②） |
| F07 | cpufreq / PM domain | OC 前置依赖；驱动修复 6.18 下仍不稳 | #22029 未合 | **已自持** `patches/vendor/fanboy/03-cpufreq-pmdomain-22029-e5d23549.patch`（939/940 + **direct-PLL fallback**，fanboy 实机验证过） | upstream-open | **OC 前必须先合**（F08 前置）；上游合入即删 |
| F08 | CPU 超频 | stock 500–1200MHz；部分机器内置 OC 启动 panic（体质差异） | OPP/PLL/governor 需整体 +200MHz（上限 1.4GHz，电压不可调） | `scripts/prepare-oc.sh`（1.3/1.4 两档，不做 1.35/1.5）+ OC 变体**默认限频 1300**（`files/etc/init.d/oc-limit`，可解锁重启回退） | n/a（硬件体质差异 + 超频非上游议题） | 双 release：stock 默认 + oc 变体 |
| F09 | U-Boot pstore | 崩溃日志无处持久化 | #22473 未合 | kernel 侧已自持（`vendor/fanboy/10-pstore-ramoops-...`）；uboot 侧待上游 | upstream-open | 可选增强 |
| F10 | DSA 重构 | switch 端口架构迁移（实验档） | #22532 未合 | specs（不默认应用） | upstream-open | 毕业条件见 patches/README |
| F11 | L2 桥接卸载 | nft flow offload L2（实验档） | #22533 未合 | specs | upstream-open | 同上 |
| F12 | NPU/MLO/诊断应用 | 应用包无独立 feed | 散落在各家 fork 内 | **已供给**（2026-08-17）：`vendor/fanboy/19-w1700k-apps-pack`（npu/flowsense/mlo/fancontrol 随树内置，MANIFEST 默认档）+ `packages-xr1710g/` src-link feed（luci-app-airoha-recovery，源 naoki66@dd9ecfeef）+ filemanager=官方 luci feed；独立 feed 保留为备选（feeds.custom.conf 注释） | n/a | 决策五件套全部就绪 |
| F13 | fanboy 资产拆分 | 原料桶 20 commit 是打包历史，混多项改动 | 打包粒度 vs 我们的档位/可追溯要求 | **555 已拆出**（`regdb-0555-...`，#OC 档）；**08 号 regdb 已拆净**（2026-08-17，与独立 0555 内容一致）；**11/13/14/15/16 评审定档**（2026-08-17）：11=HW GRO/LRO 默认→**default**、14=mt76 token/NPU debugfs→**default**、16=wifi-scripts per-radio txpower 对 master 重建为 `root/9010-wifi-scripts-txpower-ucode.patch`→**default**（原 16 仅上下文漂移 hunk #3，手工合入重 diff，从零 apply 实证）、13=**否决**（`PKG_SOURCE_URL` 个人 fork + `PKG_MIRROR_HASH=skip`，违反锁源铁律）、15=**备选**（与默认 mt76/0006-0007 功能重复）；08 余项切片待做（SPI33MHz/banner/dropbear/antenna-memo/snd-off/LED-silence→default 候选，feeds.conf.default 与 w1700k dts LED **否决**——ADR-0001 master 保真/非目标设备）；19 号 fastfetch/netspeedtest 冗余待精简 | n/a | 拆分完成前对应条目不启用（MANIFEST 注释），本行即评审记录 |
| F14 | vermagic 注入 | 自建 kmod 与官方 opkg 兼容 | 构建层机制（fanboy 85005e10 + extract-distfeeds） | **评估完成（2026-08-17）：暂不接入**——① 需随内核版本维护 `files/etc/vermagic.txt`（官方 buildbot 值），成本高；② 本层 kernel 带 11（LRO default）/14（mt76 debugfs）号配置改动，vermagic 与官方不同，注入掩盖真实差异，官方 kmod 装后可能不匹配；③ 自用场景 kmod 需求有限（能力已随镜像编译）。维持 pending | n/a | 若未来需要官方 kmod（如 wireguard/ipsec 等），重新评估并同步 11/14 的 vermagic 影响 |
| F15 | 首次构建验证（2026-08-17 CI 红） | `make defconfig` 后 XR1710G 目标未选中 | **设备 kconfig 符号拼写错误**：真实符号 = `CONFIG_TARGET_<board>_<subtarget>_DEVICE_<device>`（`scripts/metadata.pm` 的 `confstr` 只清洗 target 名；profile id 原样保留 `DEVICE_` 前缀与连字符）；seed/CI/build.sh 写成 `CONFIG_TARGET_airoha_an7581_gemtek_xr1710g-ubi=y`（缺 `_DEVICE_` 中缀）→ kconfig 静默忽略未知符号 → 默认选中 **EVB 参考板**（`DEVICE_airoha_an7581-evb`） | 修复三处为 `CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_xr1710g-ubi=y`（seed-config.diff / build.yml grep / build.sh grep）；本地 defconfig 实证：修复后 `CONFIG_TARGET_PROFILE="DEVICE_gemtek_xr1710g-ubi"`；配套 `scripts/audit-config.sh` seed 符号审计（build.sh + CI 接入，杜绝同类静默忽略） | n/a（仓库自持配置） | 教训：追加 .config 的符号必须与 metadata.pm 生成器一致，忘记 `_DEVICE_` 是静默失败 |
| F16 | 本地重复构建不可用（2026-08-17 发现） | 同一 openwrt 树第二次 `build.sh`/`apply-patches.sh` 必挂（`git apply` 一次性语义对已应用补丁直接报错）；oc 构建后换 stock 时 `revert_stock` 对未跟踪生成文件（如 patches-6.18/940）无法还原，OC 公式残留会污染后续构建 | 叠加层模型下树是持久克隆，残留无法自我清理 | ① `build.sh` 叠加前先 `git reset --hard` + `git clean -fdq -- target package`（叠加层文件在树顶层不受影响）实现可重复构建；② `prepare-oc.sh stock` 对未跟踪生成文件改为 `rm`（回到 apply 前状态，下次 apply 重建纯净版）；本地两轮「apply→oc→revert→复位→apply」实证全绿 | n/a | CI 恒用全新克隆不受影响；fork 模型（TREE==ROOT）自动跳过 |
| F17 | seed 审计在 build#2/oc-1.3 抓出两个静默缺失（2026-08-17） | ① `luci-app-airoha-recovery`：`feeds.custom.conf` 的 src-link 相对路径**缺 `../` 前缀**——`scripts/feeds` 用 `ln -s <path> feeds/<name>` 创建原文链接，相对路径从 `feeds/` 目录解析，`packages-xr1710g/…` 实际在 TOPDIR 下 → **死链接 → feed 零索引 → 包从未进 .config**（首次 stock 构建已静默漏装；默认 base feed 的写法就是 `../package` 可对照）；另 src-link 的 path 需直接包含包目录（本仓库包在 `packages-xr1710g/package/` 下，故还需该子目录后缀）；② `kmod-i2c-an7581`：该 kmod **不存在**——an7581 I2C 控制器用 `mediatek,mt7621-i2c`（`CONFIG_I2C_MT7621=y` 内置，9001 的 &i2c0 传感器节点已可工作） | 配置错误 + 陈旧符号 | ① `src-link xr1710g ../packages-xr1710g/package`（本地 feeds update 实证：`Collecting package info: feeds/xr1710g/luci-app-airoha-recovery` + 索引含 `Package:`）；② seed 删除 `kmod-i2c-an7581` 行（能力为内核内置，无损失） | n/a（自持配置） | 首次构建通过≠包都装上——F15 的教训由 audit-config.sh 制度化后才暴露，全量 seed 核对是必须的 |
| F18 | prepare-oc.sh OPP 平移两个 bug（2026-08-17 交接日发现） | ① `shift_hz` 直接把 `opp-hz` 值 `+delta`——`delta` 是 MHz 数（1.4 档 = 200），而 `opp-hz` 是 Hz 值（500000000），应 `+delta*1000000`；旧代码 500MHz→500000200Hz（+200Hz），**OPP 从未真正平移**；② `shift_label` 正则 `\bopp-(\d+)\s*\{` 替换成 `opp-<hz>` 时**丢掉 `{`** → `opp-500000200` 无花括号 → **DTS 语法错误，oc 档 kernel 编译必挂**（15 节点全破坏） | 单位换算错误 + 替换丢失分隔符 | `prepare-oc.sh`：`shift_hz` 改 `hz + delta*1000000`；`shift_label` 改 `f"opp-{hz} {{"`（保留 `{`）。本地实证：1.4 档 15 点 700→1400MHz 步进 50、花括号 16/16、required-opps 引用完好；1.3 档 600→1300；stock 恢复还原 500-1200 | n/a | 教训：树编辑脚本的"冒烟通过"只验证了公式可定位，没验证数值语义与语法完整性——OPP 平移后必须核对首尾频率与花括号配对 |
| F19 | CI 构建假绿——firmware artifact 从未产出（2026-08-17 交接日发现） | ① build.yml `make -j$(nproc) V=s 2>&1 \| tee log` **无 pipefail**——GitHub Actions 默认 bash 不带 pipefail，make 失败码被 tee（成功退出）吞掉 → step 绿 → job 绿；② upload-artifact 对无匹配文件默认 warn 不报错。实际：run 31999578409（"首次构建验证"）、32005186317（build#4 stock）、32005210085（oc-1.3c）**三次全在 openssl 下载失败**（3 mirror 全 502/404，瞬时网络问题）`Error 2`，但全部显示绿、`ci-8` pre-release 0 assets | 构建步骤未硬化退出码传播 | build.yml 构建步骤首行 `set -euo pipefail`；上传固件步骤加 `if-no-files-found: error`（无 itb=静默失败，必须红）；本地 build.sh 已有 `set -euo pipefail` 不受影响 | n/a | 教训：CI 绿≠构建成功——必须用产物存在性（firmware artifact）作为成功判据，HANDOFF 中"首次构建已通过、产物已上传"不实 |
| F20 | regdb 补丁在 wireless-regdb 2026.05.30 上构建挂（2026-08-17，三档全红——F19 修复后第一次真红，暴露真问题） | ① 本层 `regdb-0500-world-5ghz-no-no-ir.patch` 与 OpenWrt 自带 `500-world-regd-5GHz.patch` **内容完全一致**（同一作者 Felix Fietkau 同一 diff）——重复补丁：500 先应用成功（NO-IR 已去），0500 再找旧上下文必挂（Hunk #1 FAILED at 19）；② OC 档 `regdb-0555` 的 hunk 上下文基于**原始** db.txt（6g 行 `(12), NO-OUTDOOR, NO-IR`），而 OC 档 = 默认档全量 + #OC 行，应用顺序 0510→0520 已把 6g 行改成 `(29), NO-OUTDOOR` → 0555 必挂。**dry-run 对拷贝类（packages）补丁只检查目标目录存在性，从不真实应用**——regdb 系列从未被验证过（F18 教训的同类盲区） | 补丁上下文漂移 + 重复供给 + 验证盲区 | ① 删除 `regdb-0500`（能力保留：上游已自带，非降级）；② `regdb-0555` 按「0510/0520 应用后」状态重建（UNII-4 扩展至 5895MHz + 6g 29→30dBm，语义不变，注释行保留）；本地从零实证：500→0510→0520→0555 依次 `patch -p1` 全应用 + `dbparse.py` 语法校验 exit 0 + 语义核对（world 36-48 去 NO-IR / US UNII-1 29 / UNII-4 5730-5895 / 6g 29→30） | n/a（自持补丁维护） | 教训：拷贝类补丁的"验证"必须本地 `patch -p1` 模拟或靠构建真实应用，dry-run 目录检查不等于补丁有效；0530 已于 2026-08-17 重建（500→0510→0520 后状态，fuzz 2 → 0，语义不变）；0510/0520 在新版上应用 offset 42 行（上下文匹配正确，dbparse 佐证）；③ **验证盲区制度化（2026-08-17 本会话）**：新增 `scripts/verify-copy-patches.sh` 并接入 `apply-patches.sh --dry-run`——按构建语义真实应用拷贝类补丁（树内包 Makefile 派生源码 tarball → 解包 → 树内已有补丁+本层补丁同目录 → 构建同款 `patch-kernel.sh` glob 排序 `patch -f -p1` → regdb 跑 dbparse.py），应用失败=红、下载失败=⚠ 不红；scratch 树实证 regdb 4 文件全应用 + dbparse 通过 |
| F21 | uboot 构建挂（2026-08-17，F20 修复后三档重建首次真红：内核+kmod 全过、`package/boot/uboot-airoha` an7581_gemtek_xr1710g prepare 失败） | `patches/root/9002` 生成的 `package/boot/uboot-airoha/patches/1000-airoha-add-gemtek-xr1710g.patch` 在 u-boot-2026.07 上应用失败，三因叠加：① **glob 排序陷阱**——包补丁由 `patch-kernel.sh` 按 shell glob 字节序应用，"1000-" 实际排在 "100-" 与 "101-" **之间**（"1000"<"101"），在 defenvs/（999 创建）之前应用 → 新建文件 hunk 挂；② **内层补丁缺 `+++ b/` 行**——PR 提取时 3 个新建文件 hunk 的 `+++ b/` 全丢（首 hunk 靠 diff --git 头侥幸应用，2/3 号 patch 拿不到文件名必挂）；③ **外层 hunk 行数未同步**——插入修复行后 `@@ -0,0 +1,295 @@` 未改为 298，git apply 按 295 行截断创建文件，尾部 gdm1 块被丢。此前 dry-run 只验证 9002 在树根 git apply（通过），从不验证其生成的包补丁对 u-boot 源码的真实应用（F20 盲区同型第三次） | 命名排序 + 提取损坏 + 生成物残缺 + 验证盲区 | ① 9002 内层补丁重命名 `1000-` → `9990-`（glob 序排在 998/999 之后、未来 1000+- 之前）；② 补回 3 处 `+++ b/` 行；③ 外层 hunk 行数改 298；本地实证：u-boot-2026.07 + master 全量补丁（100→…→999）+ 9990 按 glob 序 `patch -f -p1` 全应用、3 个新文件（defconfig/defenvs/dts）全部创建；④ **制度化**：verify-copy-patches.sh 注册 uboot 派生目标（DERIVED_DESTS，源码取包 Makefile PKG_VERSION + denx 多镜像），dry-run 的 verify 移到 git reset **之前**调用（派生文件依赖 ROOT 应用后的树状态） | upstream-open（#22397） | 教训：① 包补丁命名必须按 glob 字节序校验（"1000"<"101"，"9990" 才在 "999" 后）；② 提取/重建补丁后必须对目标源码真实 `patch -p1`；③ ROOT 补丁生成的包补丁（包裹补丁）同样需要真实应用校验 |
| F22 | mt76 包补丁 0001/0003 应用失败（2026-08-17，F21 的 verify 扩展在下一轮构建前抓出） | `vendor/fanboy/14-mt76-patches-80f79586.patch`（ROOT，F13 定档 default）生成的 `package/kernel/mt76/patches/0001/0003` 基于旧 mt76——0001 以 fuzz 2 应用（有重复函数风险）、0003 的 mac.c hunk 失败（offset -66/-72）。**复查 pin 版 mt76 59676919（2026-07-01）**：0001 的 token/NPU debugfs 计数器与 debugfs.c:908 逐字一致；0003 的功能性改动（`get_all_sta_info(UNI_ALL_STA_TXRX_AIR_TIME)` + `get_per_sta_info(UNI_PER_STA_RSSI)` 于 mac.c:2895-2896、MCU 助手 mt7996_mcu_get_per_sta_info 于 mcu.c:5552）**全部已上游**，剩余 diff 仅删除上游已无调用者的死代码 `mt7996_mac_sta_poll` | 补丁基于旧 mt76 版本，能力已被上游吸收（F13 定档时未对 pin 版本源码验证——F20/F21 盲区同型） | **#DISABLED fanboy 14**（MANIFEST 注释记录）——能力 100% 上游自带，本层补丁冗余且会红，非降级（同 F03/0500 逻辑）；验证路径：verify-copy-patches 的 mt76 dest 现只应用本层 0006/0007 | n/a（自持补丁维护） | 教训：定档评审必须对**当前 pin 的包源码**真实应用验证；上游吸收能力后本层补丁冗余应撤而非带病携带 |
| F23 | 内核 DTS 语法错误（2026-08-17 三档重建第 4 次红：uboot 已过，kernel 编译过，`image-an7581-xr1710g-ubi.dtb` 阶段 `an7581-xr1710g-ubi.dts:330.8-331.1 syntax error`） | `9001-xr1710g-dts.patch` 外层 hunk 声明 `@@ -0,0 +1,330 @@` 而实际内容 **427 行**——git apply 对行数不匹配**静默截断**创建文件（丢尾部 97 行含 `&gdm4` 块），文件止于 `&gdm4 {` 致 DTC 语法错误。**与 F21③ 同型**（9002 丢 gdm1 块），且 dry-run 的 git apply 与 verify（只查包补丁）都放行 | 包裹补丁 hunk 行数未同步内容（重建时内容增删后未更新 @@ 头） | 9001 的 `@@` 头改 330→427；本地实证：DTS 427 行完整 + 花括号配平 ✓ + 关键节点（&gdm4/&eth_pcs/&pcie0 等）齐全；**制度化**：新增 `scripts/audit-patches.sh` 逐 hunk 核对声明行数（不一致即红，git apply 静默截断类）接入 apply-patches.sh 应用前（dry+真实模式都跑）；全仓库审计：仅 9001 一处，已修 | n/a（自持补丁维护） | 教训：**包裹补丁的 @@ 行数必须与实际内容一致——git apply 对不匹配静默截断不报错，只能靠审计**；重建补丁后除真实应用外还须核对行数（audit-patches.sh 已制度化） |
| F24 | release 空壳（2026-08-17：ci-26 首次真绿后 release job "success" 但 0 assets——F19 修复后的第一个真 release 竟仍空壳） | release job 步骤顺序错误：`download-artifact`（path: _rel）在 `actions/checkout` **之前**——checkout 默认 `clean: true` 的 `git clean -ffdx` 把下载的 `_rel/`（未跟踪）清掉 → softprops 报 "Pattern '_rel/**' does not match any files"（日志：下载 3 个 artifact 成功 → checkout → 上传 0 文件）。另：`merge-multiple: true` 会把三档同名 itb（openwrt-...-sysupgrade.itb）互相覆盖只剩一套 | 步骤顺序 + merge 覆盖 | ① release job 步骤重排：checkout 先行、download 在后；② 去掉 merge-multiple（保留 firmware-<profile>/ 子目录，三档同名 itb 全保留）；③ ci-26 真产物已手动补传（sysupgrade/initramfs/chainload-uboot + 3 manifest） | n/a（自持 CI） | 教训：download-artifact 后不得再跑默认 clean 的 checkout；同名产物跨档合并必须保留档位目录 |
| F25 | 实验档不可构建 + EXP 补丁零校验（2026-08-18 本会话） | `apply-patches.sh` 有 `--experimental` 开关但 **build.sh/CI 从未接入**——8 条 `#EXP` 补丁（fanboy 02/04-09/17/18）入库但从未被构建/校验过（F20/F21/F23 盲区同型）；且 audit-patches.sh 与 verify-copy-patches.sh 均**无条件跳过 #EXP 行**——实验档拷贝类补丁即使 dry-run 也得不到真实应用校验。另：apply-patches.sh 的 dry-run 在 verify 失败时被 `set -e` 中止、跳过 `git reset --hard`——本地二次 dry-run 残留已应用状态（git apply 一次性语义）致误报冲突。**构建实证暴露两处**：① 首个 experimental 构建（32166293045）在 target/linux 阶段红——EXP-18 生成的 `patches-6.18/992-21`（NPU mailbox 超时 1000→500ms）Hunk #1 在 linux-6.18.44 上失败：**上游内核已是 100 * MSEC_PER_SEC**（意图超额满足）→ 删该 hunk 重建（83→74 行，重试包装保留）；② 二次构建（32171565649）mt76 编译红——**9993 用的 `ieee80211_link_sta_cap_bw/cap_nss` 是 integration 树 mac80211 包 subsys 补丁（411）新增的导出**，OpenWrt mac80211 backport（6.18.39）与内核 6.18 均无 → 实验档补上该依赖（`patches/packages/mac80211-411-…`，拷贝至 `package/kernel/mac80211/patches/subsys/`） | 档位只存在于解析逻辑、未接入构建链路；审计/校验不感知实验档；**ROOT 生成的内核补丁（patches-6.18）不在 verify 覆盖内**——dry-run 只验 ROOT 能创建文件，不验生成的内核补丁能在真实内核源码上应用；**编译期 API 依赖（如 9993←411）在 patch-apply 层不可见**——只能靠真实构建暴露（构建兜底） | ① **实验档可构建化**：build.sh 加 `experimental` 档（`--experimental` 应用，OC 步骤跳过）；build.yml 加 profile 校验（stock/oc-1.3/oc-1.4/experimental/all）与档位判定（OC/EXP 标志分离），'all' 仍聚合三 release 档；release 备注泛化（firmware-<profile>/）；sync-upstream.yml 2h cron dry-run 加 `--experimental`（实验档同样享受漂移检测）；② **审计/校验感知实验档**：audit-patches.sh 与 verify-copy-patches.sh 加 `--experimental` 开关（#EXP 行纳入），apply-patches.sh 透传；③ **set -e 修复**：dry-run 的 verify 以 `set +e` 包裹，回滚无条件执行、退出码仍传播；④ **8 条 EXP 补丁实测**：对 master（cf198ccb5d）按序 dry-run 全部可应用（05-bridge-offload 依赖 02-eip93 先行——hunk 旧上下文含 02 加入的 `kmod-crypto-hw-eip93` 行，原生态测试失败系顺序假象）；⑤ **mt76 integration 9990-9993 评审**（ROADMAP P3）：9990（EHT 广告）/9991（160MHz BF fallback）/9993（op_mode 传递）**携带入实验档**（对 pin mt76 59676919 原生态可应用、mt76 master b2704cf5 无此改动、位域约定核对通过）；**9992（PS-sync TLV 校验）否决**——mt76 master 2026-08-01 已上游合入同款加固（pin 升级自然获得，携带即 F22 式冗余）；⑥ **992-21 重建**（29778aa）：删冗余超时 hunk（master 内核已 100ms），linux-6.18.44 全序应用实证零失败；⑦ **9993 依赖补齐**（mac80211-411）：backports-6.18.39 + master 24 个 subsys 补丁 + 411 全序应用实证 OK（helper 声明/实现齐备） | n/a（自持工程化） | 教训：① "开关存在 ≠ 链路接入"——档位/能力要验证到构建产物；② 实验资产同样需要持续校验（2h cron 覆盖）；③ 补丁评审必须对**当前 pin 源码**逐 hunk 真实应用（F13/F22 判据复用），且注意补丁间顺序依赖（05←02）与**跨包编译期依赖**（9993←mac80211 411，patch-apply 层不可见，构建兜底）；④ **ROOT 生成的内核补丁也要真实应用校验**——候选：verify-copy-patches 增内核 patches-6.18 dest（2h cron 每次下载内核源码 ~150MB，评估后定） |
| F26 | release 资产同名冲突（2026-08-20：all dispatch 构建全绿但 release job 红） | 三档 itb 文件名相同（openwrt-...-sysupgrade.itb 等），`softprops/action-gh-release` 按 basename 上传并更新 asset metadata——多档同时上传同名文件时，先传的资产被覆盖，后续 update 报 `Not Found` | 打包步骤：release 前把 `_rel/firmware-<profile>/` 分别 `tar -czf firmware-<profile>.tar.gz`，`files` 改为 `_rel/*.tar.gz`；重派 all=32407079282 全绿，ci-37 三个 tar.gz 资产 | n/a（自持 CI） | F24 的“保留子目录”只解决了下载覆盖，没解决 softprops 按 basename 上传的冲突；同名产物应打包成按档位命名的单文件资产 |
| F27 | 审查发现的高危/中危一致性问题（2026-08-21） | ① 9001 内核 DTS 的 `ubi`/`reserved_bmt` 分区大小与 9002 U-Boot DTS 及 FLASHING 不一致（0x1b700000/0x04200000 vs 0x1f700000/0x00200000）；② `prepare-oc.sh stock` 路径双重前缀且会误删未跟踪的 stock 基线补丁；③ dry-run 缺失补丁文件不置红；④ 0530 上下文因 0521 过期；⑤ sync-upstream push paths 漏 audit/verify 脚本；⑥ DEVICE_PACKAGES 默认 wpad-basic 与 seed wpad-openssl 可能冲突；⑦ fan 无重复启动保护；⑧ build.sh 本地无 .itb 产物不红 | ① 9001 对齐 PR/U-Boot/FLASHING；② revert_stock 改为树内相对路径 + 只恢复 tracked 文件；③ dry-run 先判 missing；④ 0530 按 0521 后状态重建；⑤ push paths 补齐；⑥ 9000 去掉 wpad-basic；⑦ fan 加 PID 检查；⑧ build.sh 产物缺失即 exit 1 | n/a（自持维护） | 审查文档未提交，修复见本次提交 |
| F28 | 单 wiphy 多 radio 默认无线配置不可用 + wifi-scripts ucode set_txpower 声明顺序错误（2026-08-21 实机验收暴露） | ① 默认 `files/etc/config/wireless` 仍按三 phy 模型（radio0→phy0 / radio1→phy1 / radio2→phy2）编写；MT7996 实机是**单 wiphy 三 radio**，phy1/phy2 不存在 → 5G/6G 配置无法绑定，只有 2.4G 能起来；② 9010 重建时把 ucode 函数 `set_txpower` 定义在 `setup_phy` 之后，而 `setup_phy` 在无 `radio` 标识的单 radio 设备上会直接调用 `set_txpower` → ucode 运行期 undeclared variable | 单 wiphy 多 radio 模型失配 + 函数声明/调用顺序错误 | ① `files/etc/config/wireless` 三 radio 统一 `phy0` + `radio 0/1/2`（与 wifi-scripts per-radio txpower 补丁的 `config.radio` 语义一致）；② 9010 中 `set_txpower` 前移至 `setup_phy` 之前；本地 audit-patches 通过，实机三频全部 up 且 txpower 正确（2.4G/5G 30dBm、6G 29dBm） | n/a（自持配置/补丁） | 教训：多 radio 单 wiphy 设备必须用 `phy + radio` 模型；补丁重建后除行数/上下文外，还要核对函数声明与调用顺序（ucode 无 hoisting） |

| F29 | FlowSense VLAN/PPPoE Offload 开关报错（2026-08-22 实机验收暴露，experimental 档） | LuCI 页面选择 Enabled 后 RPC 返回 `failed - bridge module may not be loaded`；`/proc/sys/net/bridge/bridge-nf-filter-*` 不存在 | `luci-app-airoha-flowsense` / `luci-app-airoha-npu` 的 RPC 后端仍写 br_netfilter 旧 sysctl；experimental 档的 06 号补丁已改为原生 NFPROTO_BRIDGE flowtable 架构，固件内不再有 br_netfilter，旧 sysctl 路径永远不存在 | 新增 `patches/root/9018-flowsense-offload-new-arch.patch`（MANIFEST/ORDER 置于 9017 后）：两 RPC 后端在 sysctl 存在时保持旧行为；sysctl 缺失时 VLAN/PPPoE offload 状态改由 HW Flow Offload + fw4 flowtable 判定，Enabled 操作确保 `flow_offloading`/`flow_offloading_hw` 开启并 reload fw4，Disabled 返回明确错误（当前固件由 HW Flow Offload 总开关控制）。实机 ubus 验证：get 返回 `{"enabled":1}`、set enabled 返回 ok | n/a（自持补丁） | 教训：实验档内核能力切换后，依赖旧内核接口的应用层补丁必须同步适配；实机验收应覆盖 LuCI 全部可操作项 |

| F30 | NPU offload 深度测试暴露的监控与诊断问题（2026-08-22 实机，experimental 档） | 35 分钟 3 路并发 HTTP 下载（约 3.38GB）实测 NPU offload 稳定，但暴露：① npu-monitor 默认 target `1.1.1.1` 在现网不可达，jitter 采样恒 unreachable；② PPE debugfs `bind`/`entries` 的 `packets=`/`bytes=` 恒为 0，无法按流统计；③ 单流 `.102 -> 183.232.52.102` 仅 `[OFFLOAD]`/UNB，同客户端换 `.100` 或并发后可达 `[HW_OFFLOAD]`+BND；④ 设备缺 `bridge`/`ethtool`/`devmem`，`getFrameEngine` 报错 | ① 包默认 UCI 配置了不可达 target，而 init 脚本的网关 fallback 仅在未配置 target 时生效；② PPE debugfs 统计字段不更新；③ 硬件绑定与目标 IP/流建立时机相关，非功能故障；④ 镜像未包含诊断工具 | ① `ae9a2fd`：`patches/root/9017` 默认 target 改为 `223.5.5.5`；② 测试记录：`docs/acceptance-results/2026-08-22-npu-offload-stress.md`；③④ 留待后续镜像增强/补丁修复 | n/a（自持维护） | 硬件卸载判定应综合 conntrack `[HW_OFFLOAD]` + PPE BND + CPU/中断，勿只看 PPE debugfs 统计；诊断工具缺口建议下轮镜像补入 |
| F31 | wifi down/up 后 AP 拒绝所有 STA 关联（2026-08-22 实机深度测试，experimental 档） | 执行 `wifi down` → `wifi up` 后三频 AP-ENABLED，但 hostapd 持续报 `Could not set STA to kernel driver` / `Could not add STA to kernel driver` / `handle_assoc_cb: STA ... not found`；2.4G/5G 客户端均无法重新关联；随后 `wifi reload` 也无效 | 待定位（mac80211/mt76/hostapd 在 full down/up 后的 STA 状态不同步；`wifi reload` 走 hostapd 重配，`wifi down/up` 走接口销毁重建，二者路径不同） | 未修复；**临时恢复=重启设备**。测试记录：`docs/acceptance-results/2026-08-22-wifi-deep-test.md`；下一步：复现时抓取 `iw dev`/`ps w | grep hostapd`/`/sys/kernel/debug/ieee80211/phy0/...` 与 mt76 日志定位 | n/a（待定位） | 教训：`wifi down/up` 不等于 `wifi reload`；对单 wiphy 多 radio 平台，销毁重建路径存在 STA 表残留风险，自动化测试应覆盖 down/up 路径并具备重启自愈判定 |
| F32 | 2.4G 配置 HE40 实际常落 20MHz 且 iwinfo 误报（2026-08-22 实机深度测试） | `files/etc/config/wireless` radio0 为 `htmode HE40` + 无 channel（设备侧落为 `auto`）；实测 auto 及固定 ch6 时均因 `20/40 MHz operation not permitted ... based on overlapping BSSes` 回退 20MHz（hostapd log）；仅 `channel=6` + `noscan=1` 时实际 40MHz。同时 `iwinfo` 仍显示 HT Mode: HE40，与 `iw dev` 的 20MHz 不一致 | ACS/HT40 coexistence 正常回退；iwinfo 展示的 HT Mode 未反映 nl80211 实际宽度 | 未改配置；测试记录：`docs/acceptance-results/2026-08-22-wifi-deep-test.md`。方案二选一：① 接受回退并把 radio0 意图明确为 HE20/auto（避免误读）；② 固定 `channel '6'` + `option noscan '1'` 强制 HE40（牺牲 2.4G 共存礼貌，不建议在密集环境） | n/a（自持配置 + iwinfo 上游行为） | 判读 2.4G 实际带宽以 `iw dev` 为准；`iwinfo` 的 HT Mode 字段在 ACS 回退场景会误报 |
| F33 | 单 wiphy 多 radio 启动期 hostapd 邻区扫描失败（2026-08-22 实机深度测试） | 每次冷启动/`wifi reload` 时 hostapd 对固定信道的 5G/6G 也发起 neighbor BSS scan，单 wiphy 三 radio 并发扫描互相挤占，logread 出现 9 次 `Failed to request a scan of neighboring BSSes ret=-16 (Resource busy)`（daemon.err），最终 AP-ENABLED 但日志有 error 且拖慢启动 | 单 wiphy 多 radio 共享扫描引擎；固定信道无需扫描，但 wifi-scripts 默认 `noscan=0` | `files/etc/config/wireless` 对 radio1/radio2 增加 `option noscan '1'`（本 commit，固定信道且非 DFS）；radio0 保持 auto 扫描；本机已 `wifi reload` 复核：hostapd conf 带 `noscan=1`，无新 ret=-16 | n/a（自持配置） | 固定信道 AP 应设 `noscan=1` 消除启动期无谓扫描与 error 日志；radio0 如需 HE40 确定性再评估固定信道+noscan |

| F34 | `flow_offloading_hw` 1→0→1 切换后设备疑似重启（2026-08-22 实机深度测试，experimental 档） | 软件流表基线测试时先将 `flow_offloading_hw` 置 0 + fw4 reload（正常，`[OFFLOAD]` 生效）；打流完成后恢复 `flow_offloading_hw=1` + fw4 reload，命令返回 `restored` 且 uci 值正确，但约 10–15s 后设备重启（kernel boot 08:27:54）。重启后 pstore 为空、logread 仅保留重启后日志，无 pre-reboot 线索 | 待定位：疑为 NPU flow_block 解绑/重绑路径在 fw4 reload 后的异步释放/再注册触发看门狗或 panic；需串口复现确认 | 未修复；issue #7；测试记录 `docs/acceptance-results/2026-08-22-npu-toggle-rpc-gap.md`。修复前建议 LuCI 在线切换提示"切换后建议重启"或限制 1→0→1 快速连续切换 | n/a（待定位） | 教训：硬件卸载开关的在线切换必须纳入实机验收；`fw4 reload` 成功返回不代表切换无副作用，应继续观察 1–2 分钟 |
| F35 | `getStatus.memory_regions` 依赖 dmesg，logd 消费后恒为空数组（2026-08-22 实机深度测试） | `luci.airoha_npu`/`luci.airoha_flowsense` 的 `get_status` 用 `dmesg \| grep "reserved mem.*npu" \| head -4` 取保留内存；OpenWrt logd 消费内核环缓冲后 `dmesg` 输出为空（实测 `dmesg \| wc -l` = 0），于是 `memory_regions` 恒为 `[]`。开机初期尚可显示 4 条（另漏第 5 条 npu-ba，见 issue #3） | 9017 数据源选错：dmesg 环缓冲不可持久，且 `head -4` 漏项 | 未修复；issue #8；方案：改为解析 `/proc/device-tree/reserved-memory/npu-*` 节点的 `reg` 属性（或 memblock debugfs），dmesg 仅作回退且需判空 | n/a（自持补丁） | 教训：LuCI RPC 的"状态"应来自稳定 sysfs/procfs/devicetree，不应依赖易失的 dmesg 环缓冲 |
| F36 | `getWanHealth`/`getBridgeStats` 统计恒 0 / available:false（2026-08-22 实机深度测试） | `ubus call luci.airoha_flowsense getWanHealth` 对 `pppoe-wan` 返回 `rx_bytes:0,tx_bytes:0`，但 `/sys/class/net/pppoe-wan/statistics/rx_bytes=860075,tx_bytes=7573199`；`getBridgeStats` 直接返回 `{"available":false}` | 两个 RPC 方法都使用 `ip -s link show <dev>` 解析统计，但 BusyBox `ip` 不支持 `-s` 选项（打印 usage 后退出）→ 解析结果为空 → `getBridgeStats` 误判桥不存在 | 未修复；issue #9；方案：改读 `/sys/class/net/<dev>/statistics/{rx_bytes,tx_bytes,rx_packets,tx_packets,rx_errors,tx_errors,rx_dropped,tx_dropped}` | n/a（自持补丁） | 教训：RPC 后端 shell 解析必须对目标平台 BusyBox 工具集实测；`ip -s` 这类 iproute2 专有选项在 BusyBox 上不可用 |

| F37 | 硬件驱动深度测试：1G 口 LED 默认不亮、10G 口 LED 缺失、AN7581 PHY LED 第二个 netdev 配置写回 EINVAL（2026-08-22 实机，experimental 档） | ① `wan`/`lan3` 的 MT7530/AN7581 PHY LED 在 `/etc/config/system` 无任何 LED 配置，默认 brightness=0、trigger=none——1G 网口灯不亮；② RTL8261BE（lan1/lan2）在 `/sys/class/leds` 无 LED 设备，DTS `ethernet-phy@5/@8` 无 `leds` 子节点；③ 先配置第一个 PHY LED 的 `link/rx/tx=1` 写入正常，再配置第二个 PHY LED 时 sysfs 值生效（读回=1）但 store 返回 `EINVAL`，`/etc/init.d/led start` 退出码=1 且 boot 出现 3 条 `write error: Invalid argument` | ① `files/etc/config/system` 增加 `led_wan_green`/`led_lan3_green`（`trigger=netdev`，`dev=wan/lan3`，`mode='link rx tx'`），设备实测 LED 进入 `offloaded=1` 硬件接管；② 10G LED 跟进上游 #24034，或评估 fanboy 09 号 RTL8261CE LED 支持向 RTL8261BE 移植；③ AN7581 PHY LED sysfs store 路径（`drivers/net/phy/mediatek/mtk-ge-soc.c`，9016 同文件）修正第二个 LED 写入的返回值——值已生效，应返回 0 | upstream-open（#24034 / #24619） | 测试记录：`docs/acceptance-results/2026-08-22-hardware-driver-deep-test.md`；复现脚本：`scripts/device-hw-probe.sh`。① 已修（本 commit）；② issue #13，③ issue #14，均待上游/后续补丁 |
| F38 | `lan1` (10G/USXGMII) 无链路 down/up 时 `rx_errors`/`rx_dropped` 虚增（2026-08-22 实机，experimental 档） | `lan1` 无电缆、NO-CARRIER 时，每次 `ip link set lan1 down && ip link set lan1 up` 使 `rx_errors`/`rx_dropped` 增加 0~3（实测基线 3 → 5 → 8 → 9）；`lan2` 同样操作不增加。`dmesg` 无 error/oops，仅正常 PHY attach/configuring | `airoha_eth` 在 USXGMII in-band link 重新配置期间把 link-state 字/伪帧计为 RX error，或重新 open 时未清零统计 | 未修复；方案：驱动在 admin down 或 configure link 时清零/冻结 RX error 计数，仅把 carrier up 后的真实帧错误计入 `rx_errors` | n/a（待定位/上游，issue #15） | 该计数会触发监控误报（LuCI/9017 的 Physical Bottleneck 告警）；`scripts/device-hw-probe.sh` 默认不执行 down/up，`TOGGLE_10G=1` 可复现 |
| F39 | NPU CLIENTS offload 计数漏报：4 个 Wi-Fi 客户端关联、其中 2 个有 PPE BND + conntrack `[HW_OFFLOAD]`，LuCI 仍显示 `CLIENTS 0 offloaded`（2026-08-22 实机，experimental 档） | `getPpeEntries` 的 `client_bnd`/`band_bnd` 仅用 `/sys/kernel/debug/ppe/bind` 的 `eth=` 字段匹配 station MAC；但路由型 client→WAN 流的 BND 行 `eth=` 携带的是桥/下一跳 MAC（如 `00:58:28:d7:44:5e->dc:ef:80:59:a7:0d`），**不含 station MAC**（实测 .170 的 BND 行 `orig=192.168.123.170:…` 但 eth 无 `1e:f3:6d:77:ce:23`），MAC 匹配恒为 0；同时 BND IP 匹配缺失、conntrack 兜底缺失 | 新增 `patches/root/9019-npu-client-offload-accounting.patch`（MANIFEST/ORDER 置于 9018 后）：`band_bnd` 与 `client_bnd` 增补 BND `orig/new` 中的 station IPv4 匹配，并按客户端返回 `hw_offload`/`sw_offload`（来自 conntrack `[HW_OFFLOAD]`/`[OFFLOAD]`）；`bnd` 取 eth-MAC、BND-IP、conntrack-HW 三路信号最大值。实机验证：修复前 `client_bnd` 4 站全 0、`band_bnd=[0,0,0]`；修复后 `.162 bnd=2/hw=2`、`.170 bnd=8/hw=5`、`band_bnd=[0,8,0]`（UI 从 0 offloaded 变为 2 offloaded）。测试记录：`docs/acceptance-results/2026-08-22-npu-clients-offload-deep-test.md`；复现脚本：`scripts/device-npu-probe.sh`；issue #18 | n/a（自持补丁） | 教训：PPE `eth=` 的语义不是“station MAC”，路由型 Wi-Fi 流必须用 IP 级匹配；多信号源取 max 才能避免硬件卸载假阴性 |
| F40 | IPv6 NPU offload 深度测试：TCP/UDP 可用但 RPC/PPE 呈现存在 IPv6 假阴性（2026-08-22 实机，experimental 档） | ① IPv6 TCP 多源并发（10 流）2s 内达 `[HW_OFFLOAD]`+PPE BND v6，功能正常；② IPv6 UDP 长流 conntrack 达 `[HW_OFFLOAD]`，但 PPE `bind` 无 BND、`entries` 仅 UNB——按 BND 判读 UDP 卸载会假阴性；③ `getPpeEntries` 的 `band_bnd`/`client_bnd` 仍不能计 IPv6 客户端：`@NEIGH` 显式跳过 IPv6，且 `ip neigh` 输出压缩式 IPv6 而 PPE/conntrack 为展开式，未规范化无法匹配；`bnd.ipv6` 已能统计总数但 CLIENTS 视图会漏掉纯 IPv6 客户端；④ ICMPv6 不卸载（符合预期）；⑤ PPE `packets/bytes` 恒 0 在 IPv6 下复现（issue #5） | 未修复代码；测试记录：`docs/acceptance-results/2026-08-22-ipv6-npu-offload-deep-test.md`；复现脚本：`scripts/device-npu-ipv6-probe.sh`；issue #19（IPv6 client_bnd 假阴性）/ #20（UDP UNB 可视化）。方案 P1：扩展 `patches/root/9019`——采集 `ip -6 neigh`，awk 内实现 IPv6 地址规范化（`::` 展开补零）后匹配 BND `orig`/conntrack `src/dst`，`client_bnd` 增 `ip6` 字段；UDP 卸载判读改用 conntrack `[HW_OFFLOAD]` 为主、PPE entries UNB 为辅 | n/a（自持补丁） | 教训：PPE/conntrack 的 IPv6 地址是展开式，`ip neigh` 是压缩式，任何跨源 IP 匹配必须做地址规范化；硬件卸载多数据源判读必须区分 TCP（BND）与 UDP（UNB/conntrack）路径 |

## 未确认/待实机核实清单
- 物理口 ↔ 逻辑名（netdev-name 已固化：lan1/lan2=双 10G、wan=1G-1（gsw_port1）、lan3=1G-2（gsw_port2），见 9001）→ 首次实机 `ip -br link` 核对
- U-Boot flash-slot.bin 的 SHA256 全文 → 以 YYH2913/http-uboot release 页为准（升级时校验）
- 6.18 下 PM domain PLL 公式的树内位置（prepare-oc.sh 找不到即报错并指导定位）
