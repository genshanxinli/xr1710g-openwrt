# 修复台账（FIXES）——"修复而不是降级"的执行记录

> 每遇问题：记一条。列：问题 → 根因 → 修复（本层补丁/上游） → 上游状态。上游合入后删除本层补丁并标「已上游」。
> 状态：`carried`（本层携带） / `upstream-open`（上游有 PR 待合） / `merged`（上游已合，删除补丁） / `n/a`（不打算上游）。

| # | 项目 | 问题/背景 | 根因 | 本层载体 | 上游状态 | 备注 |
|---|---|---|---|---|---|---|
| F01 | XR1710G 板级支持 | master 无 xr1710g 文件 | #22397 未合入 | **已对 master 重建**（2026-08-17）：`patches/root/9000-xr1710g-common.patch` + `9001-xr1710g-dts.patch` + `9002-xr1710g-uboot.patch`。与 PR 的偏离：① 不做 common-dtsi 重构（w1700k dts 保持上游原样，冲突面最小）；② 网口命名按自用决策（WAN=1G-1，PR 默认 10G-1=wan）；③ uboot 补丁命名 1000-（master 已有 998/999）；④ 未采纳 PR 的 airoha_fan 改动（我们的 `files/etc/init.d/fan` 动态探测覆盖 xr1710g，避免双脚本抢 PWM） | upstream-open（#22397，2026-08-14 最后活动） | 合入即删；PR 更新后 diff 对照，重取源见 fetch-sources.sh --22397 |
| F02 | 6GHz 不可用 | US 默认 regdb 6GHz NO-IR 限制 | regdb 未含设备功率 | `regdb-0510/0520`（world 5GHz 去 NO-IR 已上游自带 `500-world-regd-5GHz.patch`，F20 删重复 0500） | n/a（功率补丁不打算上游） | 530 实验室 SP 默认停用（#DISABLED）；验收 C2 需双侧国家码判据——客户端非 US 看不到 6G 属预期（IP14） |
| F03 | NPU 内存/卸载 | 只在未装 Wi-Fi 板保留 NPU 内存 | 上游 #24593 | 无需携带（master 已合 2026-08-11） | merged | 跟踪：若回移分支需重拾 |
| F04 | 10G PHY 启动/link | 非 Realtek SoC 上 rtl8261 劣化、boot 失败 | #21777/#22564/#23078/#23383 系列 | 依赖：#22397 板级（含 phylink 配置）；kmod-phy-realtek + rtl826x-firmware | 部分 merged；PHY LED #24034 仍 open | 见 F06 |
| F05 | 风扇温控 | 传感器版本差异（NCT7802/NCT7511Y）；上游 airoha_fan 只覆盖 nct7802 | hwmon 动态探测缺失 | `files/etc/init.d/fan`（动态探测+曲线，覆盖 #22391 修复） | n/a（版本差异不适合上游单一脚本） | 社区实测 NCT7802 为主 |
| F06 | 10G/1G LED 行为 | XR1710G LED 由交换芯片驱动、与 W1700K 相反 | 板级差异 | 1G LED 已实现（9001 dts gsw_phy*_led*，MT7530 驱动）；10G RTL826x LED 待 #24034（实验档候选）；#24619（mt7530 LED 内核侧）跟进 | upstream-open | 2026-08-22 实机：1G 口 LED 默认不亮已加配置（F37），10G 口 LED 仍缺 sysfs/DTS（F37 ②） |
| F07 | cpufreq / PM domain | OC 前置依赖；驱动修复 6.18 下仍不稳 | #22029 未合 | **已自持** `patches/vendor/fanboy/03-cpufreq-pmdomain-22029-e5d23549.patch`（939/940 + **direct-PLL fallback**，fanboy 实机验证过） | upstream-open | **OC 前必须先合**（F08 前置）；上游合入即删 |
| F08 | CPU 超频 | stock 500–1200MHz；部分机器内置 OC 启动 panic（体质差异） | OPP/PLL/governor 需整体 +200MHz（上限 1.4GHz，电压不可调） | `scripts/prepare-oc.sh`（1.3/1.4 两档，不做 1.35/1.5）+ OC 变体**默认限频 1300**（`files/etc/init.d/oc-limit`，可解锁重启回退） | n/a（硬件体质差异 + 超频非上游议题） | 双 release：stock 默认 + oc 变体 |
| F09 | U-Boot pstore | 崩溃日志无处持久化 | #22473 未合 | kernel 侧已自持（`vendor/fanboy/10-pstore-ramoops-...`）；uboot 侧待上游 | upstream-open | 可选增强 |
| F10 | DSA 重构 | switch 端口架构迁移（实验档） | #22532 未合 | specs（不默认应用） | upstream-open | 毕业条件见 patches/README |
| F11 | L2 桥接卸载 | nft flow offload L2（实验档） | #22533 未合 | specs | upstream-open | 同上 |
| F12 | NPU/MLO/诊断应用 | 应用包无独立 feed | 散落在各家 fork 内 | **已供给**（2026-08-17）：`vendor/fanboy/19-w1700k-apps-pack`（npu/flowsense/mlo/fancontrol 随树内置，MANIFEST 默认档）+ `packages-xr1710g/` src-link feed（luci-app-airoha-recovery，源 naoki66@dd9ecfeef）+ filemanager=官方 luci feed；独立 feed 保留为备选（feeds.custom.conf 注释） | n/a | 决策五件套全部就绪 |
| F13 | fanboy 资产拆分 | 原料桶 20 commit 是打包历史，混多项改动 | 打包粒度 vs 我们的档位/可追溯要求 | **555 已拆出**（`regdb-0555-...`，#OC 档）；**08 号 regdb 已拆净**（2026-08-17，与独立 0555 内容一致）；**11/13/14/15/16 评审定档**（2026-08-17）：11=HW GRO/LRO 默认→**default**、14=mt76 token/NPU debugfs→**default**、16=wifi-scripts per-radio txpower 对 master 重建为 `root/9010-wifi-scripts-txpower-ucode.patch`→**default**（原 16 仅上下文漂移 hunk #3，手工合入重 diff，从零 apply 实证）、13=**否决**（`PKG_SOURCE_URL` 个人 fork + `PKG_MIRROR_HASH=skip`，违反锁源铁律）、15=**备选**（与默认 mt76/0006-0007 功能重复）；08 余项切片待做（SPI33MHz/banner/dropbear/antenna-memo/snd-off/LED-silence→default 候选，feeds.conf.default 与 w1700k dts LED **否决**——ADR-0001 master 保真/非目标设备）；19 号 fastfetch/netspeedtest 冗余待精简 | n/a | 拆分完成前对应条目不启用（MANIFEST 注释），本行即评审记录 |
| F14 | vermagic 注入 | 自建 kmod 与官方 opkg 兼容 | 构建层机制（fanboy 85005e10 + extract-distfeeds） | **评估完成（2026-08-17）：暂不接入**——① 需随内核版本维护 `files/etc/vermagic.txt`（官方 buildbot 值），成本高；② 本层 kernel 带 11（LRO default）/14（mt76 debugfs）号配置改动，vermagic 与官方不同，注入掩盖真实差异，官方 kmod 装后可能不匹配；③ 自用场景 kmod 需求有限（能力已随镜像编译）。维持 pending | n/a | 若未来需要官方 kmod（如 wireguard/ipsec 等），重新评估并同步 11/14 的 vermagic 影响 |
| F15 | 首次构建验证（2026-08-17 CI 红） | `make defconfig` 后 XR1710G 目标未选中 | **设备 kconfig 符号拼写错误**：真实符号 = `CONFIG_TARGET_<board>_<subtarget>_DEVICE_<device>`（`scripts/metadata.pm` 的 `confstr` 只清洗 target 名；profile id 原样保留 `DEVICE_` 前缀与连字符）；seed/CI/build.sh 写成 `CONFIG_TARGET_airoha_an7581_gemtek_xr1710g-ubi=y`（缺 `_DEVICE_` 中缀）→ kconfig 静默忽略未知符号 → 默认选中 **EVB 参考板**（`DEVICE_airoha_an7581-evb`） | 修复三处为 `CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_xr1710g-ubi=y`（seed-config.diff / build.yml grep / build.sh grep）；本地 defconfig 实证：修复后 `CONFIG_TARGET_PROFILE="DEVICE_gemtek_xr1710g-ubi"`；配套 `scripts/audit-config.sh` seed 符号审计（build.sh + CI 接入，杜绝同类静默忽略） | n/a（仓库自持配置） | 教训：追加 .config 的符号必须与 metadata.pm 生成器一致，忘记 `_DEVICE_` 是静默失败 |
| F16 | 本地重复构建不可用（2026-08-17 发现） | 同一 openwrt 树第二次 `build.sh`/`apply-patches.sh` 必挂（`git apply` 一次性语义对已应用补丁直接报错）；oc 构建后换 stock 时 `revert_stock` 对未跟踪生成文件（如 patches-6.18/940）无法还原，OC 公式残留会污染后续构建 | 叠加层模型下树是持久克隆，残留无法自我清理 | ① `build.sh` 叠加前先 `git reset --hard` + `git clean -fdq -- target package`（叠加层文件在树顶层不受影响）实现可重复构建；② `prepare-oc.sh stock` 对未跟踪生成文件改为 `rm`（回到 apply 前状态，下次 apply 重建纯净版）；本地两轮「apply→oc→revert→复位→apply」实证全绿 | n/a | CI 恒用全新克隆不受影响；fork 模型（TREE==ROOT）自动跳过 |
| F17 | seed 审计在 build#2/oc-1.3 抓出两个静默缺失（2026-08-17） | ① `luci-app-airoha-recovery`：`feeds.custom.conf` 的 src-link 相对路径**缺 `../` 前缀**——`scripts/feeds` 用 `ln -s <path> feeds/<name>` 创建原文链接，相对路径从 `feeds/` 目录解析，`packages-xr1710g/…` 实际在 TOPDIR 下 → **死链接 → feed 零索引 → 包从未进 .config**（首次 stock 构建已静默漏装；默认 base feed 的写法就是 `../package` 可对照）；另 src-link 的 path 需直接包含包目录（本仓库包在 `packages-xr1710g/package/` 下，故还需该子目录后缀）；② `kmod-i2c-an7581`：该 kmod **不存在**——an7581 I2C 控制器用 `mediatek,mt7621-i2c`（`CONFIG_I2C_MT7621=y` 内置，9001 的 &i2c0 传感器节点已可工作） | 配置错误 + 陈旧符号 | ① `src-link xr1710g ../packages-xr1710g/package`（本地 feeds update 实证：`Collecting package info: feeds/xr1710g/luci-app-airoha-recovery` + 索引含 `Package:`）；② seed 删除 `kmod-i2c-an7581` 行（能力为内核内置，无损失） | n/a（自持配置） | 首次构建通过≠包都装上——F15 的教训由 audit-config.sh 制度化后才暴露，全量 seed 核对是必须的 |
| F18 | prepare-oc.sh OPP 平移两个 bug（2026-08-17 交接日发现） | ① `shift_hz` 直接把 `opp-hz` 值 `+delta`——`delta` 是 MHz 数（1.4 档 = 200），而 `opp-hz` 是 Hz 值（500000000），应 `+delta*1000000`；旧代码 500MHz→500000200Hz（+200Hz），**OPP 从未真正平移**；② `shift_label` 正则 `\bopp-(\d+)\s*\{` 替换成 `opp-<hz>` 时**丢掉 `{`** → `opp-500000200` 无花括号 → **DTS 语法错误，oc 档 kernel 编译必挂**（15 节点全破坏） | 单位换算错误 + 替换丢失分隔符 | `prepare-oc.sh`：`shift_hz` 改 `hz + delta*1000000`；`shift_label` 改 `f"opp-{hz} {{"`（保留 `{`）。本地实证：1.4 档 15 点 700→1400MHz 步进 50、花括号 16/16、required-opps 引用完好；1.3 档 600→1300；stock 恢复还原 500-1200 | n/a | 教训：树编辑脚本的"冒烟通过"只验证了公式可定位，没验证数值语义与语法完整性——OPP 平移后必须核对首尾频率与花括号配对 |
| F19 | CI 构建假绿——firmware artifact 从未产出（2026-08-17 交接日发现） | ① build.yml `make -j$(nproc) V=s 2>&1 \| tee log` **无 pipefail**——GitHub Actions 默认 bash 不带 pipefail，make 失败码被 tee（成功退出）吞掉 → step 绿 → job 绿；② upload-artifact 对无匹配文件默认 warn 不报错。实际：run 31999578409（"首次构建验证"）、32005186317（build#4 stock）、32005210085（oc-1.3c）**三次全在 openssl 下载失败**（3 mirror 全 502/404，瞬时网络问题）`Error 2`，但全部显示绿、`ci-8` pre-release 0 assets | 构建步骤未硬化退出码传播 | build.yml 构建步骤首行 `set -euo pipefail`；上传固件步骤加 `if-no-files-found: error`（无 itb=静默失败，必须红）；本地 build.sh 已有 `set -euo pipefail` 不受影响 | n/a | 教训：CI 绿≠构建成功——必须用产物存在性（firmware artifact）作为成功判据，HANDOFF 中"首次构建已通过、产物已上传"不实 |
| F20 | regdb 补丁在 wireless-regdb 2026.05.30 上构建挂（2026-08-17，三档全红——F19 修复后第一次真红，暴露真问题） | ① 本层 `regdb-0500-world-5ghz-no-no-ir.patch` 与 OpenWrt 自带 `500-world-regd-5GHz.patch` **内容完全一致**（同一作者 Felix Fietkau 同一 diff）——重复补丁：500 先应用成功（NO-IR 已去），0500 再找旧上下文必挂（Hunk #1 FAILED at 19）；② OC 档 `regdb-0555` 的 hunk 上下文基于**原始** db.txt（6g 行 `(12), NO-OUTDOOR, NO-IR`），而 OC 档 = 默认档全量 + #OC 行，应用顺序 0510→0520 已把 6g 行改成 `(29), NO-OUTDOOR` → 0555 必挂。**dry-run 对拷贝类（packages）补丁只检查目标目录存在性，从不真实应用**——regdb 系列从未被验证过（F18 教训的同类盲区） | 补丁上下文漂移 + 重复供给 + 验证盲区 | ① 删除 `regdb-0500`（能力保留：上游已自带，非降级）；② `regdb-0555` 按「0510/0520 应用后」状态重建（UNII-4 扩展至 5895MHz + 6g 29→30dBm，语义不变，注释行保留）；本地从零实证：500→0510→0520→0555 依次 `patch -p1` 全应用 + `dbparse.py` 语法校验 exit 0 + 语义核对（world 36-48 去 NO-IR / US UNII-1 29 / UNII-4 5730-5895 / 6g 29→30） | n/a（自持补丁维护） | 教训：拷贝类补丁的"验证"必须本地 `patch -p1` 模拟或靠构建真实应用，dry-run 目录检查不等于补丁有效；0530 已于 2026-08-17 重建（500→0510→0520 后状态，fuzz 2 → 0，语义不变）；0510/0520 在新版上应用 offset 42 行（上下文匹配正确，dbparse 佐证）；③ **验证盲区制度化（2026-08-17 本会话）**：新增 `scripts/verify-copy-patches.sh` 并接入 `apply-patches.sh --dry-run`——按构建语义真实应用拷贝类补丁（树内包 Makefile 派生源码 tarball → 解包 → 树内已有补丁+本层补丁同目录 → 构建同款 `patch-kernel.sh` glob 排序 `patch -f -p1` → regdb 跑 dbparse.py），应用失败=红、下载失败=⚠ 不红；scratch 树实证 regdb 4 文件全应用 + dbparse 通过 |
| F21 | uboot 构建挂（2026-08-17，F20 修复后三档重建首次真红：内核+kmod 全过、`package/boot/uboot-airoha` an7581_gemtek_xr1710g prepare 失败） | `patches/root/9002` 生成的 `package/boot/uboot-airoha/patches/1000-airoha-add-gemtek-xr1710g.patch` 在 u-boot-2026.07 上应用失败，三因叠加：① **glob 排序陷阱**——包补丁由 `patch-kernel.sh` 按 shell glob 字节序应用，"1000-" 实际排在 "100-" 与 "101-" **之间**（"1000"<"101"），在 defenvs/（999 创建）之前应用 → 新建文件 hunk 挂；② **内层补丁缺 `+++ b/` 行**——PR 提取时 3 个新建文件 hunk 的 `+++ b/` 全丢（首 hunk 靠 diff --git 头侥幸应用，2/3 号 patch 拿不到文件名必挂）；③ **外层 hunk 行数未同步**——插入修复行后 `@@ -0,0 +1,295 @@` 未改为 298，git apply 按 295 行截断创建文件，尾部 gdm1 块被丢。此前 dry-run 只验证 9002 在树根 git apply（通过），从不验证其生成的包补丁对 u-boot 源码的真实应用（F20 盲区同型第三次） | 命名排序 + 提取损坏 + 生成物残缺 + 验证盲区 | ① 9002 内层补丁重命名 `1000-` → `9990-`（glob 序排在 998/999 之后、未来 1000+- 之前）；② 补回 3 处 `+++ b/` 行；③ 外层 hunk 行数改 298；本地实证：u-boot-2026.07 + master 全量补丁（100→…→999）+ 9990 按 glob 序 `patch -f -p1` 全应用、3 个新文件（defconfig/defenvs/dts）全部创建；④ **制度化**：verify-copy-patches.sh 注册 uboot 派生目标（DERIVED_DESTS，源码取包 Makefile PKG_VERSION + denx 多镜像），dry-run 的 verify 移到 git reset **之前**调用（派生文件依赖 ROOT 应用后的树状态） | upstream-open（#22397） | 教训：① 包补丁命名必须按 glob 字节序校验（"1000"<"101"，"9990" 才在 "999" 后）；② 提取/重建补丁后必须对目标源码真实 `patch -p1`；③ ROOT 补丁生成的包补丁（包裹补丁）同样需要真实应用校验 |
| F22 | mt76 包补丁 0001/0003 应用失败（2026-08-17，F21 的 verify 扩展在下一轮构建前抓出） | `vendor/fanboy/14-mt76-patches-80f79586.patch`（ROOT，F13 定档 default）生成的 `package/kernel/mt76/patches/0001/0003` 基于旧 mt76——0001 以 fuzz 2 应用（有重复函数风险）、0003 的 mac.c hunk 失败（offset -66/-72）。**复查 pin 版 mt76 59676919（2026-07-01）**：0001 的 token/NPU debugfs 计数器与 debugfs.c:908 逐字一致；0003 的功能性改动（`get_all_sta_info(UNI_ALL_STA_TXRX_AIR_TIME)` + `get_per_sta_info(UNI_PER_STA_RSSI)` 于 mac.c:2895-2896、MCU 助手 mt7996_mcu_get_per_sta_info 于 mcu.c:5552）**全部已上游**，剩余 diff 仅删除上游已无调用者的死代码 `mt7996_mac_sta_poll` | 补丁基于旧 mt76 版本，能力已被上游吸收（F13 定档时未对 pin 版本源码验证——F20/F21 盲区同型） | **#DISABLED fanboy 14**（MANIFEST 注释记录）——能力 100% 上游自带，本层补丁冗余且会红，非降级（同 F03/0500 逻辑）；验证路径：verify-copy-patches 的 mt76 dest 现只应用本层 0006/0007 | n/a（自持补丁维护） | 教训：定档评审必须对**当前 pin 的包源码**真实应用验证；上游吸收能力后本层补丁冗余应撤而非带病携带 |
| F23 | 内核 DTS 语法错误（2026-08-17 三档重建第 4 次红：uboot 已过，kernel 编译过，`image-an7581-xr1710g-ubi.dtb` 阶段 `an7581-xr1710g-ubi.dts:330.8-331.1 syntax error`） | `9001-xr1710g-dts.patch` 外层 hunk 声明 `@@ -0,0 +1,330 @@` 而实际内容 **427 行**——git apply 对行数不匹配**静默截断**创建文件（丢尾部 97 行含 `&gdm4` 块），文件止于 `&gdm4 {` 致 DTC 语法错误。**与 F21③ 同型**（9002 丢 gdm1 块），且 dry-run 的 git apply 与 verify（只查包补丁）都放行 | 包裹补丁 hunk 行数未同步内容（重建时内容增删后未更新 @@ 头） | 9001 的 `@@` 头改 330→427；本地实证：DTS 427 行完整 + 花括号配平 ✓ + 关键节点（&gdm4/&eth_pcs/&pcie0 等）齐全；**制度化**：新增 `scripts/audit-patches.sh` 逐 hunk 核对声明行数（不一致即红，git apply 静默截断类）接入 apply-patches.sh 应用前（dry+真实模式都跑）；全仓库审计：仅 9001 一处，已修 | n/a（自持补丁维护） | 教训：**包裹补丁的 @@ 行数必须与实际内容一致——git apply 对不匹配静默截断不报错，只能靠审计**；重建补丁后除真实应用外还须核对行数（audit-patches.sh 已制度化） |
| F24 | release 空壳（2026-08-17：ci-26 首次真绿后 release job "success" 但 0 assets——F19 修复后的第一个真 release 竟仍空壳） | release job 步骤顺序错误：`download-artifact`（path: _rel）在 `actions/checkout` **之前**——checkout 默认 `clean: true` 的 `git clean -ffdx` 把下载的 `_rel/`（未跟踪）清掉 → softprops 报 "Pattern '_rel/**' does not match any files"（日志：下载 3 个 artifact 成功 → checkout → 上传 0 文件）。另：`merge-multiple: true` 会把三档同名 itb（openwrt-...-sysupgrade.itb）互相覆盖只剩一套 | 步骤顺序 + merge 覆盖 | ① release job 步骤重排：checkout 先行、download 在后；② 去掉 merge-multiple（保留 firmware-<profile>/ 子目录，三档同名 itb 全保留）；③ ci-26 真产物已手动补传（sysupgrade/initramfs/chainload-uboot + 3 manifest） | n/a（自持 CI） | 教训：download-artifact 后不得再跑默认 clean 的 checkout；同名产物跨档合并必须保留档位目录 |
| F25 | 实验档不可构建 + EXP 补丁零校验（2026-08-18 本会话） | `apply-patches.sh` 有 `--experimental` 开关但 **build.sh/CI 从未接入**——8 条 `#EXP` 补丁（fanboy 02/04-09/17/18）入库但从未被构建/校验过（F20/F21/F23 盲区同型）；且 audit-patches.sh 与 verify-copy-patches.sh 均**无条件跳过 #EXP 行**——实验档拷贝类补丁即使 dry-run 也得不到真实应用校验。另：apply-patches.sh 的 dry-run 在 verify 失败时被 `set -e` 中止、跳过 `git reset --hard`——本地二次 dry-run 残留已应用状态（git apply 一次性语义）致误报冲突。**构建实证暴露两处**：① 首个 experimental 构建（32166293045）在 target/linux 阶段红——EXP-18 生成的 `patches-6.18/992-21`（NPU mailbox 超时 1000→500ms）Hunk #1 在 linux-6.18.44 上失败：**上游内核已是 100 * MSEC_PER_SEC**（意图超额满足）→ 删该 hunk 重建（83→74 行，重试包装保留）；② 二次构建（32171565649）mt76 编译红——**9993 用的 `ieee80211_link_sta_cap_bw/cap_nss` 是 integration 树 mac80211 包 subsys 补丁（411）新增的导出**，OpenWrt mac80211 backport（6.18.39）与内核 6.18 均无 → 实验档补上该依赖（`patches/packages/mac80211-411-…`，拷贝至 `package/kernel/mac80211/patches/subsys/`） | 档位只存在于解析逻辑、未接入构建链路；审计/校验不感知实验档；**ROOT 生成的内核补丁（patches-6.18）不在 verify 覆盖内**——dry-run 只验 ROOT 能创建文件，不验生成的内核补丁能在真实内核源码上应用；**编译期 API 依赖（如 9993←411）在 patch-apply 层不可见**——只能靠真实构建暴露（构建兜底） | ① **实验档可构建化**：build.sh 加 `experimental` 档（`--experimental` 应用，OC 步骤跳过）；build.yml 加 profile 校验（stock/oc-1.3/oc-1.4/experimental/all）与档位判定（OC/EXP 标志分离），'all' 仍聚合三 release 档；release 备注泛化（firmware-<profile>/）；sync-upstream.yml 2h cron dry-run 加 `--experimental`（实验档同样享受漂移检测）；② **审计/校验感知实验档**：audit-patches.sh 与 verify-copy-patches.sh 加 `--experimental` 开关（#EXP 行纳入），apply-patches.sh 透传；③ **set -e 修复**：dry-run 的 verify 以 `set +e` 包裹，回滚无条件执行、退出码仍传播；④ **8 条 EXP 补丁实测**：对 master（cf198ccb5d）按序 dry-run 全部可应用（05-bridge-offload 依赖 02-eip93 先行——hunk 旧上下文含 02 加入的 `kmod-crypto-hw-eip93` 行，原生态测试失败系顺序假象）；⑤ **mt76 integration 9990-9993 评审**（ROADMAP P3）：9990（EHT 广告）/9991（160MHz BF fallback）/9993（op_mode 传递）**携带入实验档**（对 pin mt76 59676919 原生态可应用、mt76 master b2704cf5 无此改动、位域约定核对通过）；**9992（PS-sync TLV 校验）否决**——mt76 master 2026-08-01 已上游合入同款加固（pin 升级自然获得，携带即 F22 式冗余）；⑥ **992-21 重建**（29778aa）：删冗余超时 hunk（master 内核已 100ms），linux-6.18.44 全序应用实证零失败；⑦ **9993 依赖补齐**（mac80211-411）：backports-6.18.39 + master 24 个 subsys 补丁 + 411 全序应用实证 OK（helper 声明/实现齐备） | n/a（自持工程化） | 教训：① "开关存在 ≠ 链路接入"——档位/能力要验证到构建产物；② 实验资产同样需要持续校验（2h cron 覆盖）；③ 补丁评审必须对**当前 pin 源码**逐 hunk 真实应用（F13/F22 判据复用），且注意补丁间顺序依赖（05←02）与**跨包编译期依赖**（9993←mac80211 411，patch-apply 层不可见，构建兜底）；④ **ROOT 生成的内核补丁也要真实应用校验**——候选：verify-copy-patches 增内核 patches-6.18 dest（2h cron 每次下载内核源码 ~150MB，评估后定） |
| F26 | release 资产同名冲突（2026-08-20：all dispatch 构建全绿但 release job 红） | 三档 itb 文件名相同（openwrt-...-sysupgrade.itb 等），`softprops/action-gh-release` 按 basename 上传并更新 asset metadata——多档同时上传同名文件时，先传的资产被覆盖，后续 update 报 `Not Found` | 打包步骤：release 前把 `_rel/firmware-<profile>/` 分别 `tar -czf firmware-<profile>.tar.gz`，`files` 改为 `_rel/*.tar.gz`；重派 all=32407079282 全绿，ci-37 三个 tar.gz 资产 | n/a（自持 CI） | F24 的“保留子目录”只解决了下载覆盖，没解决 softprops 按 basename 上传的冲突；同名产物应打包成按档位命名的单文件资产 |
| F27 | 审查发现的高危/中危一致性问题（2026-08-21） | ① 9001 内核 DTS 的 `ubi`/`reserved_bmt` 分区大小与 9002 U-Boot DTS 及 FLASHING 不一致（0x1b700000/0x04200000 vs 0x1f700000/0x00200000）；② `prepare-oc.sh stock` 路径双重前缀且会误删未跟踪的 stock 基线补丁；③ dry-run 缺失补丁文件不置红；④ 0530 上下文因 0521 过期；⑤ sync-upstream push paths 漏 audit/verify 脚本；⑥ DEVICE_PACKAGES 默认 wpad-basic 与 seed wpad-openssl 可能冲突；⑦ fan 无重复启动保护；⑧ build.sh 本地无 .itb 产物不红 | ① 9001 对齐 PR/U-Boot/FLASHING；② revert_stock 改为树内相对路径 + 只恢复 tracked 文件；③ dry-run 先判 missing；④ 0530 按 0521 后状态重建；⑤ push paths 补齐；⑥ 9000 去掉 wpad-basic；⑦ fan 加 PID 检查；⑧ build.sh 产物缺失即 exit 1 | n/a（自持维护） | 审查文档未提交，修复见本次提交 |
| F28 | 单 wiphy 多 radio 默认无线配置不可用 + wifi-scripts ucode set_txpower 声明顺序错误（2026-08-21 实机验收暴露） | ① 默认 `files/etc/config/wireless` 仍按三 phy 模型（radio0→phy0 / radio1→phy1 / radio2→phy2）编写；MT7996 实机是**单 wiphy 三 radio**，phy1/phy2 不存在 → 5G/6G 配置无法绑定，只有 2.4G 能起来；② 9010 重建时把 ucode 函数 `set_txpower` 定义在 `setup_phy` 之后，而 `setup_phy` 在无 `radio` 标识的单 radio 设备上会直接调用 `set_txpower` → ucode 运行期 undeclared variable | 单 wiphy 多 radio 模型失配 + 函数声明/调用顺序错误 | ① `files/etc/config/wireless` 三 radio 统一 `phy0` + `radio 0/1/2`（与 wifi-scripts per-radio txpower 补丁的 `config.radio` 语义一致）；② 9010 中 `set_txpower` 前移至 `setup_phy` 之前；本地 audit-patches 通过，实机三频全部 up 且 txpower 正确（2.4G/5G 30dBm、6G 29dBm） | n/a（自持配置/补丁） | 教训：多 radio 单 wiphy 设备必须用 `phy + radio` 模型；补丁重建后除行数/上下文外，还要核对函数声明与调用顺序（ucode 无 hoisting） |

| F29 | FlowSense VLAN/PPPoE Offload 开关报错（2026-08-22 实机验收暴露，experimental 档） | LuCI 页面选择 Enabled 后 RPC 返回 `failed - bridge module may not be loaded`；`/proc/sys/net/bridge/bridge-nf-filter-*` 不存在 | `luci-app-airoha-flowsense` / `luci-app-airoha-npu` 的 RPC 后端仍写 br_netfilter 旧 sysctl；experimental 档的 06 号补丁已改为原生 NFPROTO_BRIDGE flowtable 架构，固件内不再有 br_netfilter，旧 sysctl 路径永远不存在 | 新增 `patches/root/9018-flowsense-offload-new-arch.patch`（MANIFEST/ORDER 置于 9017 后）：两 RPC 后端在 sysctl 存在时保持旧行为；sysctl 缺失时 VLAN/PPPoE offload 状态改由 HW Flow Offload + fw4 flowtable 判定，Enabled 操作确保 `flow_offloading`/`flow_offloading_hw` 开启并 reload fw4，Disabled 返回明确错误（当前固件由 HW Flow Offload 总开关控制）。实机 ubus 验证：get 返回 `{"enabled":1}`、set enabled 返回 ok | n/a（自持补丁） | 教训：实验档内核能力切换后，依赖旧内核接口的应用层补丁必须同步适配；实机验收应覆盖 LuCI 全部可操作项 |

| F30 | NPU offload 深度测试暴露的监控与诊断问题（2026-08-22 实机，experimental 档） | 35 分钟 3 路并发 HTTP 下载（约 3.38GB）实测 NPU offload 稳定，但暴露：① npu-monitor 默认 target `1.1.1.1` 在现网不可达，jitter 采样恒 unreachable；② PPE debugfs `bind`/`entries` 的 `packets=`/`bytes=` 恒为 0，无法按流统计；③ 单流 `.102 -> 183.232.52.102` 仅 `[OFFLOAD]`/UNB，同客户端换 `.100` 或并发后可达 `[HW_OFFLOAD]`+BND；④ 设备缺 `bridge`/`ethtool`/`devmem`，`getFrameEngine` 报错 | ① 包默认 UCI 配置了不可达 target，而 init 脚本的网关 fallback 仅在未配置 target 时生效；② PPE debugfs 统计字段不更新；③ 硬件绑定与目标 IP/流建立时机相关，非功能故障；④ 镜像未包含诊断工具 | ① `ae9a2fd`：`patches/root/9017` 默认 target 改为 `223.5.5.5`；② 测试记录：`docs/acceptance-results/2026-08-22-npu-offload-stress.md`；③④ 留待后续镜像增强/补丁修复 | n/a（自持维护） | 硬件卸载判定应综合 conntrack `[HW_OFFLOAD]` + PPE BND + CPU/中断，勿只看 PPE debugfs 统计；诊断工具缺口建议下轮镜像补入 |
| F31 | wifi down/up 后 AP 拒绝所有 STA 关联（2026-08-22 实机深度测试，experimental 档） | 执行 `wifi down` → `wifi up` 后三频 AP-ENABLED，但 hostapd 持续报 `Could not set STA to kernel driver` / `Could not add STA to kernel driver` / `handle_assoc_cb: STA ... not found`；2.4G/5G 客户端均无法重新关联；随后 `wifi reload` 也无效 | 根因：mt76 旧 pin 59676919 的 `mt7996_mcu_add_sta()` 在发送 STA_REC 前先发 VOW DRR 控制命令；`wifi down/up` 清空固件 STA 上下文后，新关联命中 fresh-peer 路径，固件返回 0xC0000001，旧代码随即丢弃 STA_REC 并向上返回错误，hostapd 表现为 `Could not add/set STA to kernel driver` | 已修复；issue #10（经 9028 mt76 bump c5a3bd91 携带上游 `da2c90d2`：先发 STA_REC 再配置 VOW，且 VOW 失败不再阻断连接）。CI#70 5 轮 `wifi down/up` 未复发、客户端可重连 | merged（openwrt/mt76 c5a3bd91 / `da2c90d2`） | 教训：`wifi down/up` 不等于 `wifi reload`；mt76 MCU 命令顺序（先建 peer 再配调度）必须固化进补丁评审；`scripts/device-wifi-downup-probe.sh` 保留用于回归 |
| F32 | 2.4G 配置 HE40 实际常落 20MHz 且 iwinfo 误报（2026-08-22 实机深度测试） | `files/etc/config/wireless` radio0 为 `htmode HE40` + 无 channel（设备侧落为 `auto`）；实测 auto 及固定 ch6 时均因 `20/40 MHz operation not permitted ... based on overlapping BSSes` 回退 20MHz（hostapd log）；仅 `channel=6` + `noscan=1` 时实际 40MHz。同时 `iwinfo` 仍显示 HT Mode: HE40，与 `iw dev` 的 20MHz 不一致 | ACS/HT40 coexistence 正常回退；iwinfo 展示的 HT Mode 未反映 nl80211 实际宽度 | 未改配置；测试记录：`docs/acceptance-results/2026-08-22-wifi-deep-test.md`。方案二选一：① 接受回退并把 radio0 意图明确为 HE20/auto（避免误读）；② 固定 `channel '6'` + `option noscan '1'` 强制 HE40（牺牲 2.4G 共存礼貌，不建议在密集环境） | n/a（自持配置 + iwinfo 上游行为） | 判读 2.4G 实际带宽以 `iw dev` 为准；`iwinfo` 的 HT Mode 字段在 ACS 回退场景会误报 |
| F33 | 单 wiphy 多 radio 启动期 hostapd 邻区扫描失败（2026-08-22 实机深度测试） | 每次冷启动/`wifi reload` 时 hostapd 对固定信道的 5G/6G 也发起 neighbor BSS scan，单 wiphy 三 radio 并发扫描互相挤占，logread 出现 9 次 `Failed to request a scan of neighboring BSSes ret=-16 (Resource busy)`（daemon.err），最终 AP-ENABLED 但日志有 error 且拖慢启动 | 单 wiphy 多 radio 共享扫描引擎；固定信道无需扫描，但 wifi-scripts 默认 `noscan=0` | `files/etc/config/wireless` 对 radio1/radio2 增加 `option noscan '1'`（本 commit，固定信道且非 DFS）；radio0 保持 auto 扫描；本机已 `wifi reload` 复核：hostapd conf 带 `noscan=1`，无新 ret=-16 | n/a（自持配置） | 固定信道 AP 应设 `noscan=1` 消除启动期无谓扫描与 error 日志；radio0 如需 HE40 确定性再评估固定信道+noscan |

| F34 | `flow_offloading_hw` 1→0→1 切换后设备疑似重启（2026-08-22 实机深度测试，experimental 档） | 软件流表基线测试时先将 `flow_offloading_hw` 置 0 + fw4 reload（正常，`[OFFLOAD]` 生效）；打流完成后恢复 `flow_offloading_hw=1` + fw4 reload，命令返回 `restored` 且 uci 值正确，但约 10–15s 后设备重启（kernel boot 08:27:54）。重启后 pstore 为空、logread 仅保留重启后日志，无 pre-reboot 线索 | 待定位：疑为 NPU flow_block 解绑/重绑路径在 fw4 reload 后的异步释放/再注册触发看门狗或 panic；需串口复现确认 | 未修复；issue #7；测试记录 `docs/acceptance-results/2026-08-22-npu-toggle-rpc-gap.md`。修复前建议 LuCI 在线切换提示"切换后建议重启"或限制 1→0→1 快速连续切换 | n/a（待定位） | 教训：硬件卸载开关的在线切换必须纳入实机验收；`fw4 reload` 成功返回不代表切换无副作用，应继续观察 1–2 分钟 |
| F35 | `getStatus.memory_regions` 依赖 dmesg，logd 消费后恒为空数组（2026-08-22 实机深度测试） | `luci.airoha_npu`/`luci.airoha_flowsense` 的 `get_status` 用 `dmesg \| grep "reserved mem.*npu" \| head -4` 取保留内存；OpenWrt logd 消费内核环缓冲后 `dmesg` 输出为空（实测 `dmesg \| wc -l` = 0），于是 `memory_regions` 恒为 `[]`。开机初期尚可显示 4 条（另漏第 5 条 npu-ba，见 issue #3） | 9017 数据源选错：dmesg 环缓冲不可持久，且 `head -4` 漏项 | 已修复；issue #8（经 F42 9020 改 DT + F63 ash 算术修复） | n/a（自持补丁） | CI#70 experimental 实机 `ubus call luci.airoha_npu getStatus` 返回 5 个 memory_regions（npu-ba 在列），issue #3/#8 已关闭。教训：LuCI RPC 的"状态"应来自稳定 sysfs/procfs/devicetree，不应依赖易失的 dmesg 环缓冲 |
| F36 | `getWanHealth`/`getBridgeStats` 统计恒 0 / available:false（2026-08-22 实机深度测试） | `ubus call luci.airoha_flowsense getWanHealth` 对 `pppoe-wan` 返回 `rx_bytes:0,tx_bytes:0`，但 `/sys/class/net/pppoe-wan/statistics/rx_bytes=860075,tx_bytes=7573199`；`getBridgeStats` 直接返回 `{"available":false}` | 两个 RPC 方法都使用 `ip -s link show <dev>` 解析统计，但 BusyBox `ip` 不支持 `-s` 选项（打印 usage 后退出）→ 解析结果为空 → `getBridgeStats` 误判桥不存在 | 已修复；issue #9（经 F43/9021 改读 sysfs） | n/a（自持补丁） | CI#70 实机 `getWanHealth`/`getBridgeStats` 均 `available:true` 且计数与 sysfs 一致，issue #9 已关闭。教训：RPC 后端 shell 解析必须对目标平台 BusyBox 工具集实测；`ip -s` 这类 iproute2 专有选项在 BusyBox 上不可用 |

| F37 | 硬件驱动深度测试：1G 口 LED 默认不亮、10G 口 LED 缺失、AN7581 PHY LED 第二个 netdev 配置写回 EINVAL（2026-08-22 实机，experimental 档） | ① `wan`/`lan3` 的 MT7530/AN7581 PHY LED 在 `/etc/config/system` 无任何 LED 配置，默认 brightness=0、trigger=none——1G 网口灯不亮；② RTL8261BE（lan1/lan2）在 `/sys/class/leds` 无 LED 设备，DTS `ethernet-phy@5/@8` 无 `leds` 子节点；③ 先配置第一个 PHY LED 的 `link/rx/tx=1` 写入正常，再配置第二个 PHY LED 时 sysfs 值生效（读回=1）但 store 返回 `EINVAL`，`/etc/init.d/led start` 退出码=1 且 boot 出现 3 条 `write error: Invalid argument` | ① `files/etc/config/system` 增加 `led_wan_green`/`led_lan3_green`（`trigger=netdev`，`dev=wan/lan3`，`mode='link rx tx'`），设备实测 LED 进入 `offloaded=1` 硬件接管；② 10G LED 跟进上游 #24034，或评估 fanboy 09 号 RTL8261CE LED 支持向 RTL8261BE 移植；③ AN7581 PHY LED sysfs store 路径（`drivers/net/phy/mediatek/mtk-ge-soc.c`，9016 同文件）修正第二个 LED 写入的返回值——值已生效，应返回 0 | upstream-open（#24034 / #24619） | 测试记录：`docs/acceptance-results/2026-08-22-hardware-driver-deep-test.md`；复现脚本：`scripts/device-hw-probe.sh`。① 已修（本 commit）；② issue #13（已修复：9033 carry openwrt #24034 + 9001 phy5/phy8 leds + system LED 配置）；③ issue #14（已关闭：9027/9031 实机验证） |
| F38 | `lan1` (10G/USXGMII) 无链路 down/up 时 `rx_errors`/`rx_dropped` 虚增（2026-08-22 实机，experimental 档） | `lan1` 无电缆、NO-CARRIER 时，每次 `ip link set lan1 down && ip link set lan1 up` 使 `rx_errors`/`rx_dropped` 增加 0~3（实测基线 3 → 5 → 8 → 9）；`lan2` 同样操作不增加。`dmesg` 无 error/oops，仅正常 PHY attach/configuring | `airoha_eth` 在 USXGMII in-band link 重新配置期间把 link-state 字/伪帧计为 RX error，或重新 open 时未清零统计 | 已修复；issue #15（经 F55/9025 无 carrier 只清 MIB） | n/a（自持补丁） | CI#70 实机 lan1/lan2 down/up 各一次 delta=0，issue #15 已关闭。该计数会触发监控误报（LuCI/9017 的 Physical Bottleneck 告警）；`scripts/device-hw-probe.sh` 默认不执行 down/up，`TOGGLE_10G=1` 可复现 |
| F39 | NPU CLIENTS offload 计数漏报：4 个 Wi-Fi 客户端关联、其中 2 个有 PPE BND + conntrack `[HW_OFFLOAD]`，LuCI 仍显示 `CLIENTS 0 offloaded`（2026-08-22 实机，experimental 档） | `getPpeEntries` 的 `client_bnd`/`band_bnd` 仅用 `/sys/kernel/debug/ppe/bind` 的 `eth=` 字段匹配 station MAC；但路由型 client→WAN 流的 BND 行 `eth=` 携带的是桥/下一跳 MAC（如 `00:58:28:d7:44:5e->dc:ef:80:59:a7:0d`），**不含 station MAC**（实测 .170 的 BND 行 `orig=192.168.123.170:…` 但 eth 无 `1e:f3:6d:77:ce:23`），MAC 匹配恒为 0；同时 BND IP 匹配缺失、conntrack 兜底缺失 | 新增 `patches/root/9019-npu-client-offload-accounting.patch`（MANIFEST/ORDER 置于 9018 后）：`band_bnd` 与 `client_bnd` 增补 BND `orig/new` 中的 station IPv4 匹配，并按客户端返回 `hw_offload`/`sw_offload`（来自 conntrack `[HW_OFFLOAD]`/`[OFFLOAD]`）；`bnd` 取 eth-MAC、BND-IP、conntrack-HW 三路信号最大值。实机验证：修复前 `client_bnd` 4 站全 0、`band_bnd=[0,0,0]`；修复后 `.162 bnd=2/hw=2`、`.170 bnd=8/hw=5`、`band_bnd=[0,8,0]`（UI 从 0 offloaded 变为 2 offloaded）。测试记录：`docs/acceptance-results/2026-08-22-npu-clients-offload-deep-test.md`；复现脚本：`scripts/device-npu-probe.sh`；issue #18（已关闭） | n/a（自持补丁） | 教训：PPE `eth=` 的语义不是“station MAC”，路由型 Wi-Fi 流必须用 IP 级匹配；多信号源取 max 才能避免硬件卸载假阴性 |
| F40 | IPv6 NPU offload 深度测试：TCP/UDP 可用但 RPC/PPE 呈现存在 IPv6 假阴性（2026-08-22 实机，experimental 档） | ① IPv6 TCP 多源并发（10 流）2s 内达 `[HW_OFFLOAD]`+PPE BND v6，功能正常；② IPv6 UDP 长流 conntrack 达 `[HW_OFFLOAD]`，但 PPE `bind` 无 BND、`entries` 仅 UNB——按 BND 判读 UDP 卸载会假阴性；③ `getPpeEntries` 的 `band_bnd`/`client_bnd` 仍不能计 IPv6 客户端：`@NEIGH` 显式跳过 IPv6，且 `ip neigh` 输出压缩式 IPv6 而 PPE/conntrack 为展开式，未规范化无法匹配；`bnd.ipv6` 已能统计总数但 CLIENTS 视图会漏掉纯 IPv6 客户端；④ ICMPv6 不卸载（符合预期）；⑤ PPE `packets/bytes` 恒 0 在 IPv6 下复现（issue #5） | 已修复（F44/9022）；issue #20（UDP UNB 可视化）已关闭；issue #19（纯 IPv6 客户端终验）已关闭（纯 IPv6-only 无线客户端终验转 ACCEPTANCE B5.1 跟踪）。测试记录：`docs/acceptance-results/2026-08-22-ipv6-npu-offload-deep-test.md`；复现脚本：`scripts/device-npu-ipv6-probe.sh` | n/a（自持补丁） | 教训：PPE/conntrack 的 IPv6 地址是展开式，`ip neigh` 是压缩式，任何跨源 IP 匹配必须做地址规范化；硬件卸载多数据源判读必须区分 TCP（BND）与 UDP（UNB/conntrack）路径 |
| F41 | iQOO Neo9 Pro 无法连接 5G WiFi（同 SSID 下只连 2.4G，5G 无 auth/assoc 记录）（2026-08-22 实机，experimental 档；2026-08-26 实机二分定位并关闭） | ① 三频统一 SSID `K2P` 但无 band steering / 802.11k/v / 独立 5G SSID，iQOO 黏在 2.4G（2.4G SAE 成功，`logread` 中 5G 对 `e2:0d:5e:29:67:6d` 零记录）；② 5G 默认 `ch149/HE160`（center1 5815，覆盖 5735–5895MHz），越界到 UNII-4（ch169/173/177，regdb 0521 自制 30dBm），国行 iQOO 在 5.8G 仅支持 80MHz，存在 BSS 过滤/连不上风险；③ 设备 2.4G/5G 口令为 8 位 `12345689`，与仓库占位 `123456789` 漂移（弱口令）；④ 诊断工具缺 `tcpdump`/`hostapd_cli`，probe 级证据无法取得 | 2026-08-26 实机矩阵（iQOO Neo9，MAC `42:f5:40:1e:e6:e1`，贴脸 -34dBm）：K2P-5G `sae-mixed`+HE160 = 手机提示“拒绝请求”且 AP 零 auth/assoc；K2P-5G `psk2`+HE160 = 同样“拒绝请求”；K2P-5G `psk2`+HE80 = **成功关联**（HE80 2SS，PHY 1200.9Mbps）；K2P-5G `sae-mixed`+HE80 = **成功关联**（`auth_alg=sae`，mfp=true）。结论：**根因是 ch149/HE160（中心 5815 覆盖 UNII-4 前段）被国行 iQOO 拒绝，HE80 即可恢复，与加密无关**。修复：`files/etc/config/wireless` 默认 5G 改 `HE80`；HE160 改为注释化可选档（非国行/支持 5.8G 160MHz 终端）；K2P-5G 保留 `sae-mixed`。测试记录：`docs/acceptance-results/2026-08-26-iqoo-5g-he80-fix.md`；只读取证脚本：`scripts/device-wifi-iqoo-5g-probe.sh`；issue #21（关闭） | n/a（自持配置；regdb 0521 合规风险自担） | 教训：单 SSID 多频合一≠band steering；5.8G 160MHz 在中国不可用（国行终端仅 80MHz），自制 US regdb 扩展 UNII-4 在国行终端上可能被过滤；口令我漂移会被 hostapd 4-way 失败误诊为“无法连接”；“拒绝请求”且 AP 无 auth 记录 = 客户端在 MAC 层拒绝 BSS 参数，不是认证失败 |
| F42 | NPU RPC memory_regions 数据源（#3/#8，承接 F35） | getStatus.memory_regions 依赖易失 dmesg 且 head -4 漏第 5 区 npu-ba | 9017 用 dmesg 环缓冲 + head -4 | `patches/root/9020-npu-memory-regions-dt.patch`：改读 /proc/device-tree/reserved-memory/npu-* reg，dmesg 仅回退 | n/a（自持补丁） | #66 实机：DT 数据源与 5 节点在位，但 getStatus 因 9020 的 `16#` 算术在 BusyBox ash 上报 Invalid argument（issue #22）；F63 修复后 CI#70 实机 `getStatus` 返回 5 区（npu-ba 在列），issue #3/#8 已关闭 |
| F43 | FlowSense 接口统计恒 0（#9，承接 F36） | getWanHealth/getBridgeStats 用 BusyBox 不支持的 `ip -s` | BusyBox ip 无 -s；9017 遗留 ip -s 解析 | `patches/root/9021-flowsense-sysfs-stats.patch`：改读 /sys/class/net/<dev>/statistics/* | n/a（自持补丁） | #66 实机复核通过：getWanHealth/getBridgeStats 与 sysfs 一致，issue #9 已关闭 |
| F44 | IPv6 NPU offload 呈现假阴性 + UDP 判读（#19/#20，承接 F40） | getPpeEntries 的 client_bnd/band_bnd 漏计 IPv6；UDP 卸载只能看 UNB | 9019 仅 IPv4；PPE/conntrack IPv6 展开式与 ip -6 neigh 压缩式不匹配；无 conntrack UDP 兜底 | `patches/root/9022-npu-ipv6-udp-visibility.patch`：awk canon6 + ip -6 neigh + client_bnd.ip6 + udp_hw（conntrack） | n/a（自持补丁） | CI#70 实机：client_bnd.ip6 已生效、`unb.udp_hw=12`（issue #20 已关闭）；纯 IPv6 客户端路径经 awk 合成用例验证通过（mac2ip6/canon6/BND/conntrack 匹配），issue #19 已关闭；纯 IPv6-only 无线客户端终验转 ACCEPTANCE B5.1 跟踪 |
| F45 | NPU RPC 诊断工具缺口（#2，承接 F30④） | getFrameEngine 报 devmem 错、pll_freq_mhz 恒 0 | 根因：镜像缺 devmem/ethtool/bridge，且内核 `CONFIG_DEVMEM` 未开启——busybox devmem 在，但 `/dev/mem` 驱动缺失，`mknod /dev/mem c 1 1` 后 open 仍 ENXIO | `config/seed-config.diff` 补 ethtool/bridge/tcpdump/iperf3 + busybox devmem + **`CONFIG_KERNEL_DEVMEM=y`**（映射内核 `CONFIG_DEVMEM`，提供 /dev/mem 驱动）；`patches/root/9023-npu-rpc-graceful-tools.patch` 优雅降级兜底 | n/a（自持配置/补丁） | 根因修复 + 兜底：内核有 /dev/mem 时可读 PLL/Frame Engine 寄存器；缺 devmem 或 devmem 读失败时 getFrameEngine 返回 available:false、pll_freq_mhz 输出 null，前端 renderFeDiagram 显示不可用 |
| F46 | bridge-flow-offload 包未安装 + 破坏性表名 + bridge 族 flow 表达式 EPROTO（#1 E1/E2/E3） | 05 号包未装；apply-rules.sh destroy table bridge fw4；E2 为 nft_flow_offload_init 对 bridge 族取 conntrack 失败 | ① 05 号 `DEPENDS=+kmod-nft-bridge` 与 06 去 br_netfilter 冲突；② CI 两次 defconfig 顺序：首次 defconfig（默认 target=mediatek）把实验档专有包写成 `# CONFIG_PACKAGE_* is not set`，第二次 defconfig 不会重新启用——共享 seed 未含该包，E1 因此仍缺；③ 表名与用户 fw4 冲突；④ init/destroy 未处理 NFPROTO_BRIDGE | ① `patches/root/9024-bridge-flow-offload-deps-table.patch`（#EXP，DEPENDS 改 kmod-nft-offload + 独立表名 flow_offload）；② `patches/root/9026-bridge-flow-offload-init-conntrack.patch`（#EXP，E2，生成 675-04 内核补丁：bridge 族复用 inet conntrack 语义）；③ `config/seed-config.experimental.diff` + CI/build.sh 在实验档追加 `CONFIG_PACKAGE_bridge-flow-offload=y`；2026-08-31 stock ci-83 发现 stock 档仍未装，改为移入共享 `config/seed-config.diff` | upstream-open（#22533） | E2 已在 #66 实机通过（bridge 族 `flow offload @ft` 注入成功）；E1/E3 仓库层已修，已毕业（2026-08-31 ci-74 实机：`apk info bridge-flow-offload` 存在；`nft list table bridge flow_offload` 有 `flags offload`；`nft list table bridge fw4` 不存在；logread 无 nft 报错。`vendor/05/06`+`root/9024/9026` 转 default）。**2026-08-31 stock ci-83 补充**：stock 档仍缺 `bridge-flow-offload`（`DEFAULT_PACKAGES` 未生效），已移入共享 seed；新 stock build 后 fresh flash 复验 |
| F47 | iQOO Neo9 Pro 5G 兼容（#21，承接 F41） | 单 SSID 黏 2.4G；5G ch149/HE160 越界 UNII-4 | 国行终端 5.8G 仅 80MHz；无 5G-only 入口 | `files/etc/config/wireless` 默认 5G 改 `HE80`（K2P-5G 保留 `sae-mixed`）；HE160 注释化为可选档（非国行终端） | n/a（自持配置） | 实机矩阵已闭环：HE160（psk2/sae-mixed 均拒）→ HE80（psk2/sae-mixed 均可连，PHY 1200.9Mbps，auth_alg=sae），issue #21 关闭 |
| F48 | 2.4G HE40 意图与 20MHz 实际不符（#11/F32） | iwinfo 误报，需确定性配置决策 | ACS/HT40 共存回退是正确行为；iwinfo 展示意图 | `files/etc/config/wireless` radio0 显式 channel auto + 注释；判读以 iw dev 为准 | n/a（自持配置 + iwinfo 上游） | 不降级到 HE20；不强制 noscan |
| F49 | wifi down/up 后 AP 拒绝关联（#10/F31） | 三频 AP-ENABLED 但 STA 无法关联 | mt76 旧 pin 先 VOW DRR 后 STA_REC，固件 fresh-peer 拒绝导致 STA_REC 丢弃 | `patches/root/9028-mt76-bump-c5a3bd91.patch`（携带上游 `da2c90d2`） | merged（openwrt/mt76 c5a3bd91） | CI#70 5 轮 `wifi down/up` 未复发且客户端可重连；issue #10 已关闭 |
| F50 | F22 复核结论不成立（#4） | fanboy14 0001/0003 并非冗余 | F22 对比源码版本/位置错误；pin 59676919 实机无 token_info、仍含 mt7996_mac_sta_poll | `patches/packages/mt76-0001-wifi-mt76-mt7996-add-token-and-NPU-debugfs-counters.patch` + `patches/packages/mt76-0003-wifi-mt76-mt7996-replace-direct-WTBL-access-with-MCU-for-station-statistics.patch`（对 pin 59676919 重建，`patch -p1 --dry-run` 零失败） | upstream-open | 已重新启用；#66 实机 token_info 存在、`mt7996_mac_sta_poll` 消失，issue #4 已关闭 |
| F51 | PPE debugfs packets/bytes 恒 0（#5） | 无法按流统计 NPU 转发量 | 根因：OpenWrt `target/linux/airoha/an7581/config-6.18` 显式 `# CONFIG_NET_AIROHA_FLOW_STATS is not set`（PR #22300：该选项=y 会破坏 AN7581+MT7996 的 NPU offload，故上游暂关）。驱动 `airoha_ppe_get_num_stats_entries()` 在 `!IS_ENABLED(CONFIG_NET_AIROHA_FLOW_STATS)` 时返回 `-EOPNOTSUPP`，`airoha_ppe_foe_entry_get_stats()` 提前返回，debugfs 的 `packets/bytes` 因此恒 0 | 9032 新增 `getPpeFlowStats` RPC（issue 建议方案 2）：用 conntrack 双向 tuple（orig/reply）关联每流 `ct_packets`/`ct_bytes`/`hw_offload`，并给出 BND/UNB 汇总；实机验证可用（如 BND `ct_packets=2191, ct_bytes=918597`）。PPE debugfs 计数本身待上游修复；2026-08-31 9035 转 default 后允许 CONFIG_NET_AIROHA_FLOW_STATS=y 与 NPU offload 共存（ci-88 experimental fresh flash：dmesg `NPU flow stats unavailable (-22)`、offload 存活），待 #94 stock 复验 | upstream-open（PR #22300 未 revert 前禁用；PPE debugfs 计数） | 判读补强已落地；`scripts/device-npu-probe.sh` 应增测 `getPpeFlowStats`（见 HANDOFF） |
| F52 | EIP93 硬件 crypto 未使用（#6） | 驱动已加载 refcnt=0 | 仅编译 kmod，缺用户态 IPsec/xfrm 配置；EIP93 未注册 GCM-AEAD | docs/comment only；ROADMAP 增加 IPsec/xfrm 硬件卸载评估项，实机验证后按档位供给 | n/a | 不提前塞默认镜像 |
| F53 | 10G 口 LED 无 sysfs/DTS 节点（#13） | RTL8261BE 无 LED class 设备 | ① 上游 realtek 驱动未实现 RTL826x LED hooks；② OpenWrt 通用内核配置 `CONFIG_PHYLIB_LEDS` 未启用，DTS `leds` 子节点不会实例化为 LED class；③ 9001 DTS `phy5`/`phy8` 缺 `leds` 子节点；④ `files/etc/config/system` 缺 10G LED 配置 | `patches/root/9033-openwrt-24034-rtl826x-led.patch`（carry openwrt/openwrt #24034：生成 `target/linux/generic/hack-6.18/743-net-phy-realtek-add-rtl826x-led-support.patch` + 启用 `CONFIG_PHYLIB_LEDS=y`）+ `9001` `phy5`/`phy8` 增加 `leds` 子节点 + `files/etc/config/system` 增加 `led_lan1_green`/`led_lan2_green` | upstream-open（#24034；合入后删 9033） | 根因修复：驱动 LED hooks + PHYLIB_LEDS + DTS leds + 用户态 netdev 触发；实机验证已确认 sysfs 名 `mt7530_dsa-0:05/08:green:lan`，`led start` rc=0 且 `offloaded=1` |
| F54 | AN7581 PHY LED 第二个 netdev 写回 EINVAL（#14） | 值已生效但 store 返回 -EINVAL，led start 退出码 1 | ledtrig-netdev：① `hw_control_get` 预置 `TRIGGER_NETDEV_LINK` + 具体 link speed 位，`netdev_led_attr_store` 互斥检查拒绝写 `link`；② hw-control 模式下 `interval` 属性被内核故意拒绝写入，led 脚本未跳过 | `patches/root/9027-ledtrig-netdev-link-mode-mutex.patch`（生成 `680-`：写 generic link 时清除具体 link speed 位；写具体 link speed 时清除 generic link）+ `patches/root/9031-base-files-led-skip-interval-hw-offload.patch`（hw-offloaded netdev LED 跳过 interval 写入） | n/a（自持补丁） | CI#70 experimental 实机 `led start` rc=0、无 EINVAL（9031 随本批构建生效），issue #14 已关闭 |
| F55 | lan1 无链路 down/up 时 rx_errors/rx_dropped 虚增（#15/F38） | 每次 down/up +0~3 | airoha_eth 无 carrier 时仍把 USXGMII 伪帧计入 MIB | `patches/root/9025-xr1710g-airoha-no-carrier-rx-stats.patch`（生成 9991 内核补丁：`netif_carrier_ok()` 门控，无 carrier 只清 MIB 不并入统计） | n/a（自持补丁） | #66 实机：lan1/lan2 down/up 各一次 delta=0，issue #15 已关闭 |
| F56 | mt7996e-hif 仅 Gen2 x1（#16） | 端点能力 Gen3 x2，根端口仅 x1/Gen2 | pcie2 共享 USB3 PHY 单 lane 硬件约束；pcie0 x2 已上游（#21978 merged，openwrt main patches-6.18 已含 609-04 x2/913 PCIe HB reset）；2026-08-24 实机 D0 判定：`0002:00:00.0` LnkCap2 仅报 2.5/5GT/s（LnkCtl2 Target=5GT/s），根端口不声明 Gen3——**pcie2 Gen3 x1 不可达，Gen2 x1 为板级上限** | docs/comment only；`docs/acceptance-results/2026-08-24-pcie-gen3-baseline.md` 存档；CONTEXT/README/ACCEPTANCE 已文档化 PCIe 拓扑与验收口径（主 7990 pcie0 x2 为准，7991/hif 不作为高速数据面假设） | upstream-open（#21978 merged；pcie2 Gen3 降级为上游/厂商跟踪）；issue #16 已关闭 | 不伪造 num-lanes；不实施 pcie2 Gen3 实验补丁（`docs/PCIe-GEN3-PLAN.md` 已 D0-stop） |
| F57 | 1G 口黄灯不亮（#17） | gpio43/44 UNCLAIMED | 9001 DTS pinctrl-1 未应用 | 已修复：commit 9adafd9 + 8e33788（pinctrl-0 合并 + amber LED 配置） | n/a | 仅需关闭 issue |
| F58 | CLIENTS 0 offloaded（#18/F39） | 路由型 Wi-Fi 流被漏计 | PPE eth= 不含 station MAC | 已修复：commit a13bb0c + 9019 补丁 | n/a | 仅需关闭 issue |
| F59 | 9992 否决复核（2026-08-22） | F25 以“mt76 master 2026-08-01 已合入”否决 9992，但 openwrt main pin mt76 59676919（07-01）尚未包含该加固 | pin 未升级，漏洞（PS-sync 畸形 TLV 解析死循环/越界）仍存在 | `patches/packages/mt76-9992-wifi-mt76-mt7996-validate-PS-sync-event-TLVs.patch`（#EXP；对 pin 59676919 `patch -p1 --dry-run` 零失败） | merged（mt76 master）/ carried（pin 59676919 未升级前） | pin 升级后删 9992；F25 历史记录保留 |
| F60 | mt76 编译失败：`mt76_connac_mcu.h` `UNI_PER_STA_INFO_TAG` redeclaration（2026-08-22 四档 CI 全红） | 0003 重建时把两个关键块都复制了两遍：① 第一 hunk（mt76_connac_mcu.h）`#define PER_STA_INFO_MAX_NUM` + `enum UNI_PER_STA_INFO_TAG` 重复；② mcu.c hunk `mt7996_mcu_get_per_sta_info()` 整个函数重复。补丁可应用但编译时 mt7915/mt7996 先报 enum redeclaration，修掉后还会撞函数 redefinition | `patches/packages/mt76-0003-…` 第一 hunk 去重（`+1409,20`→`+1409,13`）；mcu.c hunk 删除第二份函数（`+5576,218`→`+5576,112`）；`patch -p1 --dry-run` + 10 个 mt76 补丁全序实际应用通过；`audit-patches.sh --experimental` 通过 | n/a（自持补丁） | 教训：补丁“可应用”≠“可编译”；重建补丁后应 grep 关键标识符（enum/函数）出现次数，重复定义只会在编译期暴露 |
| F61 | mt76 编译失败：`mt7996/mcu.c` `wlan_idx`/`res`/`i`/`wcid`/`ac` undeclared（2026-08-22 四档 CI，d52fdfa） | 0003 重建时把 `UNI_ALL_STA_TXRX_AIR_TIME` case 同时插入到 `mt7996_mcu_rx_all_sta_info_event` 与 `mt7996_mcu_ie_countdown`；后者作用域无这些变量 | 删除 `mt7996_mcu_ie_countdown` 中的重复 hunk（`@@ -513,6 +513,33`），仅保留正确位置的 case；真实应用后 `grep -c` 确认该 case 仅 1 处 | n/a（自持补丁） | 教训：F60 只 grep 了 enum/函数重复，漏了 case 块重复；重建补丁后应 grep 所有新增标识符/块的插入位置 |
| F62 | mt76 c5a3bd91 自 bump 构建四档全红（2026-08-22，commit 4805814） | c5a3bd91 期望新版 mac80211 API：`ieee80211_get_fils_discovery_tmpl(hw,vif,link_id)`、`ieee80211_get_unsol_bcast_probe_resp_tmpl(hw,vif,link_id)`、`IEEE80211_MIN_ACTION_SIZE(action_code)`/`mgmt->u.action.action_code`；openwrt main 当前 mac80211/kernel 仍是旧两参 API | 新增 `mt76-9994-mac80211-api-compat.patch`：将上述调用适配回 6.18 API（两参 tmpl、`IEEE80211_MIN_ACTION_SIZE+1`、`u.action.u.addba_req.*`）；重新携带 9028 bump c5a3bd91（commit `438a4f8`） | carried（等待 openwrt main 联动 bump mt76+mac80211，届时删 9994） | 教训：mt76 master 不能单独 bump——必须核对 openwrt main 的 mac80211 包/kernel 头文件 API 是否匹配；下次自 bump 前先 compare mac80211 包 pin |
| F63 | `getStatus` RPC 整体报 `Invalid argument`（#66 实机复查，issue #22） | 9020 DT 解析用 `$((16#${b0}...))`，BusyBox ash 不支持 `base#` 算术，`read_npu_memory_regions` 报 `arithmetic syntax error` 后只输出 `[`，JSON 非法 | `patches/root/9020-npu-memory-regions-dt.patch` 改用 `$((0x${b0}...))`（ash 可用） | n/a（自持补丁） | CI#70 experimental 实机 `ubus call luci.airoha_npu getStatus` 返回 5 个 memory_regions（npu-ba/npu-binary/npu-pkt/npu-txbufid/npu-txpkt），issue #22 已关闭 |

| F64 | reserved_bmt 66MiB 布局对齐（A1/IP02/05/06/07/15） | 上游已证旧 2MiB 布局与厂商 BMT 尾部 528 块重叠，会损坏 UBI；本仓库 9001/9002 原为旧 2MiB 布局 | `patches/root/9001-xr1710g-dts.patch` + `9002-xr1710g-uboot.patch`：ubi `0x1b700000`、`reserved_bmt@1be00000 0x04200000`；`docs/FLASHING.md` 布局表 | carried（对齐 openwrt main 3d1645e 的 W1700K 布局；上游已含 ee771d3d/72ed389 同类修复） | 实机 `ubinfo -a` 无坏块、多轮重启不新增；新布局镜像须用 HTTP U-Boot 布局选择器 UBI 2.0 刷入 |
| F65 | sysupgrade compat 一致性（A2/IP15） | 9000 曾抄 W1700K 的 `DEVICE_COMPAT_VERSION := 2.0`，但当时布局仍是旧 1.0 | 本轮 A1 已把布局升到 2.0，9000 保持 `DEVICE_COMPAT_VERSION := 2.0` 与布局一致（无需 2.0→1.0 过渡） | n/a（自持一致） | `sysupgrade -T` 同版镜像返回可升级；旧 1.0 布局设备因 compat 主版本不同会被拒（正确） |
| F66 | rdinit 修复（A3/IP17） | U-Boot env bootargs 覆盖 DTS chosen，缺 `rdinit=/sbin/init` 导致每次开机 `rdinit=/init failed: -2` 假警告 | `patches/root/9001-xr1710g-dts.patch`：chosen bootargs 末尾加 `rdinit=/sbin/init`（对不覆盖 bootargs 的 U-Boot 生效）；但当前 HTTP U-Boot（2026.07-00766-g53b73174c0fc）默认 env `bootargs` 无 rdinit 且会覆盖 DTS chosen，CI#70 实机仍见该警告 | carried（待 U-Boot 侧补 `rdinit=/sbin/init`；已尝试写 UBI env，U-Boot 不读取） | 当前 HTTP U-Boot 下仍为良性假警告；若换用 9002 OpenWrt U-Boot 或更新 HTTP U-Boot 默认 env 后应消失 |
| F67 | 风扇双控制器去重（A4/IP25） | `files/etc/init.d/fan` 与 9017 fancontrol 的 `/etc/init.d/fan` 双入口抢 PWM | `files/etc/init.d/fan` 合并为单控制器 + 动态探测 + 迟滞/最低稳定档/失效满速兜底；9017 移除 `root/etc/init.d/fan`，仅留 LuCI 前端/RPC | n/a（自持脚本） | `/etc/init.d/fan start` 后 `pwm1` 仅一个写入者；温度曲线实机复核 |
| F68 | mt76 tx_failed 记账修复（A5/IP25/IP26） | c5a3bd91 仍把 retries 计入 tx_failed，终端计数失真 | 新增 `patches/packages/mt76-0009-report-only-terminal-tx-failures.patch`（default） | carried | 有损链路 `iw dev wlanX station dump` 中 `tx_retries>0` 而 `tx_failed≈0` |
| F69 | mt76 NPU RX skb->dev（A6/IP25/IP26） | NPU RX 路径不设 `skb->dev`，PPE 未命中回退时 bridge/FDB 上下文缺失 | 新增 `patches/packages/mt76-0010-set-skb-device-for-npu-rx.patch`（experimental；用 `mt76_queue_is_npu_rx(q)` 覆盖全部 NPU RX 队列） | carried | 仍 `#EXP`（需 6G 客户端；2026-08-31 用户口径延后，发现后修复） |
| F70 | FlowSense 1.1.8-r5 bump（A7/IP08） | 1.1.8 新增 airtime 离载吞吐估计 + UI 重绘；修复 offload 场景吞吐针恒 0 | 新增 `patches/root/9030-flowsense-bump-1.1.8-r5.patch`；MANIFEST 顺序：9017→9030→9018→…→9023；9022 已在 9030 基线上重建 | carried（default） | ci-74 实机：`uci show npu-monitor.settings.air_eff`=80、`getStatus` 正常、9018-9023 无回归（2026-08-31） |
| F71 | JCPLL TCLVAR recal（A8/IP10） | lan2 10G 不 link 根因：PLL enable 前写 TCLVAR，lane 配置完成后未重写 | **2026-09-07 按 rdmitry OW1700k PR#143 机制整体重写** `patches/root/9029-xr1710g-airoha-pcs-jcpll-tclvar-recal.patch`（内层 9992）：post_config hook（match-data，仅 `an7581_pcs_eth` 注册）+ `an7581_pcs_jcpll_recal()`（限 USXGMII/10GBASER，USXGMII AN enable 后、`airoha_pcs_config()` 末尾调用）+ TCLVAR `0x3→0x5` 脉冲（1–2ms）与 ETH/PON 分值 `0x5/0x3`。旧版（bringup 内同值重写）=HW no-op（rdmitry devmem `0x1fa7a030` 实测：写 `0x1D` 立即 link、写回同值仍 dead）已失效 | carried（experimental） | 内层对 6.18.44+master 补丁集真应用通过；仍 `#EXP`（需 10G 对端实机验证；与 9038/628、9041/743-744 同域互补，实机对照后定去留） |
| F72 | 6GHz 客户端国家码判据（A9/IP14） | 客户端国家码与 AP 不一致时看不到 6G SSID，会造成“广播了但连不上”误判 | `docs/ACCEPTANCE.md` C2 增客户端国家码双侧判据；`docs/FIXES.md` F02 备注补一行 | n/a（验收门槛） | C2 双侧判据：AP 侧 US regdb + 客户端 US 可见可连 |
| F73 | FLASHING 坏版本/救砖（A10/IP19） | 8/8 U-Boot 恢复页上传必 failed；缺 OS 内覆写 chainloader 的救砖路径 | `docs/FLASHING.md`：坏版本清单（8/8 `g93da8f980ef0`）+ 8/11 `b7710e5cc851` 候选锁版 + kmod-mtd-rw 救砖步骤 | n/a（文档/运维） | 文档自查；后续升级按 ADR-0002 核对 SHA256 |
| F74 | 验收方法学补洞（A11/IP20/24/28） | 吞吐测试端点误跑路由器本机；管理面改址回连从未自验；C3 缺外部端点判据 | `docs/ACCEPTANCE.md`：测试方法学节 + B6 管理面改址回连 + C3 外部端点判据；`OC与高功率实现-调研报告.md` §①D 本机 iperf3 降级为 CPU 基线 | n/a（验收门槛） | 下一轮验收按新判据执行 |
| F75 | device-hw-probe 增强（A12/IP04） | 10G PHY 型号判据写死/缺失；EFR32 去除未断言 | `scripts/device-hw-probe.sh`：新增 B2.1 10G PHY VEND1 `0x103/0x104` 判据（phytool）+ B7 EFR32 去除断言；B2.1 修正 MDIO 访问路径——10G PHY 挂在 mt7530-0 总线上（PHYAD 5=lan2、8=lan1），lan1/lan2 的 Airoha GDM ioctl 返回 -95，须借道 DSA 用户口（wan/lan3）以 C45 MMD30 读 `IFACE/<phy>:30/0x103` | n/a（脚本） | 实机 probe 全绿（B2.1：0x103=0x8261、0x104=0x1141；driver=RTL8261BE 10Gbps PHY） |
| F76 | fanboy 08 号 regdb 覆盖重叠确认（08-26 §6 遗留疑问①收口，2026-08-30） | fanboy `ubi2-oc` 08-25/08-30 两轮整枝 rebase 后，08 Reliability 仍新增 `wireless-regdb/patches/555-w1700k-fix.patch`（18 行），与本仓库 regdb 补丁体系是否重叠未核对 | 逐行比对：该补丁 = UNII-4 `5730-5895 @ 160 (30)` + 6G `(5925-7125 @ 320) (30)` 且保留 NO-OUTDOOR，恰为本仓库 `regdb-0521`（UNII-3/4 默认档）+ `regdb-0555`（6G 30，OC 档，保 NO-OUTDOOR）的并集 | 无需吸收（非降级；本仓库拆分粒度更细：默认/OC 分档） | n/a（fanboy 自持对照记录） | 08-30 rebase（`765535cf`）后内容不变；同轮复核 06/07/mt76-0005 与本地 vendor 语义一致（06=`7828198`、07=`4f19f7b`、0005=`171bc4b` 仅 hunk 偏移漂移） |
| F77 | fanboy 18 smartrg `992-21` 83 行版（F77 收口，2026-09-07） | fanboy 08-25 rebase 后 `992-21-net-airoha-npu-init-stability.patch` 由 74 行变 83 行：`airoha_npu_send_msg` mbox 轮询超时 `1000*MSEC_PER_SEC`→`500*MSEC_PER_SEC`（配合既有 3 次重试包装：mailbox 挂死更快放弃并重试，聚合最坏等待预算同量级） | **已吸收**：`patches/vendor/fanboy/18-smartrg-stability-b45d5117.patch` 的 992-21 段更新为 e352c48673 83 行版（新增 hunk `__airoha_npu_send_msg` DONE 轮询 1000→500ms；992-20 184 行逐字节未动；外层 hunk 行数/diffstat/index 同步） | carried（default） | OWT 真应用后 992-21 blob 与 e352c48673 逐字节一致（`e1195d809e9abd…`）；风险：若硬件正常完成落在 500–1000ms 窗口会多走一次重试（幂等写入，最坏时延略增可接受） |
| F78 | naoki66 LAN2 RTL826x SDS-mode 方案（已吸收，2026-09-07） | lan2 10G 链路建立依赖厂商 U-Boot 遗留的板级 SerDes 设置（RTL826x SDS page6 reg3=`0x88c6`），Linux 公共初始化不带 | 与 `9029` JCPLL 机制互补：SDS mode=板级 SerDes 初始化，9029=PLL enable 时序 recal | **已吸收**：`patches/root/9041-xr1710g-rtk-serdes-bundle.patch`（#EXP：742=622 SDS-mode（naoki66 09feeff 版，删 export hunk——6.18.44 下 `rtl826x_phy_patch_sds_set` 已非 static）+ 743/744=restore/reapply（b74553b/d3a0fa1 语义，6.18.44 适配））+ `9001` phy5（`realtek,sds-mode=<0x88c6>`/`reset-before-id-read`/`patch-rtk-serdes`）phy8（`patch-rtk-serdes`）属性 | n/a（社区自持，无上游 PR） | 内层 742→743→744 对 6.18.44+master 真应用通过；`reset-before-id-read` 的内核侧消费（naoki66 hack-6.18/705）未吸收——属性当前惰性无害，若实机需先复位再读 ID 再补吸收；naoki66 源仓 622 文件 hunk 计数损坏（§09feeff），其 fork 直接搬用会红，本地已修正 |
| F79 | mt76 NPU RX descriptor ownership（2026-09-07 WP-0011） | NPU 与主机通过 DONE 位交接 RX ring：consume 侧按 PKT_COUNT 走链不比对 `q->queued`，槽位不足时越过已消费描述符 → 重复投递/垃圾地址进 `dma_sync_single_for_cpu()`（实机 W1700K `dcache_inval_poc` Oops 3/3 wedged→5/5 存活）；refill 零地址竞态 | 缺界检查 + refill 竞态 | 新增 `patches/packages/mt76-0011-wifi-mt76-npu-fix-rx-descriptor-ownership.patch`（default；Oever González，论坛 t/222776 素材，对 be5ce791 `git apply` 干净） | carried（default，上游未托管） | 与 0012/0010 同域不同 hunk（0011=rx 消费/refill 区，0012=del_sta 新函数，0010=skb->dev）；需实机 NPU 长稳回归 |
| F80 | mt76 NPU del_sta（2026-09-07 WP-delsta） | STA 断开后 NPU 残留 per-station 路由，同 MAC 重连（新 WCID）下载卡 ~1Mbps，换私有 MAC 可绕过 | NPU 未被告知 station 移除 | 新增 `patches/packages/mt76-0012-wifi-mt76-npu-del-sta.patch`（experimental；naoki66 2d3aa30 / Ryan Chen：`mt7996_mac_sta_remove_link` 经 NPU mailbox 发 `WLAN_FUNC_SET_WAIT_DEL_STA`；宏已在 airoha_offload.h L122） | carried（experimental，上游未含） | 对 be5ce791 真应用通过（应用偏移 8/8/12 行系 naoki66 基线略早，落盘正确）；实机：断开→同 MAC 重连吞吐回归 |
| F81 | USXGMII RX 校准 SDK crossing 搜索（2026-09-07 WP-USXGMII） | AN7581 RX CDR PR IDAC 用"数值最近"启发式，易落 0x580 不 lock；SDK 序选 0x5a5、FL_OUT 精确 0x9edf、FBCK_LOCK 达成——**#22397 "LAN2 No FBCK Lock after 50 calibration try" 根因域** | 校准搜索算法与 SDK 不同 | 新增 `patches/root/9038-xr1710g-pcs-usxgmii-stabilize.patch`（#EXP，内层 9994=YYH e61a1bb 628 重建：保留最后一个高于 target 的 major、MSB→LSB 保留 `FL_OUT>=target` 位、删 closest-value 回退与 >100 重试块） | carried（experimental） | 内层对 6.18.44+master 真应用通过（4 hunk 因 `abs_diff` vs `abs` 漂移重建，新增行与原文逐字节等价）；**625（rate adaptation）未吸收**——语义前提 = YYH 私有 620/622（master 无 `airoha_pcs_set_usxgmii_speed` helper），9993 号段保留待 620/622 吸收后补；实机：10G 冷启动×20 + FBCK_LOCK dmesg + #22397 ifdown/up 复现 |
| F82 | PPE 本地流留 CPU（2026-09-07 WP-PPE） | PACKET_HOST（目的=本机）包走 bridge L2 fallback：桥与端口共享 MAC 时 FDB 把本机地址解析回入口端口，路由器自身发起的高速流被错误绑离 CPU（reverse UDP 实证：ppe_bind_rate=30 限 30-34 包） | 桥 L2 fallback 对本地流误判 | 新增 `patches/root/9039-xr1710g-airoha-ppe-keep-local-flows-cpu.patch`（#EXP，内层 9990 = hurryman2212 底座 + YYH 916e91a 增量 HEAD 态；先保留真实 L4 flowtable 匹配，无匹配保持 unbound） | carried（experimental；上游无底座） | 对 6.18.44+master 直接可应用（仅行号漂移）；与 9035（9995）同文件分域不重叠，9990→9995 双向顺序验证通过；实机：路由器自发 UDP 高速流对照 GDM 计数 |
| F83 | pinctrl force-GPIO（2026-09-07 WP-pinctrl） | 9001 phy5/phy8 reset-gpios（GPIO46/31）存在"reset 写了但 pad 未 mux 成 GPIO"隐患：SDK gtk_boot.sh 先置 0x228 bit31/0x22c bit14 再 RTL8261 reset；OpenWrt probe 只清 0x228 → 方向/数据寄存器写入不生效（依赖 U-Boot 遗留状态） | 驱动 probe 未恢复 force-GPIO 两 bank | 新增 `patches/root/9040-xr1710g-airoha-pinctrl-force-gpio.patch`（#EXP，内层 9998=YYH 436765d：an7581 hwinit 复位 0x228/0x22c + `force_gpio_ranges`（{0x228,gpio6-31,bit6}、{0x22c,gpio32-46,bit0}）+ `gpio_request_enable` 置 force 位 + `set_mux` 清 force 位） | carried（experimental） | master（74eb10e）已合 202-01~28/203-01/02/06/08/09（评估报告编号半对：203-01/02 确在 09-08 前后合入），内层对 KB 直接适用零重建；与 F57（gpio43/44 DTS 层申请）互补；实机：/sys/kernel/debug/gpio 46/31 方向 + PHY 复位行为 |
| F84 | RTK SerDes/SDS bundle（2026-09-07 WP-SerDes，F78 落地） | XR1710G 10G host SerDes 需板级 SDS mode（0x88c6）+ RTK SerDes flow_s 补充（PSDS0：SDS7.0x10→0x80aa / SDS6.0x12→0x5078，删二次 vendor reset）+ copper AN 后重放（PULSE→RELEASE→CHECK_CARRIER，脉冲 0x8003 保持 10-12ms 回 0x80aa，120 poll 宽限） | 公共初始化缺板级设置 + AN 后 SerDes 需要刷新 | 新增 `patches/root/9041-xr1710g-rtk-serdes-bundle.patch`（#EXP，内层 742=622（naoki66 09feeff 版）/743/744 按文件名序应用）+ `9001` phy5/phy8 dts 属性（见 F78） | carried（experimental） | 内层 742→743→744 对 6.18.44+master 真应用通过；与 9033（LED carry #24034）hunk 区不相交；与 9029/9038（PCS 侧）同域互补，实机对照后定机制取舍；`reset-before-id-read` 内核侧消费未吸收（见 F78 备注） |
| F85 | nft L2 675 系列替换（2026-09-07 WP-675） | fanboy 07-18 提交在 rebuild 中精简：675-01/02/03 由 174/406/296 行 → 121/277/235 行（-28%）：删 650-netfilter-xt_FLOWOFFLOAD 的 KEEP_HW 增量与 `DEV_PATH_BR_VLAN_KEEP_HW` 枚举、`nf_ct_bridge_inner` 签名重构（出参 proto）、Q-in-Q/PPPoE 解析调整、封装分片不 defrag 守卫 | fanboy 重构语义收敛（KEEP_HW 仅面向 switchdev 桥口，本地卸载走 nft flowtable+PPE，无消费方；枚举已删、保留 650 段即编译错） | **已替换** `patches/vendor/fanboy/06-nft-flow-offload-l2-22533-c0ed8295.patch` = `git format-patch 2ed1af79c7` 原样（无 650 段）；`9026` 内层 675-04 复核通过**未改动**（新 675-02 上下文命中，偏 18 行的旧行号属外观） | carried（default） | KB 全量应用 675-01→02→03→04 真应用全过（9 文件）；与 main pending-6.18/700 排序兼容反而改善（偏移 18→1 行）；06 文件名仍含旧 hash `c0ed8295`；bridge+FLOW_STATS 共存实机回归后转 default 评估 |
| F86 | vendor/07 内层 0014 按 be5ce791 重生成（2026-09-07 WP-vendor07） | mt76 bump be5ce791 后 `wed_rro_event` 行号 1043→766，旧内层 0014 靠 GNU patch 自动偏移（-60/-264/+1）才能过构建，审计式行号核对会红 | 上下文行号漂移 | **已更新** `patches/vendor/fanboy/07-mt76-release-hw-rro-session-5b917d4b.patch`（0014 重生成：hunk 行号 main.c 1713→1653、mcu.c 1043→779/1055→791、mt7996.h 300→301；纯行号重基，语义零变化；依赖符号 `mt7996_mcu_wed_rro_reset_sessions`(mcu.c:5611)/`mt7996_has_hwrro`(mt7996.h:861) 实测在位） | carried（default） | 对 be5ce791 真应用零 offset 通过；fanboy 重建已删同款（原因未知，本地继续自持观察）；实机 NPU/RRO 无回归 |
| F87 | F77 吸收回退：992-21 重建版重引入 F25⑥ 已删超时 hunk（2026-09-08） | F77（09-07）把 992-21 更新为 naoki66 e352c48673 83 行版，**重新带回了 F25⑥（29778aa）已删除的 `__airoha_npu_send_msg` 轮询超时 hunk（1000→500ms）**——master 内核该行已是 `100 * MSEC_PER_SEC`，hunk 上下文在 linux-6.18.44 上无法命中（ci-97 各 run 均在 mac80211 411 处先红，kernel-prep 未被触达，回归被掩盖） | 吸收校验只做「blob 与上游逐字节一致」，未重复 F25⑥ 的**内核全序真实应用**校验 | **已重建** `patches/vendor/fanboy/18-smartrg-stability-b45d5117.patch` 的 992-21 段（83→74 行：删冗余超时 hunk，重试包装 3 处调用保留）；外层 new-file 行数同步（1,83→1,74） | carried（default） | linux-6.18.44 + main 74eb10e 全序（742→…→9998 + 999-dsa 前后序）真实应用 19/19 通过；教训：**外层 blob 逐字节一致 ≠ 内核可应用**——内层补丁吸收/更新必须复跑内核全序真实应用（本轮另验 742→743→744 顺序无冲突、999-dsa-01 对 base 干净可应用） |
| F88 | SoC Status CDM1 HW Offload 恒 0% 显示缺陷（2026-09-08 实机诊断/修复） | SoC Status 页 CDM1 恒显 `HW Offload 0.0%`（CDM2 100%），误读为 offload 失效；实测 CDM1 硬件转发流量全走 RXHWF_FAST 子路径（466Mbps 下载窗口 RXHWF_FAST +105,566 帧、基线 883,324 帧），页面/JSON 从未读取该计数器，下载窗口按含 FAST 口径硬件路径份额 99.8% | `getFrameEngine` RPC 只读 RXHWF_OK（0x1fb…594），漏读 RXHWF_FAST_OK（0x1fb…598）与 FAST_DROP（0x1fb…5a8）；fanboy `12-lorenzo` tx-debug.sh 定义了 6 个 CDM RX 计数，9017 的 RPC/UI 只取 2 个；另 P0/P5 为 UI 硬编码 PSE 端口标签，非优先级 | **已修复** `patches/root/9017-w1700k-apps-pack-core.patch`：flowsense/npu 两 RPC 各补 `cdm{1,2}_rxhwf_fast`（0x1fb50598/0x1fb51598）并输出 JSON `rx_hwf_fast`；`status.js` cdmCard 改 `rx_hwf=(rx_hwf||0)+(rx_hwf_fast||0)` 计入百分比与 HWF 显示；`9018` 对应 context 行同步；hunk 行数审计 50 补丁全绿 | n/a（自持 UI 补丁；计数器语义无公开资料，属判定口径修复） | 实机热修验证（r0-7b39600）：CDM1 显示 0.0% → 56.4%（CDM2 100% 不变）；offload 健康度独立确认——NPU loaded、PPE BND=6、conntrack `[HW_OFFLOAD]`、466Mbps 下载 load 0.06、337 万包洪泛 load 0.00；诊断报告 `.dsh/2026-09-07-cdm1-hw-offload-diagnosis.md`。**深挖（09-08）**：CDM1/CDM2 为 PSE 独立 CPU 端口（P0/P5），各带三条 RX 计数通道（RXCPU 软件环 / RXHWF 硬件面普通投递 / RXHWF_FAST 硬件面 fast 数据），承载不同帧类——实测软件路径（SSH/ubus）→CDM1.RXCPU、硬件丢弃帧（UDP/ICMP 打本机）→CDM2.RXHWF、NPU 转发数据面（466Mbps 下载）→CDM1.RXHWF_FAST；且 `REG_PPE_DFT_CPORT0(0/1)` 把 PPE0→CDM1、PPE1→CDM2。故“CDM2 100% / CDM1 0%”=通道承载差异 + UI 漏读 FAST，非 offload 故障。skipped：FAST_DROP 未入 JSON/告警（原 drop 告警口径不变，需要时再补） |
| F89 | OC 分档取消 → 唯一档 OPP 650–1350 + 自动退档（2026-09-08 WP-OC-auto） | oc-1.3/oc-1.4 双变体构建 + oc-limit 静态限频（1300）复杂度高；1350 为社区已知“两分钟后重置”档位且个体体质差异（部分机器启动 panic），需要稳性兜底而非静态限频 | 分档矩阵 + 静态限频无法在体质差异的机器上自适应（08 调研：1.35GHz 中间档有“两分钟重置”不稳案例） | **已实施**：`scripts/prepare-oc.sh` 改 `<oc|stock>`（oc = 唯一档 OPP 650–1350，PLL base 650，15 档网格全覆盖 1200/1300/1350）；`scripts/build.sh` + `.github/workflows/build.yml` 矩阵去除 oc-1.3/1.4（唯一 CPU 档对所有档位生效；apply-patches `--oc` 及 #OC 资产（regdb 555）随档取消，无线回落默认 regdb 521）；`files/etc/init.d/oc-limit` → `oc-auto`：performance + `scaling_max_freq=level`，5min 稳性确认窗口（覆盖 2min 不稳案例），窗口内崩溃/重启 → 下电自动退档 1350→1300→1200（`/etc/oc-state` overlay 持久化；1200 兜底不再退；可 sysfs 调低、可写 level 手工回高挡） | n/a（自持 OC 策略） | 待 ci 构建（OPP 650–1350 实机 `cpuinfo_max_freq=1350000`）+ 实机退档链演练（人工模拟确认窗口缺失/置 level=1300 验证降频与日志） |

## 已评估待吸收清单（IP-EVAL 2026-08-23，下一轮执行）

> 来源：`docs/IP-EVAL-2026-08-23.md` 的 28 个信息点深究。以下为本轮判定“可立即吸收”的条目，按执行顺序排列。
> 执行完成后，将本表对应条目转正为上方 F63+ 台账行（问题→根因→载体→上游状态），并从本清单移除。

（本轮 A1–A12 已全部执行完毕并转正为 F64–F75，清单清空。）

## 未确认/待实机核实清单
- 物理口 ↔ 逻辑名（netdev-name 已固化：lan1/lan2=双 10G、wan=1G-1（gsw_port1）、lan3=1G-2（gsw_port2），见 9001）→ 首次实机 `ip -br link` 核对
- U-Boot flash-slot.bin 的 SHA256 全文 → 以 YYH2913/http-uboot release 页为准（升级时校验）
- 6.18 下 PM domain PLL 公式的树内位置（prepare-oc.sh 找不到即报错并指导定位）

## 2026-09-11 吸收批次（深度调研落地）

| # | 项目 | 问题/背景 | 根因 | 本层载体 | 上游状态 | 备注 |
|---|---|---|---|---|---|---|
| F90 | 第三方源覆盖度核对（2026-09-11 调研） | Gilly `openwrt-patches/`（2026-09-11 记为 ~65 条；**2026-09-12 实测为 81 项**，清单存 `research-20260912/gilly_list_now.txt`）与 naoki66 的 airoha/mt76 补丁与本仓大量同号同域，需判定"已覆盖/部分/缺失"，避免重复吸收或漏吸收 | 无台账化覆盖判定 | 产出 `docs/absorptions/2026-09-11-absorption-plan.md`（含机制矩阵与优先级） | n/a | 关键结论：Gilly 745/746(DSA) **已逐字包含**于 `vendor/fanboy/09`（default）；747 机制等价于 `9029`；033 等价于 `vendor/07`；037/040/041/043 上游 `be5ce791` 已合；746(PON) 对本仓不适用；naoki 628/622/743/744/204/mt76-0010/0012 已同源覆盖；naoki 0003 与 `mt76-0012` 逐字一致 |
| F91 | bridge-flow-offload 注入缺两防护（2026-09-11） | ① `flow_offloading_hw != 1` 时仍注入 `flags offload` 流表，与用户关闭 HW offload 的意图冲突；② 上一版留在 `ruleset-post` 的陈旧 include 不被清理，开关来回切换后旧规则仍被 fw4 加载 | 脚本无守门、无陈旧文件清理（表名与 reload 重入已由 9024 处理） | 新增 `patches/root/9043-xr1710g-bridge-offload-guard.patch`（default） | n/a（本仓自持供给链；范式取自 yahuisme/w1700k-openwrt） | 在"9024 之后"基线 `git apply --check` + `bash -n` 通过；判据：关 HW offload 后 `nft list table bridge flow_offload` 为空且无 `30-bridge-offload.nft`，开回恢复且 PPE BND 计数增长。**2026-09-12 P4 已删除 `9043`**：上游 fanboy `05` 重基后 `apply-rules.sh` 已内置同一守门（F123），本层载体改为 `9024`（表名/守门表名同步） |
| F92 | airoha ring 4（force-to-CPU）硬件 DMA 越界（2026-09-11） | ring 4 承载 DHCP/PPPoE Discovery/PPP LCP/IPCP/CHAP/PAP/IPv6CP/ISAKMP/DHCPv6/SIP/LLDP（两个 CDM 的 `CDM_VIP_QSEL_MASK`=4），16 描述符只占 4096B page 的 512B；硬件越过环尾把"描述符形状"数据写入内核内存 | `RX_DSCP_NUM()` fallback 过小（16）；ring 4 未与 ring 2/11/15 同样给 128 | 新增 `patches/root/9042-xr1710g-airoha-rx-ring-grow.patch`（default，内层 `182-v7.4-…` 逐字节等同上游 PR） | upstream-open（openwrt `#24872`，`mergeable_state=blocked`；kernel 7.4 队列 `e84b89f17a12`） | **顺序约束：必须在 `vendor/fanboy/11` 之前**（本补丁同步 916-02 的 `16→32` 上下文与 310-10/920-12/920-13 内层 hunk 行号；顺序实证 9042→vendor/11 连续可应用）。上游后果实测：DHCP/PPPoE 握手期 panic 5 次（垃圾指针落在 cpufreq `dbs_irq_work`/scheduler/nf_conntrack）或 PPPoE 协商卡死（`REG_RX_DMA_IDX` 前进而 `REG_RX_CPU_IDX` 卡 15）。合入上游后删除 |
| F93 | USXGMII 速率自适应被 else 分支关死（2026-09-11） | 上游 `airoha_pcs_link_up()` 把 `RATE_UPDATE_MODE`/`FORCE_RATE_ADAPT_MODE` 整段放在 `neg_mode != PHYLINK_PCS_NEG_INBAND_ENABLED` 的 else 里，而 USXGMII 走 in-band → **永不执行**，速率切换后寄存器停在旧档 | 速率块错误地被 forced-mode 门控（SGMII 的 in-band FIFO 阈值调整才应门控） | 新增 `patches/root/9044-xr1710g-pcs-usxgmii-rate-adaptation.patch`（#EXP，内层 625 = naoki66/Ryan Chen） | n/a（作者自持；上游无对应 PR） | **直接关联本仓 2026-09-08 验收记录"PHY 宣告 10G、AN 落 2.5G"**（`docs/acceptance-results/2026-09-08-p1-regression.md`/`d25a994`）。与 9029（JCPLL）/9038（RX CDR）机制不同、互补：9029/9038 修"链路能否起来/校准"，本项修"起来后速率档位"。内层在 naoki66 基线 clean applies（offset -53）；本仓构建树未实跑 → #EXP。判据：强制 10G↔2.5G↔1G 切换后 `ethtool` 与寄存器一致 |
| F94 | NPU offload 下 tx-agg(BlockAck) 会话定时器无人刷新（2026-09-11） | 对端（Intel 站点常见）协商 BlockAck timeout 后，传输中途 DELBA，吞吐塌到未聚合速率且不自恢复；社区实测"单包 ping 即恢复、持续 ping 全程正常"（论坛 t/222776 #3900/#3901、t/249319 #440~#442，09-08~09-11 多用户复现） | `ieee80211_refresh_tx_agg_session_timer()` 全驱动仅 1 个调用点（`mt7996/mac.c` 的 `mt7996_mac_add_txs_skb()`），其门为 `mtk_wed_device_active()`，NPU 路径被排除 | 新增 `patches/packages/mt76-0013-…refresh-tx-agg-timer-under-npu-offload.patch`（#EXP，= Gilly 045） | n/a（mt76 上游无对应提交；`be5ce791` 该门仍只判 WED） | **机制前提未定**：TXFREE 必然上报（token 归还依赖它），但 TXS 无强制来源，是固件策略 → 门扩展有效当且仅当 NPU 卸载下固件仍报 TXS。判定与加固（改 `mt7996_mac_work()` 周期刷新、与 `mt76-0003` 已有轮询合并）见 `docs/absorptions/2026-09-11-absorption-plan.md` §3.1 |
| F95 | 整机复位后 NPU 未重启 + NAPI 空指针（2026-09-11 P1 批次） | 上游 `mt7996_mac_restart()` 只重启 WED：NPU 板整机复位后 NPU 仍持旧环/描述符地址 → host TX 永死；且该函数两个 NAPI 循环只对 WED 跳过 RRO 队列，NPU 下会对**未 `napi_add` 的 RRO/TXFREE 队列** `napi_disable()` → 解引用 NULL oops | 上游 restart 路径缺 NPU 分支（`mac_reset_work` 已有，`mac_restart` 没有） | 新增 `patches/packages/mt76-0014-wifi-mt76-npu-reset-panic-race-hardening.patch`（#EXP，内层 047+034+035+036+042+026+044） | n/a（mt76 上游无对应提交） | 047/034 对 `be5ce791` 真实源码逐条 `patch -p1` 命中（034 offset 26 行；047 与 `mt76-0010/0013` 同文件顺序共存实证通过）；035/036/042 命中；026/044 偏移 237 行系 Gilly 基线前进、命中成功。判据：整机复位后 host TX 恢复、dmesg 无 NAPI NULL oops |
| F96 | airoha RX 环停顿 / 中断覆盖不足 / QDMA TX BQL UAF / thermal 两处 1 行 bug（2026-09-11 P2） | ① RX 环硬件完成停顿后无恢复路径（与 F92 的"DMA 越环"是不同故障面：F92 防越界、本条治停顿）；② 只处理 RX_DONE 不处理 `RX_NO_CPU_DSCP`；③ RX queue 31 未开 RX_DONE 中断；④ 共享 QDMA TX 环 BQL underflow 与 UAF；⑤ thermal `FIELD_PREP` 掩码用错；⑥ low-trip 钳位用错变量 | 上游缺对应修复 | 新增 `patches/root/9045-xr1710g-airoha-rx-recover-irq-thermal.patch`（#EXP，内层 981+980+976+957+960+961） | n/a（作者自持；与 #24872=9042 不同 hunk，互补） | 内层为新增文件、名称与上游 6.18 无冲突（981/980/976/957/960/961 均不存在）；dry-run 67/67 全绿；未运行时验证 |
| F97 | 9016 替换：PHY LED pinctrl 错误处理（2026-09-11 P2） | 原 9016 无条件删掉 `an7581_phy_probe()` 的 `dev_err` + 检查——**掩盖**真实 pinctrl 配置错误，且不覆盖 `mt7988_phy_fix_leds_polarity()` | 以"删日志"处理噪声，而非区分错误类型 | **已替换** `patches/root/9016-xr1710g-silence-phy-led-error.patch`（内层改为 `998-net-phy-mediatek-make-LED-pinctrl-optional.patch` = Gilly 748）：`-ENODEV`（DT 无 pinctrl-0）静默；其他错误 `dev_warn` + `%pe` + "LEDs disabled"（非致命）；覆盖 an7581 与 mt7988 两处 | n/a | 内层对 Linux v6.18 `drivers/net/phy/mediatek/mtk-ge-soc.c`（47398B）两 hunk 全命中（offset +3/+10）；原版备份 `docs/absorptions/9016-before-replace-20260911.patch.bak`。判据：无 <3> 级 "Failed to setup PHY LED pinctrl"；pinctrl-0 被占用时出现 dev_warn 且 PHY 正常 probe |
| F98 | OC 前置 PLL 公式断言缺失（2026-09-11） | `scripts/prepare-oc.sh` 第 2 步（PM domain PLL 公式 `freq_mhz = <n> + state * 50` → 650）在原实现中**找不到只告警不报错** → 若上游/补丁挪位置或改写法，OC 会**静默不完整**（OPP 表平移了、PLL 公式没跟上 → 频率语义错误，比不超频更危险） | 该步骤是"尽力而为"而非断言；范式取自 yahuisme/w1700k-openwrt（基线断言 + 硬失败） | **已实施** `scripts/prepare-oc.sh`：公式存在则按基线替换并计数；**存在 PM domain 补丁但公式不匹配 → 硬失败 exit 1**（提示更新断言模式与参考文件）；**无 PM domain 补丁 → exit 1**（提示先跑 `apply-patches.sh`） | n/a（本仓脚本自持） | 三条路径实测（工作区临时树）：A 公式匹配 → 成功改为 `650 + state * 50`（exit 0）；B 补丁在但公式改为 `state * 40` → **exit 1** 且打印"含义/处理"；C 无 pmdomain 补丁 → **exit 1** 且提示先 apply-patches。`bash -n` 通过 |
| F99 | 上游基线重基预警缺失（2026-09-11） | 本仓多个补丁的**内层补丁基线**是上游具体文件（`root/9041` 内层 742/743/744 与 `root/9033` 的 LED hack 基于 `generic/pending-6.18/742\|743` 旧版上下文）。上游改这些文件时 **dry-run 不会立刻变红**（拷贝类/内层补丁不在 dry-run 覆盖内），却会让内层补丁在**构建期**才失败——典型：上游 PR #25092 改 `generic/pending-6.18/742\|743` 并新增 744/745 | 缺少"上游关键文件版本"的确定性比对手段 | 新增 `scripts/audit-upstream-watch.sh`（只读：按 git blob SHA 比对 3 个关键上游文件 + 探测 #25092 新增的 744/745 是否已出现；`--fail-on-drift` 时漂移即 exit 1）；**接入 `.github/workflows/sync-upstream.yml`**（dry-run 之前的独立 step） | n/a（本仓 CI/脚本自持） | 正例（当前上游 `e59c7876`：3 项全 ✓、exit 0）与反例（篡改 742 后报漂移并点出"受影响补丁 root/9041、root/9033"、`--fail-on-drift` exit 1）均实测通过；基线表注明"取自 openwrt main e59c7876，上游 bump 时有意更新" |
| F100 | experimental 档 kernel prepare 失败（2026-09-11 CI 实证） | ci experimental（run **34625416045**，commit `344d15e8`）在 kernel prepare 阶段失败：`992-20-net-airoha-stability.patch` **Hunk #3 FAILED at 526** → `.prepared_...` Error 1 → `target/linux failed to build`。**stock/all 均绿**，仅 experimental 暴露 | 992-20 内层 hunk#3（改 `RX_IRQ0_BANK_PIN_MASK 0x839f→0xbb9f`）的上下文写死 `RX_IRQ1_BANK_PIN_MASK 0x7fe00000`，而本批 `9045` 的内层 **976**（enable-RX_DONE-interrupt-for-RX-queue-31）已把该值改为 `0xffe00000` → **严格上下文失配**（patch 不做跨行模糊匹配） | 新增 `patches/root/9046-xr1710g-fix-992-20-context.patch`（置于 `vendor/fanboy/18` **之后**，因 992-20 由 18 生成）：仅同步该上下文值为 `0xffe00000`，**语义不变**（992-20 仍改 RX_IRQ0）；对 default 档无影响 | n/a（本仓补丁层整合修复） | 验证：audit **68/68**；`apply-patches --dry-run --oc --experimental` 对上游 `e59c7876` 全绿；树内最终 inner 992-20 的 hunk#3 上下文已同步；对 kernel v6.18 源实测该 hunk 由 FAILED → **succeeded**。**教训**：新增会改 `airoha_eth.h` 的内核补丁时，必须检查后续补丁（尤其 `992-20/992-21`）在同一文件同区域的上下文——dry-run 只覆盖外层补丁，**内层失效只能在构建期暴露**（F21/F23 盲区同型，本批新增 976 时未做该交叉检查） |
| F101 | 上游 RTL8261CE 与本地私有驱动**同硅片双实现**（2026-09-12 调研修正） | 本地 `vendor/fanboy/09`（default 档）以"新增文件"携带一整套私有 RTL8261CE 驱动（`generic/files/drivers/net/phy/rtl8261ce/` 4 文件 + `generic/hack-6.18/999-net-phy-realtek-rtl8261ce.patch`）。上游 PR **#23644**（hurrian，09-11 20:40 更新，`mergeable_state=blocked`）把**同一批功能做进主线 realtek 驱动**：`RTL_8261CE_REV_C 0x001cc899`、VEND1 SDS option slots（0x6972~0x6977）、SerDes OCP 窗口（0x7587/7588/7589/758a）、**SerDes lane polarity（DT）**、VND2 EEE（0xd036/0xd038）、过温降速位、`rtl8261c.bin`；其提交信息自述"based on a dump of the vendor driver from a **Lumen W1700K2**"，与本地驱动头注释同源。落点是 `generic/**hack**-6.18/744\|745`（**不是** pending-6.18） | ① **同 PHY ID 双驱动**：本地驱动 `PHY_ID_MATCH_MODEL(0x001cc890)` 按模型位匹配 `0x001cc89x` **全部 revision**，而主线已注册 `RTL_8261C_CG 0x001cc898`、#23644 再加 `_REV_C 0x001cc899` → 两者同时编入即对同一 C45 ID **争抢 binding**；② 上一轮 F99 守护**探错目录**（只探 `pending-6.18/744\|745`），看不到这条线；③ 上一轮判定口径是"本机硬件是否适用"（本机 c45 `0x001ccaf3` = RTL8261BE → CE 路径不适用），**对实机成立但对补丁层维护不成立**——仓库仍背上一份与上游重复的驱动 | **已实施** `scripts/audit-upstream-watch.sh` 扩面（见 F103 行：`hack-6.18/744\|745`、`pending-6.18/743-0x`、`rtl8261ce/` 目录、`dts/*.dtso` 存在性探测 + 主线 742 补丁内 `RTL_8261CE\|0x001cc899` 文本探测，并区分 [基线]/[冲突]/[缺失] 三类）；口径修正写入 `docs/absorptions/2026-09-11-absorption-plan.md` §5.3 与 §14 | upstream-open（**#23644 / backport #25092 均 open，未合**） | 判据链：**关键有利事实**——本机 10G PHY 的 c45 ID `0x001ccaf3` **就是 `RTL_8261N`**，上游 `742` 补丁已定义 `RTL_8261N 0x001ccaf3` 并有 `rtl8261be/n_match_phy_device`，即**主线本就支持本机 PHY**；私有 CE 驱动对本机 10G 链路**很可能非必需**。**尚未验证**：移除私有驱动后 LAN2 10G link 是否仍稳定（需实机）；以及 SDS-mode（本地 `9041`/F78 的 `realtek,sds-mode=0x88c6`）属板级 SerDes 配置、与 PHY 驱动选择是否解耦（需实机）。**合入后二选一**：(A) 删私有驱动改跟主线（首选，省长期 rebase 债）；(B) 保留私有驱动则把 `999` 改名避号并声明与主线 CE 入口互斥 |
| F102 | 上游 `#25132` 补齐 `PHY_AIROHA_AN7583_PCIE` 符号（2026-09-12 上游新增） | 上游 `e59c78762`（09-11 13:07Z，PR **#25132 已合**）给 `target/linux/airoha/an7581/config-6.18` 加 `# CONFIG_PHY_AIROHA_AN7583_PCIE is not set`、`an7583/config-6.18` 加 `=y`。提交信息："neither subtarget config carries, so syncconfig prompts for it and **every airoha kernel build fails**"（`Fixes: 443e267b`） | 本仓 602-04（`patches-6.18/602-04-phy-airoha-add-support-for-AN7583-PCIe-PHY-driver.patch`）从 **patches-6.18 侧**引入同一 Kconfig 符号——即本仓曾处在"加了 Kconfig 符号但磁盘 config 未显式置位"的同一条构建红线上，只是补丁形态不同而未爆 | **无需新增补丁**：实测对上游 `f0d3e332` 全量 dry-run **68/68 全绿**，602-04 与上游 config 改动不冲突（一个改 Kconfig 源、一个改 config 值）。仅登记为"同类构建风险"参照 | upstream（已合入 main） | 验证：`apply-patches . --dry-run --oc --experimental` 对 `f0d3e332` → **处理 68 / 跳过 0 / 缺失 0**、exit 0；拷贝类真实应用 **4/4**。**留观察项**：本仓后续若再增 airoha Kconfig 符号（如 602-05 类），应同步检查 `an7581/config-6.18` 是否需显式 `is not set` 以避免 syncconfig 交互 |
| F103 | 上游守护扩面 + 基线锚点可读性（2026-09-12，F99 缺陷修复） | F99 的守护有两个实证缺陷：① **探错目录**——只有 `pending-6.18/744\|745` 两个**写死文件名**，而 #23644 实测落在 `hack-6.18/744\|745`、且新增了 `rtl8261ce/` 驱动目录与 `dts/*.dtso`，全部漏报；② **基线硬化**——基线表硬编码 `e59c7876` 且不显示"当前上游 sha"，上游前进后输出"漂移 0"虽正确但无法区分"真没碰"与"基线该刷新"；③ 实现层**误判**——`git rev-parse HEAD:<path>` 在**已删除的工作区文件**上仍返回 blob（不报错），且冲突探测只看 git 对象不看工作区，导致"磁盘上有、未提交"的文件被漏报 | 探测面不足 + 只读 git 对象 + 报告不区分故障类别 | **已重写** `scripts/audit-upstream-watch.sh`：基线表扩到 **6 个 blob**（新增 `an7581/config-6.18`、`image/an7581.mk`、`uboot-airoha/Makefile`）+ **7 条同址冲突 glob**（`hack-6.18/744\|745`、`pending-6.18/743-0*`、`pending-6.18/744\|745`、`rtl8261ce/*`、`airoha/dts/*.dtso`）+ 主线 742 补丁 `RTL_8261CE` 文本探测；路径清单取 **git 提交视图 ∪ 工作区视图**（`ls-tree` + `ls-files --cached --others`）；文件 SHA 取工作区 `git hash-object` 且**先判存在性**；输出分 **`[基线]`/`[冲突]`/`[缺失]`** 三类并打印"上游 HEAD vs 基线锚点" | n/a（本仓 CI/脚本自持） | 实证 **10/10**：正例（上游 `f0d3e332` 干净树）全 ✓ exit 0；反例 6 组均正确 exit 1——`hack-6.18/744` 冲突、`rtl8261ce/` 目录被占、工作区漂移（无 flag exit 0 / `--fail-on-drift` exit 1）、主线 742 含 `0x001cc899`、`dts/*.dtso`、上游删除基线路径（合成树）。端到端 CI 等价序列复跑：守护 exit 0 → `audit-patches` **68/68** → dry-run **68/0/0** + 拷贝类 **4/4**，且 dry-run 后 tracked 工作区无残留。**注**：一次中间态失败系测试把树弄脏（`an7581.mk` 与索引不匹配），`git checkout -- .` 复原后全绿，非脚本缺陷 |
| F104 | PPE flow bind rate 过高导致交互流永不卸载（2026-09-12 调研吸收） | AN7581 PPE 只在流**超过 `REG_PPE_BIND_RATE` 包数后**才加速：低于阈值的流整个生命周期都由 CPU 处理。vendor 默认 `0x1e`(30) **高于多数交互流的包速率**（游戏/VoIP/延迟探针），空闲链路上无感，但与批量卸载传输并存时该流吃 **60-250ms 周期尖峰**（即 bufferbloat 测试里的周期尖刺）。实测（150ms 探针对已卸载传输）：阈值 32 → loaded p90 **157ms**、>40ms 占比 30%；阈值 8 → **15ms**、0%；bufferbloat 评级 **B→A+**（双向 loaded latency +1ms） | ① 硬件阈值写死在 `airoha_ppe_hw_init()` 的 `FIELD_PREP(PPE_BIND_RATE_*_BIND_MASK, 0x1e)`；② 无 debugfs 旋钮，无法在线调参 | 新增 `patches/root/9047-xr1710g-airoha-ppe-bind-rate.patch`（内层 `target/linux/airoha/patches-6.18/9997-net-airoha-ppe-bind-rate.patch`，来源 openwrt 论坛 t/222776 **#3890** NoTengoBattery/Oever González，2026-09-04；**与本仓 F79=mt76-0011 同帖同作者的 Patch 2**）：默认降到 **8**（1..10 实测同值，8 = 最小且仍放过 DNS/NTP 类偶发流以免占表项）+ `debugfs "bind_rate"`（0600）读写旋钮 | n/a（作者自持，未提上游 PR） | **全序实证**：裸核源 → 9039 内层 `9990`（改 `airoha_ppe.c` 853/1046/1057 区）→ 本项内层 `9997`（改 136/223 区 + `airoha_ppe_debugfs.c` + `airoha_eth.h`）连续应用成功，终态 `AIROHA_PPE_BIND_RATE_DEFAULT 8` + `bind_rate` debugfs 落位；`audit-patches` **69/69**；`apply-patches --dry-run --oc --experimental` **69/0/0** + 拷贝类 **4/4**。⚠️ 内层 `9997` 的 hunk#1 在真源上为 **fuzz 1（offset 39）**——结果已校验正确，但按 F87/F100 纪律**待消 fuzz**（重排 hunk#1 上下文）。实机判据：`cat /sys/kernel/debug/ppe/bind_rate` = 8；`echo N >` 可热调；负载下探针 p90 显著下降 |
| F105 | 9046 无条件应用导致 **stock 档 kernel prepare 必红**（2026-09-12 CI 实证 + 修正） | push 触发的 stock 构建在 kernel prepare 失败：`992-20-net-airoha-stability.patch` **Hunk #3 FAILED at 526** → `airoha_eth.h.rej` → `.prepared_xxx Error 1` → `target/linux failed to build`（run `34658979893`@`f989b80f`、`34658934256`@`c0ce5710` 两次实证；`629b3e8` 尚绿——因 9046 于 `c0ce571` 才加入）。**F100 当时判"仅 experimental 暴露"是误判**：experimental 那次（`34625416045`）先在同一处红，掩盖了 stock 同样会红——两档皆红，只是先看到 experimental | `9046` 把内层 `992-20` 的 hunk#3 上下文 `RX_IRQ1_BANK_PIN_MASK` 由 `0x7fe00000` 改写为 `0xffe00000`，但该值**只在实验档**被 `9045`（#EXP）的内层 `976` 改掉；default/stock 不应用 `9045`，裸核源（linux-6.18.44 `airoha_eth.h:518`）仍是 `0x7fe00000` → 992-20 找不到上下文 → 整份 patch 失败。即"档位耦合的上下文同步补丁被当成无条件补丁" | **已修正** `patches/MANIFEST`：`9046` 标记 **`#EXP`**（与 `9045` 同档，二者为一对：`976` 改源 → `9046` 同步内层上下文）；并同步该条目注释说明"必须 #EXP" | n/a（本仓补丁层档位修正） | 验证：`audit-patches` default **53/53**（9046 已剔除）、experimental **69/69**；`apply-patches --dry-run` 默认档 exit 0（53 处理/16 实验跳过）、`--dry-run --oc --experimental` exit 0（69/0/0）；交叉核对：裸源 `RX_IRQ1=0x7fe00000` **正是** 992-20 hunk#3 的期望上下文（修复前 9046 会改成 `0xffe00000` → 与失败日志 "Hunk #3 FAILED at 526" 完全一致）；`sync-upstream` 对 `4ec4359` success。**教训**：新增"改内层补丁上下文"的补丁时，必须先判定该上下文是否**档位相关**——dry-run 只覆盖外层，档位耦合错误只能在构建期暴露（F21/F23/F100 盲区同型，本次是该盲区的第二次实例） |
| F106 | 运行期 offload sysctl 不在升级保留清单（2026-09-12 P1 批次） | `/etc/sysctl.d/{14-vlan,15-pppoe}-offload.conf` 由 FlowSense 的 VLAN/PPPoE 卸载开关**运行期写入**（`9018`），而上游 keep 清单 `package/base-files/files/lib/upgrade/keep.d/base-files-essential` **只保 `/etc/sysctl.conf`、不含 `/etc/sysctl.d/`** → "保留配置" sysupgrade 后用户设过的开关**静默丢失**、桥过滤策略回到镜像默认 | 上游 keep 清单不覆盖运行期生成的 sysctl 片段 | 新增 `files/lib/upgrade/keep.d/xr1710g-offload`（两行路径 + 说明注释；`files/` 由 CI 落 `$(TOPDIR)/files`，rootfs 组装时覆盖，**零补丁成本**）。范式取自 yahuisme/w1700k-* 的 keep.d 手法 | n/a（overlay 文件） | 判据：启用开关后做一次"保留配置"sysupgrade，`ls /etc/sysctl.d/14-vlan-offload.conf /etc/sysctl.d/15-pppoe-offload.conf` 均存在；`cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged` 与用户设定一致。**注**：工作区根 `13-nf-bridge.conf` 经核实**未被任何脚本/CI 引用**（不部署），故未纳入 |
| F107 | `airoha_ppe_flush_sram_entries()` 内层 `int err;` 遮蔽 → 恒返回 0（2026-09-12 P1 批次） | 函数外层 `int i, err = 0;`，循环体内又声明 `int err;`（遮蔽）；循环失败时 `break` 后 `return err` 读到的是**外层**变量，恒为 **0** → SRAM FOE 刷写失败被静默吞掉，`airoha_ppe_init()` 拿不到真实 errno | 变量遮蔽（-Wshadow 类缺陷），6.18.44 真源 `airoha_ppe.c:1397/1400` 仍存活 | 新增 `patches/root/9048-xr1710g-airoha-ppe-flush-sram-err.patch`（内层 `9998`，Gilly 949；default 档） | n/a（作者自持；B2 逐行核对**推翻**了工具初判的"已覆盖"假阳性） | 内层对 6.18.44 真源实证：hunk#1/#2 命中（offset 64），终态 `int i;` + `return err;` + `return 0;`。`audit-patches` 73/73；dry-run 73/0/0 |
| F108 | NPU 板 per-STA 流量统计恒 0 + 固件计数器不上报（2026-09-12 P1 批次） | ① `mt7996_mac_work()` 的 ADM_STAT/MSDU_COUNT 轮询门**只判 WED**——本仓 `mt76-0003` 恰好改了同一 hunk 的上面两行（加 AIR_TIME/PER_STA_RSSI）却**没动这个门** → NPU 板 `wcid->stats.{tx,rx}_{bytes,packets}` 恒 0（per-link 速率仍在更新，因 TXRX_RATE 轮询无条件）；② `mt7996_sta_statistics()` 同样只在 WED 上报固件计数器，NPU 绕过 mac80211 → 软件计数 30s 只看到 **161** 包 vs 固件 **8937** 包（5GHz 实测） | NPU 卸载与 WED 同样绕过 host，但两处门未覆盖 NPU | 新增 `patches/packages/mt76-0015-wifi-mt76-mt7996-npu-firmware-sta-counters.patch`（#EXP，= Gilly 039+046 **合一个补丁**：046 的计数器由 039 扩开的轮询喂给，必须同档）。039 的 hunk 已**按 0003 之后的上下文重基**（原作者上下文不含 0003 加的两行，直接应用会 FAILED） | n/a（mt76 上游无对应提交） | 真源全序实证：pin `be5ce791` → `mt76-0003` → `0015`，mac.c/main.c 均命中（main.c offset -79）；终态 mac.c 的 WED 门带 `\|\| mt76_npu_device_active()`、main.c 走 WED\|\|NPU 且固件计数为 0 时留给 mac80211。实机判据：NPU 板 `iw station dump` 每站 bytes/packets 非 0 且随流量增长 |
| F109 | mt76 per-STA debugfs 被整块编译掉（2026-09-12 P1 批次） | mt76 走 **backports** 构建，autoconf 只定义 `CPTCFG_MAC80211_DEBUGFS`（由 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 经 mac80211 包 `Makefile:255-258` 映射），而 mt76 源码用 `#ifdef CONFIG_MAC80211_DEBUGFS` 判断（pin `be5ce791` 共 **6 处**：mt7996/debugfs.c:1006、main.c:2633、mt7996.h:992 + mt7915 三处）→ `CONFIG_` 前缀名**从未定义** → 全部编译掉，per-STA debugfs（hw-queues/fixed_rate）静默缺失；也是 Gilly 038（per-link sta stats debugfs）的**前置**（不修映射 038 是死代码） | 上游 openwrt mt76 包只映射 MESH/TESTMODE，未映射 DEBUGFS | 新增 `patches/root/9049-xr1710g-mt76-mac80211-debugfs.patch`（default 档，改上游 `package/kernel/mt76/Makefile`：按 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 条件加 `NOSTDINC_FLAGS += -DCONFIG_MAC80211_DEBUGFS`，与同文件 MESH/TESTMODE 写法一致——**默认档不改变镜像**）；`config/seed-config.diff` 加注释化的开关说明 | n/a（上游缺此映射） | 真源应用实证：上游 `package/kernel/mt76/Makefile` hunk 命中，映射落在 417-420。判据：打开 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 后实机 `ls /sys/kernel/debug/ieee80211/*/netdev:*/` 出现 stations 目录 |
| F110 | airoha GDM MIB 采集三处缺陷（2026-09-12 P1 批次） | ① **64 位** H+L 计数器（TX/RX OK pkt/byte、E64/L64/L127）被 `+=` **累加**——硬件本身已累计，每次 poll 再加一遍 → `/sys/class/net/*/statistics` 随 poll 次数**虚增**；② **32 位**计数器（drop/BC/MC/RUNT）**无绕回处理** → 硬件绕回后统计**倒退**；③ RUNT(32b) 与 E64(64b) 混入同一 `tx_len[0]/rx_len[0]` 桶且口径不一致。另缺 `port->lock` 串行化 | 上游 `airoha_dev_get_hw_stats()` 累加语义错误 | 新增 `patches/root/9050-xr1710g-airoha-mib-stats-lossless.patch`（**#EXP**，内层 `9999`，Gilly 963）：64 位改直接赋值、32 位改 delta 累加（`mib_prev`）、RUNT 单独 `run64` 运行和、加 `port->lock` | n/a（作者自持） | 真源全序实证：9025（内层 9991）→ 9050 连续应用成功；终态 `airoha_update_hw_stats()`（含 spin_lock）+ `mib_prev.tx_runt64` 等字段落位。**档位 #EXP 的理由**：改的是**对外可见统计语义**（累加→绝对），影响 F38 的 rx_errors 判定口径，需实机确认 sysfs/ethtool/FlowSense 读数自洽后再毕业 |
| F111 | 9047 内层 hunk#1 的 **fuzz 1** 消解（2026-09-12，F104 收尾） | `9047` 吸取的论坛补丁其 hunk#1 以作者基线为准（该基线 `airoha_ppe_hw_init` 紧跟 `DFT_CPORT` 函数），而 6.18.44 真源在两者之间还有一个 `airoha_ppe_set_mtu()` → 上下文第 3 行起不匹配，GNU patch 以 **fuzz 1** 容错通过（默认值与本意均正确，但按 F87/F100 纪律应消） | 内层补丁上下文基于作者基线，非本仓真源 | **已修复**：按 6.18.44 真源重建 hunk#1（上下文严格取真源 134-137 + 138-141、hunk 头 old/new = 8/36），fuzz → **0**；`9047` 外层与 MANIFEST 同步重建（`@@ -0,0 +1,160 @@`，声明=实际） | n/a（本仓内层重基） | 实证：重建后内层对真源应用**无 fuzz**（`Hunk #1 succeeded`，默认值落 `airoha_ppe.c:139`）；`audit-patches` **73/73**；dry-run **73/0/0**；拷贝类 **4/4**。**教训**：吸取外部补丁时，hunk 头行数必须按**本仓真源**重算——本次连续踩了两次（先是漏算尾部上下文导致 32≠36，再是注释行缺 `+` 前缀），都靠 audit 的"声称 vs 实际"检查兜住 |
| F112 | mt7996 radar 事件误归属到全部 band（2026-09-12 P2 批次） | `ieee80211_radar_detected(mphy->hw, NULL)` 用 NULL chanctx 上报：**单 wiphy 多 radio**（本机 MT7996 = phy0 承载 radio0/1/2）下内核会把**所有** channel context 标记为 radar → 连 2.4G 这种**没有 radar 检测器**的 BSS 也记 radar 事件（触发 DFS/CAC 逻辑） | radar 上报未带"检测方 phy 的 chanctx" | 新增 `patches/packages/mt76-0016-wifi-mt76-mt7996-radar-detecting-chanctx.patch`（#EXP，= Gilly 020）：传检测方 phy 的 chanctx（`container_of(mphy->chanctx, ...)`），phy 无 chanctx 时回退 NULL（保持原行为） | n/a（作者自持） | pin `be5ce791` 真源应用实证（offset -237）。实机判据：5G/6G 触发 radar 后，2.4G BSS 不再出现 radar 事件日志 |
| F113 | 亚 64 窗口 BlockAck 使 HW_RRO 状态机非法（2026-09-12 P2 批次） | Apple macOS/iOS 站点在延迟敏感 TID（TID6 `buf_size=8`、TID1 `buf_size=16`）协商**亚 64 窗口** BlockAck，与标准 64/256 窗口批量队列并存。MT7996 的 HW_RRO（硬件重排卸载）按 **64 帧最小窗口**设计 → 亚 64 会话使片上重排环与 SRAM 会话查找状态机进入非法态，**复用该 WCID 的后续关联硬件 RX 卸载停摆** | `ba_rdd_rro` 只判 `mt7996_has_hwrro(dev)`，未看窗口大小 | 新增 `patches/packages/mt76-0017-wifi-mt76-mt7996-bypass-hwrro-sub64-ba.patch`（#EXP，= Gilly 032）:`ba->ba_rdd_rro = !tx && enable && mt7996_has_hwrro(dev) && params->buf_size >= 64`——亚 64 交回 mac80211 软件重排，>=64 仍走硬件 | n/a（作者自持） | pin 真源应用实证（offset -252）。实机判据：Apple 站点断连重连后 RX 卸载不停摆；亚 64 会话 `ba_rdd_rro` 为 0 |
| F114 | GRO fraglist 套 GSO 的嵌套缺陷（2026-09-12 P2 批次） | Fraglist GRO 与硬件 GRO 可叠出"fraglist 里套 GSO skb"结构：P1（普通 TCP）与 P2（**HW-GRO 聚合出的 GSO skb**，数据在 `frags[]`）被 `skb_gro_receive_list()` 聚成 P3，P3 的 frag_list 里 P2 仍带 `frags[]`；后续 `tcp4_gso_segment()`/`skb_segment_list()` **只切 frag_list 条目、不切 P2 的 frags[]** → 还原不回原始包 → Tethering/转发场景 IP 分片或丢包 | `is_flist = !sk;` 未排除 GSO skb | 新增 `patches/root/9051-xr1710g-gro-fraglist-gso-nesting.patch`（#EXP，内层 `9999-net-gro-fix-fraglist-gso-nesting.patch`，= Gilly 993 / MediaTek netdev v5，**上游未合**）：`is_flist = !sk && !skb_is_gso(skb)`（已有 fraglist 遇 HW-GRO 包先 flush）。**本仓命中前提**：`vendor/11` 默认给 airoha 设备开 `NETIF_F_GRO_HW`，且上游 `hack-6.18/600` 默认开 `GRO_FRAGLIST`（Gilly 自称"stock 上 no-op"**对本仓不成立**） | upstream-open（netdev v5 未合；Gilly 自述每次 master sync 复查，合入即删） | 6.18.44 真源全序实证：`net/ipv4/tcp_offload.c` 4 hunks + `net/ipv6/tcpv6_offload.c` 2 hunks 全命中，终态 `is_flist = !sk && !skb_is_gso(skb)`。实机判据：HW-GRO 与 Tethering/转发同开时 TCP 大包不被 IP 分片、无异常丢包 |
| F115 | **IPsec 硬件卸载基础：xfrm packet-offload 框架 + EIP93 动态软回退**（2026-09-12 P2 批次，**承接 F52/issue #6**） | ① **A 系列（xfrm 框架）**：引入 xfrm packet-offload manager——`ndo` 发送回调、per-netns manager 跟踪、state 生命周期/校验/UAF 加固、driver-own-transmit、policy 变更通知、用户更新时保留 ownership。这是让 **AN7581 SOE** 能接管 ESP 发送路径的**基础设施**。② **D 系列（EIP93 动态软回退）**：crypto 层引入动态 fallback，EIP93 能加速的走硬件、**不能的（GCM/rfc4106）透明回退软件**——正面解开 F52 的死结（F52 原结论："EIP93 未注册 GCM-AEAD，故 ESP 主用 AEAD 落不到硬件，先验证后供给"） | 本仓无 xfrm packet-offload 框架（上游 6.18 亦无）；EIP93 只编了驱动、无回退机制 → 硬件加速路径不可用 | 新增两个 `#EXP` 捆绑（来源 `hurryman2212/OpenW1700k-test@offload-oc`，**同作者即 PR #23644 的 hurrian**）：<br>· `patches/root/9053-xr1710g-xfrm-packet-offload.patch`（内层 `9999-xfrm-packet-offload-framework.patch`，A 系列 9 补丁）<br>· `patches/root/9054-xr1710g-crypto-eip93-dynamic-fallback.patch`（内层 `9999-crypto-eip93-dynamic-fallback.patch`，D 系列 4 补丁 + 1 backport） | n/a（作者自持；未提上游 PR） | 真源全序实证：**A 9/9** 干净应用（最大 offset 8）、**D 5/5** 干净应用（正确新增 `drivers/crypto/inside-secure/eip93/eip93-fallback.{c,h}` 并接入 Makefile）。**依赖链**：D 依赖 `vendor/fanboy/02`（EIP93 驱动，同为 #EXP）；A+D 是 **B 系列 SOE 主体（未做，见 `handoff/handoff_p2.md` §2.9）**的前置。**三者同档才有完整意义**。判据：① 构建通过；② `ip xfrm state add` 后对照 `authenc(hmac(sha256),cbc(aes))`（硬件）vs `rfc4106(gcm(aes))`（应软回退）；`grep eip93 /proc/crypto` 出现在实际使用的算法链 + EIP93 统计增长；③ SOE 主体落地后测 iperf3 over IPsec 吞吐 |
| F116 | PCIe AER 只读诊断探针 + 判读文档（**F56 / F95 的 AER 视角**） | MT7996 走 PCIe，社区已有**两台 W1700K V1.1 无线芯片永久失效**案例，日志链固定为 `14c3:6899` Correctable RxErr → Uncorrectable Fatal `[ 5] SDES` / `[14] CmpltTO` → `mt7996e_hif AER: can't recover` → `Root Port link has been reset` → `device recovery failed` → `/sys/bus/pci/devices` 里设备消失；本仓此前**没有 AER 视角**——F56（hif 仅 Gen2 x1 板级上限）与 F95（整机复位后 NPU/chip full reset failed）只是同一条 Root Port 物理路径的两个侧面，都无法区分"可恢复"与"硬件死亡" | AER 是唯一能区分"可恢复（Correctable/Non-Fatal）vs 硬件死亡（Fatal + root port link reset + recovery failed）"的观测面，而本仓既无采集脚本也无判读口径；且 AER **Uncorrectable Error Severity** 基线 `0x00462030` 中 bit5(SDES) **出厂即 Fatal**，无降级手段时内核直接走 link reset，后续证据（Status/dmesg）看不到 | 新增 `scripts/device-pcie-aer-probe.sh`（默认**只读**：W0 目标 BDF 存在性 → W1 `lspci -nn` + 每设备 link 属性 → W2 每设备读 config `seek=131/129/132`（`dd \| hexdump -C`）小端解析 + 逐位标注 → W3 `lspci -vv` 的 AER 段 → W4 dmesg 关键链（末 60 行）→ W6 "可贴进 issue"汇总 + 结尾判读结论行）<br>+ `docs/PCIe-AER.md`（寄存器/位 → 含义 → 观测值 → 三类结论判读表 + 只读/降级两种用法 + 风险）<br>写模式**仅** `AER_DEMOTE=1`：先打印待写字节/目标设备/还原提示，再 `printf '\x10\x20\x46\x00' \| dd of=<config> bs=4 seek=131 count=1`（`0x00462030`→`0x00462010`，只清 bit5 的 severity 位；需 reboot 或重新 enable 才完全还原） | n/a（本仓脚本 + 文档自持，**零补丁成本**，不碰 patches/config/files） | 位名取自本仓内核树 `drivers/pci/pcie/aer.c`：`aer_uncorrectable_error_string`（bit4 DLP / **bit5 SDES** / bit13 FCP / **bit14 CmpltTO** / bit18 MalfTLP / bit22 UncorrIntErr）与 `aer_correctable_error_string`（**bit0 RxErr** / bit6 BadTLP / bit7 BadDLLP / bit8 Rollover / bit12 Timeout）。退出码 `0`=正常 / `2`=SSH 不可达（`ConnectTimeout=8`+`BatchMode`，不挂死）/ `3`=目标 BDF 不在 `/sys/bus/pci/devices`（**已被 AER 摘除**）。**F56 的 AER 视角**：根端口不声明 Gen3 ⇒ 没有"降速保命"退路，Correctable 计数增长应视为硬信号而非可调优项。**F95 的 AER 视角**：整机复位与 AER root port link reset 命中同一 Root Port 路径，复位后若出现 SDES / `device recovery failed`，说明链路本身没起来，与 F95 的复位可靠性问题叠加。验证：`bash -n` 通过（远程 payload 单独 `sh -n` 亦通过）；`grep -n -B3 'dd of='` 证明唯一可执行写点在 `AER_DEMOTE=1` 分支内。判据：无线失效复现时本脚本输出 `AER 判读：Fatal + Root Port link reset + device recovery failed ⇒ 硬件/PHY 层失效，非软件可修` |
| ↳ | 判读链速查（F116） | — | — | — | — | Correctable `RxErr` = 可恢复/早期劣化信号；`Status bit5 SDES=1` ⇒ **链路对端消失（Surprise Down）**；`Severity bit5=1` ⇒ 该错误按 Fatal 处理（内核直接走 root port reset）；`Fatal + Root Port link reset + device recovery failed`（或 BDF 从 `/sys/bus/pci/devices` 消失）= **硬件/PHY 层失效，非软件可修**；降级写只把 severity 由 Fatal 改 Non-Fatal 以便**继续观测**，不修复任何东西——降级后判读一律以 Status 位 + dmesg 链为准 |
| F117 | br-lan 与 10G LAN 口共用 MAC 导致管理面失联（2026-09-12 P2 第 2.7 项；**已实机复现（stock r0-f36067d，2026-09-12）**） | 10G WAN 与 10G LAN **同时插上**时 LuCI/管理页打不开（上网正常）；拔 WAN 也不恢复，必须换到 1G LAN 口才能进管理页。同族第二症状：lan2 拿不到 DHCP。社区三源独立报告（论坛 t/247242 #167 2026-08-15、恩山 8484725 #23188342 XFZAIMFQ「naoki66 与 YYH2913 两条构建线都中」、naoki66 issue #17 CLOSED）。**实机复核（`device-hw-probe.sh` route_e）**：`br-lan 00:58:28:d7:44:5d == lan1 00:58:28:d7:44:5d` → `VERDICT: CONFLICT`；`br-lan/brif = lan1 lan2 lan3 phy0.{0,1,2}-ap*`（lan1 为第一成员） | `files/etc/config/network` 的 `config device 'br-lan'` 只声明 `list ports`、**无 macaddr**；内核建桥时 br-lan 从**第一个成员口**继承 MAC → br-lan 与第一成员口 lan1 **逐字节同 MAC**（实机确证）。lan1 的 MAC 来自板级 DTS nvmem：`9001` 的 `&gdm2`（lan1）与 `&gdm4`（lan2）**都写 `<&lan_mac 0>`**（**不是**任务书所述的 `<&lan_mac 1>`/`<&lan_mac 2>`——按仓内 9001 原文与实机 MAC 均为 `<&lan_mac 0>`）；桥/PPE(hw flow offload) 下发往路由器自身（管理面 192.168.123.1）的帧在 L2 上被判为成员口本地流量、走入硬件卸载/FDB 路径被吞 → 管理面失联（上网路径不受影响）。**实机并见第二组重号**（另一项，本项未处理）：`eth0 == lan2 == lan3 == wan == 00:58:28:d7:44:5e`（1G 口继承 eth0 CPU 口 MAC）——若实机仍见管理/DHCP 异常，应连载此项一并审（见备注） | 新增 `files/etc/uci-defaults/98-xr1710g-brlan-mac-unique`（**迁移脚本，只在检测到冲突时改写**）：① 板型门 `gemtek,xr1710g-ubi`；② 用户已显式设 `network.@device[br-lan].macaddr` → 不动；③ 逐成员口比 MAC，**只有冲突才动手**（br-lan 未 up/无 MAC 视为冲突）；④ 派生 = 前 5 字节取第一个成员口（**Gemtek OUI 00:58:28 不变**）+ 末字节从 `0x02` 起取第一个不与任何成员口相同的值（末字节 0x02 使**整条地址的 bit1=1**（802 本地管理位即全 48 位的 bit1）→ 本身已是本地管理地址，故无需改首个八位组；改它会得到未分配 OUI `02:58:28`，是净损失）；⑤ `uci set` + `uci commit network` + `logger -t xr1710g-brlan`；⑥ 幂等（第二次零写入零 commit）；⑦ POSIX sh、`sh -n` 通过、**不新建 macaddr option 以外任何配置**。配套 `scripts/device-hw-probe.sh` 新增**只读** route_e：E1 `ip -br link`（回退 `ip -o link`）+ E2 各口 MAC + E3 **冲突/唯一**判定与逐口 CONFLICT/unique 标注 + E4 `br-lan/brif` 成员面 + E6 ethtool + E7 MAC 字节图（`LAN_MAC_PING=1` 才追加 lan1/lan2 → 网关 ICMP） | n/a（自持 overlay + 迁移脚本；DTS 根因改动风险高，用户明确选脚本路线，`patches/root/9001-xr1710g-dts.patch` 与 `files/etc/config/network` 默认 MAC **均未改**，避免影响所有镜像） | **本地行为自测 19/19 PASS**：`research-20260912/p2-verify/test-brlan-mac-unique.sh`（mock uci/board_name/logger/ip/cat）——板型不符/用户已设/无冲突三条路径**零写入**；冲突路径写 `00:58:28:d7:44:02`（0x02 被占时递增 0x03）；幂等重跑零写入；多 device 段写对 `[1]` 不误写 `[0]`；成员口全缺席不动；`bash -n scripts/device-hw-probe.sh` + `sh -n 98-xr1710g-brlan-mac-unique` 通过。**实机验证（stock r0-f36067d，2026-09-12，只读）**：route_e 判 `CONFLICT`；脚本路径预演（无 uci 写）`user macaddr=空` → `conflict=1` → 派生 `00:58:28:d7:44:02` → 与**现存全部 netdev**（含 4 个 AP 口）**零重号**，且与三成员口 lan1/lan2/lan3 互不相同（幂等成立）。**实机判据（待用户执行后回填）**：① 10G WAN + 10G LAN 同插时**管理页可访问**；② `ip -br link`（或 route_e）显示 br-lan 与 lan1/lan2 MAC **互不相同**、E3 判 `VERDICT: UNIQUE`；③ **lan2 单独接主机能拿到 DHCP**。**⚠ 应用后 br-lan MAC 变更 → 桥重建、DHCP/邻居缓存失效，需重连（管理面经 br-lan 时 ssh 会断一次，属预期）** |

| F118 | PPPoE 自动 IPv6 与显式 DHCPv6 双客户端互斥（2026-09-12 P2 第 2.2 项） | 本仓 `files/etc/config/network` 预置 `config interface 'wan6' { proto 'dhcpv6' }`，注释写"PPPoE 在 LuCI 面板改"。用户切成 PPPoE 后，PPP 的 `ipv6=auto` 会自动再建一个动态 DHCPv6（`ppp6-up` 的 `AUTOIPV6` 路径）→ **两个 `odhcp6c` 跑在同一 L3 设备**上，而 odhcp6c 的 ubus 对象名固定为 `odhcp6c.<设备>`（真源 `src/ubus.c` `ubus_init()`；`ifname` = `proto_run_command` 末参）→ 第二个 `ubus_add_object` 返回 **EEXIST** → 进程退出 → netifd 重启 → **IPv6 间歇不可用**（触发条件是本仓**已文档化**的操作路径，不是边角场景） | 上游 PPP/odhcp6c 未内建双客户端互斥；本仓预置 `wan6` 与 PPP 自动 IPv6 各自独立决策 | ① 新增 overlay `files/lib/netifd/xr1710g-dhcpv6-guard.sh`（`config_load`/`config_get_*` 全包在**子 shell**，不污染 PPP hook 的 JSON/env；只认同设备的 netifd 客户端，**绝不杀**无 `INTERFACE=` 的第三方 odhcp6c；`@parent` 间接引用带深度上限 16 防环）；② `patches/root/9056-xr1710g-dhcpv6-duplicate-guard.patch`（外层包裹 4 份内层：`ppp/patches/001` 在 `AUTOIPV6` 判定前 source guard → 清空 `AUTOIPV6`、`ppp/patches/002` `PKG_RELEASE` 2→3、`odhcp6c/patches/001` 在 `proto_dhcpv6_setup()` 入口检测重复 → `proto_notify_error DHCPV6_DUPLICATE_CLIENT` + `proto_block_restart`、`odhcp6c/patches/002` `PKG_RELEASE` 3→4）。**档位 default**：无冲突时两条路径与现状逐字等价，只在真冲突时生效；`odhcp6c` 上游无 `patches/` 目录 → 由外层 `new file mode` 直接创建（构建期按目录 glob 应用，无需脚本 mkdir） | n/a（orangeyoo 0103/0104 自持，未提上游 PR） | **基线核对（f0d3e332 真源 grep）**：`ppp/Makefile:13 PKG_VERSION:=2.5.3 / PKG_RELEASE:=2`、`odhcp6c/Makefile:11 PKG_RELEASE:=3` —— 与缓存 0104 基线**一致**（无需重算）。**实证**：外层 `git apply --check` ✓；抽取内层后 `patch -p1 --fuzz=0 --dry-run` **4/4 洁净**（0003 上下文按真源重算为 `@@ -72,6 +72,16 @@` / `@@ -15,6 +15,13 @@`，修正了缓存 0103 的 `@@ -85,6` / `@@ -16,6` 作者树行号）；`sh -n` guard 脚本 ✓；guard 逻辑本地 mock 回归 12/12（disabled/auto=0/异设备不误判、`@wan6x→@wan` 二级间接命中、纯环终止、无 `INTERFACE=` 第三方不触发）。**实机判据（待做）**：切成 PPPoE 后 `pidof odhcp6c \| wc -w` = 1、`logread` 无重复 ubus 注册失败、修正重复配置 + reconnect 后 IPv6 恢复；不回归——普通 PPP 自动 IPv6、独立 dhcpv6 接口各自单独存在时仍正常起 |
| F119 | AN7581 PCS RX-lock **诊断**（2026-09-12 P2 §2.1 吸收；naoki66 原作 / orangeyoo `0923` v1.6.1 抑噪版） | 本仓 F93（"PHY 宣告 10G、AN 落 2.5G"）与 F81（`#22397 LAN2 No FBCK Lock after 50 calibration try`）卡在**同一盲点**：PCS 无 carrier 时分不清"对端没信号"还是"CDR 没锁"，没有可判读证据 | 本仓无 PCS RX-lock 诊断（`grep -rln "rxlock_reset_count\|an7581_pcs_report_rxlock" patches/` 为空）；同域 9029（JCPLL TCLVAR recal）/9038（USXGMII RX CDR crossing）/9044（rate adaptation）治"能不能起来"，本项治"为什么起不来" | 新增 `patches/root/9055-xr1710g-pcs-rxlock-diagnostics.patch`（**#EXP**，内层 `target/linux/airoha/patches-6.18/9999-net-pcs-airoha-an7581-rx-lock-diagnostics.patch`）：`an7581_pcs_report_rxlock()` 打 PCS/Base-R/AN/SigDet/FreqDet/reset 寄存器，`have_rx_signal=false` → `action=no-signal`（每周期一次），有信号但 PCS 未 up → `action=cdr-reset` + `resets=` 自增（每次打印）；RX-lock recovery 行为不变 | n/a（作者自持；naoki66 20260903 release 的处置是整体移除该诊断，orangeyoo 是门控保留，本仓取第三种=携带抑噪版） | **抑噪是硬约束**：早期版每 poll 都打印，空置 WAN 口 ~2 行/秒刷屏，~8 分钟冲空 logread 环形缓冲、挤掉 hostapd 历史（orangeyoo 自述三因素之一）。实证：kernel 6.18.44 真源干净应用（pcs-an7581.c offset -49、`pcs-airoha.h` 插入点与 9992 改的 match-data 区不同行区）；**双向顺序共存实证**：`9999(RX-lock)→9992→9994` 与 `9992→9994→9999(RX-lock)` 两种顺序终态三文件 sha256 **完全一致**（`b61e5328…`/`5c9aad49…`/`a27e77d3…`）；全序内层实证（168 airoha 补丁 + 本仓新增内层按内核应用顺序）本项 ✓ fuzz=0。实机判据：① 拔线 `no-signal` 每周期 ≤1 条；② 插线不 lock 出现 `action=cdr-reset` 且 `resets=` 递增；③ 空置 WAN 跑 10 分钟 `AN down`/`no-signal` 各 ≤1 条且 hostapd 历史不被冲掉 |
| F120 | hwrng/TRNG 先开 SCU 时钟门再置 `RNG_EN\|RNG_OSC_EN`（2026-09-12 P2 §2.4 吸收） | 冷启动 hwrng 注册 `-ENODEV`（"Timeout waiting for Health Check"）→ 整机无硬件熵源；上游 `an7581/config-6.18` 实测 `CONFIG_HW_RANDOM_AIROHA=y`（本仓镜像该驱动**已启用**，竞态可达） | airoha-trng 在 SCU 总线/外设时钟门之后，而 `TRNG_NS_SEK_AND_DAT_EN` 的使能写（振荡器+环）发生在时钟门之前，且**本仓该驱动原本完全没有时钟门代码**（无 `RNG_OSC_EN`、无 `trng->scu`、TRNG 节点也无 `airoha,scu` 句柄） | 新增**成对**两补丁（**#EXP**，必须同档）：<br>· `patches/root/9058-xr1710g-hwrng-scu-clock-order.patch`（改 `drivers/char/hw_random/airoha-trng.c`：加 `RNG_OSC_EN`、SCU 门常量、`trng->scu` 句柄；`airoha_trng_init()` 里**先** `regmap_set_bits()` 开 BUS_CLK_GAT bit3 / PER_CLK_GAT_1 bit21 / PER_CLK_GAT_2 bit0，**再**写 `RNG_EN \| RNG_OSC_EN`）<br>· `patches/root/9059-xr1710g-hwrng-scu-dts.patch`（`arch/arm64/boot/dts/airoha/en7581.dtsi`：SCU 节点下加只覆盖时钟门寄存器段 `0x1e4…0x204` 的 syscon 子节点 `scu_clk_gat`，TRNG 节点加 `airoha,scu = <&scu_clk_gat>`；并给 `rng/airoha,en7581-trng.yaml` 补该属性——该绑定当前 `additionalProperties: false`） | n/a（上游 airoha-trng 无 SCU/时钟门处理；TRNG 绑定无 `airoha,scu`） | **前瞻依赖实证（关键）**：orangeyoo 缓存 `0921` 在本仓真源上**畸形/失败**——它假定 `RNG_OSC_EN` 与 `trng->scu` 代码已存在，而这两样来自**本仓未携带**的 yyh2913/fanboy `920-hwrng-airoha-fix-init-sequence-default-to-DRBG`（+45 行：SCU 支持 + DRBG 默认 + raw-mode DT）；本机 DTS 的 TRNG 节点也没有 `airoha,scu` → 单独落地=死代码+构建必红。故按**本仓真源重写为一份自洽补丁**（取 920 的 SCU 上下文 + 0921 的顺序修正，**不含** 920 的 DRBG 输出改动以免顺带改变熵质量/读取路径）。实证：kernel 6.18.44 真源 `patch -p1 --fuzz=0` 干净应用（9058/9059 内层）；全序内层实证 ✓。风险：作者自述"仍需两台机反复冷启动验证"，收益概率低（原评级 P3），故 #EXP。实机判据：**N≥20 次冷启动** `dmesg \| grep -c "airoha-trng"` 每次都有且无 `-ENODEV`；`cat /sys/class/misc/hw_random/rng_current` = airoha |
| F121 | MLO 编辑器隐藏 `mode=mesh` 接口（2026-09-12 P2 §2.3 吸收） | MLO 页把**所有** wifi-iface 列进可编辑的 AP/STA 网格；802.11s mesh 回程接口一旦被选中，会被通用 AP/STA 表单改写 `mode`/`ssid`/`network` → 6GHz 回程**静默断开**。本仓当前无 mesh 接口，但 `files/etc/config/wireless` 头注释已写"EHT320 回程/802.11s 见 ROADMAP 漫游优化计划"——一旦按计划开 mesh 就是静默陷阱 | 页面 `GridSection` 无 mode 过滤；无 mesh 提示 | 新增 `patches/root/9057-xr1710g-mlo-hide-mesh.patch`（**default**；内层装到 `luci-app-mlo/patches/`… 实为 `luci-app-mlo/patches/001-xr1710g-mlo-hide-mesh.patch`）：① `s.filter = section_id => optionValue(section_id,'mode') != 'mesh'`；② 存在 mesh 接口时页面顶部插 `alert-message notice` 提示，指向 Network - Wireless（唯一受支持的 mesh 编辑入口） | n/a（luci-app-mlo 为本仓 9017 携带的第三方包，上游无此页面） | 基线**逐字吻合**：本仓 9017 内 `luci-app-mlo/htdocs/luci-static/resources/view/mlo.js`（693 行）在 `sectiontitle` 的 `};` 之后紧接 `quickAdd = function(mode) {`，与 0660 插入点完全一致。实证：抽取该 mlo.js 后 `patch -p1 --fuzz=0` 两个 hunk 干净命中（450 后 / 688 后）。顺序：必须置于 `9017` 之后（本补丁改 9017 生成的文件）。判据：构建后 `grep -c "mode') != 'mesh'" luci-app-mlo/htdocs/luci-static/resources/view/mlo.js` = 1；实机造 `wireless.mesh6`（`mode=mesh`）后打开 MLO 页面：网格**不出现**该接口且有提示；`uci show wireless.mesh6.mode` 仍 `mesh` |
| F122 | **SOE 主体（B 系列）：AN7581 inline IPSec 硬件卸载的驱动层**（2026-09-12 P2 §2.9 吸收；hurryman2212 `@offload-oc`） | 本仓 F52（issue #6）的终点：让 AN7581 的 **SOE（Security Offload Engine）** 直接做 inline IPSec（含 NAT-T）——XFRM state 经 nft flowtable 下发 PPE/SOE，数据面由硬件加解密，而不是让 EIP93 当通用 crypto 设备。A 系列（xfrm packet-offload 框架）与 D 系列（EIP93 动态软回退）本批已落地，B 是最后一块 | 本仓无 SOE 驱动（`grep -rln "airoha_soe\|NET_AIROHA_SOE" patches/` 仅命中本批新增）；缺 SOE/PPE IPsec 寄存器定义、SOE 状态机与数据面、PPE 接口、flowtable XFRM 元数据、autonomous flow offload | 新增 **19 个 `#EXP` 捆绑补丁**（`patches/root/9999-net-airoha-soe-10..28-*.patch`，内层同名装到 `target/linux/airoha/patches-6.18/`，按 NN 排序保证内核应用顺序）：DT binding · MAINTAINERS · dtsi SOE 节点 · SOE/PPE IPsec 寄存器 · SOE state/数据面元数据 · SOE-PPE 驱动接口 · 以太网数据面集成 · flow_offload XFRM action · flowtable 携带 XFRM route 元数据 / 保留 XFRM input state / 发现 XFRM state / 下发硬件 action / XFRM flow 不进软件路径 / 接纳已认证 flow / ctx 透传 · **autonomous SOE XFRM flow offload（53KB）** · **SOE XFRM packet offload（110KB，新增 `airoha_soe.{c,h}`）** · 构建 provider · Kconfig | n/a（作者自持，未提上游 PR；同作者 hurrian = 上游 PR #23644 作者） | **重基（本项最大工作量）**：作者基线 ≠ 本仓真源——① `airoha_ppe.c` 的 L2/subflow 重构在本仓由 `9039` 内层 `9990` 提供（作者用了 `wdma_*` 命名，仓库用 `bridge_*`）；② `airoha_eth.c` 的 QDMA start/stop helper 化在本仓由 `yyh2913 179` 提供（引入 `airoha_queue.flushing`，是 B14 的上下文前提）；③ flowtable 侧真源无 `net/xfrm.h`、函数名是 `nft_flow_route()`（非 `nft_flow_route_routing()`）、`nft_dev_forward_path()` 返回 void。19 个补丁逐个按真源重写上下文/锚点（25 另有 2 个 hunk 在"真源+9990"上重锚），逐条处置见 `research-20260912/inner/soe-rebased/*.patch` 顶部 `# Reason:`。**实证**：真实构建顺序（airoha 全序 168 + 179 + 9039/9990 + 9053/9054 内层 + 本 19 个）上**逐个 `patch -p1 --fuzz=0` 全部干净应用、0 FAILED**；仓库级 `audit-patches` **103/103**、`apply-patches --dry-run --oc --experimental` **103/0/0** + 拷贝类 **4/4**。可复现脚本：`research-20260912/inner/soe-basetree.sh`、`gen-25-real9990.py`。**判据（实机）**：① experimental 构建通过；② `ip xfrm state add` 后 devmem/debugfs 看 SOE/PPE 计数增长；③ `authenc(hmac(sha256),cbc(aes))`（硬件）vs `rfc4106(gcm(aes))`（软回退，由 F115/9054 保证）；④ iperf3 over IPsec 吞吐 vs 软件路径；⑤ 开 IPsec 卸载后普通转发/无线不回归。**注意**：干净应用 ≠ 可编译/可跑；SOE 是否真跑起来取决于 NPU/SOE 固件版本与 dtsi SOE 节点，必须实机构建验证 |
| F123 | fanboy `ubi2-oc` 重基（2026-09-12 P4；上游 force-push `f08c3d1e`→`9631414c`，22 commit 中 3 变 1 增） | ① `05` 包 `bridge-flow-offload`→`bridge-hw-offload`（hotplug/apply-rules 收进包 + 新增 `files/etc/init.d/bridge-hw-offload` procd `procd_add_reload_trigger firewall`），且上游 `apply-rules.sh` **已内置** `flow_offloading_hw` 守门；② `06` 内层 `675-02` 的 VLAN/PPPoE encap 由覆写 `info.encap[0]` 改为**追加** `info.encap[num_encaps]` + `NF_FLOW_TABLE_ENCAP_MAX` 越界检查；③ apps pack `00ed581b75`→`0a4f2632de`（flowsense 1.1.7→**1.1.9-r5**、新增 `npu-monitor.settings.air_eff`、status.js 罗盘→CPU-gauge）+ `8bf1c9253c`（删 VLAN/PPPoE 徽章、`set_flow_offload` 发 `config.change`） | 上游 force-push 重基（非 bug）；本仓 `9043` 守门被上游内置 → 冗余；`9030`（1.1.8-r5 bump）被新 pack 超集化 | ① `05`→`2f4fccb1`（**target.mk hunk 改写为末尾追加**：上游以内嵌 `kmod-crypto-hw-eip93` 行为上下文，与本仓 `02`(#EXP) 顺序耦合）；② `06`→`3c8d3e6664`；③ `9024` 重写为 `bridge-hw-offload` 路径 + 表名 `flow_offload`（**保留本仓独立表策略**，避开用户 `bridge fw4`）+ 去 reload `&`；④ `9043` **整条删除**；⑤ `9030` **删除**；⑥ `9017` 按新 pack 重做 19-core 切片（保持无 `root/etc/init.d/fan`＝F67/A4、jitter `223.5.5.5`、F88 RXHWF_FAST 重放）；⑦ `9019`/`9022` 按新 pack 基线重基（保留 pack 的 MLD 多频段 `if/if/if` 语义）；⑧ 静态符号 `bridge-flow-offload`→`bridge-hw-offload`（`scripts/build.sh`、`.github/workflows/build.yml`、`config/seed-config.diff`、MANIFEST/ORDER） | n/a（vendor 漂移吸收） | **实证**：`audit-patches --oc --experimental` **102/102**；`apply-patches --dry-run --oc --experimental` **102/0/0** + 拷贝类 **4/4**；`06` 内层 675-01/02/03 对 6.18.44 真源 `patch -p1 --fuzz=0` 三件全绿（终态 `net/netfilter/nf_flow_table_path.c` 含两处 `info.num_encaps < NF_FLOW_TABLE_ENCAP_MAX` 追加 + `br_vlan_get_offload_info_rcu`）；`9057` 内层对新版 mlo.js `--fuzz=0` 命中（`mode') != 'mesh'` 计数 = 1）。**顺带修复**：原 `9022` 的 `band_unb` awk **多一个 `}`**（awk 语法错误，仅 `$entries_file` 存在时触发；已纠正并复核三个 awk 程序 rc=0）。**实机判据（待）**：开关 `flow_offloading_hw` 后 `nft list table bridge flow_offload` 随之出现/清空、无需 reboot；flowsense 页面为 gauge（无 `◄BND`）、无 VLAN/PPPoE 徽章、`air_eff=80` 生效；F88 CDM1 百分比仍含 `rx_hwf_fast` |
| F124 | 上游**已自带** airoha NPU `924-use-coherent-mailbox-dma`（2026-09-12 P3 前置核对，纠正 P3 handoff 误判） | P3 handoff §1/§5.1 把上游 openwrt main（`f0d3e332`）里**已存在**的 `target/linux/airoha/patches-6.18/924-net-airoha-npu-use-coherent-mailbox-dma.patch` 当成"dryrun 树污染（来自别处）"，并据此要求重建干净树 | 上游 2026-08 已收录 Daniel Pawlik 的同款修复（`6f884eb87a79` 把 `airoha_npu_send_msg()` 改 `DMA_BIDIRECTIONAL` 后 EN7581+MT7996 probe 回退 → 改为 per-core 复用 device-managed coherent bounce buffer；`Fixes: 6f884eb87a79`；patchwork 2026-08-05/08-06 两版），与本仓 `git ls-tree HEAD:target/linux/airoha/patches-6.18/` 逐字相符 | **无需吸收 YYH 的 `924-use-coherent`**（已上游）；P3-A 仍需 YYH `923-recover-stalled-cores`（`airoha_npu_recover()`）+ `924-fix-watchdog-work-lifetime`（`airoha_npu_wait_recovery()`）+ `925`/`926`/`927`/`928`/`929`。**号段冲突**：上游占 `924`，本仓承载这些内层补丁时必须避号（逐号对账后再定号） | merged（openwrt main `f0d3e332`） | 教训：P3 §6.3 第 9 条（跨层先证明符号存在）之外，**还须先核对上游是否已收录同款内层补丁**——一行 `git ls-tree HEAD:target/linux/airoha/patches-6.18/ \| grep <号>` 即可避免重复携带；P3 handoff 的"污染告警"已作废，`research-20260912/dryrun/owrt` 实为干净的上游树（HEAD == 上游 main） |
| F125 | airoha NPU mailbox 可靠性六项（2026-09-12 P3-A 吸收；YYH2913 `xr1710g-6.18-integration` 923..928） | ① NPU watchdog 只抓 coredump、不重启停滞的 firmware core（mailbox 用户只能等满超时，无线卸载不重启整机无法恢复）；② watchdog work 生命周期：devres 解开时先 free IRQ 再 sync work（probe 失败/remove 竞态），且无法等待进行中的 recovery；③ recovery flag 只在 core 0 锁下更新，多 mailbox core 后成数据竞争；④ `airoha_npu_get()`/`airoha_ppe_get_dev()` 用 `DL_FLAG_AUTOREMOVE_SUPPLIER` → Wi-Fi consumer unbind 后 device link 残留；⑤ `wlan_irq_status_get()` 返回整个 `REG_IRQ_STATUS`，mt76 handler 写回 W1C → 一个 ring 清掉另一个 ring 的 pending（丢中断）；⑥ mailbox 先自增 `MBQ0_CTRL2`（固件视作门铃）再写 `CTRL3` → 固件消费上一轮命令，probe 读固件版本 **0.0**、PPE/WLAN 卸载请求被拒 | 上游缺对应修复；⑥ 是 probe 0.1111 的关键之一 | 新增 `patches/root/9061-xr1710g-airoha-npu-mailbox-recovery.patch`（**#EXP**，单包裹 6 内层 `9999-30..35`：recover-stalled-cores / fix-watchdog-work-lifetime / close-recovery-sync-race / fix-offload-provider-device-link-lifetime / acknowledge-only-requested-WLAN-RX-IRQ / publish-mailbox-command-before-doorbell）。**提供 `airoha_npu_recover()` + `airoha_npu_wait_recovery()` —— P3-B（mt76 `0147`/`0155`）的硬前置**。同族 `924-use-coherent` 上游已收录 → 不携带（F124）；YYH `929`（低地址 coherent pool）**未吸收**——它要求 DT `shared-dma-pool` 名为 `mbox`，而本仓/上游均无该 reserved-memory 节点，直接落地会使 probe `of_reserved_mem_device_init_by_name` 失败（详见备注） | n/a（作者自持） | **重基**：本仓真源已含上游 924-use-coherent（`__airoha_npu_send_msg` + per-core coherent buf），故 923 的 `send_msg` 两个 hunk 由旧 `dma_map_single` 上下文重锚到 `__airoha_npu_send_msg`。**实证**：linux 6.18.44 真源 + 本仓 airoha 内层全序（含 9990/9995/9996/9997/9998）之上，6 个内层按序 `patch -p1 --fuzz=0` **全部成功**；`audit-patches`/dry-run 见备注。**实机判据（待）**：probe 时 `WLAN_FUNC_GET_WAIT_NPU_VERSION == 0.1111`；watchdog 触发后无需重启即可恢复；NPU offload 绑定成功。**⚠ 929 的后续条件**：若实机出现“probe 固件版本 0.0 且 928 已修仍复现”，需先按 2GiB DDR 布局在 `en7581.dtsi`/板级 dts 划一块低于 `0xbfffffff` 的 `shared-dma-pool`（名 `mbox`）再吸收 929 |
| F126 | eip93 动态回退的**打包侧配套**修正（2026-09-12 补 `9060` 的 KCONFIG 回归） | 实验档内核编译期 kconfig 报未决符号并中断：`Max skcipher software fallback length (CRYPTO_DEV_EIP93_GENERIC_SW_MAX_LEN) [256] (NEW)` → `scripts/kconfig/Makefile:85: syncconfig Error 1` （CI run 34687410355） | 我先前误判「两个静态阈值已被动态回退取代」而把它们从 `modules/crypto.mk` 的 `KCONFIG` 里删除（照搬作者 commit `96247057` 的改法，但那是按其自身树）。本仓基线上这两个符号是 `int` + 有 prompt，删除即产生未决符号；且驱动 `eip93-cipher.c:186` 仍读取 `GENERIC_SW_MAX_LEN` | `patches/root/9060-…-crypto-eip93-package-deps.patch`：**KCONFIG 原样保留**（4 行），只加 `+kmod-crypto-dynamic-fallback`；注释记明判据 | n/a（本仓打包侧） | 实证：openwrt/openwrt master（blob `c7b782e9`）该段原生即 4 行；对 master 真实 `crypto.mk` `git apply --check` 通过、应用后 KCONFIG 4 行完整。CI run 34687410355 的补丁层已全绿（`Hunk FAILED: 0`），仅此配置项致败 |
| F127 | mt76 NPU 生命周期九补（2026-09-12 P3-B 吸收；YYH2913 0147..0162） | ① NPU hw init / reserved-memory / stop 握手遇 mailbox 超时或 recovery 竞争时直接放弃（probe 静默退化为无 NPU offload；SER 只恢复部分状态）；② NPU RX IRQ 是 device-managed 但绑在 Wi-Fi PCI 设备上，remove 后仍注册而 DMA/NAPI/page pool 已拆 → in-flight completion 重新使能中断线；③ NPU offload 下 `dma_dev` 改为 NPU，`dmam_alloc_coherent()` 把描述符/TXD 挂到 supplier → 重载 Wi-Fi 驱动泄漏；④ NPU RX queue teardown 不完整；⑤ NPU RX 环预留一个空描述符 + 先推 `q->tail` 再 refill，让"已被消费、page 已归 skb"的槽位对 NPU 可见；⑥ NPU TX 无 prefetch 余量、不验 host-owned → 可能覆写 firmware 仍在读的描述符；⑦ host 重启时 MT7990 仍供电发事务 → 下次启动 HIF 枚举失败；⑧ `mt76_dma_init()` 跳过的 WED RRO 队列在 WED 未激活时被 `mt7996_dma_rro_start()` 注册了 NAPI，清理侧只看 WED 标志→ NAPI 实例被留在 `free_netdev()`（上游 revert `332bbe9b2784` 后的真实缺口）；⑨ 原顺序下 NPU stop 握手在 mac80211 teardown 之后 → 超时 + 后续 mailbox 持续无响应 | 上游 mt76 无对应提交；⑧ 是上游 revert 之后留下的缺口（0161 的 `Fixes:` 直指被 revert 的 commit） | 新增 **9 个 `#EXP`**（`patches/packages/mt76-0147/0153/0154/0156/0158/0159/0160/0161/0162-*.patch`，按文件名 glob 序应用＝依赖序）：**0147 = YYH 0147+0155 合并**（`mt7996_npu_prepare_retry()` 为唯一 retry 入口：`-EAGAIN` → `airoha_npu_wait_recovery()`、`-ETIMEDOUT` → `airoha_npu_recover()`，不循环）；0153 NPU RX IRQ teardown 排序 + `mt76_npu_deinit` 的 RCU 顺序；0154 NPU DMA 内存挂 Wi-Fi consumer；0156 完成 NPU RX queue teardown；0158 满环填充 + 发布移到 refill；0159 NPU TX 5 描述符余量 + host-owned 校验；0160 `pci_driver.shutdown` quiesce；0161 `dev->napi_registered` bitmap；0162 unregister 前先 stop NPU。**0157 不吸收**（与本仓 0011 同处且被其覆盖：0011 有实机复现证据 + clamp 容错）；0130/0151 跳过（已由 0014 覆盖）。**硬前置 = root/9061**（`airoha_npu_recover`/`wait_recovery`） | n/a（作者自持） | **本地增补三处（均已落地）**：① **0154 泄漏修复**——`devm_add_action_or_reset()` 失败时补 `dma_free_coherent + put_device + kfree`；② **0161 重基**——上游 `6d1c6a75` revert 后 `mt76_dma_cleanup()` 已改为 `mt76_for_each_q_rx` + 无条件 `netif_napi_del`，按新形态重写 dma.c hunk#2（只对 `test_bit(i)` 为真的队列做 del，正是 revert 所要求）；③ **0159 stub 配对核对**——`mt76_queue_is_npu_tx()` = `q->flags & MT_QFLAG_NPU`，该 flag 仅由 `mt7996_init_tx_queues()` 在 `mt76_npu_device_active()` 为真时设置 → flag 真 ⇒ NPU 代码已编译且活跃 ⇒ 走真实实现；非 NPU 构建 flag 恒 0 → 短路，stub 不被求值。**不存在“非 NPU 构建全丢包”路径**。**实证**：`audit-patches --oc --experimental` 99/99（含 SOE-16..28 停用后的状态）；`apply-patches --dry-run --oc --experimental` 全绿 + **拷贝类 mt76 28 个文件**在真源 `be5ce791` 上按 glob 序全部 `patch -f -p1` 成功（含新增 9 个）；本地 mt76 全序树逐个 fuzz=0。**实机判据（待）**：`rmmod/insmod mt7996e`（NPU 活跃）20 轮无 `NAPI instance ... already registered`/`not been added`；`reboot` 20 次 Wi-Fi 必枚举；NPU 活跃下反复 reload 无 coherent 泄漏；高负载（>1Gbps + 6GHz 320MHz）12h `HOSTADPT_API_Q_FULL` 不增长、RX 环 head/tail 不漂移 |
| F128 | mt76 候选甄别（2026-09-12 P3-C） | YYH 的 7 个 mt76 候选逐条判定：① `0047`（RRO 3.0 RX 数据投递队列）被 handoff 列为"候选·需重基"，**实际 be5ce791 已含同款**（`mac80211.c` 的 `mt76_rx_release_amsdu()` 已有 `mt76_queue_is_wed_rro_data(rxq) && dev->hwrro_mode == MT76_HWRRO_V3 → q = MT_RXQ_RRO_IND`）→ 冗余，不携带；② `0080`（check ddone for tx queues）捕获版本里 `MT_QFLAG_CHECK_DDONE` **只被定义和读取，从未被置位**（`mt7996/dma.c` 仅 `=`→`|=`，`MT_NPU_Q_TX` 宏不含该位）→ 死代码 + 作者自述"未遇到问题，只是对齐 logan 配置，预防潜在 issue"，且 dma.c hunk 对真源 FAILED → 不携带，待有置位版本再收；③ `0038`（wcid→wcid_idx）改 `struct mt76_rx_status` 语义、4 文件 ~50 行，MTK 私有改法与主线演进易冲突（handoff 自评风险中-高）→ 暂缓；handoff 的"只改 mt76_rx_aggr_reorder 一处"缩小版仍需在 `struct mt76_rx_status` 里加 `wcid_idx` 字段才能重取 wcid，仍动结构体，故本轮不做。**吸收 4 项**：`0018`(=YYH 0010，避号)/`0021`/`0101`/`0145` | ① 已上游（openwrt/mt76 `be5ce791`）；② n/a（作者自持）；③ n/a（MTK 内部树） | 载体：`patches/packages/mt76-0018`（#EXP，避 0010 号冲突）/`mt76-0021`（#EXP）/`mt76-0101`（**default**，上游 next 线已含，pin bump 时复查删除）/`mt76-0145`（#EXP） | ① merged（`be5ce791`） | **实证**：`apply-patches --dry-run --oc --experimental` + 拷贝类 mt76 真源应用全绿（含新 4 项，glob 序 0018<0021<0101<0145<0147）；本地全序（含 vendor/07 内层 0014）逐个 fuzz=0。**教训**：判候选是否可吸收时，必须用**本仓 pin 真源**跑 `patch --dry-run` 并**看 "Reversed (or previously applied)" 提示**（只 grep FAILED 会漏报已上游） |
| F129 | 无线栈缺口：mac80211 8 项吸收 + hostapd 4 项暂缓（2026-09-12 P3-D） | ① mac80211（YYH 40 条）：本仓只携 411 一条；精选 397（STA CSA punct bitmap）/399（bootup CAC 删接口 call trace）/400（non-TX BSS 解析）/405（partner link CSA finalize）/406（CSA 期间不按旧信道 beacon 更新信道）/407（direct CSA mbss finalize）/409（MLD AQL BMC）/410（`nl80211_set_wiphy_radio` 把 RTS 误置 0）；② hostapd（本仓 **0 条**，明显缺口）：603（6GHz 不需 DFS，同 Gilly 015）/905（STA identity 缓冲区溢出，安全）/901（6GHz ACS）/906（HT40 副信道） | ① MTK 内部树/上游风格（未进 backports-7.2）；② hostapd 侧为社区刚需 + 安全修复 | ① **已吸收 8 条 `#EXP`**：`patches/packages/mac80211-397/399/400/405/406/407/409/410-*.patch` → 装到 `package/kernel/mac80211/patches/subsys/`（glob 序 392 之后、411 之前）；410 已按 backports-7.2 重基（去掉作者树多出的 `const struct wiphy_radio *radio;` 上下文行）；② **hostapd 4 项未吸收**（见备注） | n/a | **mac80211 实证**：`audit-patches` **111/111**；`apply-patches --dry-run --oc --experimental` 全绿；拷贝类 mac80211 subsys **29 个文件**（上游 20 + 本仓新增 8 + 411）在 backports-7.2 上按 glob 序全部应用成功。**hostapd 暂缓原因**：本宿主无法获取 pin 源（`PKG_SOURCE_URL=https://w1.fi/hostap.git`，`PKG_SOURCE_VERSION=831364bf02710ad09c2f27d3efa92abeeb5634c0`，2026-08-07）——w1.fi TLS 连接失败、GitHub 无同 SHA 镜像；按 F20/F21 铁律（拷贝类补丁必须真源 `patch -p1` 实证）**不盲上**（未实测的拷贝补丁会在构建期直接红）。**后续执行计划**：① 给 `scripts/verify-copy-patches.sh` 的 `prep_src` 加 hostapd 映射（git 源：按 `PKG_SOURCE_VERSION` 拉 hostap.git）；② 在可联网环境跑 verify；③ 通过后 603/905 倾向 default（6GHz 可用性 + 安全）、901/906 `#EXP`；④ 补 `config/seed-config*` 与 MANIFEST。候选文件位置：`research-20260912/repocmp/suntyrael/rest/package__network__services__hostapd__patches__{603,901,905,906}-*.patch` |
| F130 | 上游守护扩面：远端来源 tip 监视（2026-09-12 P3-F） | `scripts/audit-upstream-watch.sh` 原只监视 **上游 openwrt main 的 6 个 blob + 7 条同址冲突**，而本仓补丁的一大来源是**多个 fork 分支**（YYH/fanboy/Gilly/naoki66/hurryman）——它们 tip 变化时不会让任何本地检查变红，却会让“已覆盖/新增”判定过期（P3 整轮就是因为 suntyrael 只是 YYH 的重传载体才误判追踪对象） | 守护面无“来源分支 tip”维度 | **已实施** `scripts/audit-upstream-watch.sh` 新增 `REMOTE_TIPS` + `remote_report()`：7 个来源（YYH openwrt@xr1710g-6.18-integration / YYH mt76 / YYH http-uboot / fanboy OpenW1700k@ubi2-oc / Gilly Gemtek-W1700K-6.18 / naoki66 ImmortalWrt-for-Gemtek-XR1710G / hurryman OpenW1700k-test@offload-oc）；`git ls-remote` 带 `timeout`（`AUDIT_REMOTE_TIMEOUT`，默认 20s）；**离线/超时只打 `ⓘ` 不计失败**；记录 tip 固定者 tip 前进计 `drift`（`--fail-on-drift` 即 exit 1）；未固定者只打印当前值 | n/a（本仓 CI/脚本自持） | 实证（2026-09-12 17:2x）：`bash -n` 通过；对 `research-20260912/dryrun/owrt` 运行 **exit 0**，6 基线 ✓ / 7 冲突探测 ✓ / 7 远端 **全部 `✓ 未变`**（即 P3/P4 handoff 记录的 tip 快照仍有效）；汇总行新增“远端跳过 N”可区分“真未变”与“未联网” |
| F131 | P3-E 非 mt76 残余对账（2026-09-12；YYH 291 项 → 真残余清单） | P3 handoff 的 §5 分组是**人工启发式**，需一次可复核的**机器对账**产出“到底还剩什么未覆盖” | ① 纯 `git patch-id --stable` 集合比对失败率极高（本仓内层多为重基/重写，上下文不同 → id 不同）；② 即使 id 命中（9 条）也不能回答“未命中的哪些只是重复” | **方法**：提取本仓全部外层补丁的内层（`research-20260912/inner/extract_inner.py`，99 个）→ 与 YYH `rest/` 209 项各算 patch-id（命中 9：本批 mac80211 7 项 + SOE-27 + eip93-9911）；再把 YYH 的 **airoha+generic 147 项**逐条 `patch -p1 --fuzz=0 --dry-run` 打到“本仓 airoha 内层全序树”上，按 `Reversed→COVERED` / `rc=0→APPLIES` / 其它 `CONFLICT` 分类：**COVERED 28 / APPLIES 20 / CONFLICT 99**。**实残余（APPLIES 且真新增）已吸收 3 项**：新增 `patches/root/9062-xr1710g-eip93-reliability.patch`（#EXP，内层 9999-40..42 = YYH backport-6.18 9915/9916/9918）。**已经甄别为“不携带”**：9910（纯注释：13 文件 email 少 `>`）/9912（ECONET 选型）/9990 hack（AES fallback 阈值，已被 9054 动态回退取代）/675-01（= fanboy/06 内层，命名不同）/9991+9999-03 bonding（已被 9053/9999-soe 族覆盖）/9999-07（已被 SOE 19 块取代）/929（需 DT `mbox` pool，F124/F125 已记）。**仍留观察（本批不吸收，原因各自）**：`914/203/220-11/910-02`（PCIe PERST/USB2 SerDes——需与 F56/F116 实机 AER 联动验证）、`624`（PCS TXPLL regmap 字段——与 9029/9038/9044 同域，需 10G 对端实测）、`0401`（pmdomain 回退到 PLL 寄存器——与 OC/`vendor/03` 同域，需实机）、`310-02`（XSI deassert——需 F92 ring 类回归）、`330`（device_set_node 助手，低成本但无实机驱动力）、`920-DRBG`（我们 9058/9059 故意不含其 DRBG 输出改动，避免顺带改熵质量） | n/a（作者自持/MTK 内部树） | **证据留存**：`research-20260912/p3e/kernel_class.tsv`（147 行三态）、`ours.ids`/`yyh.ids`、`residual-applies.txt`（20 项）。**方法学教训**：patch-id 只能判“逐字相同”；对重基过的补丁必须用“在已含本仓补丁的树上跑 `--dry-run`”分类（`Reversed` 是“已覆盖”的强信号）。CONFLICT 99 项中包含大量“因本仓重建/上游漂移而上下文不匹配”的重复项，不应当作“待吸收”（handoff §5 已逐组给出归并结论） |
| F132 | SOE-16 重建损伤：四处 `-Werror` 编译错误（2026-09-12 P6） | CI run `34700107822`（合并后 `e7d1f64`）已越过**整个补丁层**（`Hunk FAILED: 0`）与 **kconfig**（eip93 未决符号已修），红在内核编译：`airoha_eth.c:1234 unused variable 'reason'` / `:1453 implicit conversion to 'enum pkt_hash_types'` / `:1189 'airoha_qdma_rx_check_stall' defined but not used` / `:163 'airoha_qdma_set_rx_irq' defined but not used` | **P5 手工重建 SOE-16 内层时删错行**（作者 `soe16-author.patch` 含同样的 `reason` 缺陷，属沿用）：① 删掉了 `reason = FIELD_GET(AIROHA_RXD4_PPE_CPU_REASON, msg1)` 与其 PPE 回退块，**却保留了 `reason == PPE_CPU_REASON_HIT_UNBIND_RATE_REACHED` 的读取**——不只是 `-Werror`，是**读未初始化变量**；② `skb_set_hash` 第三参数被写成 `false`（应为 `PKT_HASH_TYPE_L4`）；③ 丢掉了 `9045` 的 RX 停滞恢复检测调用 `airoha_qdma_rx_check_stall(q)`，使该 helper 成为未使用静态函数（功能静默退化）；④ `airoha_qdma_set_rx_irq` 唯一调用点位于 `#if IS_REACHABLE(CONFIG_NET_AIROHA_SOE)` 内，非 SOE 配置下必为未使用；⑤ 重建还掉了 `if (!(desc_ctrl & QDMA_DESC_DONE_MASK))` 的花括号，留下孤立 `}` | **修订既有载体**（不改文件名/编号）：`patches/root/9999-net-airoha-soe-16-integrate-soe-eth-datapath.patch`。改法：把被误删的行由 `-` 改回**上下文**（不引入重复逻辑，故 compile 后语义 = vendor 语义），`airoha_qdma_set_rx_irq` 加 `__maybe_unused`，补回花括号；**重算全部 hunk 头**（本 hunk 新侧 +10，其后 23 个 hunk 的新侧起点随之 +10） | n/a（SOE 补丁作者自持；损伤系本仓重建引入） | **实证**：`audit-patches` **113/113**；`apply-patches --dry-run --oc --experimental` 全绿 + 拷贝类 **4/4**（41 文件真实应用）；外层 wrapper 与内层**逐字节一致**（`extract_inner.py` round-trip）；大 hunk 新侧花括号 **29/29 平衡、最大负深度 1**；改法差异仅 **27 行**（逐一为上述修点 + 重算头）。**编译结论以 CI 为准**：修复轮 run `34705859414`（提交 `5f8876a`）。**证据留存**：`research-20260912/p6-soe16/`（`extracted/` 原始内层、`fixed-inner.patch`、`fix_inner.py` 含断言自检、`wrapper-fixed.patch`、`fix/` 参考目标态）；失败日志 `research-20260912/fail-e7d1f64.log`、`logs-e7d1f64/job.log` |
| F133 | fanboy 上游 force-push 后漂移吸收：HSUART 波特率修复 + 远端锚点（2026-09-12 P6） | `sync-upstream` 自 `e7d1f64` 起**每次 cron/push 都红**：`--fail-on-drift` 报 `OpenW1700k.git refs/heads/ubi2-oc` tip 前进（记录 `9631414c` → 实际 `b1f669b68`）。该守卫本身正确（P3-F 建立的 7 来源 tip 监视），是本仓记录的锚点过期——`9631414c` 是 P4 重基时的快照，fanboy **习惯性 force-push 重基**（P4 就是被它逼着重基的） | ① 上游 force-push（旧 tip 不是新 tip 祖先，确认为重基）；② 远端锚点是**静态快照**，没有"重基后重新锚定"的流程，于是第一次真漂移就把 CI 打红 | 新增原料桶 `patches/vendor/fanboy/21-hsuart-baud-calculations-4e2b80ff.patch`（git format-patch 原样，`default` 活动项，紧随 11 号——二者父提交同主题）；`patches/MANIFEST`/`ORDER` 注册；`scripts/audit-upstream-watch.sh` 远端记录 tip 更新为 `b1f669b68` | n/a（vendor 漂移吸收） | **甄别（逐 commit patch-id，两侧各 30 个）**：**29 个纯重基（内容未变）+ 1 个真新内容**。纯重基例证：T20 `20-oc-governor-200mhz-ed7cbc80.patch` 的 patch-id `5724590e` 与新 tip 的 OC commit **完全一致**（仅文件名 hash 过期，内容无需动）。**唯一真新增**：`airoha: fix HSUART baud calculations`（`4e2b80ff`，Matt Merhar）——`an7581/an7583 dtsi` 的 hsuart `clock-frequency` `7372800→20000000`、uart3 `compatible` 改 `en7523-hsuart`，并同步改写 `patches-6.18/886-uart-add-en7523-support.patch`；commit message 实测：修前 ttyS2 请求 460800 实得 921600（HSUART/GPS 串口波特率错乱）。本仓此前**未携带**（`patches/` 全树 0 命中 `en7523-hsuart`/`clock-frequency = <20000000>`，20 支 fanboy 桶无一触及 hsuart/dts）。**实证**：HSUART 补丁对真实上游树 `git apply` 全成功（`an7581.dtsi` hunk 偏移 -10 行）；`audit-patches` **114/114**（113→114）；`apply-patches --dry-run --oc --experimental` 全绿 + 拷贝类 4/4；`audit-upstream-watch --fail-on-drift` **exit 0**（基线漂移 0、路径缺失 0、7 远端全"未变"）。**留下的机制问题**：远端锚点仍是静态快照，下次 fanboy 再 force-push 会**再次打红**——届时应先跑一次 patch-id 比对：若仍为纯重基，只需重新锚定；若含真内容，按本行流程吸收。若该噪音不可接受，可选择把远端 tip 降为 informational，但那是**降级**，须在台账写明 |
