# 修复台账（FIXES）——"修复而不是降级"的执行记录

> 每遇问题：记一条。列：问题 → 根因 → 修复（本层补丁/上游） → 上游状态。上游合入后删除本层补丁并标「已上游」。
> 状态：`carried`（本层携带） / `upstream-open`（上游有 PR 待合） / `merged`（上游已合，删除补丁） / `n/a`（不打算上游）。

| # | 项目 | 问题/背景 | 根因 | 本层载体 | 上游状态 | 备注 |
|---|---|---|---|---|---|---|
| F01 | XR1710G 板级支持 | master 无 xr1710g 文件 | #22397 未合入 | **已对 master 重建**（2026-08-17）：`patches/root/9000-xr1710g-common.patch` + `9001-xr1710g-dts.patch` + `9002-xr1710g-uboot.patch`。与 PR 的偏离：① 不做 common-dtsi 重构（w1700k dts 保持上游原样，冲突面最小）；② 网口命名按自用决策（WAN=1G-1，PR 默认 10G-1=wan）；③ uboot 补丁命名 1000-（master 已有 998/999）；④ 未采纳 PR 的 airoha_fan 改动（我们的 `files/etc/init.d/fan` 动态探测覆盖 xr1710g，避免双脚本抢 PWM） | upstream-open（#22397，2026-08-14 最后活动） | 合入即删；PR 更新后 diff 对照，重取源见 fetch-sources.sh --22397 |
| F02 | 6GHz 不可用 | US 默认 regdb 6GHz NO-IR 限制 | regdb 未含设备功率 | `regdb-0510/0520`（world 5GHz 去 NO-IR 已上游自带 `500-world-regd-5GHz.patch`，F20 删重复 0500） | n/a（功率补丁不打算上游） | 530 实验室 SP 默认停用（#DISABLED）；验收 C2 需双侧国家码判据——客户端非 US 看不到 6G 属预期（IP14） |
| F03 | NPU 内存/卸载 | 只在未装 Wi-Fi 板保留 NPU 内存 | 上游 #24593 | 无需携带（master 已合 2026-08-11） | merged | 跟踪：若回移分支需重拾 |
| F04 | 10G PHY 启动/link | 非 Realtek SoC 上 rtl8261 劣化、boot 失败 | #21777/#22564/#23078/#23383 系列 | 依赖：#22397 板级（含 phylink 配置）；kmod-phy-realtek + rtl826x-firmware | 部分 merged；PHY LED #24034 仍 open | 见 F06 |
| F05 | 风扇温控 | 传感器版本差异（NCT7802/NCT7511Y）；上游 airoha_fan 只覆盖 nct7802 | hwmon 动态探测缺失 | `files/etc/init.d/fan`（动态探测+曲线，覆盖 #22391 修复） | n/a（版本差异不适合上游单一脚本） | 社区实测 NCT7802 为主 |
| F06 | 10G/1G LED 行为 | XR1710G LED 由交换芯片驱动、与 W1700K 相反 | 板级差异 | 1G LED 已实现（9001 dts gsw_phy*_led*，MT7530 驱动）；10G RTL826x LED 待 #24034（实验档候选）；#24619（mt7530 LED 内核侧）跟进 | upstream-open | 2026-08-22 实机：1G 口 LED 默认不亮已加配置（F37），10G 口 LED 仍缺 sysfs/DTS（F37 ②） |
| F07 | cpufreq / PM domain | OC 前置依赖；驱动修复 6.18 下仍不稳 | #22029 未合 | **已自持** `patches/vendor/fanboy/03-cpufreq-pmdomain-22029-e5d23549.patch`（939/940 + **direct-PLL fallback**，fanboy 实机验证过） | upstream-open | **OC 前必须先合**（F08 前置）；上游合入即删 |
| F08 | CPU 超频 | stock 500–1200MHz；部分机器内置 OC 启动 panic（体质差异） | OPP/PLL/governor 需整体 +200MHz（上限 1.4GHz，电压不可调） | `scripts/prepare-oc.sh`（1.3/1.4 两档，不做 1.35/1.5）+ OC 变体**默认限频 1300**（`files/etc/init.d/oc-limit`，可解锁重启回退） | n/a（硬件体质差异 + 超频非上游议题） | 双 release：stock 默认 + oc 变体 |
| F09 | U-Boot pstore | 崩溃日志无处持久化 | #22473 未合 | kernel 侧已自持（`vendor/fanboy/10-pstore-ramoops-...`）；uboot 侧待上游 | upstream-open | 可选增强 |
| F10 | DSA 重构 | switch 端口架构迁移（实验档） | #22532 未合 | specs（不默认应用） | upstream-open | 毕业条件见 patches/README |
| F11 | L2 桥接卸载 | nft flow offload L2（实验档） | #22533 未合 | specs | upstream-open | 同上 |
| F12 | NPU/MLO/诊断应用 | 应用包无独立 feed | 散落在各家 fork 内 | **已供给**（2026-08-17）：`vendor/fanboy/19-w1700k-apps-pack`（npu/flowsense/mlo/fancontrol 随树内置，MANIFEST 默认档）+ `packages-xr1710g/` src-link feed（luci-app-airoha-recovery，源 naoki66@dd9ecfeef）+ filemanager=官方 luci feed；独立 feed 保留为备选（feeds.custom.conf 注释） | n/a | 决策五件套全部就绪 |
| F13 | fanboy 资产拆分 | 原料桶 20 commit 是打包历史，混多项改动 | 打包粒度 vs 我们的档位/可追溯要求 | **555 已拆出**（`regdb-0555-...`，#OC 档）；**08 号 regdb 已拆净**（2026-08-17，与独立 0555 内容一致）；**11/13/14/15/16 评审定档**（2026-08-17）：11=HW GRO/LRO 默认→**default**、14=mt76 token/NPU debugfs→**default**、16=wifi-scripts per-radio txpower 对 master 重建为 `root/9010-wifi-scripts-txpower-ucode.patch`→**default**（原 16 仅上下文漂移 hunk #3，手工合入重 diff，从零 apply 实证）、13=**否决**（`PKG_SOURCE_URL` 个人 fork + `PKG_MIRROR_HASH=skip`，违反锁源铁律）、15=**备选**（与默认 mt76/0006-0007 功能重复）；08 余项切片待做（SPI33MHz/banner/dropbear/antenna-memo/snd-off/LED-silence→default 候选，feeds.conf.default 与 w1700k dts LED **否决**——ADR-0001 master 保真/非目标设备）；19 号 fastfetch/netspeedtest 冗余待精简 | n/a | 拆分完成前对应条目不启用（MANIFEST 注释），本行即评审记录 |
| F14 | vermagic 注入 | 自建 kmod 与官方 opkg 兼容 | 构建层机制（fanboy 85005e10 + extract-distfeeds） | **评估完成（2026-08-17）：暂不接入**——① 需随内核版本维护 `files/etc/vermagic.txt`（官方 buildbot 值），成本高；② 本层 kernel 带 11（LRO default）/14（mt76 debugfs）号配置改动，vermagic 与官方不同，注入掩盖真实差异，官方 kmod 装后可能不匹配；③ 自用场景 kmod 需求有限（能力已随镜像编译）。维持 pending | n/a | 若未来需要官方 kmod（如 wireguard/ipsec 等），重新评估并同步 11/14 的 vermagic 影响 |
| F15 | 首次构建验证（2026-08-17 CI 红） | `make defconfig` 后 XR1710G 目标未选中 | **设备 kconfig 符号拼写错误**：真实符号 = `CONFIG_TARGET_<board>_<subtarget>_DEVICE_<device>`（`scripts/metadata.pm` 的 `confstr` 只清洗 target 名；profile id 原样保留 `DEVICE_` 前缀与连字符）；seed/CI/build.sh 写成 `CONFIG_TARGET_airoha_an7581_gemtek_xr1710g-ubi=y`（缺 `_DEVICE_` 中缀）→ kconfig 静默忽略未知符号 → 默认选中 **EVB 参考板**（`DEVICE_airoha_an7581-evb`） | 修复三处为 `CONFIG_TARGET_airoha_an7581_DEVICE_gemtek_xr1710g-ubi=y`（seed-config.diff / build.yml grep / build.sh grep）；本地 defconfig 实证：修复后 `CONFIG_TARGET_PROFILE="DEVICE_gemtek_xr1710g-ubi"`；配套 `scripts/audit-config.sh` seed 符号审计（build.sh + CI 接入，杜绝同类静默忽略） | n/a（仓库自持配置） | 教训：追加 .config 的符号必须与 metadata.pm 生成器一致，忘记 `_DEVICE_` 是静默失败 |
| F16 | 本地重复构建不可用（2026-08-17 发现） | 同一 openwrt 树第二次 `build.sh`/`apply-patches.sh` 必挂（`git apply` 一次性语义对已应用补丁直接报错）；oc 构建后换 stock 时 `revert_stock` 对未跟踪生成文件（如 patches-6.18/940）无法还原，OC 公式残留会污染后续构建 | 叠加层模型下树是持久克隆，残留无法自我清理 | ① `build.sh` 叠加前先 `git reset --hard` + `git clean -fdq -- target package`（叠加层文件在树顶层不受影响）实现可重复构建；② `prepare-oc.sh stock` 对未跟踪生成文件改为 `rm`（回到 apply 前状态，下次 apply 重建纯净版）；本地两轮「apply→oc→revert→复位→apply」实证全绿 | n/a | CI 恒用全新克隆不受影响；fork 模型（TREE==ROOT）自动跳过 |
| F17 | seed 审计在 build#2/oc-1.3 抓出两个静默缺失（2026-08-17） | ① `luci-app-airoha-recovery`：`feeds.custom.conf` 的 src-link 相对路径**缺 `../` 前缀**——`scripts/feeds` 用 `ln -s <path> feeds/<name>` 创建原文链接，相对路径从 `feeds/` 目录解析，`packages-xr1710g/…` 实际在 TOPDIR 下 → **死链接 → feed 零索引 → 包从未进 .config**（首次 stock 构建已静默漏装；默认 base feed 的写法就是 `../package` 可对照）；另 src-link 的 path 需直接包含包目录（本仓库包在 `packages-xr1710g/package/` 下，故还需该子目录后缀）；② `kmod-i2c-an7581`：该 kmod **不存在**——an7581 I2C 控制器用 `mediatek,mt7621-i2c`（`CONFIG_I2C_MT7621=y` 内置，9001 的 &i2c0 传感器节点已可工作） | 配置错误 + 陈旧符号 | ① `src-link xr1710g ../packages-xr1710g/package`（本地 feeds update 实证：`Collecting package info: feeds/xr1710g/luci-app-airoha-recovery` + 索引含 `Package:`）；② seed 删除 `kmod-i2c-an7581` 行（能力为内核内置，无损失） | n/a（自持配置） | 首次构建通过≠包都装上——F15 的教训由 audit-config.sh 制度化后才暴露，全量 seed 核对是必须的 |
| F18 | prepare-oc.sh OPP 平移两个 bug（2026-08-17 交接日发现） | ① `shift_hz` 直接把 `opp-hz` 值 `+delta`——`delta` 是 MHz 数（1.4 档 = 200），而 `opp-hz` 是 Hz 值（500000000），应 `+delta*1000000`；旧代码 500MHz→500000200Hz（+200Hz），**OPP 从未真正平移**；② `shift_label` 正则 `\bopp-(\d+)\s*\{` 替换成 `opp-<hz>` 时**丢掉 `{`** → `opp-500000200` 无花括号 → **DTS 语法错误，oc 档 kernel 编译必挂**（15 节点全破坏） | 单位换算错误 + 替换丢失分隔符 | `prepare-oc.sh`：`shift_hz` 改 `hz + delta*1000000`；`shift_label` 改 `f"opp-{hz} {{"`（保留 `{`）。本地实证：1.4 档 15 点 700→1400MHz 步进 50、花括号 16/16、required-opps 引用完好；1.3 档 600→1300；stock 恢复还原 500-1200 | n/a | 教训：树编辑脚本的"冒烟通过"只验证了公式可定位，没验证数值语义与语法完整性——OPP 平移后必须核对首尾频率与花括号配对 |
| F19 | CI 构建假绿——firmware artifact 从未产出（2026-08-17 交接日发现） | ① build.yml `make -j$(nproc) V=s 2>&1 \| tee log` **无 pipefail**——GitHub Actions 默认 bash 不带 pipefail，make 失败码被 tee（成功退出）吞掉 → step 绿 → job 绿；② upload-artifact 对无匹配文件默认 warn 不报错。实际：run 31999578409（"首次构建验证"）、32005186317（build#4 stock）、32005210085（oc-1.3c）**三次全在 openssl 下载失败**（3 mirror 全 502/404，瞬时网络问题）`Error 2`，但全部显示绿、`ci-8` pre-release 0 assets | 构建步骤未硬化退出码传播 | build.yml 构建步骤首行 `set -euo pipefail`；上传固件步骤加 `if-no-files-found: error`（无 itb=静默失败，必须红）；本地 build.sh 已有 `set -euo pipefail` 不受影响 | n/a | 教训：CI 绿≠构建成功——必须用产物存在性（firmware artifact）作为成功判据，HANDOFF 中"首次构建已通过、产物已上传"不实 |
| F20 | regdb 补丁在 wireless-regdb 2026.05.30 上构建挂（2026-08-17，三档全红——F19 修复后第一次真红，暴露真问题） | ① 本层 `regdb-0500-world-5ghz-no-no-ir.patch` 与 OpenWrt 自带 `500-world-regd-5GHz.patch` **内容完全一致**（同一作者 Felix Fietkau 同一 diff）——重复补丁：500 先应用成功（NO-IR 已去），0500 再找旧上下文必挂（Hunk #1 FAILED at 19）；② OC 档 `regdb-0555` 的 hunk 上下文基于**原始** db.txt（6g 行 `(12), NO-OUTDOOR, NO-IR`），而 OC 档 = 默认档全量 + #OC 行，应用顺序 0510→0520 已把 6g 行改成 `(29), NO-OUTDOOR` → 0555 必挂。**dry-run 对拷贝类（packages）补丁只检查目标目录存在性，从不真实应用**——regdb 系列从未被验证过（F18 教训的同类盲区） | 补丁上下文漂移 + 重复供给 + 验证盲区 | ① 删除 `regdb-0500`（能力保留：上游已自带，非降级）；② `regdb-0555` 按「0510/0520 应用后」状态重建（UNII-4 扩展至 5895MHz + 6g 29→30dBm，语义不变，注释行保留）；本地从零实证：500→0510→0520→0555 依次 `patch -p1` 全应用 + `dbparse.py` 语法校验 exit 0 + 语义核对（world 36-48 去 NO-IR / US UNII-1 29 / UNII-4 5730-5895 / 6g 29→30） | n/a（自持补丁维护） | 教训：拷贝类补丁的"验证"必须本地 `patch -p1` 模拟或靠构建真实应用，dry-run 目录检查不等于补丁有效；0530 已于 2026-08-17 重建（500→0510→0520 后状态，fuzz 2 → 0，语义不变）；0510/0520 在新版上应用 offset 42 行（上下文匹配正确，dbparse 佐证）；③ **验证盲区制度化（2026-08-17 本会话）**：新增 `scripts/verify-copy-patches.sh` 并接入 `apply-patches.sh --dry-run`——按构建语义真实应用拷贝类补丁（树内包 Makefile 派生源码 tarball → 解包 → 树内已有补丁+本层补丁同目录 → 构建同款 `patch-kernel.sh` glob 排序 `patch -f -p1` → regdb 跑 dbparse.py），应用失败=红、下载失败=⚠ 不红；scratch 树实证 regdb 4 文件全应用 + dbparse 通过 |
| F21 | uboot 构建挂（2026-08-17，F20 修复后三档重建首次真红：内核+kmod 全过、`package/boot/uboot-airoha` an7581_gemtek_xr1710g prepare 失败） | `patches/root/9002` 生成的 `package/boot/uboot-airoha/patches/1000-airoha-add-gemtek-xr1710g.patch` 在 u-boot-2026.07 上应用失败，三因叠加：① **glob 排序陷阱**——包补丁由 `patch-kernel.sh` 按 shell glob 字节序应用，"1000-" 实际排在 "100-" 与 "101-" **之间**（"1000"<"101"），在 defenvs/（999 创建）之前应用 → 新建文件 hunk 挂；② **内层补丁缺 `+++ b/` 行**——PR 提取时 3 个新建文件 hunk 的 `+++ b/` 全丢（首 hunk 靠 diff --git 头侥幸应用，2/3 号 patch 拿不到文件名必挂）；③ **外层 hunk 行数未同步**——插入修复行后 `@@ -0,0 +1,295 @@` 未改为 298，git apply 按 295 行截断创建文件，尾部 gdm1 块被丢。此前 dry-run 只验证 9002 在树根 git apply（通过），从不验证其生成的包补丁对 u-boot 源码的真实应用（F20 盲区同型第三次） | 命名排序 + 提取损坏 + 生成物残缺 + 验证盲区 | ① 9002 内层补丁重命名 `1000-` → `9990-`（glob 序排在 998/999 之后、未来 1000+- 之前）；② 补回 3 处 `+++ b/` 行；③ 外层 hunk 行数改 298；本地实证：u-boot-2026.07 + master 全量补丁（100→…→999）+ 9990 按 glob 序 `patch -f -p1` 全应用、3 个新文件（defconfig/defenvs/dts）全部创建；④ **制度化**：verify-copy-patches.sh 注册 uboot 派生目标（DERIVED_DESTS，源码取包 Makefile PKG_VERSION + denx 多镜像），dry-run 的 verify 移到 git reset **之前**调用（派生文件依赖 ROOT 应用后的树状态） | upstream-open（#22397） | 教训：① 包补丁命名必须按 glob 字节序校验（"1000"<"101"，"9990" 才在 "999" 后）；② 提取/重建补丁后必须对目标源码真实 `patch -p1`；③ ROOT 补丁生成的包补丁（包裹补丁）同样需要真实应用校验 |
| F22 | mt76 包补丁 0001/0003 应用失败（2026-08-17，F21 的 verify 扩展在下一轮构建前抓出） | `vendor/fanboy/14-mt76-patches-80f79586.patch`（ROOT，F13 定档 default）生成的 `package/kernel/mt76/patches/0001/0003` 基于旧 mt76——0001 以 fuzz 2 应用（有重复函数风险）、0003 的 mac.c hunk 失败（offset -66/-72）。**复查 pin 版 mt76 59676919（2026-07-01）**：0001 的 token/NPU debugfs 计数器与 debugfs.c:908 逐字一致；0003 的功能性改动（`get_all_sta_info(UNI_ALL_STA_TXRX_AIR_TIME)` + `get_per_sta_info(UNI_PER_STA_RSSI)` 于 mac.c:2895-2896、MCU 助手 mt7996_mcu_get_per_sta_info 于 mcu.c:5552）**全部已上游**，剩余 diff 仅删除上游已无调用者的死代码 `mt7996_mac_sta_poll` | 补丁基于旧 mt76 版本，能力已被上游吸收（F13 定档时未对 pin 版本源码验证——F20/F21 盲区同型） | **#DISABLED fanboy 14**（MANIFEST 注释记录）——能力 100% 上游自带，本层补丁冗余且会红，非降级（同 F03/0500 逻辑）；验证路径：verify-copy-patches 的 mt76 dest 现只应用本层 0006/0007 | n/a（自持补丁维护） | 教训：定档评审必须对**当前 pin 的包源码**真实应用验证；上游吸收能力后本层补丁冗余应撤而非带病携带 |
| F23 | 内核 DTS 语法错误（2026-08-17 三档重建第 4 次红：uboot 已过，kernel 编译过，`image-an7581-xr1710g-ubi.dtb` 阶段 `an7581-xr1710g-ubi.dts:330.8-331.1 syntax error`） | `9001-xr1710g-dts.patch` 外层 hunk 声明 `@@ -0,0 +1,330 @@` 而实际内容 **427 行**——git apply 对行数不匹配**静默截断**创建文件（丢尾部 97 行含 `&gdm4` 块），文件止于 `&gdm4 {` 致 DTC 语法错误。**与 F21③ 同型**（9002 丢 gdm1 块），且 dry-run 的 git apply 与 verify（只查包补丁）都放行 | 包裹补丁 hunk 行数未同步内容（重建时内容增删后未更新 @@ 头） | 9001 的 `@@` 头改 330→427；本地实证：DTS 427 行完整 + 花括号配平 ✓ + 关键节点（&gdm4/&eth_pcs/&pcie0 等）齐全；**制度化**：新增 `scripts/audit-patches.sh` 逐 hunk 核对声明行数（不一致即红，git apply 静默截断类）接入 apply-patches.sh 应用前（dry+真实模式都跑）；全仓库审计：仅 9001 一处，已修 | n/a（自持补丁维护） | 教训：**包裹补丁的 @@ 行数必须与实际内容一致——git apply 对不匹配静默截断不报错，只能靠审计**；重建补丁后除真实应用外还须核对行数（audit-patches.sh 已制度化） |
| F24 | release 空壳（2026-08-17：ci-26 首次真绿后 release job "success" 但 0 assets——F19 修复后的第一个真 release 竟仍空壳） | release job 步骤顺序错误：`download-artifact`（path: _rel）在 `actions/checkout` **之前**——checkout 默认 `clean: true` 的 `git clean -ffdx` 把下载的 `_rel/`（未跟踪）清掉 → softprops 报 "Pattern '_rel/**' does not match any files"（日志：下载 3 个 artifact 成功 → checkout → 上传 0 文件）。另：`merge-multiple: true` 会把三档同名 itb（openwrt-...-sysupgrade.itb）互相覆盖只剩一套 | 步骤顺序 + merge 覆盖 | ① release job 步骤重排：checkout 先行、download 在后；② 去掉 merge-multiple（保留 firmware-<profile>/ 子目录，三档同名 itb 全保留）；③ ci-26 真产物已手动补传（sysupgrade/initramfs/chainload-uboot + 3 manifest） | n/a（自持 CI） | 教训：download-artifact 后不得再跑默认 clean 的 checkout；同名产物跨档合并必须保留档位目录 |
| F25 | 实验档不可构建 + EXP 补丁零校验（2026-08-18 本会话） | `apply-patches.sh` 有 `--experimental` 开关但 **build.sh/CI 从未接入**——8 条 `#EXP` 补丁（fanboy 02/04-09/17/18）入库但从未被构建/校验过（F20/F21/F23 盲区同型）；且 audit-patches.sh 与 verify-copy-patches.sh 均**无条件跳过 #EXP 行**——实验档拷贝类补丁即使 dry-run 也得不到真实应用校验。另：apply-patches.sh 的 dry-run 在 verify 失败时被 `set -e` 中止、跳过 `git reset --hard`——本地二次 dry-run 残留已应用状态（git apply 一次性语义）致误报冲突。**构建实证暴露两处**：① 首个 experimental 构建（32166293045）在 target/linux 阶段红——EXP-18 生成的 `patches-6.18/992-21`（NPU mailbox 超时 1000→500ms）Hunk #1 在 linux-6.18.44 上失败：**上游内核已是 100 * MSEC_PER_SEC**（意图超额满足）→ 删该 hunk 重建（83→74 行，重试包装保留）；② 二次构建（32171565649）mt76 编译红——**9993 用的 `ieee80211_link_sta_cap_bw/cap_nss` 是 integration 树 mac80211 包 subsys 补丁（411）新增的导出**，OpenWrt mac80211 backport（6.18.39）与内核 6.18 均无 → 实验档补上该依赖（`patches/packages/mac80211-411-…`，拷贝至 `package/kernel/mac80211/patches/subsys/`） | 档位只存在于解析逻辑、未接入构建链路；审计/校验不感知实验档；**ROOT 生成的内核补丁（patches-6.18）不在 verify 覆盖内**——dry-run 只验 ROOT 能创建文件，不验生成的内核补丁能在真实内核源码上应用；**编译期 API 依赖（如 9993←411）在 patch-apply 层不可见**——只能靠真实构建暴露（构建兜底） | ① **实验档可构建化**：build.sh 加 `experimental` 档（`--experimental` 应用，OC 步骤跳过）；build.yml 加 profile 校验（stock/oc-1.3/oc-1.4/experimental/all）与档位判定（OC/EXP 标志分离），'all' 仍聚合三 release 档；release 备注泛化（firmware-<profile>/）；sync-upstream.yml 2h cron dry-run 加 `--experimental`（实验档同样享受漂移检测）；② **审计/校验感知实验档**：audit-patches.sh 与 verify-copy-patches.sh 加 `--experimental` 开关（#EXP 行纳入），apply-patches.sh 透传；③ **set -e 修复**：dry-run 的 verify 以 `set +e` 包裹，回滚无条件执行、退出码仍传播；④ **8 条 EXP 补丁实测**：对 master（cf198ccb5d）按序 dry-run 全部可应用（05-bridge-offload 依赖 02-eip93 先行——hunk 旧上下文含 02 加入的 `kmod-crypto-hw-eip93` 行，原生态测试失败系顺序假象）；⑤ **mt76 integration 9990-9993 评审**（ROADMAP P3）：9990（EHT 广告）/9991（160MHz BF fallback）/9993（op_mode 传递）**携带入实验档**（对 pin mt76 59676919 原生态可应用、mt76 master b2704cf5 无此改动、位域约定核对通过）；**9992（PS-sync TLV 校验）否决**——mt76 master 2026-08-01 已上游合入同款加固（pin 升级自然获得，携带即 F22 式冗余）；⑥ **992-21 重建**（29778aa）：删冗余超时 hunk（master 内核已 100ms），linux-6.18.44 全序应用实证零失败；⑦ **9993 依赖补齐**（mac80211-411）：backports-6.18.39 + master 24 个 subsys 补丁 + 411 全序应用实证 OK（helper 声明/实现齐备） | n/a（自持工程化） | 教训：① "开关存在 ≠ 链路接入"——档位/能力要验证到构建产物；② 实验资产同样需要持续校验（2h cron 覆盖）；③ 补丁评审必须对**当前 pin 源码**逐 hunk 真实应用（F13/F22 判据复用），且注意补丁间顺序依赖（05←02）与**跨包编译期依赖**（9993←mac80211 411，patch-apply 层不可见，构建兜底）；④ **ROOT 生成的内核补丁也要真实应用校验**——候选：verify-copy-patches 增内核 patches-6.18 dest（2h cron 每次下载内核源码 ~150MB，评估后定） |
| F26 | release 资产同名冲突（2026-08-20：all dispatch 构建全绿但 release job 红） | 三档 itb 文件名相同（openwrt-...-sysupgrade.itb 等），`softprops/action-gh-release` 按 basename 上传并更新 asset metadata——多档同时上传同名文件时，先传的资产被覆盖，后续 update 报 `Not Found` |  | 打包步骤：release 前把 `_rel/firmware-<profile>/` 分别 `tar -czf firmware-<profile>.tar.gz`，`files` 改为 `_rel/*.tar.gz`；重派 all=32407079282 全绿，ci-37 三个 tar.gz 资产 | n/a（自持 CI） | F24 的“保留子目录”只解决了下载覆盖，没解决 softprops 按 basename 上传的冲突；同名产物应打包成按档位命名的单文件资产 |
| F27 | 审查发现的高危/中危一致性问题（2026-08-21） | ① 9001 内核 DTS 的 `ubi`/`reserved_bmt` 分区大小与 9002 U-Boot DTS 及 FLASHING 不一致（0x1b700000/0x04200000 vs 0x1f700000/0x00200000）；② `prepare-oc.sh stock` 路径双重前缀且会误删未跟踪的 stock 基线补丁；③ dry-run 缺失补丁文件不置红；④ 0530 上下文因 0521 过期；⑤ sync-upstream push paths 漏 audit/verify 脚本；⑥ DEVICE_PACKAGES 默认 wpad-basic 与 seed wpad-openssl 可能冲突；⑦ fan 无重复启动保护；⑧ build.sh 本地无 .itb 产物不红 |  | ① 9001 对齐 PR/U-Boot/FLASHING；② revert_stock 改为树内相对路径 + 只恢复 tracked 文件；③ dry-run 先判 missing；④ 0530 按 0521 后状态重建；⑤ push paths 补齐；⑥ 9000 去掉 wpad-basic；⑦ fan 加 PID 检查；⑧ build.sh 产物缺失即 exit 1 | n/a（自持维护） | 审查文档未提交，修复见本次提交 |
| F28 | 单 wiphy 多 radio 默认无线配置不可用 + wifi-scripts ucode set_txpower 声明顺序错误（2026-08-21 实机验收暴露） | ① 默认 `files/etc/config/wireless` 仍按三 phy 模型（radio0→phy0 / radio1→phy1 / radio2→phy2）编写；MT7996 实机是**单 wiphy 三 radio**，phy1/phy2 不存在 → 5G/6G 配置无法绑定，只有 2.4G 能起来；② 9010 重建时把 ucode 函数 `set_txpower` 定义在 `setup_phy` 之后，而 `setup_phy` 在无 `radio` 标识的单 radio 设备上会直接调用 `set_txpower` → ucode 运行期 undeclared variable | 单 wiphy 多 radio 模型失配 + 函数声明/调用顺序错误 | ① `files/etc/config/wireless` 三 radio 统一 `phy0` + `radio 0/1/2`（与 wifi-scripts per-radio txpower 补丁的 `config.radio` 语义一致）；② 9010 中 `set_txpower` 前移至 `setup_phy` 之前；本地 audit-patches 通过，实机三频全部 up 且 txpower 正确（2.4G/5G 30dBm、6G 29dBm） | n/a（自持配置/补丁） | 教训：多 radio 单 wiphy 设备必须用 `phy + radio` 模型；补丁重建后除行数/上下文外，还要核对函数声明与调用顺序（ucode 无 hoisting） |

| F29 | FlowSense VLAN/PPPoE Offload 开关报错（2026-08-22 实机验收暴露，experimental 档） | LuCI 页面选择 Enabled 后 RPC 返回 `failed - bridge module may not be loaded`；`/proc/sys/net/bridge/bridge-nf-filter-*` 不存在 | `luci-app-airoha-flowsense` / `luci-app-airoha-npu` 的 RPC 后端仍写 br_netfilter 旧 sysctl；experimental 档的 06 号补丁已改为原生 NFPROTO_BRIDGE flowtable 架构，固件内不再有 br_netfilter，旧 sysctl 路径永远不存在 | 新增 `patches/root/9018-flowsense-offload-new-arch.patch`（MANIFEST/ORDER 置于 9017 后）：两 RPC 后端在 sysctl 存在时保持旧行为；sysctl 缺失时 VLAN/PPPoE offload 状态改由 HW Flow Offload + fw4 flowtable 判定，Enabled 操作确保 `flow_offloading`/`flow_offloading_hw` 开启并 reload fw4，Disabled 返回明确错误（当前固件由 HW Flow Offload 总开关控制）。实机 ubus 验证：get 返回 `{"enabled":1}`、set enabled 返回 ok | n/a（自持补丁） | 教训：实验档内核能力切换后，依赖旧内核接口的应用层补丁必须同步适配；实机验收应覆盖 LuCI 全部可操作项 |

| F30 | NPU offload 深度测试暴露的监控与诊断问题（2026-08-22 实机，experimental 档） | 35 分钟 3 路并发 HTTP 下载（约 3.38GB）实测 NPU offload 稳定，但暴露：① npu-monitor 默认 target `1.1.1.1` 在现网不可达，jitter 采样恒 unreachable；② PPE debugfs `bind`/`entries` 的 `packets=`/`bytes=` 恒为 0，无法按流统计；③ 单流 `.102 -> 183.232.52.102` 仅 `[OFFLOAD]`/UNB，同客户端换 `.100` 或并发后可达 `[HW_OFFLOAD]`+BND；④ 设备缺 `bridge`/`ethtool`/`devmem`，`getFrameEngine` 报错 | ① 包默认 UCI 配置了不可达 target，而 init 脚本的网关 fallback 仅在未配置 target 时生效；② PPE debugfs 统计字段不更新；③ 硬件绑定与目标 IP/流建立时机相关，非功能故障；④ 镜像未包含诊断工具 | ① `ae9a2fd`：`patches/root/9017` 默认 target 改为 `223.5.5.5`；② 测试记录：`docs/acceptance-results/2026-08-22-npu-offload-stress.md`；③④ 留待后续镜像增强/补丁修复 | n/a（自持维护） | 硬件卸载判定应综合 conntrack `[HW_OFFLOAD]` + PPE BND + CPU/中断，勿只看 PPE debugfs 统计；诊断工具缺口建议下轮镜像补入 |
| F31 | wifi down/up 后 AP 拒绝所有 STA 关联（2026-08-22 实机深度测试，experimental 档） | 执行 `wifi down` → `wifi up` 后三频 AP-ENABLED，但 hostapd 持续报 `Could not set STA to kernel driver` / `Could not add STA to kernel driver` / `handle_assoc_cb: STA ... not found`；2.4G/5G 客户端均无法重新关联；随后 `wifi reload` 也无效 | 根因：mt76 旧 pin 59676919 的 `mt7996_mcu_add_sta()` 在发送 STA_REC 前先发 VOW DRR 控制命令；`wifi down/up` 清空固件 STA 上下文后，新关联命中 fresh-peer 路径，固件返回 0xC0000001，旧代码随即丢弃 STA_REC 并向上返回错误，hostapd 表现为 `Could not add/set STA to kernel driver` | 已修复；issue #10（经 9028 mt76 bump c5a3bd91 携带上游 `da2c90d2`：先发 STA_REC 再配置 VOW，且 VOW 失败不再阻断连接）。CI#70 5 轮 `wifi down/up` 未复发、客户端可重连 | merged（openwrt/mt76 c5a3bd91 / `da2c90d2`） | 教训：`wifi down/up` 不等于 `wifi reload`；mt76 MCU 命令顺序（先建 peer 再配调度）必须固化进补丁评审；`scripts/device-wifi-downup-probe.sh` 保留用于回归 |
| F32 | 2.4G 配置 HE40 实际常落 20MHz 且 iwinfo 误报（2026-08-22 实机深度测试） | `files/etc/config/wireless` radio0 为 `htmode HE40` + 无 channel（设备侧落为 `auto`）；实测 auto 及固定 ch6 时均因 `20/40 MHz operation not permitted ... based on overlapping BSSes` 回退 20MHz（hostapd log）；仅 `channel=6` + `noscan=1` 时实际 40MHz。同时 `iwinfo` 仍显示 HT Mode: HE40，与 `iw dev` 的 20MHz 不一致 | ACS/HT40 coexistence 正常回退；iwinfo 展示的 HT Mode 未反映 nl80211 实际宽度 | 未改配置；测试记录：`docs/acceptance-results/2026-08-22-wifi-deep-test.md`。方案二选一：① 接受回退并把 radio0 意图明确为 HE20/auto（避免误读）；② 固定 `channel '6'` + `option noscan '1'` 强制 HE40（牺牲 2.4G 共存礼貌，不建议在密集环境） | n/a（自持配置 + iwinfo 上游行为） | 判读 2.4G 实际带宽以 `iw dev` 为准；`iwinfo` 的 HT Mode 字段在 ACS 回退场景会误报 |
| F33 | 单 wiphy 多 radio 启动期 hostapd 邻区扫描失败（2026-08-22 实机深度测试） | 每次冷启动/`wifi reload` 时 hostapd 对固定信道的 5G/6G 也发起 neighbor BSS scan，单 wiphy 三 radio 并发扫描互相挤占，logread 出现 9 次 `Failed to request a scan of neighboring BSSes ret=-16 (Resource busy)`（daemon.err），最终 AP-ENABLED 但日志有 error 且拖慢启动 | 单 wiphy 多 radio 共享扫描引擎；固定信道无需扫描，但 wifi-scripts 默认 `noscan=0` | `files/etc/config/wireless` 对 radio1/radio2 增加 `option noscan '1'`（本 commit，固定信道且非 DFS）；radio0 保持 auto 扫描；本机已 `wifi reload` 复核：hostapd conf 带 `noscan=1`，无新 ret=-16 | n/a（自持配置） | 固定信道 AP 应设 `noscan=1` 消除启动期无谓扫描与 error 日志；radio0 如需 HE40 确定性再评估固定信道+noscan |

| F34 | `flow_offloading_hw` 1→0→1 切换后设备疑似重启（2026-08-22 实机深度测试，experimental 档） | 软件流表基线测试时先将 `flow_offloading_hw` 置 0 + fw4 reload（正常，`[OFFLOAD]` 生效）；打流完成后恢复 `flow_offloading_hw=1` + fw4 reload，命令返回 `restored` 且 uci 值正确，但约 10–15s 后设备重启（kernel boot 08:27:54）。重启后 pstore 为空、logread 仅保留重启后日志，无 pre-reboot 线索 | 待定位：疑为 NPU flow_block 解绑/重绑路径在 fw4 reload 后的异步释放/再注册触发看门狗或 panic；需串口复现确认 | 未修复；issue #7；测试记录 `docs/acceptance-results/2026-08-22-npu-toggle-rpc-gap.md`。修复前建议 LuCI 在线切换提示"切换后建议重启"或限制 1→0→1 快速连续切换 | n/a（待定位） | 教训：硬件卸载开关的在线切换必须纳入实机验收；`fw4 reload` 成功返回不代表切换无副作用，应继续观察 1–2 分钟 |
| F35 | `getStatus.memory_regions` 依赖 dmesg，logd 消费后恒为空数组（2026-08-22 实机深度测试） | `luci.airoha_npu`/`luci.airoha_flowsense` 的 `get_status` 用 `dmesg \| grep "reserved mem.*npu" \| head -4` 取保留内存；OpenWrt logd 消费内核环缓冲后 `dmesg` 输出为空（实测 `dmesg \| wc -l` = 0），于是 `memory_regions` 恒为 `[]`。开机初期尚可显示 4 条（另漏第 5 条 npu-ba，见 issue #3） | 9017 数据源选错：dmesg 环缓冲不可持久，且 `head -4` 漏项 | 已修复；issue #8（经 F42 9020 改 DT + F63 ash 算术修复） | n/a（自持补丁） | CI#70 experimental 实机 `ubus call luci.airoha_npu getStatus` 返回 5 个 memory_regions（npu-ba 在列），issue #3/#8 已关闭。教训：LuCI RPC 的"状态"应来自稳定 sysfs/procfs/devicetree，不应依赖易失的 dmesg 环缓冲 |
| F36 | `getWanHealth`/`getBridgeStats` 统计恒 0 / available:false（2026-08-22 实机深度测试） | `ubus call luci.airoha_flowsense getWanHealth` 对 `pppoe-wan` 返回 `rx_bytes:0,tx_bytes:0`，但 `/sys/class/net/pppoe-wan/statistics/rx_bytes=860075,tx_bytes=7573199`；`getBridgeStats` 直接返回 `{"available":false}` | 两个 RPC 方法都使用 `ip -s link show <dev>` 解析统计，但 BusyBox `ip` 不支持 `-s` 选项（打印 usage 后退出）→ 解析结果为空 → `getBridgeStats` 误判桥不存在 | 已修复；issue #9（经 F43/9021 改读 sysfs） | n/a（自持补丁） | CI#70 实机 `getWanHealth`/`getBridgeStats` 均 `available:true` 且计数与 sysfs 一致，issue #9 已关闭。教训：RPC 后端 shell 解析必须对目标平台 BusyBox 工具集实测；`ip -s` 这类 iproute2 专有选项在 BusyBox 上不可用 |

| F37 | 硬件驱动深度测试：1G 口 LED 默认不亮、10G 口 LED 缺失、AN7581 PHY LED 第二个 netdev 配置写回 EINVAL（2026-08-22 实机，experimental 档） | ① `wan`/`lan3` 的 MT7530/AN7581 PHY LED 在 `/etc/config/system` 无任何 LED 配置，默认 brightness=0、trigger=none——1G 网口灯不亮；② RTL8261BE（lan1/lan2）在 `/sys/class/leds` 无 LED 设备，DTS `ethernet-phy@5/@8` 无 `leds` 子节点；③ 先配置第一个 PHY LED 的 `link/rx/tx=1` 写入正常，再配置第二个 PHY LED 时 sysfs 值生效（读回=1）但 store 返回 `EINVAL`，`/etc/init.d/led start` 退出码=1 且 boot 出现 3 条 `write error: Invalid argument` |  | ① `files/etc/config/system` 增加 `led_wan_green`/`led_lan3_green`（`trigger=netdev`，`dev=wan/lan3`，`mode='link rx tx'`），设备实测 LED 进入 `offloaded=1` 硬件接管；② 10G LED 跟进上游 #24034，或评估 fanboy 09 号 RTL8261CE LED 支持向 RTL8261BE 移植；③ AN7581 PHY LED sysfs store 路径（`drivers/net/phy/mediatek/mtk-ge-soc.c`，9016 同文件）修正第二个 LED 写入的返回值——值已生效，应返回 0 | upstream-open（#24034 / #24619） | 测试记录：`docs/acceptance-results/2026-08-22-hardware-driver-deep-test.md`；复现脚本：`scripts/device-hw-probe.sh`。① 已修（本 commit）；② issue #13（已修复：9033 carry openwrt #24034 + 9001 phy5/phy8 leds + system LED 配置）；③ issue #14（已关闭：9027/9031 实机验证） |
| F38 | `lan1` (10G/USXGMII) 无链路 down/up 时 `rx_errors`/`rx_dropped` 虚增（2026-08-22 实机，experimental 档） | `lan1` 无电缆、NO-CARRIER 时，每次 `ip link set lan1 down && ip link set lan1 up` 使 `rx_errors`/`rx_dropped` 增加 0~3（实测基线 3 → 5 → 8 → 9）；`lan2` 同样操作不增加。`dmesg` 无 error/oops，仅正常 PHY attach/configuring | `airoha_eth` 在 USXGMII in-band link 重新配置期间把 link-state 字/伪帧计为 RX error，或重新 open 时未清零统计 | 已修复；issue #15（经 F55/9025 无 carrier 只清 MIB） | n/a（自持补丁） | CI#70 实机 lan1/lan2 down/up 各一次 delta=0，issue #15 已关闭。该计数会触发监控误报（LuCI/9017 的 Physical Bottleneck 告警）；`scripts/device-hw-probe.sh` 默认不执行 down/up，`TOGGLE_10G=1` 可复现 |
| F39 | NPU CLIENTS offload 计数漏报：4 个 Wi-Fi 客户端关联、其中 2 个有 PPE BND + conntrack `[HW_OFFLOAD]`，LuCI 仍显示 `CLIENTS 0 offloaded`（2026-08-22 实机，experimental 档） | `getPpeEntries` 的 `client_bnd`/`band_bnd` 仅用 `/sys/kernel/debug/ppe/bind` 的 `eth=` 字段匹配 station MAC；但路由型 client→WAN 流的 BND 行 `eth=` 携带的是桥/下一跳 MAC（如 `00:58:28:d7:44:5e->dc:ef:80:59:a7:0d`），**不含 station MAC**（实测 .170 的 BND 行 `orig=192.168.123.170:…` 但 eth 无 `1e:f3:6d:77:ce:23`），MAC 匹配恒为 0；同时 BND IP 匹配缺失、conntrack 兜底缺失 |  | 新增 `patches/root/9019-npu-client-offload-accounting.patch`（MANIFEST/ORDER 置于 9018 后）：`band_bnd` 与 `client_bnd` 增补 BND `orig/new` 中的 station IPv4 匹配，并按客户端返回 `hw_offload`/`sw_offload`（来自 conntrack `[HW_OFFLOAD]`/`[OFFLOAD]`）；`bnd` 取 eth-MAC、BND-IP、conntrack-HW 三路信号最大值。实机验证：修复前 `client_bnd` 4 站全 0、`band_bnd=[0,0,0]`；修复后 `.162 bnd=2/hw=2`、`.170 bnd=8/hw=5`、`band_bnd=[0,8,0]`（UI 从 0 offloaded 变为 2 offloaded）。测试记录：`docs/acceptance-results/2026-08-22-npu-clients-offload-deep-test.md`；复现脚本：`scripts/device-npu-probe.sh`；issue #18（已关闭） | n/a（自持补丁） | 教训：PPE `eth=` 的语义不是“station MAC”，路由型 Wi-Fi 流必须用 IP 级匹配；多信号源取 max 才能避免硬件卸载假阴性 |
| F40 | IPv6 NPU offload 深度测试：TCP/UDP 可用但 RPC/PPE 呈现存在 IPv6 假阴性（2026-08-22 实机，experimental 档） | ① IPv6 TCP 多源并发（10 流）2s 内达 `[HW_OFFLOAD]`+PPE BND v6，功能正常；② IPv6 UDP 长流 conntrack 达 `[HW_OFFLOAD]`，但 PPE `bind` 无 BND、`entries` 仅 UNB——按 BND 判读 UDP 卸载会假阴性；③ `getPpeEntries` 的 `band_bnd`/`client_bnd` 仍不能计 IPv6 客户端：`@NEIGH` 显式跳过 IPv6，且 `ip neigh` 输出压缩式 IPv6 而 PPE/conntrack 为展开式，未规范化无法匹配；`bnd.ipv6` 已能统计总数但 CLIENTS 视图会漏掉纯 IPv6 客户端；④ ICMPv6 不卸载（符合预期）；⑤ PPE `packets/bytes` 恒 0 在 IPv6 下复现（issue #5） |  | 已修复（F44/9022）；issue #20（UDP UNB 可视化）已关闭；issue #19（纯 IPv6 客户端终验）已关闭（纯 IPv6-only 无线客户端终验转 ACCEPTANCE B5.1 跟踪）。测试记录：`docs/acceptance-results/2026-08-22-ipv6-npu-offload-deep-test.md`；复现脚本：`scripts/device-npu-ipv6-probe.sh` | n/a（自持补丁） | 教训：PPE/conntrack 的 IPv6 地址是展开式，`ip neigh` 是压缩式，任何跨源 IP 匹配必须做地址规范化；硬件卸载多数据源判读必须区分 TCP（BND）与 UDP（UNB/conntrack）路径 |
| F41 | iQOO Neo9 Pro 无法连接 5G WiFi（同 SSID 下只连 2.4G，5G 无 auth/assoc 记录）（2026-08-22 实机，experimental 档；2026-08-26 实机二分定位并关闭） | ① 三频统一 SSID `K2P` 但无 band steering / 802.11k/v / 独立 5G SSID，iQOO 黏在 2.4G（2.4G SAE 成功，`logread` 中 5G 对 `e2:0d:5e:29:67:6d` 零记录）；② 5G 默认 `ch149/HE160`（center1 5815，覆盖 5735–5895MHz），越界到 UNII-4（ch169/173/177，regdb 0521 自制 30dBm），国行 iQOO 在 5.8G 仅支持 80MHz，存在 BSS 过滤/连不上风险；③ 设备 2.4G/5G 口令为 8 位 `12345689`，与仓库占位 `123456789` 漂移（弱口令）；④ 诊断工具缺 `tcpdump`/`hostapd_cli`，probe 级证据无法取得 |  | 2026-08-26 实机矩阵（iQOO Neo9，MAC `42:f5:40:1e:e6:e1`，贴脸 -34dBm）：K2P-5G `sae-mixed`+HE160 = 手机提示“拒绝请求”且 AP 零 auth/assoc；K2P-5G `psk2`+HE160 = 同样“拒绝请求”；K2P-5G `psk2`+HE80 = **成功关联**（HE80 2SS，PHY 1200.9Mbps）；K2P-5G `sae-mixed`+HE80 = **成功关联**（`auth_alg=sae`，mfp=true）。结论：**根因是 ch149/HE160（中心 5815 覆盖 UNII-4 前段）被国行 iQOO 拒绝，HE80 即可恢复，与加密无关**。修复：`files/etc/config/wireless` 默认 5G 改 `HE80`；HE160 改为注释化可选档（非国行/支持 5.8G 160MHz 终端）；K2P-5G 保留 `sae-mixed`。测试记录：`docs/acceptance-results/2026-08-26-iqoo-5g-he80-fix.md`；只读取证脚本：`scripts/device-wifi-iqoo-5g-probe.sh`；issue #21（关闭） | n/a（自持配置；regdb 0521 合规风险自担） | 教训：单 SSID 多频合一≠band steering；5.8G 160MHz 在中国不可用（国行终端仅 80MHz），自制 US regdb 扩展 UNII-4 在国行终端上可能被过滤；口令我漂移会被 hostapd 4-way 失败误诊为“无法连接”；“拒绝请求”且 AP 无 auth 记录 = 客户端在 MAC 层拒绝 BSS 参数，不是认证失败 |
| F42 | NPU RPC memory_regions 数据源（#3/#8，承接 F35） | getStatus.memory_regions 依赖易失 dmesg 且 head -4 漏第 5 区 npu-ba | 9017 用 dmesg 环缓冲 + head -4 | `patches/root/9020-npu-memory-regions-dt.patch`：改读 /proc/device-tree/reserved-memory/npu-* reg，dmesg 仅回退 | n/a（自持补丁） | #66 实机：DT 数据源与 5 节点在位，但 getStatus 因 9020 的 `16#` 算术在 BusyBox ash 上报 Invalid argument（issue #22）；F63 修复后 CI#70 实机 `getStatus` 返回 5 区（npu-ba 在列），issue #3/#8 已关闭 |
| F43 | FlowSense 接口统计恒 0（#9，承接 F36） | getWanHealth/getBridgeStats 用 BusyBox 不支持的 `ip -s` | BusyBox ip 无 -s；9017 遗留 ip -s 解析 | `patches/root/9021-flowsense-sysfs-stats.patch`：改读 /sys/class/net/<dev>/statistics/* | n/a（自持补丁） | #66 实机复核通过：getWanHealth/getBridgeStats 与 sysfs 一致，issue #9 已关闭 |
| F44 | IPv6 NPU offload 呈现假阴性 + UDP 判读（#19/#20，承接 F40） | getPpeEntries 的 client_bnd/band_bnd 漏计 IPv6；UDP 卸载只能看 UNB | 9019 仅 IPv4；PPE/conntrack IPv6 展开式与 ip -6 neigh 压缩式不匹配；无 conntrack UDP 兜底 | `patches/root/9022-npu-ipv6-udp-visibility.patch`：awk canon6 + ip -6 neigh + client_bnd.ip6 + udp_hw（conntrack） | n/a（自持补丁） | CI#70 实机：client_bnd.ip6 已生效、`unb.udp_hw=12`（issue #20 已关闭）；纯 IPv6 客户端路径经 awk 合成用例验证通过（mac2ip6/canon6/BND/conntrack 匹配），issue #19 已关闭；纯 IPv6-only 无线客户端终验转 ACCEPTANCE B5.1 跟踪 |
| F45 | NPU RPC 诊断工具缺口（#2，承接 F30④） | getFrameEngine 报 devmem 错、pll_freq_mhz 恒 0 | 根因：镜像缺 devmem/ethtool/bridge，且内核 `CONFIG_DEVMEM` 未开启——busybox devmem 在，但 `/dev/mem` 驱动缺失，`mknod /dev/mem c 1 1` 后 open 仍 ENXIO | `config/seed-config.diff` 补 ethtool/bridge/tcpdump/iperf3 + busybox devmem + **`CONFIG_KERNEL_DEVMEM=y`**（映射内核 `CONFIG_DEVMEM`，提供 /dev/mem 驱动）；`patches/root/9023-npu-rpc-graceful-tools.patch` 优雅降级兜底 | n/a（自持配置/补丁） | 根因修复 + 兜底：内核有 /dev/mem 时可读 PLL/Frame Engine 寄存器；缺 devmem 或 devmem 读失败时 getFrameEngine 返回 available:false、pll_freq_mhz 输出 null，前端 renderFeDiagram 显示不可用 |
| F46 | bridge-flow-offload 包未安装 + 破坏性表名 + bridge 族 flow 表达式 EPROTO（#1 E1/E2/E3） | 05 号包未装；apply-rules.sh destroy table bridge fw4；E2 为 nft_flow_offload_init 对 bridge 族取 conntrack 失败 | ① 05 号 `DEPENDS=+kmod-nft-bridge` 与 06 去 br_netfilter 冲突；② CI 两次 defconfig 顺序：首次 defconfig（默认 target=mediatek）把实验档专有包写成 `# CONFIG_PACKAGE_* is not set`，第二次 defconfig 不会重新启用——共享 seed 未含该包，E1 因此仍缺；③ 表名与用户 fw4 冲突；④ init/destroy 未处理 NFPROTO_BRIDGE | ① `patches/root/9024-bridge-flow-offload-deps-table.patch`（#EXP，DEPENDS 改 kmod-nft-offload + 独立表名 flow_offload）；② `patches/root/9026-bridge-flow-offload-init-conntrack.patch`（#EXP，E2，生成 675-04 内核补丁：bridge 族复用 inet conntrack 语义）；③ `config/seed-config.experimental.diff` + CI/build.sh 在实验档追加 `CONFIG_PACKAGE_bridge-flow-offload=y`；2026-08-31 stock ci-83 发现 stock 档仍未装，改为移入共享 `config/seed-config.diff` | upstream-open（#22533） | E2 已在 #66 实机通过（bridge 族 `flow offload @ft` 注入成功）；E1/E3 仓库层已修，已毕业（2026-08-31 ci-74 实机：`apk info bridge-flow-offload` 存在；`nft list table bridge flow_offload` 有 `flags offload`；`nft list table bridge fw4` 不存在；logread 无 nft 报错。`vendor/05/06`+`root/9024/9026` 转 default）。**2026-08-31 stock ci-83 补充**：stock 档仍缺 `bridge-flow-offload`（`DEFAULT_PACKAGES` 未生效），已移入共享 seed；新 stock build 后 fresh flash 复验 |
| F47 | iQOO Neo9 Pro 5G 兼容（#21，承接 F41） | 单 SSID 黏 2.4G；5G ch149/HE160 越界 UNII-4 | 国行终端 5.8G 仅 80MHz；无 5G-only 入口 | `files/etc/config/wireless` 默认 5G 改 `HE80`（K2P-5G 保留 `sae-mixed`）；HE160 注释化为可选档（非国行终端） | n/a（自持配置） | 实机矩阵已闭环：HE160（psk2/sae-mixed 均拒）→ HE80（psk2/sae-mixed 均可连，PHY 1200.9Mbps，auth_alg=sae），issue #21 关闭 |
| F48 | 2.4G HE40 意图与 20MHz 实际不符（#11/F32） | iwinfo 误报，需确定性配置决策 | ACS/HT40 共存回退是正确行为；iwinfo 展示意图 | `files/etc/config/wireless` radio0 显式 channel auto + 注释；判读以 iw dev 为准 | n/a（自持配置 + iwinfo 上游） | 不降级到 HE20；不强制 noscan |
| F49 | wifi down/up 后 AP 拒绝关联（#10/F31） | 三频 AP-ENABLED 但 STA 无法关联 | mt76 旧 pin 先 VOW DRR 后 STA_REC，固件 fresh-peer 拒绝导致 STA_REC 丢弃 | `patches/root/9028-mt76-bump-c5a3bd91.patch`（携带上游 `da2c90d2`） | merged（openwrt/mt76 c5a3bd91） | CI#70 5 轮 `wifi down/up` 未复发且客户端可重连；issue #10 已关闭 |
| F50 | F22 复核结论不成立（#4） | fanboy14 0001/0003 并非冗余 | F22 对比源码版本/位置错误；pin 59676919 实机无 token_info、仍含 mt7996_mac_sta_poll | `patches/packages/mt76-0001-wifi-mt76-mt7996-add-token-and-NPU-debugfs-counters.patch` + `patches/packages/mt76-0003-wifi-mt76-mt7996-replace-direct-WTBL-access-with-MCU-for-station-statistics.patch`（对 pin 59676919 重建，`patch -p1 --dry-run` 零失败） | upstream-open | 已重新启用；#66 实机 token_info 存在、`mt7996_mac_sta_poll` 消失，issue #4 已关闭 |
| F51 | PPE debugfs packets/bytes 恒 0（#5） | 无法按流统计 NPU 转发量 | 根因：OpenWrt `target/linux/airoha/an7581/config-6.18` 显式 `# CONFIG_NET_AIROHA_FLOW_STATS is not set`（PR #22300：该选项=y 会破坏 AN7581+MT7996 的 NPU offload，故上游暂关）。驱动 `airoha_ppe_get_num_stats_entries()` 在 `!IS_ENABLED(CONFIG_NET_AIROHA_FLOW_STATS)` 时返回 `-EOPNOTSUPP`，`airoha_ppe_foe_entry_get_stats()` 提前返回，debugfs 的 `packets/bytes` 因此恒 0 | 9032 新增 `getPpeFlowStats` RPC（issue 建议方案 2）：用 conntrack 双向 tuple（orig/reply）关联每流 `ct_packets`/`ct_bytes`/`hw_offload`，并给出 BND/UNB 汇总；实机验证可用（如 BND `ct_packets=2191, ct_bytes=918597`）。PPE debugfs 计数本身待上游修复；2026-08-31 9035 转 default 后允许 CONFIG_NET_AIROHA_FLOW_STATS=y 与 NPU offload 共存（ci-88 experimental fresh flash：dmesg `NPU flow stats unavailable (-22)`、offload 存活），待 #94 stock 复验 | upstream-open（PR #22300 未 revert 前禁用；PPE debugfs 计数） | 判读补强已落地；`scripts/device-npu-probe.sh` 应增测 `getPpeFlowStats`（见 HANDOFF） |
| F52 | EIP93 硬件 crypto 未使用（#6） | 驱动已加载 refcnt=0 | 仅编译 kmod，缺用户态 IPsec/xfrm 配置；EIP93 未注册 GCM-AEAD | docs/comment only；ROADMAP 增加 IPsec/xfrm 硬件卸载评估项，实机验证后按档位供给 | n/a | 不提前塞默认镜像 |
| F53 | 10G 口 LED 无 sysfs/DTS 节点（#13） | RTL8261BE 无 LED class 设备 | ① 上游 realtek 驱动未实现 RTL826x LED hooks；② OpenWrt 通用内核配置 `CONFIG_PHYLIB_LEDS` 未启用，DTS `leds` 子节点不会实例化为 LED class；③ 9001 DTS `phy5`/`phy8` 缺 `leds` 子节点；④ `files/etc/config/system` 缺 10G LED 配置 | `patches/root/9033-openwrt-24034-rtl826x-led.patch`（carry openwrt/openwrt #24034：生成 `target/linux/generic/hack-6.18/743-net-phy-realtek-add-rtl826x-led-support.patch` + 启用 `CONFIG_PHYLIB_LEDS=y`）+ `9001` `phy5`/`phy8` 增加 `leds` 子节点 + `files/etc/config/system` 增加 `led_lan1_green`/`led_lan2_green` | upstream-open（#24034；合入后删 9033） | 根因修复：驱动 LED hooks + PHYLIB_LEDS + DTS leds + 用户态 netdev 触发；实机验证已确认 sysfs 名 `mt7530_dsa-0:05/08:green:lan`，`led start` rc=0 且 `offloaded=1` |
| F54 | AN7581 PHY LED 第二个 netdev 写回 EINVAL（#14） | 值已生效但 store 返回 -EINVAL，led start 退出码 1 | ledtrig-netdev：① `hw_control_get` 预置 `TRIGGER_NETDEV_LINK` + 具体 link speed 位，`netdev_led_attr_store` 互斥检查拒绝写 `link`；② hw-control 模式下 `interval` 属性被内核故意拒绝写入，led 脚本未跳过 | `patches/root/9027-ledtrig-netdev-link-mode-mutex.patch`（生成 `680-`：写 generic link 时清除具体 link speed 位；写具体 link speed 时清除 generic link）+ `patches/root/9031-base-files-led-skip-interval-hw-offload.patch`（hw-offloaded netdev LED 跳过 interval 写入） | n/a（自持补丁） | CI#70 experimental 实机 `led start` rc=0、无 EINVAL（9031 随本批构建生效），issue #14 已关闭 |
| F55 | lan1 无链路 down/up 时 rx_errors/rx_dropped 虚增（#15/F38） | 每次 down/up +0~3 | airoha_eth 无 carrier 时仍把 USXGMII 伪帧计入 MIB | `patches/root/9025-xr1710g-airoha-no-carrier-rx-stats.patch`（生成 9991 内核补丁：`netif_carrier_ok()` 门控，无 carrier 只清 MIB 不并入统计） | n/a（自持补丁） | #66 实机：lan1/lan2 down/up 各一次 delta=0，issue #15 已关闭 |
| F56 | mt7996e-hif 仅 Gen2 x1（#16） | 端点能力 Gen3 x2，根端口仅 x1/Gen2 | pcie2 共享 USB3 PHY 单 lane 硬件约束；pcie0 x2 已上游（#21978 merged，openwrt main patches-6.18 已含 609-04 x2/913 PCIe HB reset）；2026-08-24 实机 D0 判定：`0002:00:00.0` LnkCap2 仅报 2.5/5GT/s（LnkCtl2 Target=5GT/s），根端口不声明 Gen3——**pcie2 Gen3 x1 不可达，Gen2 x1 为板级上限** | docs/comment only；`docs/acceptance-results/2026-08-24-pcie-gen3-baseline.md` 存档；CONTEXT/README/ACCEPTANCE 已文档化 PCIe 拓扑与验收口径（主 7990 pcie0 x2 为准，7991/hif 不作为高速数据面假设） | upstream-open（#21978 merged；pcie2 Gen3 降级为上游/厂商跟踪）；issue #16 已关闭 | 不伪造 num-lanes；不实施 pcie2 Gen3 实验补丁（`docs/PCIe-GEN3-PLAN.md` 已 D0-stop） |
| F57 | 1G 口黄灯不亮（#17） | gpio43/44 UNCLAIMED | 9001 DTS pinctrl-1 未应用 | 已修复：commit 9adafd9 + 8e33788（pinctrl-0 合并 + amber LED 配置） | n/a | 仅需关闭 issue |
| F58 | CLIENTS 0 offloaded（#18/F39） | 路由型 Wi-Fi 流被漏计 | PPE eth= 不含 station MAC | 已修复：commit a13bb0c + 9019 补丁 | n/a | 仅需关闭 issue |
| F59 | 9992 否决复核（2026-08-22） | F25 以“mt76 master 2026-08-01 已合入”否决 9992，但 openwrt main pin mt76 59676919（07-01）尚未包含该加固 | pin 未升级，漏洞（PS-sync 畸形 TLV 解析死循环/越界）仍存在 | `patches/packages/mt76-9992-wifi-mt76-mt7996-validate-PS-sync-event-TLVs.patch`（#EXP；对 pin 59676919 `patch -p1 --dry-run` 零失败） | merged（mt76 master）/ carried（pin 59676919 未升级前） | pin 升级后删 9992；F25 历史记录保留 |
| F60 | mt76 编译失败：`mt76_connac_mcu.h` `UNI_PER_STA_INFO_TAG` redeclaration（2026-08-22 四档 CI 全红） | 0003 重建时把两个关键块都复制了两遍：① 第一 hunk（mt76_connac_mcu.h）`#define PER_STA_INFO_MAX_NUM` + `enum UNI_PER_STA_INFO_TAG` 重复；② mcu.c hunk `mt7996_mcu_get_per_sta_info()` 整个函数重复。补丁可应用但编译时 mt7915/mt7996 先报 enum redeclaration，修掉后还会撞函数 redefinition |  | `patches/packages/mt76-0003-…` 第一 hunk 去重（`+1409,20`→`+1409,13`）；mcu.c hunk 删除第二份函数（`+5576,218`→`+5576,112`）；`patch -p1 --dry-run` + 10 个 mt76 补丁全序实际应用通过；`audit-patches.sh --experimental` 通过 | n/a（自持补丁） | 教训：补丁“可应用”≠“可编译”；重建补丁后应 grep 关键标识符（enum/函数）出现次数，重复定义只会在编译期暴露 |
| F61 | mt76 编译失败：`mt7996/mcu.c` `wlan_idx`/`res`/`i`/`wcid`/`ac` undeclared（2026-08-22 四档 CI，d52fdfa） | 0003 重建时把 `UNI_ALL_STA_TXRX_AIR_TIME` case 同时插入到 `mt7996_mcu_rx_all_sta_info_event` 与 `mt7996_mcu_ie_countdown`；后者作用域无这些变量 |  | 删除 `mt7996_mcu_ie_countdown` 中的重复 hunk（`@@ -513,6 +513,33`），仅保留正确位置的 case；真实应用后 `grep -c` 确认该 case 仅 1 处 | n/a（自持补丁） | 教训：F60 只 grep 了 enum/函数重复，漏了 case 块重复；重建补丁后应 grep 所有新增标识符/块的插入位置 |
| F62 | mt76 c5a3bd91 自 bump 构建四档全红（2026-08-22，commit 4805814） | c5a3bd91 期望新版 mac80211 API：`ieee80211_get_fils_discovery_tmpl(hw,vif,link_id)`、`ieee80211_get_unsol_bcast_probe_resp_tmpl(hw,vif,link_id)`、`IEEE80211_MIN_ACTION_SIZE(action_code)`/`mgmt->u.action.action_code`；openwrt main 当前 mac80211/kernel 仍是旧两参 API |  | 新增 `mt76-9994-mac80211-api-compat.patch`：将上述调用适配回 6.18 API（两参 tmpl、`IEEE80211_MIN_ACTION_SIZE+1`、`u.action.u.addba_req.*`）；重新携带 9028 bump c5a3bd91（commit `438a4f8`） | carried（等待 openwrt main 联动 bump mt76+mac80211，届时删 9994） | 教训：mt76 master 不能单独 bump——必须核对 openwrt main 的 mac80211 包/kernel 头文件 API 是否匹配；下次自 bump 前先 compare mac80211 包 pin |
| F63 | `getStatus` RPC 整体报 `Invalid argument`（#66 实机复查，issue #22） | 9020 DT 解析用 `$((16#${b0}...))`，BusyBox ash 不支持 `base#` 算术，`read_npu_memory_regions` 报 `arithmetic syntax error` 后只输出 `[`，JSON 非法 |  | `patches/root/9020-npu-memory-regions-dt.patch` 改用 `$((0x${b0}...))`（ash 可用） | n/a（自持补丁） | CI#70 experimental 实机 `ubus call luci.airoha_npu getStatus` 返回 5 个 memory_regions（npu-ba/npu-binary/npu-pkt/npu-txbufid/npu-txpkt），issue #22 已关闭 |

| F64 | reserved_bmt 66MiB 布局对齐（A1/IP02/05/06/07/15） | 上游已证旧 2MiB 布局与厂商 BMT 尾部 528 块重叠，会损坏 UBI；本仓库 9001/9002 原为旧 2MiB 布局 |  | `patches/root/9001-xr1710g-dts.patch` + `9002-xr1710g-uboot.patch`：ubi `0x1b700000`、`reserved_bmt@1be00000 0x04200000`；`docs/FLASHING.md` 布局表 | carried（对齐 openwrt main 3d1645e 的 W1700K 布局；上游已含 ee771d3d/72ed389 同类修复） | 实机 `ubinfo -a` 无坏块、多轮重启不新增；新布局镜像须用 HTTP U-Boot 布局选择器 UBI 2.0 刷入 |
| F65 | sysupgrade compat 一致性（A2/IP15） | 9000 曾抄 W1700K 的 `DEVICE_COMPAT_VERSION := 2.0`，但当时布局仍是旧 1.0 |  | 本轮 A1 已把布局升到 2.0，9000 保持 `DEVICE_COMPAT_VERSION := 2.0` 与布局一致（无需 2.0→1.0 过渡） | n/a（自持一致） | `sysupgrade -T` 同版镜像返回可升级；旧 1.0 布局设备因 compat 主版本不同会被拒（正确） |
| F66 | rdinit 修复（A3/IP17） | U-Boot env bootargs 覆盖 DTS chosen，缺 `rdinit=/sbin/init` 导致每次开机 `rdinit=/init failed: -2` 假警告 |  | `patches/root/9001-xr1710g-dts.patch`：chosen bootargs 末尾加 `rdinit=/sbin/init`（对不覆盖 bootargs 的 U-Boot 生效）；但当前 HTTP U-Boot（2026.07-00766-g53b73174c0fc）默认 env `bootargs` 无 rdinit 且会覆盖 DTS chosen，CI#70 实机仍见该警告 | carried（待 U-Boot 侧补 `rdinit=/sbin/init`；已尝试写 UBI env，U-Boot 不读取） | 当前 HTTP U-Boot 下仍为良性假警告；若换用 9002 OpenWrt U-Boot 或更新 HTTP U-Boot 默认 env 后应消失 |
| F67 | 风扇双控制器去重（A4/IP25） | `files/etc/init.d/fan` 与 9017 fancontrol 的 `/etc/init.d/fan` 双入口抢 PWM |  | `files/etc/init.d/fan` 合并为单控制器 + 动态探测 + 迟滞/最低稳定档/失效满速兜底；9017 移除 `root/etc/init.d/fan`，仅留 LuCI 前端/RPC | n/a（自持脚本） | `/etc/init.d/fan start` 后 `pwm1` 仅一个写入者；温度曲线实机复核 |
| F68 | mt76 tx_failed 记账修复（A5/IP25/IP26） | c5a3bd91 仍把 retries 计入 tx_failed，终端计数失真 |  | 新增 `patches/packages/mt76-0009-report-only-terminal-tx-failures.patch`（default） | carried | 有损链路 `iw dev wlanX station dump` 中 `tx_retries>0` 而 `tx_failed≈0` |
| F69 | mt76 NPU RX skb->dev（A6/IP25/IP26） | NPU RX 路径不设 `skb->dev`，PPE 未命中回退时 bridge/FDB 上下文缺失 |  | 新增 `patches/packages/mt76-0010-set-skb-device-for-npu-rx.patch`（experimental；用 `mt76_queue_is_npu_rx(q)` 覆盖全部 NPU RX 队列） | carried | 仍 `#EXP`（需 6G 客户端；2026-08-31 用户口径延后，发现后修复） |
| F70 | FlowSense 1.1.8-r5 bump（A7/IP08） | 1.1.8 新增 airtime 离载吞吐估计 + UI 重绘；修复 offload 场景吞吐针恒 0 |  | 新增 `patches/root/9030-flowsense-bump-1.1.8-r5.patch`；MANIFEST 顺序：9017→9030→9018→…→9023；9022 已在 9030 基线上重建 | carried（default） | ci-74 实机：`uci show npu-monitor.settings.air_eff`=80、`getStatus` 正常、9018-9023 无回归（2026-08-31） |
| F71 | JCPLL TCLVAR recal（A8/IP10） | lan2 10G 不 link 根因：PLL enable 前写 TCLVAR，lane 配置完成后未重写 |  | **2026-09-07 按 rdmitry OW1700k PR#143 机制整体重写** `patches/root/9029-xr1710g-airoha-pcs-jcpll-tclvar-recal.patch`（内层 9992）：post_config hook（match-data，仅 `an7581_pcs_eth` 注册）+ `an7581_pcs_jcpll_recal()`（限 USXGMII/10GBASER，USXGMII AN enable 后、`airoha_pcs_config()` 末尾调用）+ TCLVAR `0x3→0x5` 脉冲（1–2ms）与 ETH/PON 分值 `0x5/0x3`。旧版（bringup 内同值重写）=HW no-op（rdmitry devmem `0x1fa7a030` 实测：写 `0x1D` 立即 link、写回同值仍 dead）已失效 | carried（experimental） | 内层对 6.18.44+master 补丁集真应用通过；仍 `#EXP`（需 10G 对端实机验证；与 9038/628、9041/743-744 同域互补，实机对照后定去留） |
| F72 | 6GHz 客户端国家码判据（A9/IP14） | 客户端国家码与 AP 不一致时看不到 6G SSID，会造成“广播了但连不上”误判 |  | `docs/ACCEPTANCE.md` C2 增客户端国家码双侧判据；`docs/FIXES.md` F02 备注补一行 | n/a（验收门槛） | C2 双侧判据：AP 侧 US regdb + 客户端 US 可见可连 |
| F73 | FLASHING 坏版本/救砖（A10/IP19） | 8/8 U-Boot 恢复页上传必 failed；缺 OS 内覆写 chainloader 的救砖路径 |  | `docs/FLASHING.md`：坏版本清单（8/8 `g93da8f980ef0`）+ 8/11 `b7710e5cc851` 候选锁版 + kmod-mtd-rw 救砖步骤 | n/a（文档/运维） | 文档自查；后续升级按 ADR-0002 核对 SHA256 |
| F74 | 验收方法学补洞（A11/IP20/24/28） | 吞吐测试端点误跑路由器本机；管理面改址回连从未自验；C3 缺外部端点判据 |  | `docs/ACCEPTANCE.md`：测试方法学节 + B6 管理面改址回连 + C3 外部端点判据；`OC与高功率实现-调研报告.md` §①D 本机 iperf3 降级为 CPU 基线 | n/a（验收门槛） | 下一轮验收按新判据执行 |
| F75 | device-hw-probe 增强（A12/IP04） | 10G PHY 型号判据写死/缺失；EFR32 去除未断言 |  | `scripts/device-hw-probe.sh`：新增 B2.1 10G PHY VEND1 `0x103/0x104` 判据（phytool）+ B7 EFR32 去除断言；B2.1 修正 MDIO 访问路径——10G PHY 挂在 mt7530-0 总线上（PHYAD 5=lan2、8=lan1），lan1/lan2 的 Airoha GDM ioctl 返回 -95，须借道 DSA 用户口（wan/lan3）以 C45 MMD30 读 `IFACE/<phy>:30/0x103` | n/a（脚本） | 实机 probe 全绿（B2.1：0x103=0x8261、0x104=0x1141；driver=RTL8261BE 10Gbps PHY） |
| F76 | fanboy 08 号 regdb 覆盖重叠确认（08-26 §6 遗留疑问①收口，2026-08-30） | fanboy `ubi2-oc` 08-25/08-30 两轮整枝 rebase 后，08 Reliability 仍新增 `wireless-regdb/patches/555-w1700k-fix.patch`（18 行），与本仓库 regdb 补丁体系是否重叠未核对 | 逐行比对：该补丁 = UNII-4 `5730-5895 @ 160 (30)` + 6G `(5925-7125 @ 320) (30)` 且保留 NO-OUTDOOR，恰为本仓库 `regdb-0521`（UNII-3/4 默认档）+ `regdb-0555`（6G 30，OC 档，保 NO-OUTDOOR）的并集 | 无需吸收（非降级；本仓库拆分粒度更细：默认/OC 分档） | n/a（fanboy 自持对照记录） | 08-30 rebase（`765535cf`）后内容不变；同轮复核 06/07/mt76-0005 与本地 vendor 语义一致（06=`7828198`、07=`4f19f7b`、0005=`171bc4b` 仅 hunk 偏移漂移） |
| F77 | fanboy 18 smartrg `992-21` 83 行版（F77 收口，2026-09-07） | fanboy 08-25 rebase 后 `992-21-net-airoha-npu-init-stability.patch` 由 74 行变 83 行：`airoha_npu_send_msg` mbox 轮询超时 `1000*MSEC_PER_SEC`→`500*MSEC_PER_SEC`（配合既有 3 次重试包装：mailbox 挂死更快放弃并重试，聚合最坏等待预算同量级） |  | **已吸收**：`patches/vendor/fanboy/18-smartrg-stability-b45d5117.patch` 的 992-21 段更新为 e352c48673 83 行版（新增 hunk `__airoha_npu_send_msg` DONE 轮询 1000→500ms；992-20 184 行逐字节未动；外层 hunk 行数/diffstat/index 同步） | carried（default） | OWT 真应用后 992-21 blob 与 e352c48673 逐字节一致（`e1195d809e9abd…`）；风险：若硬件正常完成落在 500–1000ms 窗口会多走一次重试（幂等写入，最坏时延略增可接受） |
| F78 | naoki66 LAN2 RTL826x SDS-mode 方案（已吸收，2026-09-07） | lan2 10G 链路建立依赖厂商 U-Boot 遗留的板级 SerDes 设置（RTL826x SDS page6 reg3=`0x88c6`），Linux 公共初始化不带 | 与 `9029` JCPLL 机制互补：SDS mode=板级 SerDes 初始化，9029=PLL enable 时序 recal | **已吸收**：`patches/root/9041-xr1710g-rtk-serdes-bundle.patch`（#EXP：742=622 SDS-mode（naoki66 09feeff 版，删 export hunk——6.18.44 下 `rtl826x_phy_patch_sds_set` 已非 static）+ 743/744=restore/reapply（b74553b/d3a0fa1 语义，6.18.44 适配））+ `9001` phy5（`realtek,sds-mode=<0x88c6>`/`reset-before-id-read`/`patch-rtk-serdes`）phy8（`patch-rtk-serdes`）属性 | n/a（社区自持，无上游 PR） | 内层 742→743→744 对 6.18.44+master 真应用通过；`reset-before-id-read` 的内核侧消费（naoki66 hack-6.18/705）未吸收——属性当前惰性无害，若实机需先复位再读 ID 再补吸收；naoki66 源仓 622 文件 hunk 计数损坏（§09feeff），其 fork 直接搬用会红，本地已修正 |
| F79 | mt76 NPU RX descriptor ownership（2026-09-07 WP-0011） | NPU 与主机通过 DONE 位交接 RX ring：consume 侧按 PKT_COUNT 走链不比对 `q->queued`，槽位不足时越过已消费描述符 → 重复投递/垃圾地址进 `dma_sync_single_for_cpu()`（实机 W1700K `dcache_inval_poc` Oops 3/3 wedged→5/5 存活）；refill 零地址竞态 | 缺界检查 + refill 竞态 | 新增 `patches/packages/mt76-0011-wifi-mt76-npu-fix-rx-descriptor-ownership.patch`（default；Oever González，论坛 t/222776 素材，对 be5ce791 `git apply` 干净） | carried（default，上游未托管） | 与 0012/0010 同域不同 hunk（0011=rx 消费/refill 区，0012=del_sta 新函数，0010=skb->dev）；需实机 NPU 长稳回归 |
| F80 | mt76 NPU del_sta（2026-09-07 WP-delsta） | STA 断开后 NPU 残留 per-station 路由，同 MAC 重连（新 WCID）下载卡 ~1Mbps，换私有 MAC 可绕过 | NPU 未被告知 station 移除 | 新增 `patches/packages/mt76-0012-wifi-mt76-npu-del-sta.patch`（experimental；naoki66 2d3aa30 / Ryan Chen：`mt7996_mac_sta_remove_link` 经 NPU mailbox 发 `WLAN_FUNC_SET_WAIT_DEL_STA`；宏已在 airoha_offload.h L122） | carried（experimental，上游未含） | 对 be5ce791 真应用通过（应用偏移 8/8/12 行系 naoki66 基线略早，落盘正确）；实机：断开→同 MAC 重连吞吐回归 |
| F81 | USXGMII RX 校准 SDK crossing 搜索（2026-09-07 WP-USXGMII） | AN7581 RX CDR PR IDAC 用"数值最近"启发式，易落 0x580 不 lock；SDK 序选 0x5a5、FL_OUT 精确 0x9edf、FBCK_LOCK 达成——**#22397 "LAN2 No FBCK Lock after 50 calibration try" 根因域** | 校准搜索算法与 SDK 不同 | 新增 `patches/root/9038-xr1710g-pcs-usxgmii-stabilize.patch`（#EXP，内层 9994=YYH e61a1bb 628 重建：保留最后一个高于 target 的 major、MSB→LSB 保留 `FL_OUT>=target` 位、删 closest-value 回退与 >100 重试块） | carried（experimental） | 内层对 6.18.44+master 真应用通过（4 hunk 因 `abs_diff` vs `abs` 漂移重建，新增行与原文逐字节等价）；**625（rate adaptation）未吸收**——语义前提 = YYH 私有 620/622（master 无 `airoha_pcs_set_usxgmii_speed` helper），9993 号段保留待 620/622 吸收后补；实机：10G 冷启动×20 + FBCK_LOCK dmesg + #22397 ifdown/up 复现 |
| F82 | PPE 本地流留 CPU（2026-09-07 WP-PPE） | PACKET_HOST（目的=本机）包走 bridge L2 fallback：桥与端口共享 MAC 时 FDB 把本机地址解析回入口端口，路由器自身发起的高速流被错误绑离 CPU（reverse UDP 实证：ppe_bind_rate=30 限 30-34 包） | 桥 L2 fallback 对本地流误判 | 新增 `patches/root/9039-xr1710g-airoha-ppe-keep-local-flows-cpu.patch`（#EXP，内层 9990 = hurryman2212 底座 + YYH 916e91a 增量 HEAD 态；先保留真实 L4 flowtable 匹配，无匹配保持 unbound） | carried（experimental；上游无底座） | 对 6.18.44+master 直接可应用（仅行号漂移）；与 9035（9995）同文件分域不重叠，9990→9995 双向顺序验证通过；实机：路由器自发 UDP 高速流对照 GDM 计数 |
| F83 | pinctrl force-GPIO（2026-09-07 WP-pinctrl） | 9001 phy5/phy8 reset-gpios（GPIO46/31）存在"reset 写了但 pad 未 mux 成 GPIO"隐患：SDK gtk_boot.sh 先置 0x228 bit31/0x22c bit14 再 RTL8261 reset；OpenWrt probe 只清 0x228 → 方向/数据寄存器写入不生效（依赖 U-Boot 遗留状态） | 驱动 probe 未恢复 force-GPIO 两 bank | 新增 `patches/root/9040-xr1710g-airoha-pinctrl-force-gpio.patch`（#EXP，内层 9998=YYH 436765d：an7581 hwinit 复位 0x228/0x22c + `force_gpio_ranges`（{0x228,gpio6-31,bit6}、{0x22c,gpio32-46,bit0}）+ `gpio_request_enable` 置 force 位 + `set_mux` 清 force 位） | carried（experimental） | master（74eb10e）已合 202-01~28/203-01/02/06/08/09（评估报告编号半对：203-01/02 确在 09-08 前后合入），内层对 KB 直接适用零重建；与 F57（gpio43/44 DTS 层申请）互补；实机：/sys/kernel/debug/gpio 46/31 方向 + PHY 复位行为 |
| F84 | RTK SerDes/SDS bundle（2026-09-07 WP-SerDes，F78 落地） | XR1710G 10G host SerDes 需板级 SDS mode（0x88c6）+ RTK SerDes flow_s 补充（PSDS0：SDS7.0x10→0x80aa / SDS6.0x12→0x5078，删二次 vendor reset）+ copper AN 后重放（PULSE→RELEASE→CHECK_CARRIER，脉冲 0x8003 保持 10-12ms 回 0x80aa，120 poll 宽限） | 公共初始化缺板级设置 + AN 后 SerDes 需要刷新 | 新增 `patches/root/9041-xr1710g-rtk-serdes-bundle.patch`（#EXP，内层 742=622（naoki66 09feeff 版）/743/744 按文件名序应用）+ `9001` phy5/phy8 dts 属性（见 F78） | carried（experimental） | 内层 742→743→744 对 6.18.44+master 真应用通过；与 9033（LED carry #24034）hunk 区不相交；与 9029/9038（PCS 侧）同域互补，实机对照后定机制取舍；`reset-before-id-read` 内核侧消费未吸收（见 F78 备注） |
| F85 | nft L2 675 系列替换（2026-09-07 WP-675） | fanboy 07-18 提交在 rebuild 中精简：675-01/02/03 由 174/406/296 行 → 121/277/235 行（-28%）：删 650-netfilter-xt_FLOWOFFLOAD 的 KEEP_HW 增量与 `DEV_PATH_BR_VLAN_KEEP_HW` 枚举、`nf_ct_bridge_inner` 签名重构（出参 proto）、Q-in-Q/PPPoE 解析调整、封装分片不 defrag 守卫 | fanboy 重构语义收敛（KEEP_HW 仅面向 switchdev 桥口，本地卸载走 nft flowtable+PPE，无消费方；枚举已删、保留 650 段即编译错） | **已替换** `patches/vendor/fanboy/06-nft-flow-offload-l2-22533-c0ed8295.patch` = `git format-patch 2ed1af79c7` 原样（无 650 段）；`9026` 内层 675-04 复核通过**未改动**（新 675-02 上下文命中，偏 18 行的旧行号属外观） | carried（default） | KB 全量应用 675-01→02→03→04 真应用全过（9 文件）；与 main pending-6.18/700 排序兼容反而改善（偏移 18→1 行）；06 文件名仍含旧 hash `c0ed8295`；bridge+FLOW_STATS 共存实机回归后转 default 评估 |
| F86 | vendor/07 内层 0014 按 be5ce791 重生成（2026-09-07 WP-vendor07） | mt76 bump be5ce791 后 `wed_rro_event` 行号 1043→766，旧内层 0014 靠 GNU patch 自动偏移（-60/-264/+1）才能过构建，审计式行号核对会红 | 上下文行号漂移 | **已更新** `patches/vendor/fanboy/07-mt76-release-hw-rro-session-5b917d4b.patch`（0014 重生成：hunk 行号 main.c 1713→1653、mcu.c 1043→779/1055→791、mt7996.h 300→301；纯行号重基，语义零变化；依赖符号 `mt7996_mcu_wed_rro_reset_sessions`(mcu.c:5611)/`mt7996_has_hwrro`(mt7996.h:861) 实测在位） | carried（default） | 对 be5ce791 真应用零 offset 通过；fanboy 重建已删同款（原因未知，本地继续自持观察）；实机 NPU/RRO 无回归 |
| F87 | F77 吸收回退：992-21 重建版重引入 F25⑥ 已删超时 hunk（2026-09-08） | F77（09-07）把 992-21 更新为 naoki66 e352c48673 83 行版，**重新带回了 F25⑥（29778aa）已删除的 `__airoha_npu_send_msg` 轮询超时 hunk（1000→500ms）**——master 内核该行已是 `100 * MSEC_PER_SEC`，hunk 上下文在 linux-6.18.44 上无法命中（ci-97 各 run 均在 mac80211 411 处先红，kernel-prep 未被触达，回归被掩盖） | 吸收校验只做「blob 与上游逐字节一致」，未重复 F25⑥ 的**内核全序真实应用**校验 | **已重建** `patches/vendor/fanboy/18-smartrg-stability-b45d5117.patch` 的 992-21 段（83→74 行：删冗余超时 hunk，重试包装 3 处调用保留）；外层 new-file 行数同步（1,83→1,74） | carried（default） | linux-6.18.44 + main 74eb10e 全序（742→…→9998 + 999-dsa 前后序）真实应用 19/19 通过；教训：**外层 blob 逐字节一致 ≠ 内核可应用**——内层补丁吸收/更新必须复跑内核全序真实应用（本轮另验 742→743→744 顺序无冲突、999-dsa-01 对 base 干净可应用） |
| F88 | SoC Status CDM1 HW Offload 恒 0% 显示缺陷（2026-09-08 实机诊断/修复） | SoC Status 页 CDM1 恒显 `HW Offload 0.0%`（CDM2 100%），误读为 offload 失效；实测 CDM1 硬件转发流量全走 RXHWF_FAST 子路径（466Mbps 下载窗口 RXHWF_FAST +105,566 帧、基线 883,324 帧），页面/JSON 从未读取该计数器，下载窗口按含 FAST 口径硬件路径份额 99.8% | `getFrameEngine` RPC 只读 RXHWF_OK（0x1fb…594），漏读 RXHWF_FAST_OK（0x1fb…598）与 FAST_DROP（0x1fb…5a8）；fanboy `12-lorenzo` tx-debug.sh 定义了 6 个 CDM RX 计数，9017 的 RPC/UI 只取 2 个；另 P0/P5 为 UI 硬编码 PSE 端口标签，非优先级 | **已修复** `patches/root/9017-w1700k-apps-pack-core.patch`：flowsense/npu 两 RPC 各补 `cdm{1,2}_rxhwf_fast`（0x1fb50598/0x1fb51598）并输出 JSON `rx_hwf_fast`；`status.js` cdmCard 改 `rx_hwf=(rx_hwf\|\|0)+(rx_hwf_fast\|\|0)` 计入百分比与 HWF 显示；`9018` 对应 context 行同步；hunk 行数审计 50 补丁全绿 | n/a（自持 UI 补丁；计数器语义无公开资料，属判定口径修复） | 实机热修验证（r0-7b39600）：CDM1 显示 0.0% → 56.4%（CDM2 100% 不变）；offload 健康度独立确认——NPU loaded、PPE BND=6、conntrack `[HW_OFFLOAD]`、466Mbps 下载 load 0.06、337 万包洪泛 load 0.00；诊断报告 `.dsh/2026-09-07-cdm1-hw-offload-diagnosis.md`。**深挖（09-08）**：CDM1/CDM2 为 PSE 独立 CPU 端口（P0/P5），各带三条 RX 计数通道（RXCPU 软件环 / RXHWF 硬件面普通投递 / RXHWF_FAST 硬件面 fast 数据），承载不同帧类——实测软件路径（SSH/ubus）→CDM1.RXCPU、硬件丢弃帧（UDP/ICMP 打本机）→CDM2.RXHWF、NPU 转发数据面（466Mbps 下载）→CDM1.RXHWF_FAST；且 `REG_PPE_DFT_CPORT0(0/1)` 把 PPE0→CDM1、PPE1→CDM2。故“CDM2 100% / CDM1 0%”=通道承载差异 + UI 漏读 FAST，非 offload 故障。skipped：FAST_DROP 未入 JSON/告警（原 drop 告警口径不变，需要时再补） |
| F89 | OC 分档取消 → 唯一档 OPP 650–1350 + 自动退档（2026-09-08 WP-OC-auto） | oc-1.3/oc-1.4 双变体构建 + oc-limit 静态限频（1300）复杂度高；1350 为社区已知“两分钟后重置”档位且个体体质差异（部分机器启动 panic），需要稳性兜底而非静态限频 | 分档矩阵 + 静态限频无法在体质差异的机器上自适应（08 调研：1.35GHz 中间档有“两分钟重置”不稳案例） | **已实施**：`scripts/prepare-oc.sh` 改 `<oc\|stock>`（oc = 唯一档 OPP 650–1350，PLL base 650，15 档网格全覆盖 1200/1300/1350）；`scripts/build.sh` + `.github/workflows/build.yml` 矩阵去除 oc-1.3/1.4（唯一 CPU 档对所有档位生效；apply-patches `--oc` 及 #OC 资产（regdb 555）随档取消，无线回落默认 regdb 521）；`files/etc/init.d/oc-limit` → `oc-auto`：performance + `scaling_max_freq=level`，5min 稳性确认窗口（覆盖 2min 不稳案例），窗口内崩溃/重启 → 下电自动退档 1350→1300→1200（`/etc/oc-state` overlay 持久化；1200 兜底不再退；可 sysfs 调低、可写 level 手工回高挡） | n/a（自持 OC 策略） | 待 ci 构建（OPP 650–1350 实机 `cpuinfo_max_freq=1350000`）+ 实机退档链演练（人工模拟确认窗口缺失/置 level=1300 验证降频与日志） |

## 已评估待吸收清单（IP-EVAL 2026-08-23，下一轮执行）

> 来源：`docs/IP-EVAL-2026-08-23.md` 的 28 个信息点深究。以下为本轮判定“可立即吸收”的条目，按执行顺序排列。
> 执行完成后，将本表对应条目转正为上方 F63+ 台账行（问题→根因→载体→上游状态），并从本清单移除。

（本轮 A1–A12 已全部执行完毕并转正为 F64–F75，清单清空。）

## 未确认/待实机核实清单
- 物理口 ↔ 逻辑名（netdev-name 已固化：lan1/lan2=双 10G、wan=1G-1（gsw_port1）、lan3=1G-2（gsw_port2），见 9001）→ 首次实机 `ip -br link` 核对
- U-Boot flash-slot.bin 的 SHA256 全文 → 以 YYH2913/http-uboot release 页为准（升级时校验）
- 6.18 下 PM domain PLL 公式的树内位置（prepare-oc.sh 找不到即报错并指导定位）

## 2026-09-11 吸收批次（深度调研落地）

| # | 项目 | 问题/背景 | 根因 | 本层载体 | 上游状态 | 备注 |
|---|---|---|---|---|---|---|
| F90 | 第三方源覆盖度核对（2026-09-11 调研） | Gilly `openwrt-patches/`（2026-09-11 记为 ~65 条；**2026-09-12 实测为 81 项**，清单存 `research-20260912/gilly_list_now.txt`）与 naoki66 的 airoha/mt76 补丁与本仓大量同号同域，需判定"已覆盖/部分/缺失"，避免重复吸收或漏吸收 | 无台账化覆盖判定 | 产出 `docs/absorptions/2026-09-11-absorption-plan.md`（含机制矩阵与优先级） | n/a | 关键结论：Gilly 745/746(DSA) **已逐字包含**于 `vendor/fanboy/09`（default）；747 机制等价于 `9029`；033 等价于 `vendor/07`；037/040/041/043 上游 `be5ce791` 已合；746(PON) 对本仓不适用；naoki 628/622/743/744/204/mt76-0010/0012 已同源覆盖；naoki 0003 与 `mt76-0012` 逐字一致 |
| F91 | bridge-flow-offload 注入缺两防护（2026-09-11） | ① `flow_offloading_hw != 1` 时仍注入 `flags offload` 流表，与用户关闭 HW offload 的意图冲突；② 上一版留在 `ruleset-post` 的陈旧 include 不被清理，开关来回切换后旧规则仍被 fw4 加载 | 脚本无守门、无陈旧文件清理（表名与 reload 重入已由 9024 处理） | 新增 `patches/root/9043-xr1710g-bridge-offload-guard.patch`（default） | n/a（本仓自持供给链；范式取自 yahuisme/w1700k-openwrt） | 在"9024 之后"基线 `git apply --check` + `bash -n` 通过；判据：关 HW offload 后 `nft list table bridge flow_offload` 为空且无 `30-bridge-offload.nft`，开回恢复且 PPE BND 计数增长。**2026-09-12 P4 已删除 `9043`**：上游 fanboy `05` 重基后 `apply-rules.sh` 已内置同一守门（F123），本层载体改为 `9024`（表名/守门表名同步） |
| F92 | airoha ring 4（force-to-CPU）硬件 DMA 越界（2026-09-11） | ring 4 承载 DHCP/PPPoE Discovery/PPP LCP/IPCP/CHAP/PAP/IPv6CP/ISAKMP/DHCPv6/SIP/LLDP（两个 CDM 的 `CDM_VIP_QSEL_MASK`=4），16 描述符只占 4096B page 的 512B；硬件越过环尾把"描述符形状"数据写入内核内存 | `RX_DSCP_NUM()` fallback 过小（16）；ring 4 未与 ring 2/11/15 同样给 128 | 新增 `patches/root/9042-xr1710g-airoha-rx-ring-grow.patch`（default，内层 `182-v7.4-…` 逐字节等同上游 PR） | **merged**（上游 2026-09-13 合入 `#24872`；kernel 7.4 队列 `e84b89f17a12`）→ 本层 `#DISABLED`，见 F151 | **顺序约束（已随上游合入失效）**：曾在 `vendor/fanboy/11` 之前（本补丁同步 916-02 的 `16→32` 上下文与 310-10/920-12/920-13 内层 hunk 行号；顺序实证 9042→vendor/11 连续可应用）；上游自带 182 与新基线后，9042 五段对上游树 `git apply -R --check` 全部干净（F151），vendor/11 照旧干净应用。上游后果实测：DHCP/PPPoE 握手期 panic 5 次（垃圾指针落在 cpufreq `dbs_irq_work`/scheduler/nf_conntrack）或 PPPoE 协商卡死（`REG_RX_DMA_IDX` 前进而 `REG_RX_CPU_IDX` 卡 15）。合入上游后删除 |
| F93 | USXGMII 速率自适应被 else 分支关死（2026-09-11） | 上游 `airoha_pcs_link_up()` 把 `RATE_UPDATE_MODE`/`FORCE_RATE_ADAPT_MODE` 整段放在 `neg_mode != PHYLINK_PCS_NEG_INBAND_ENABLED` 的 else 里，而 USXGMII 走 in-band → **永不执行**，速率切换后寄存器停在旧档 | 速率块错误地被 forced-mode 门控（SGMII 的 in-band FIFO 阈值调整才应门控） | 新增 `patches/root/9044-xr1710g-pcs-usxgmii-rate-adaptation.patch`（#EXP，内层 625 = naoki66/Ryan Chen） | n/a（作者自持；上游无对应 PR） | **直接关联本仓 2026-09-08 验收记录"PHY 宣告 10G、AN 落 2.5G"**（`docs/acceptance-results/2026-09-08-p1-regression.md`/`d25a994`）。与 9029（JCPLL）/9038（RX CDR）机制不同、互补：9029/9038 修"链路能否起来/校准"，本项修"起来后速率档位"。内层在 naoki66 基线 clean applies（offset -53）；本仓构建树未实跑 → #EXP。判据：强制 10G↔2.5G↔1G 切换后 `ethtool` 与寄存器一致 |
| F94 | NPU offload 下 tx-agg(BlockAck) 会话定时器无人刷新（2026-09-11） | 对端（Intel 站点常见）协商 BlockAck timeout 后，传输中途 DELBA，吞吐塌到未聚合速率且不自恢复；社区实测"单包 ping 即恢复、持续 ping 全程正常"（论坛 t/222776 #3900/#3901、t/249319 #440~#442，09-08~09-11 多用户复现） | `ieee80211_refresh_tx_agg_session_timer()` 全驱动仅 1 个调用点（`mt7996/mac.c` 的 `mt7996_mac_add_txs_skb()`），其门为 `mtk_wed_device_active()`，NPU 路径被排除 | 新增 `patches/packages/mt76-0013-…refresh-tx-agg-timer-under-npu-offload.patch`（#EXP，= Gilly 045） | n/a（mt76 上游无对应提交；`be5ce791` 该门仍只判 WED） | **机制前提未定**：TXFREE 必然上报（token 归还依赖它），但 TXS 无强制来源，是固件策略 → 门扩展有效当且仅当 NPU 卸载下固件仍报 TXS。判定与加固（改 `mt7996_mac_work()` 周期刷新、与 `mt76-0003` 已有轮询合并）见 `docs/absorptions/2026-09-11-absorption-plan.md` §3.1 |
| F95 | 整机复位后 NPU 未重启 + NAPI 空指针（2026-09-11 P1 批次） | 上游 `mt7996_mac_restart()` 只重启 WED：NPU 板整机复位后 NPU 仍持旧环/描述符地址 → host TX 永死；且该函数两个 NAPI 循环只对 WED 跳过 RRO 队列，NPU 下会对**未 `napi_add` 的 RRO/TXFREE 队列** `napi_disable()` → 解引用 NULL oops | 上游 restart 路径缺 NPU 分支（`mac_reset_work` 已有，`mac_restart` 没有） | 新增 `patches/packages/mt76-0014-wifi-mt76-npu-reset-panic-race-hardening.patch`（#EXP，内层 047+034+035+036+042+026+044） | n/a（mt76 上游无对应提交） | 047/034 对 `be5ce791` 真实源码逐条 `patch -p1` 命中（034 offset 26 行；047 与 `mt76-0010/0013` 同文件顺序共存实证通过）；035/036/042 命中；026/044 偏移 237 行系 Gilly 基线前进、命中成功。判据：整机复位后 host TX 恢复、dmesg 无 NAPI NULL oops |
| F96 | airoha RX 环停顿 / 中断覆盖不足 / QDMA TX BQL UAF / thermal 两处 1 行 bug（2026-09-11 P2） | ① RX 环硬件完成停顿后无恢复路径（与 F92 的"DMA 越环"是不同故障面：F92 防越界、本条治停顿）；② 只处理 RX_DONE 不处理 `RX_NO_CPU_DSCP`；③ RX queue 31 未开 RX_DONE 中断；④ 共享 QDMA TX 环 BQL underflow 与 UAF；⑤ thermal `FIELD_PREP` 掩码用错；⑥ low-trip 钳位用错变量 | 上游缺对应修复 | 新增 `patches/root/9045-xr1710g-airoha-rx-recover-irq-thermal.patch`（#EXP，内层 981+980+976+957+960+961） | n/a（作者自持；与 #24872=9042 不同 hunk，互补） | 内层为新增文件、名称与上游 6.18 无冲突（981/980/976/957/960/961 均不存在）；dry-run 67/67 全绿；未运行时验证 |
| F97 | 9016 替换：PHY LED pinctrl 错误处理（2026-09-11 P2） | 原 9016 无条件删掉 `an7581_phy_probe()` 的 `dev_err` + 检查——**掩盖**真实 pinctrl 配置错误，且不覆盖 `mt7988_phy_fix_leds_polarity()` | 以"删日志"处理噪声，而非区分错误类型 | **已替换** `patches/root/9016-xr1710g-silence-phy-led-error.patch`（内层改为 `998-net-phy-mediatek-make-LED-pinctrl-optional.patch` = Gilly 748）：`-ENODEV`（DT 无 pinctrl-0）静默；其他错误 `dev_warn` + `%pe` + "LEDs disabled"（非致命）；覆盖 an7581 与 mt7988 两处 | n/a | 内层对 Linux v6.18 `drivers/net/phy/mediatek/mtk-ge-soc.c`（47398B）两 hunk 全命中（offset +3/+10）；原版备份 `docs/absorptions/9016-before-replace-20260911.patch.bak`。判据：无 <3> 级 "Failed to setup PHY LED pinctrl"；pinctrl-0 被占用时出现 dev_warn 且 PHY 正常 probe |
| F98 | OC 前置 PLL 公式断言缺失（2026-09-11） | `scripts/prepare-oc.sh` 第 2 步（PM domain PLL 公式 `freq_mhz = <n> + state * 50` → 650）在原实现中**找不到只告警不报错** → 若上游/补丁挪位置或改写法，OC 会**静默不完整**（OPP 表平移了、PLL 公式没跟上 → 频率语义错误，比不超频更危险） | 该步骤是"尽力而为"而非断言；范式取自 yahuisme/w1700k-openwrt（基线断言 + 硬失败） | **已实施** `scripts/prepare-oc.sh`：公式存在则按基线替换并计数；**存在 PM domain 补丁但公式不匹配 → 硬失败 exit 1**（提示更新断言模式与参考文件）；**无 PM domain 补丁 → exit 1**（提示先跑 `apply-patches.sh`） | n/a（本仓脚本自持） | 三条路径实测（工作区临时树）：A 公式匹配 → 成功改为 `650 + state * 50`（exit 0）；B 补丁在但公式改为 `state * 40` → **exit 1** 且打印"含义/处理"；C 无 pmdomain 补丁 → **exit 1** 且提示先 apply-patches。`bash -n` 通过 |
| F99 | 上游基线重基预警缺失（2026-09-11） | 本仓多个补丁的**内层补丁基线**是上游具体文件（`root/9041` 内层 742/743/744 与 `root/9033` 的 LED hack 基于 `generic/pending-6.18/742\|743` 旧版上下文）。上游改这些文件时 **dry-run 不会立刻变红**（拷贝类/内层补丁不在 dry-run 覆盖内），却会让内层补丁在**构建期**才失败——典型：上游 PR #25092 改 `generic/pending-6.18/742\|743` 并新增 744/745 | 缺少"上游关键文件版本"的确定性比对手段 | 新增 `scripts/audit-upstream-watch.sh`（只读：按 git blob SHA 比对 3 个关键上游文件 + 探测 #25092 新增的 744/745 是否已出现；`--fail-on-drift` 时漂移即 exit 1）；**接入 `.github/workflows/sync-upstream.yml`**（dry-run 之前的独立 step） | n/a（本仓 CI/脚本自持） | 正例（当前上游 `e59c7876`：3 项全 ✓、exit 0）与反例（篡改 742 后报漂移并点出"受影响补丁 root/9041、root/9033"、`--fail-on-drift` exit 1）均实测通过；基线表注明"取自 openwrt main e59c7876，上游 bump 时有意更新" |
| F100 | experimental 档 kernel prepare 失败（2026-09-11 CI 实证） | ci experimental（run **34625416045**，commit `344d15e8`）在 kernel prepare 阶段失败：`992-20-net-airoha-stability.patch` **Hunk #3 FAILED at 526** → `.prepared_...` Error 1 → `target/linux failed to build`。**stock/all 均绿**，仅 experimental 暴露 | 992-20 内层 hunk#3（改 `RX_IRQ0_BANK_PIN_MASK 0x839f→0xbb9f`）的上下文写死 `RX_IRQ1_BANK_PIN_MASK 0x7fe00000`，而本批 `9045` 的内层 **976**（enable-RX_DONE-interrupt-for-RX-queue-31）已把该值改为 `0xffe00000` → **严格上下文失配**（patch 不做跨行模糊匹配） | 新增 `patches/root/9046-xr1710g-fix-992-20-context.patch`（置于 `vendor/fanboy/18` **之后**，因 992-20 由 18 生成）：仅同步该上下文值为 `0xffe00000`，**语义不变**（992-20 仍改 RX_IRQ0）；对 default 档无影响 | n/a（本仓补丁层整合修复） | 验证：audit **68/68**；`apply-patches --dry-run --oc --experimental` 对上游 `e59c7876` 全绿；树内最终 inner 992-20 的 hunk#3 上下文已同步；对 kernel v6.18 源实测该 hunk 由 FAILED → **succeeded**。**教训**：新增会改 `airoha_eth.h` 的内核补丁时，必须检查后续补丁（尤其 `992-20/992-21`）在同一文件同区域的上下文——dry-run 只覆盖外层补丁，**内层失效只能在构建期暴露**（F21/F23 盲区同型，本批新增 976 时未做该交叉检查） |
| F101 | 上游 RTL8261CE 与本地私有驱动**同硅片双实现**（2026-09-12 调研修正） | 本地 `vendor/fanboy/09`（default 档）以"新增文件"携带一整套私有 RTL8261CE 驱动（`generic/files/drivers/net/phy/rtl8261ce/` 4 文件 + `generic/hack-6.18/999-net-phy-realtek-rtl8261ce.patch`）。上游 PR **#23644**（hurrian，09-11 20:40 更新，`mergeable_state=blocked`）把**同一批功能做进主线 realtek 驱动**：`RTL_8261CE_REV_C 0x001cc899`、VEND1 SDS option slots（0x6972~0x6977）、SerDes OCP 窗口（0x7587/7588/7589/758a）、**SerDes lane polarity（DT）**、VND2 EEE（0xd036/0xd038）、过温降速位、`rtl8261c.bin`；其提交信息自述"based on a dump of the vendor driver from a **Lumen W1700K2**"，与本地驱动头注释同源。落点是 `generic/**hack**-6.18/744\|745`（**不是** pending-6.18） | ① **同 PHY ID 双驱动**：本地驱动 `PHY_ID_MATCH_MODEL(0x001cc890)` 按模型位匹配 `0x001cc89x` **全部 revision**，而主线已注册 `RTL_8261C_CG 0x001cc898`、#23644 再加 `_REV_C 0x001cc899` → 两者同时编入即对同一 C45 ID **争抢 binding**；② 上一轮 F99 守护**探错目录**（只探 `pending-6.18/744\|745`），看不到这条线；③ 上一轮判定口径是"本机硬件是否适用"（本机 c45 `0x001ccaf3` = RTL8261BE → CE 路径不适用），**对实机成立但对补丁层维护不成立**——仓库仍背上一份与上游重复的驱动 | **已实施** `scripts/audit-upstream-watch.sh` 扩面（见 F103 行：`hack-6.18/744\|745`、`pending-6.18/743-0x`、`rtl8261ce/` 目录、`dts/*.dtso` 存在性探测 + 主线 742 补丁内 `RTL_8261CE\|0x001cc899` 文本探测，并区分 [基线]/[冲突]/[缺失] 三类）；口径修正写入 `docs/absorptions/2026-09-11-absorption-plan.md` §5.3 与 §14 | upstream-open（**#23644 / backport #25092 均 open，未合**） | 判据链：**关键有利事实**——本机 10G PHY 的 c45 ID `0x001ccaf3` **就是 `RTL_8261N`**，上游 `742` 补丁已定义 `RTL_8261N 0x001ccaf3` 并有 `rtl8261be/n_match_phy_device`，即**主线本就支持本机 PHY**；私有 CE 驱动对本机 10G 链路**很可能非必需**。**尚未验证**：移除私有驱动后 LAN2 10G link 是否仍稳定（需实机）；以及 SDS-mode（本地 `9041`/F78 的 `realtek,sds-mode=0x88c6`）属板级 SerDes 配置、与 PHY 驱动选择是否解耦（需实机）。**合入后二选一**：(A) 删私有驱动改跟主线（首选，省长期 rebase 债）；(B) 保留私有驱动则把 `999` 改名避号并声明与主线 CE 入口互斥 |
| F102 | 上游 `#25132` 补齐 `PHY_AIROHA_AN7583_PCIE` 符号（2026-09-12 上游新增） | 上游 `e59c78762`（09-11 13:07Z，PR **#25132 已合**）给 `target/linux/airoha/an7581/config-6.18` 加 `# CONFIG_PHY_AIROHA_AN7583_PCIE is not set`、`an7583/config-6.18` 加 `=y`。提交信息："neither subtarget config carries, so syncconfig prompts for it and **every airoha kernel build fails**"（`Fixes: 443e267b`） | 本仓 602-04（`patches-6.18/602-04-phy-airoha-add-support-for-AN7583-PCIe-PHY-driver.patch`）从 **patches-6.18 侧**引入同一 Kconfig 符号——即本仓曾处在"加了 Kconfig 符号但磁盘 config 未显式置位"的同一条构建红线上，只是补丁形态不同而未爆 | **无需新增补丁**：实测对上游 `f0d3e332` 全量 dry-run **68/68 全绿**，602-04 与上游 config 改动不冲突（一个改 Kconfig 源、一个改 config 值）。仅登记为"同类构建风险"参照 | upstream（已合入 main） | 验证：`apply-patches . --dry-run --oc --experimental` 对 `f0d3e332` → **处理 68 / 跳过 0 / 缺失 0**、exit 0；拷贝类真实应用 **4/4**。**留观察项**：本仓后续若再增 airoha Kconfig 符号（如 602-05 类），应同步检查 `an7581/config-6.18` 是否需显式 `is not set` 以避免 syncconfig 交互 |
| F103 | 上游守护扩面 + 基线锚点可读性（2026-09-12，F99 缺陷修复） | F99 的守护有两个实证缺陷：① **探错目录**——只有 `pending-6.18/744\|745` 两个**写死文件名**，而 #23644 实测落在 `hack-6.18/744\|745`、且新增了 `rtl8261ce/` 驱动目录与 `dts/*.dtso`，全部漏报；② **基线硬化**——基线表硬编码 `e59c7876` 且不显示"当前上游 sha"，上游前进后输出"漂移 0"虽正确但无法区分"真没碰"与"基线该刷新"；③ 实现层**误判**——`git rev-parse HEAD:<path>` 在**已删除的工作区文件**上仍返回 blob（不报错），且冲突探测只看 git 对象不看工作区，导致"磁盘上有、未提交"的文件被漏报 | 探测面不足 + 只读 git 对象 + 报告不区分故障类别 | **已重写** `scripts/audit-upstream-watch.sh`：基线表扩到 **6 个 blob**（新增 `an7581/config-6.18`、`image/an7581.mk`、`uboot-airoha/Makefile`）+ **7 条同址冲突 glob**（`hack-6.18/744\|745`、`pending-6.18/743-0*`、`pending-6.18/744\|745`、`rtl8261ce/*`、`airoha/dts/*.dtso`）+ 主线 742 补丁 `RTL_8261CE` 文本探测；路径清单取 **git 提交视图 ∪ 工作区视图**（`ls-tree` + `ls-files --cached --others`）；文件 SHA 取工作区 `git hash-object` 且**先判存在性**；输出分 **`[基线]`/`[冲突]`/`[缺失]`** 三类并打印"上游 HEAD vs 基线锚点" | n/a（本仓 CI/脚本自持） | 实证 **10/10**：正例（上游 `f0d3e332` 干净树）全 ✓ exit 0；反例 6 组均正确 exit 1——`hack-6.18/744` 冲突、`rtl8261ce/` 目录被占、工作区漂移（无 flag exit 0 / `--fail-on-drift` exit 1）、主线 742 含 `0x001cc899`、`dts/*.dtso`、上游删除基线路径（合成树）。端到端 CI 等价序列复跑：守护 exit 0 → `audit-patches` **68/68** → dry-run **68/0/0** + 拷贝类 **4/4**，且 dry-run 后 tracked 工作区无残留。**注**：一次中间态失败系测试把树弄脏（`an7581.mk` 与索引不匹配），`git checkout -- .` 复原后全绿，非脚本缺陷 |
| F104 | PPE flow bind rate 过高导致交互流永不卸载（2026-09-12 调研吸收） | AN7581 PPE 只在流**超过 `REG_PPE_BIND_RATE` 包数后**才加速：低于阈值的流整个生命周期都由 CPU 处理。vendor 默认 `0x1e`(30) **高于多数交互流的包速率**（游戏/VoIP/延迟探针），空闲链路上无感，但与批量卸载传输并存时该流吃 **60-250ms 周期尖峰**（即 bufferbloat 测试里的周期尖刺）。实测（150ms 探针对已卸载传输）：阈值 32 → loaded p90 **157ms**、>40ms 占比 30%；阈值 8 → **15ms**、0%；bufferbloat 评级 **B→A+**（双向 loaded latency +1ms） | ① 硬件阈值写死在 `airoha_ppe_hw_init()` 的 `FIELD_PREP(PPE_BIND_RATE_*_BIND_MASK, 0x1e)`；② 无 debugfs 旋钮，无法在线调参 | 新增 `patches/root/9047-xr1710g-airoha-ppe-bind-rate.patch`（内层 `target/linux/airoha/patches-6.18/9997-net-airoha-ppe-bind-rate.patch`，来源 openwrt 论坛 t/222776 **#3890** NoTengoBattery/Oever González，2026-09-04；**与本仓 F79=mt76-0011 同帖同作者的 Patch 2**）：默认降到 **8**（1..10 实测同值，8 = 最小且仍放过 DNS/NTP 类偶发流以免占表项）+ `debugfs "bind_rate"`（0600）读写旋钮 | n/a（作者自持，未提上游 PR） | **全序实证**：裸核源 → 9039 内层 `9990`（改 `airoha_ppe.c` 853/1046/1057 区）→ 本项内层 `9997`（改 136/223 区 + `airoha_ppe_debugfs.c` + `airoha_eth.h`）连续应用成功，终态 `AIROHA_PPE_BIND_RATE_DEFAULT 8` + `bind_rate` debugfs 落位；`audit-patches` **69/69**；`apply-patches --dry-run --oc --experimental` **69/0/0** + 拷贝类 **4/4**。⚠️ 内层 `9997` 的 hunk#1 在真源上为 **fuzz 1（offset 39）**——结果已校验正确，但按 F87/F100 纪律**待消 fuzz**（重排 hunk#1 上下文）。实机判据：`cat /sys/kernel/debug/ppe/bind_rate` = 8；`echo N >` 可热调；负载下探针 p90 显著下降 |
| F105 | 9046 无条件应用导致 **stock 档 kernel prepare 必红**（2026-09-12 CI 实证 + 修正） | push 触发的 stock 构建在 kernel prepare 失败：`992-20-net-airoha-stability.patch` **Hunk #3 FAILED at 526** → `airoha_eth.h.rej` → `.prepared_xxx Error 1` → `target/linux failed to build`（run `34658979893`@`f989b80f`、`34658934256`@`c0ce5710` 两次实证；`629b3e8` 尚绿——因 9046 于 `c0ce571` 才加入）。**F100 当时判"仅 experimental 暴露"是误判**：experimental 那次（`34625416045`）先在同一处红，掩盖了 stock 同样会红——两档皆红，只是先看到 experimental | `9046` 把内层 `992-20` 的 hunk#3 上下文 `RX_IRQ1_BANK_PIN_MASK` 由 `0x7fe00000` 改写为 `0xffe00000`，但该值**只在实验档**被 `9045`（#EXP）的内层 `976` 改掉；default/stock 不应用 `9045`，裸核源（linux-6.18.44 `airoha_eth.h:518`）仍是 `0x7fe00000` → 992-20 找不到上下文 → 整份 patch 失败。即"档位耦合的上下文同步补丁被当成无条件补丁" | **已修正** `patches/MANIFEST`：`9046` 标记 **`#EXP`**（与 `9045` 同档，二者为一对：`976` 改源 → `9046` 同步内层上下文）；并同步该条目注释说明"必须 #EXP" | n/a（本仓补丁层档位修正） | 验证：`audit-patches` default **53/53**（9046 已剔除）、experimental **69/69**；`apply-patches --dry-run` 默认档 exit 0（53 处理/16 实验跳过）、`--dry-run --oc --experimental` exit 0（69/0/0）；交叉核对：裸源 `RX_IRQ1=0x7fe00000` **正是** 992-20 hunk#3 的期望上下文（修复前 9046 会改成 `0xffe00000` → 与失败日志 "Hunk #3 FAILED at 526" 完全一致）；`sync-upstream` 对 `4ec4359` success。**教训**：新增"改内层补丁上下文"的补丁时，必须先判定该上下文是否**档位相关**——dry-run 只覆盖外层，档位耦合错误只能在构建期暴露（F21/F23/F100 盲区同型，本次是该盲区的第二次实例） |
| F106 | 运行期 offload sysctl 不在升级保留清单（2026-09-12 P1 批次） | `/etc/sysctl.d/{14-vlan,15-pppoe}-offload.conf` 由 FlowSense 的 VLAN/PPPoE 卸载开关**运行期写入**（`9018`），而上游 keep 清单 `package/base-files/files/lib/upgrade/keep.d/base-files-essential` **只保 `/etc/sysctl.conf`、不含 `/etc/sysctl.d/`** → "保留配置" sysupgrade 后用户设过的开关**静默丢失**、桥过滤策略回到镜像默认 | 上游 keep 清单不覆盖运行期生成的 sysctl 片段 | 新增 `files/lib/upgrade/keep.d/xr1710g-offload`（两行路径 + 说明注释；`files/` 由 CI 落 `$(TOPDIR)/files`，rootfs 组装时覆盖，**零补丁成本**）。范式取自 yahuisme/w1700k-* 的 keep.d 手法 | n/a（overlay 文件） | 判据：启用开关后做一次"保留配置"sysupgrade，`ls /etc/sysctl.d/14-vlan-offload.conf /etc/sysctl.d/15-pppoe-offload.conf` 均存在；`cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged` 与用户设定一致。**注**：工作区根 `13-nf-bridge.conf` 经核实**未被任何脚本/CI 引用**（不部署），故未纳入 |
| F107 | `airoha_ppe_flush_sram_entries()` 内层 `int err;` 遮蔽 → 恒返回 0（2026-09-12 P1 批次） | 函数外层 `int i, err = 0;`，循环体内又声明 `int err;`（遮蔽）；循环失败时 `break` 后 `return err` 读到的是**外层**变量，恒为 **0** → SRAM FOE 刷写失败被静默吞掉，`airoha_ppe_init()` 拿不到真实 errno | 变量遮蔽（-Wshadow 类缺陷），6.18.44 真源 `airoha_ppe.c:1397/1400` 仍存活 | 新增 `patches/root/9048-xr1710g-airoha-ppe-flush-sram-err.patch`（内层 `9998`，Gilly 949；default 档） | n/a（作者自持；B2 逐行核对**推翻**了工具初判的"已覆盖"假阳性） | 内层对 6.18.44 真源实证：hunk#1/#2 命中（offset 64），终态 `int i;` + `return err;` + `return 0;`。`audit-patches` 73/73；dry-run 73/0/0 |
| F108 | NPU 板 per-STA 流量统计恒 0 + 固件计数器不上报（2026-09-12 P1 批次） | ① `mt7996_mac_work()` 的 ADM_STAT/MSDU_COUNT 轮询门**只判 WED**——本仓 `mt76-0003` 恰好改了同一 hunk 的上面两行（加 AIR_TIME/PER_STA_RSSI）却**没动这个门** → NPU 板 `wcid->stats.{tx,rx}_{bytes,packets}` 恒 0（per-link 速率仍在更新，因 TXRX_RATE 轮询无条件）；② `mt7996_sta_statistics()` 同样只在 WED 上报固件计数器，NPU 绕过 mac80211 → 软件计数 30s 只看到 **161** 包 vs 固件 **8937** 包（5GHz 实测） | NPU 卸载与 WED 同样绕过 host，但两处门未覆盖 NPU | 新增 `patches/packages/mt76-0015-wifi-mt76-mt7996-npu-firmware-sta-counters.patch`（#EXP，= Gilly 039+046 **合一个补丁**：046 的计数器由 039 扩开的轮询喂给，必须同档）。039 的 hunk 已**按 0003 之后的上下文重基**（原作者上下文不含 0003 加的两行，直接应用会 FAILED） | n/a（mt76 上游无对应提交） | 真源全序实证：pin `be5ce791` → `mt76-0003` → `0015`，mac.c/main.c 均命中（main.c offset -79）；终态 mac.c 的 WED 门带 `\|\| mt76_npu_device_active()`、main.c 走 WED\|\|NPU 且固件计数为 0 时留给 mac80211。实机判据：NPU 板 `iw station dump` 每站 bytes/packets 非 0 且随流量增长 |
| F109 | mt76 per-STA debugfs 被整块编译掉（2026-09-12 P1 批次） | mt76 走 **backports** 构建，autoconf 只定义 `CPTCFG_MAC80211_DEBUGFS`（由 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 经 mac80211 包 `Makefile:255-258` 映射），而 mt76 源码用 `#ifdef CONFIG_MAC80211_DEBUGFS` 判断（pin `be5ce791` 共 **6 处**：mt7996/debugfs.c:1006、main.c:2633、mt7996.h:992 + mt7915 三处）→ `CONFIG_` 前缀名**从未定义** → 全部编译掉，per-STA debugfs（hw-queues/fixed_rate）静默缺失；也是 Gilly 038（per-link sta stats debugfs）的**前置**（不修映射 038 是死代码） | 上游 openwrt mt76 包只映射 MESH/TESTMODE，未映射 DEBUGFS | 新增 `patches/root/9049-xr1710g-mt76-mac80211-debugfs.patch`（default 档，改上游 `package/kernel/mt76/Makefile`：按 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 条件加 `NOSTDINC_FLAGS += -DCONFIG_MAC80211_DEBUGFS`，与同文件 MESH/TESTMODE 写法一致——**默认档不改变镜像**）；`config/seed-config.diff` 加注释化的开关说明 | n/a（上游缺此映射） | 真源应用实证：上游 `package/kernel/mt76/Makefile` hunk 命中，映射落在 417-420。判据：打开 `CONFIG_PACKAGE_MAC80211_DEBUGFS` 后实机 `ls /sys/kernel/debug/ieee80211/*/netdev:*/` 出现 stations 目录 |
| F110 | airoha GDM MIB 采集三处缺陷（2026-09-12 P1 批次） | ① **64 位** H+L 计数器（TX/RX OK pkt/byte、E64/L64/L127）被 `+=` **累加**——硬件本身已累计，每次 poll 再加一遍 → `/sys/class/net/*/statistics` 随 poll 次数**虚增**；② **32 位**计数器（drop/BC/MC/RUNT）**无绕回处理** → 硬件绕回后统计**倒退**；③ RUNT(32b) 与 E64(64b) 混入同一 `tx_len[0]/rx_len[0]` 桶且口径不一致。另缺 `port->lock` 串行化 | 上游 `airoha_dev_get_hw_stats()` 累加语义错误 | 新增 `patches/root/9050-xr1710g-airoha-mib-stats-lossless.patch`（**#EXP**，内层 `9999`，Gilly 963）：64 位改直接赋值、32 位改 delta 累加（`mib_prev`）、RUNT 单独 `run64` 运行和、加 `port->lock` | n/a（作者自持） | 真源全序实证：9025（内层 9991）→ 9050 连续应用成功；终态 `airoha_update_hw_stats()`（含 spin_lock）+ `mib_prev.tx_runt64` 等字段落位。**档位 #EXP 的理由**：改的是**对外可见统计语义**（累加→绝对），影响 F38 的 rx_errors 判定口径，需实机确认 sysfs/ethtool/FlowSense 读数自洽后再毕业 |
| F111 | 9047 内层 hunk#1 的 **fuzz 1** 消解（2026-09-12，F104 收尾） | `9047` 吸取的论坛补丁其 hunk#1 以作者基线为准（该基线 `airoha_ppe_hw_init` 紧跟 `DFT_CPORT` 函数），而 6.18.44 真源在两者之间还有一个 `airoha_ppe_set_mtu()` → 上下文第 3 行起不匹配，GNU patch 以 **fuzz 1** 容错通过（默认值与本意均正确，但按 F87/F100 纪律应消） | 内层补丁上下文基于作者基线，非本仓真源 | **已修复**：按 6.18.44 真源重建 hunk#1（上下文严格取真源 134-137 + 138-141、hunk 头 old/new = 8/36），fuzz → **0**；`9047` 外层与 MANIFEST 同步重建（`@@ -0,0 +1,160 @@`，声明=实际） | n/a（本仓内层重基） | 实证：重建后内层对真源应用**无 fuzz**（`Hunk #1 succeeded`，默认值落 `airoha_ppe.c:139`）；`audit-patches` **73/73**；dry-run **73/0/0**；拷贝类 **4/4**。**教训**：吸取外部补丁时，hunk 头行数必须按**本仓真源**重算——本次连续踩了两次（先是漏算尾部上下文导致 32≠36，再是注释行缺 `+` 前缀），都靠 audit 的"声称 vs 实际"检查兜住 |
| F112 | mt7996 radar 事件误归属到全部 band（2026-09-12 P2 批次） | `ieee80211_radar_detected(mphy->hw, NULL)` 用 NULL chanctx 上报：**单 wiphy 多 radio**（本机 MT7996 = phy0 承载 radio0/1/2）下内核会把**所有** channel context 标记为 radar → 连 2.4G 这种**没有 radar 检测器**的 BSS 也记 radar 事件（触发 DFS/CAC 逻辑） | radar 上报未带"检测方 phy 的 chanctx" | 新增 `patches/packages/mt76-0016-wifi-mt76-mt7996-radar-detecting-chanctx.patch`（#EXP，= Gilly 020）：传检测方 phy 的 chanctx（`container_of(mphy->chanctx, ...)`），phy 无 chanctx 时回退 NULL（保持原行为） | n/a（作者自持） | pin `be5ce791` 真源应用实证（offset -237）。实机判据：5G/6G 触发 radar 后，2.4G BSS 不再出现 radar 事件日志 |
| F113 | 亚 64 窗口 BlockAck 使 HW_RRO 状态机非法（2026-09-12 P2 批次） | Apple macOS/iOS 站点在延迟敏感 TID（TID6 `buf_size=8`、TID1 `buf_size=16`）协商**亚 64 窗口** BlockAck，与标准 64/256 窗口批量队列并存。MT7996 的 HW_RRO（硬件重排卸载）按 **64 帧最小窗口**设计 → 亚 64 会话使片上重排环与 SRAM 会话查找状态机进入非法态，**复用该 WCID 的后续关联硬件 RX 卸载停摆** | `ba_rdd_rro` 只判 `mt7996_has_hwrro(dev)`，未看窗口大小 | 新增 `patches/packages/mt76-0017-wifi-mt76-mt7996-bypass-hwrro-sub64-ba.patch`（#EXP，= Gilly 032）:`ba->ba_rdd_rro = !tx && enable && mt7996_has_hwrro(dev) && params->buf_size >= 64`——亚 64 交回 mac80211 软件重排，>=64 仍走硬件 | n/a（作者自持） | pin 真源应用实证（offset -252）。实机判据：Apple 站点断连重连后 RX 卸载不停摆；亚 64 会话 `ba_rdd_rro` 为 0 |
| F114 | GRO fraglist 套 GSO 的嵌套缺陷（2026-09-12 P2 批次） | Fraglist GRO 与硬件 GRO 可叠出"fraglist 里套 GSO skb"结构：P1（普通 TCP）与 P2（**HW-GRO 聚合出的 GSO skb**，数据在 `frags[]`）被 `skb_gro_receive_list()` 聚成 P3，P3 的 frag_list 里 P2 仍带 `frags[]`；后续 `tcp4_gso_segment()`/`skb_segment_list()` **只切 frag_list 条目、不切 P2 的 frags[]** → 还原不回原始包 → Tethering/转发场景 IP 分片或丢包 | `is_flist = !sk;` 未排除 GSO skb | 新增 `patches/root/9051-xr1710g-gro-fraglist-gso-nesting.patch`（#EXP，内层 `9999-net-gro-fix-fraglist-gso-nesting.patch`，= Gilly 993 / MediaTek netdev v5，**上游未合**）：`is_flist = !sk && !skb_is_gso(skb)`（已有 fraglist 遇 HW-GRO 包先 flush）。**本仓命中前提**：`vendor/11` 默认给 airoha 设备开 `NETIF_F_GRO_HW`，且上游 `hack-6.18/600` 默认开 `GRO_FRAGLIST`（Gilly 自称"stock 上 no-op"**对本仓不成立**） | upstream-open（netdev v5 未合；Gilly 自述每次 master sync 复查，合入即删） | 6.18.44 真源全序实证：`net/ipv4/tcp_offload.c` 4 hunks + `net/ipv6/tcpv6_offload.c` 2 hunks 全命中，终态 `is_flist = !sk && !skb_is_gso(skb)`。实机判据：HW-GRO 与 Tethering/转发同开时 TCP 大包不被 IP 分片、无异常丢包 |
| F115 | **IPsec 硬件卸载基础：xfrm packet-offload 框架 + EIP93 动态软回退**（2026-09-12 P2 批次，**承接 F52/issue #6**） | ① **A 系列（xfrm 框架）**：引入 xfrm packet-offload manager——`ndo` 发送回调、per-netns manager 跟踪、state 生命周期/校验/UAF 加固、driver-own-transmit、policy 变更通知、用户更新时保留 ownership。这是让 **AN7581 SOE** 能接管 ESP 发送路径的**基础设施**。② **D 系列（EIP93 动态软回退）**：crypto 层引入动态 fallback，EIP93 能加速的走硬件、**不能的（GCM/rfc4106）透明回退软件**——正面解开 F52 的死结（F52 原结论："EIP93 未注册 GCM-AEAD，故 ESP 主用 AEAD 落不到硬件，先验证后供给"） | 本仓无 xfrm packet-offload 框架（上游 6.18 亦无）；EIP93 只编了驱动、无回退机制 → 硬件加速路径不可用 | 新增两个 `#EXP` 捆绑（来源 `hurryman2212/OpenW1700k-test@offload-oc`，**同作者即 PR #23644 的 hurrian**）：<br>· `patches/root/9053-xr1710g-xfrm-packet-offload.patch`（内层 `9999-xfrm-packet-offload-framework.patch`，A 系列 9 补丁）<br>· `patches/root/9054-xr1710g-crypto-eip93-dynamic-fallback.patch`（内层 `9999-crypto-eip93-dynamic-fallback.patch`，D 系列 4 补丁 + 1 backport） | n/a（作者自持；未提上游 PR） | 真源全序实证：**A 9/9** 干净应用（最大 offset 8）、**D 5/5** 干净应用（正确新增 `drivers/crypto/inside-secure/eip93/eip93-fallback.{c,h}` 并接入 Makefile）。**依赖链**：D 依赖 `vendor/fanboy/02`（EIP93 驱动，同为 #EXP）；A+D 是 **B 系列 SOE 主体（未做，见 `handoff/handoff_p2.md` §2.9）**的前置。**三者同档才有完整意义**。判据：① 构建通过；② `ip xfrm state add` 后对照 `authenc(hmac(sha256),cbc(aes))`（硬件）vs `rfc4106(gcm(aes))`（应软回退）；`grep eip93 /proc/crypto` 出现在实际使用的算法链 + EIP93 统计增长；③ SOE 主体落地后测 iperf3 over IPsec 吞吐 |
| F116 | PCIe AER 只读诊断探针 + 判读文档（**F56 / F95 的 AER 视角**） | MT7996 走 PCIe，社区已有**两台 W1700K V1.1 无线芯片永久失效**案例，日志链固定为 `14c3:6899` Correctable RxErr → Uncorrectable Fatal `[ 5] SDES` / `[14] CmpltTO` → `mt7996e_hif AER: can't recover` → `Root Port link has been reset` → `device recovery failed` → `/sys/bus/pci/devices` 里设备消失；本仓此前**没有 AER 视角**——F56（hif 仅 Gen2 x1 板级上限）与 F95（整机复位后 NPU/chip full reset failed）只是同一条 Root Port 物理路径的两个侧面，都无法区分"可恢复"与"硬件死亡" | AER 是唯一能区分"可恢复（Correctable/Non-Fatal）vs 硬件死亡（Fatal + root port link reset + recovery failed）"的观测面，而本仓既无采集脚本也无判读口径；且 AER **Uncorrectable Error Severity** 基线 `0x00462030` 中 bit5(SDES) **出厂即 Fatal**，无降级手段时内核直接走 link reset，后续证据（Status/dmesg）看不到 | 新增 `scripts/device-pcie-aer-probe.sh`（默认**只读**：W0 目标 BDF 存在性 → W1 `lspci -nn` + 每设备 link 属性 → W2 每设备读 config `seek=131/129/132`（`dd \| hexdump -C`）小端解析 + 逐位标注 → W3 `lspci -vv` 的 AER 段 → W4 dmesg 关键链（末 60 行）→ W6 "可贴进 issue"汇总 + 结尾判读结论行）<br>+ `docs/PCIe-AER.md`（寄存器/位 → 含义 → 观测值 → 三类结论判读表 + 只读/降级两种用法 + 风险）<br>写模式**仅** `AER_DEMOTE=1`：先打印待写字节/目标设备/还原提示，再 `printf '\x10\x20\x46\x00' \| dd of=<config> bs=4 seek=131 count=1`（`0x00462030`→`0x00462010`，只清 bit5 的 severity 位；需 reboot 或重新 enable 才完全还原） | n/a（本仓脚本 + 文档自持，**零补丁成本**，不碰 patches/config/files） | 位名取自本仓内核树 `drivers/pci/pcie/aer.c`：`aer_uncorrectable_error_string`（bit4 DLP / **bit5 SDES** / bit13 FCP / **bit14 CmpltTO** / bit18 MalfTLP / bit22 UncorrIntErr）与 `aer_correctable_error_string`（**bit0 RxErr** / bit6 BadTLP / bit7 BadDLLP / bit8 Rollover / bit12 Timeout）。退出码 `0`=正常 / `2`=SSH 不可达（`ConnectTimeout=8`+`BatchMode`，不挂死）/ `3`=目标 BDF 不在 `/sys/bus/pci/devices`（**已被 AER 摘除**）。**F56 的 AER 视角**：根端口不声明 Gen3 ⇒ 没有"降速保命"退路，Correctable 计数增长应视为硬信号而非可调优项。**F95 的 AER 视角**：整机复位与 AER root port link reset 命中同一 Root Port 路径，复位后若出现 SDES / `device recovery failed`，说明链路本身没起来，与 F95 的复位可靠性问题叠加。验证：`bash -n` 通过（远程 payload 单独 `sh -n` 亦通过）；`grep -n -B3 'dd of='` 证明唯一可执行写点在 `AER_DEMOTE=1` 分支内。判据：无线失效复现时本脚本输出 `AER 判读：Fatal + Root Port link reset + device recovery failed ⇒ 硬件/PHY 层失效，非软件可修`。**2026-09-16 P19 修正（F172 取证副产品，坑 42）**：默认 BDF 由硬编码 `0002:00:00.0` 改为**按 driver=mt7996e 自动探测**——本机 mt7996e 实为 `0000:01:00.0`（两个 PCI domain：0000 主 + 0002 hif2），旧默认指向错误目标；显式覆盖仍用 `AER_BDF=<BDF>`，探测失败退出码新增 `4`；`bash -n` + 远程 payload `sh -n` 通过 |
| ↳ | 判读链速查（F116） | — | — | — | — | Correctable `RxErr` = 可恢复/早期劣化信号；`Status bit5 SDES=1` ⇒ **链路对端消失（Surprise Down）**；`Severity bit5=1` ⇒ 该错误按 Fatal 处理（内核直接走 root port reset）；`Fatal + Root Port link reset + device recovery failed`（或 BDF 从 `/sys/bus/pci/devices` 消失）= **硬件/PHY 层失效，非软件可修**；降级写只把 severity 由 Fatal 改 Non-Fatal 以便**继续观测**，不修复任何东西——降级后判读一律以 Status 位 + dmesg 链为准 |
| F117 | br-lan 与 10G LAN 口共用 MAC 导致管理面失联（2026-09-12 P2 第 2.7 项；**已实机复现（stock r0-f36067d，2026-09-12）**） | 10G WAN 与 10G LAN **同时插上**时 LuCI/管理页打不开（上网正常）；拔 WAN 也不恢复，必须换到 1G LAN 口才能进管理页。同族第二症状：lan2 拿不到 DHCP。社区三源独立报告（论坛 t/247242 #167 2026-08-15、恩山 8484725 #23188342 XFZAIMFQ「naoki66 与 YYH2913 两条构建线都中」、naoki66 issue #17 CLOSED）。**实机复核（`device-hw-probe.sh` route_e）**：`br-lan 00:58:28:d7:44:5d == lan1 00:58:28:d7:44:5d` → `VERDICT: CONFLICT`；`br-lan/brif = lan1 lan2 lan3 phy0.{0,1,2}-ap*`（lan1 为第一成员） | `files/etc/config/network` 的 `config device 'br-lan'` 只声明 `list ports`、**无 macaddr**；内核建桥时 br-lan 从**第一个成员口**继承 MAC → br-lan 与第一成员口 lan1 **逐字节同 MAC**（实机确证）。lan1 的 MAC 来自板级 DTS nvmem：`9001` 的 `&gdm2`（lan1）与 `&gdm4`（lan2）**都写 `<&lan_mac 0>`**（**不是**任务书所述的 `<&lan_mac 1>`/`<&lan_mac 2>`——按仓内 9001 原文与实机 MAC 均为 `<&lan_mac 0>`）；桥/PPE(hw flow offload) 下发往路由器自身（管理面 192.168.123.1）的帧在 L2 上被判为成员口本地流量、走入硬件卸载/FDB 路径被吞 → 管理面失联（上网路径不受影响）。**实机并见第二组重号**（另一项，本项未处理）：`eth0 == lan2 == lan3 == wan == 00:58:28:d7:44:5e`（1G 口继承 eth0 CPU 口 MAC）——若实机仍见管理/DHCP 异常，应连载此项一并审（见备注） | 新增 `files/etc/uci-defaults/98-xr1710g-brlan-mac-unique`（**迁移脚本，只在检测到冲突时改写**）：① 板型门 `gemtek,xr1710g-ubi`；② 用户已显式设 `network.@device[br-lan].macaddr` → 不动；③ 逐成员口比 MAC，**只有冲突才动手**（br-lan 未 up/无 MAC 视为冲突）；④ 派生 = 前 5 字节取第一个成员口（**Gemtek OUI 00:58:28 不变**）+ 末字节从 `0x02` 起取第一个不与任何成员口相同的值（末字节 0x02 使**整条地址的 bit1=1**（802 本地管理位即全 48 位的 bit1）→ 本身已是本地管理地址，故无需改首个八位组；改它会得到未分配 OUI `02:58:28`，是净损失）；⑤ `uci set` + `uci commit network` + `logger -t xr1710g-brlan`；⑥ 幂等（第二次零写入零 commit）；⑦ POSIX sh、`sh -n` 通过、**不新建 macaddr option 以外任何配置**。配套 `scripts/device-hw-probe.sh` 新增**只读** route_e：E1 `ip -br link`（回退 `ip -o link`）+ E2 各口 MAC + E3 **冲突/唯一**判定与逐口 CONFLICT/unique 标注 + E4 `br-lan/brif` 成员面 + E6 ethtool + E7 MAC 字节图（`LAN_MAC_PING=1` 才追加 lan1/lan2 → 网关 ICMP） | n/a（自持 overlay + 迁移脚本；DTS 根因改动风险高，用户明确选脚本路线，`patches/root/9001-xr1710g-dts.patch` 与 `files/etc/config/network` 默认 MAC **均未改**，避免影响所有镜像） | **本地行为自测 19/19 PASS**：`research-20260912/p2-verify/test-brlan-mac-unique.sh`（mock uci/board_name/logger/ip/cat）——板型不符/用户已设/无冲突三条路径**零写入**；冲突路径写 `00:58:28:d7:44:02`（0x02 被占时递增 0x03）；幂等重跑零写入；多 device 段写对 `[1]` 不误写 `[0]`；成员口全缺席不动；`bash -n scripts/device-hw-probe.sh` + `sh -n 98-xr1710g-brlan-mac-unique` 通过。**实机验证（stock r0-f36067d，2026-09-12，只读）**：route_e 判 `CONFLICT`；脚本路径预演（无 uci 写）`user macaddr=空` → `conflict=1` → 派生 `00:58:28:d7:44:02` → 与**现存全部 netdev**（含 4 个 AP 口）**零重号**，且与三成员口 lan1/lan2/lan3 互不相同（幂等成立）。**实机判据（待用户执行后回填）**：① 10G WAN + 10G LAN 同插时**管理页可访问**；② `ip -br link`（或 route_e）显示 br-lan 与 lan1/lan2 MAC **互不相同**、E3 判 `VERDICT: UNIQUE`；③ **lan2 单独接主机能拿到 DHCP**。**⚠ 应用后 br-lan MAC 变更 → 桥重建、DHCP/邻居缓存失效，需重连（管理面经 br-lan 时 ssh 会断一次，属预期）** |

| F118 | PPPoE 自动 IPv6 与显式 DHCPv6 双客户端互斥（2026-09-12 P2 第 2.2 项） | 本仓 `files/etc/config/network` 预置 `config interface 'wan6' { proto 'dhcpv6' }`，注释写"PPPoE 在 LuCI 面板改"。用户切成 PPPoE 后，PPP 的 `ipv6=auto` 会自动再建一个动态 DHCPv6（`ppp6-up` 的 `AUTOIPV6` 路径）→ **两个 `odhcp6c` 跑在同一 L3 设备**上，而 odhcp6c 的 ubus 对象名固定为 `odhcp6c.<设备>`（真源 `src/ubus.c` `ubus_init()`；`ifname` = `proto_run_command` 末参）→ 第二个 `ubus_add_object` 返回 **EEXIST** → 进程退出 → netifd 重启 → **IPv6 间歇不可用**（触发条件是本仓**已文档化**的操作路径，不是边角场景） | 上游 PPP/odhcp6c 未内建双客户端互斥；本仓预置 `wan6` 与 PPP 自动 IPv6 各自独立决策 | ① 新增 overlay `files/lib/netifd/xr1710g-dhcpv6-guard.sh`（`config_load`/`config_get_*` 全包在**子 shell**，不污染 PPP hook 的 JSON/env；只认同设备的 netifd 客户端，**绝不杀**无 `INTERFACE=` 的第三方 odhcp6c；`@parent` 间接引用带深度上限 16 防环）；② `patches/root/9056-xr1710g-dhcpv6-duplicate-guard.patch`（外层包裹 4 份内层：`ppp/patches/001` 在 `AUTOIPV6` 判定前 source guard → 清空 `AUTOIPV6`、`ppp/patches/002` `PKG_RELEASE` 2→3、`odhcp6c/patches/001` 在 `proto_dhcpv6_setup()` 入口检测重复 → `proto_notify_error DHCPV6_DUPLICATE_CLIENT` + `proto_block_restart`、`odhcp6c/patches/002` `PKG_RELEASE` 3→4）。**档位 default**：无冲突时两条路径与现状逐字等价，只在真冲突时生效；`odhcp6c` 上游无 `patches/` 目录 → 由外层 `new file mode` 直接创建（构建期按目录 glob 应用，无需脚本 mkdir） | n/a（orangeyoo 0103/0104 自持，未提上游 PR） | **基线核对（f0d3e332 真源 grep）**：`ppp/Makefile:13 PKG_VERSION:=2.5.3 / PKG_RELEASE:=2`、`odhcp6c/Makefile:11 PKG_RELEASE:=3` —— 与缓存 0104 基线**一致**（无需重算）。**实证**：外层 `git apply --check` ✓；抽取内层后 `patch -p1 --fuzz=0 --dry-run` **4/4 洁净**（0003 上下文按真源重算为 `@@ -72,6 +72,16 @@` / `@@ -15,6 +15,13 @@`，修正了缓存 0103 的 `@@ -85,6` / `@@ -16,6` 作者树行号）；`sh -n` guard 脚本 ✓；guard 逻辑本地 mock 回归 12/12（disabled/auto=0/异设备不误判、`@wan6x→@wan` 二级间接命中、纯环终止、无 `INTERFACE=` 第三方不触发）。**实机判据（待做）**：切成 PPPoE 后 `pidof odhcp6c \| wc -w` = 1、`logread` 无重复 ubus 注册失败、修正重复配置 + reconnect 后 IPv6 恢复；不回归——普通 PPP 自动 IPv6、独立 dhcpv6 接口各自单独存在时仍正常起 |
| F119 | AN7581 PCS RX-lock **诊断**（2026-09-12 P2 §2.1 吸收；naoki66 原作 / orangeyoo `0923` v1.6.1 抑噪版） | 本仓 F93（"PHY 宣告 10G、AN 落 2.5G"）与 F81（`#22397 LAN2 No FBCK Lock after 50 calibration try`）卡在**同一盲点**：PCS 无 carrier 时分不清"对端没信号"还是"CDR 没锁"，没有可判读证据 | 本仓无 PCS RX-lock 诊断（`grep -rln "rxlock_reset_count\|an7581_pcs_report_rxlock" patches/` 为空）；同域 9029（JCPLL TCLVAR recal）/9038（USXGMII RX CDR crossing）/9044（rate adaptation）治"能不能起来"，本项治"为什么起不来" | 新增 `patches/root/9055-xr1710g-pcs-rxlock-diagnostics.patch`（**#EXP**，内层 `target/linux/airoha/patches-6.18/9999-net-pcs-airoha-an7581-rx-lock-diagnostics.patch`）：`an7581_pcs_report_rxlock()` 打 PCS/Base-R/AN/SigDet/FreqDet/reset 寄存器，`have_rx_signal=false` → `action=no-signal`（每周期一次），有信号但 PCS 未 up → `action=cdr-reset` + `resets=` 自增（每次打印）；RX-lock recovery 行为不变 | n/a（作者自持；naoki66 20260903 release 的处置是整体移除该诊断，orangeyoo 是门控保留，本仓取第三种=携带抑噪版） | **抑噪是硬约束**：早期版每 poll 都打印，空置 WAN 口 ~2 行/秒刷屏，~8 分钟冲空 logread 环形缓冲、挤掉 hostapd 历史（orangeyoo 自述三因素之一）。实证：kernel 6.18.44 真源干净应用（pcs-an7581.c offset -49、`pcs-airoha.h` 插入点与 9992 改的 match-data 区不同行区）；**双向顺序共存实证**：`9999(RX-lock)→9992→9994` 与 `9992→9994→9999(RX-lock)` 两种顺序终态三文件 sha256 **完全一致**（`b61e5328…`/`5c9aad49…`/`a27e77d3…`）；全序内层实证（168 airoha 补丁 + 本仓新增内层按内核应用顺序）本项 ✓ fuzz=0。实机判据：① 拔线 `no-signal` 每周期 ≤1 条；② 插线不 lock 出现 `action=cdr-reset` 且 `resets=` 递增；③ 空置 WAN 跑 10 分钟 `AN down`/`no-signal` 各 ≤1 条且 hostapd 历史不被冲掉 |
| F120 | hwrng/TRNG 先开 SCU 时钟门再置 `RNG_EN\|RNG_OSC_EN`（2026-09-12 P2 §2.4 吸收） | 冷启动 hwrng 注册 `-ENODEV`（"Timeout waiting for Health Check"）→ 整机无硬件熵源；上游 `an7581/config-6.18` 实测 `CONFIG_HW_RANDOM_AIROHA=y`（本仓镜像该驱动**已启用**，竞态可达） | airoha-trng 在 SCU 总线/外设时钟门之后，而 `TRNG_NS_SEK_AND_DAT_EN` 的使能写（振荡器+环）发生在时钟门之前，且**本仓该驱动原本完全没有时钟门代码**（无 `RNG_OSC_EN`、无 `trng->scu`、TRNG 节点也无 `airoha,scu` 句柄） | 新增**成对**两补丁（**#EXP**，必须同档）：<br>· `patches/root/9058-xr1710g-hwrng-scu-clock-order.patch`（改 `drivers/char/hw_random/airoha-trng.c`：加 `RNG_OSC_EN`、SCU 门常量、`trng->scu` 句柄；`airoha_trng_init()` 里**先** `regmap_set_bits()` 开 BUS_CLK_GAT bit3 / PER_CLK_GAT_1 bit21 / PER_CLK_GAT_2 bit0，**再**写 `RNG_EN \| RNG_OSC_EN`）<br>· `patches/root/9059-xr1710g-hwrng-scu-dts.patch`（`arch/arm64/boot/dts/airoha/en7581.dtsi`：SCU 节点下加只覆盖时钟门寄存器段 `0x1e4…0x204` 的 syscon 子节点 `scu_clk_gat`，TRNG 节点加 `airoha,scu = <&scu_clk_gat>`；并给 `rng/airoha,en7581-trng.yaml` 补该属性——该绑定当前 `additionalProperties: false`） | n/a（上游 airoha-trng 无 SCU/时钟门处理；TRNG 绑定无 `airoha,scu`） | **前瞻依赖实证（关键）**：orangeyoo 缓存 `0921` 在本仓真源上**畸形/失败**——它假定 `RNG_OSC_EN` 与 `trng->scu` 代码已存在，而这两样来自**本仓未携带**的 yyh2913/fanboy `920-hwrng-airoha-fix-init-sequence-default-to-DRBG`（+45 行：SCU 支持 + DRBG 默认 + raw-mode DT）；本机 DTS 的 TRNG 节点也没有 `airoha,scu` → 单独落地=死代码+构建必红。故按**本仓真源重写为一份自洽补丁**（取 920 的 SCU 上下文 + 0921 的顺序修正，**不含** 920 的 DRBG 输出改动以免顺带改变熵质量/读取路径）。实证：kernel 6.18.44 真源 `patch -p1 --fuzz=0` 干净应用（9058/9059 内层）；全序内层实证 ✓。风险：作者自述"仍需两台机反复冷启动验证"，收益概率低（原评级 P3），故 #EXP。实机判据：**N≥20 次冷启动** `dmesg \| grep -c "airoha-trng"` 每次都有且无 `-ENODEV`；`cat /sys/class/misc/hw_random/rng_current` = airoha |
| F121 | MLO 编辑器隐藏 `mode=mesh` 接口（2026-09-12 P2 §2.3 吸收） | MLO 页把**所有** wifi-iface 列进可编辑的 AP/STA 网格；802.11s mesh 回程接口一旦被选中，会被通用 AP/STA 表单改写 `mode`/`ssid`/`network` → 6GHz 回程**静默断开**。本仓当前无 mesh 接口，但 `files/etc/config/wireless` 头注释已写"EHT320 回程/802.11s 见 ROADMAP 漫游优化计划"——一旦按计划开 mesh 就是静默陷阱 | 页面 `GridSection` 无 mode 过滤；无 mesh 提示 | 新增 `patches/root/9057-xr1710g-mlo-hide-mesh.patch`（**default**；内层装到 `luci-app-mlo/patches/`… 实为 `luci-app-mlo/patches/001-xr1710g-mlo-hide-mesh.patch`）：① `s.filter = section_id => optionValue(section_id,'mode') != 'mesh'`；② 存在 mesh 接口时页面顶部插 `alert-message notice` 提示，指向 Network - Wireless（唯一受支持的 mesh 编辑入口） | n/a（luci-app-mlo 为本仓 9017 携带的第三方包，上游无此页面） | 基线**逐字吻合**：本仓 9017 内 `luci-app-mlo/htdocs/luci-static/resources/view/mlo.js`（693 行）在 `sectiontitle` 的 `};` 之后紧接 `quickAdd = function(mode) {`，与 0660 插入点完全一致。实证：抽取该 mlo.js 后 `patch -p1 --fuzz=0` 两个 hunk 干净命中（450 后 / 688 后）。顺序：必须置于 `9017` 之后（本补丁改 9017 生成的文件）。判据：构建后 `grep -c "mode') != 'mesh'" luci-app-mlo/htdocs/luci-static/resources/view/mlo.js` = 1；实机造 `wireless.mesh6`（`mode=mesh`）后打开 MLO 页面：网格**不出现**该接口且有提示；`uci show wireless.mesh6.mode` 仍 `mesh` |
| F122 | **SOE 主体（B 系列）：AN7581 inline IPSec 硬件卸载的驱动层**（2026-09-12 P2 §2.9 吸收；hurryman2212 `@offload-oc`） | 本仓 F52（issue #6）的终点：让 AN7581 的 **SOE（Security Offload Engine）** 直接做 inline IPSec（含 NAT-T）——XFRM state 经 nft flowtable 下发 PPE/SOE，数据面由硬件加解密，而不是让 EIP93 当通用 crypto 设备。A 系列（xfrm packet-offload 框架）与 D 系列（EIP93 动态软回退）本批已落地，B 是最后一块 | 本仓无 SOE 驱动（`grep -rln "airoha_soe\|NET_AIROHA_SOE" patches/` 仅命中本批新增）；缺 SOE/PPE IPsec 寄存器定义、SOE 状态机与数据面、PPE 接口、flowtable XFRM 元数据、autonomous flow offload | 新增 **19 个 `#EXP` 捆绑补丁**（`patches/root/9999-net-airoha-soe-10..28-*.patch`，内层同名装到 `target/linux/airoha/patches-6.18/`，按 NN 排序保证内核应用顺序）：DT binding · MAINTAINERS · dtsi SOE 节点 · SOE/PPE IPsec 寄存器 · SOE state/数据面元数据 · SOE-PPE 驱动接口 · 以太网数据面集成 · flow_offload XFRM action · flowtable 携带 XFRM route 元数据 / 保留 XFRM input state / 发现 XFRM state / 下发硬件 action / XFRM flow 不进软件路径 / 接纳已认证 flow / ctx 透传 · **autonomous SOE XFRM flow offload（53KB）** · **SOE XFRM packet offload（110KB，新增 `airoha_soe.{c,h}`）** · 构建 provider · Kconfig | n/a（作者自持，未提上游 PR；同作者 hurrian = 上游 PR #23644 作者） | **重基（本项最大工作量）**：作者基线 ≠ 本仓真源——① `airoha_ppe.c` 的 L2/subflow 重构在本仓由 `9039` 内层 `9990` 提供（作者用了 `wdma_*` 命名，仓库用 `bridge_*`）；② `airoha_eth.c` 的 QDMA start/stop helper 化在本仓由 `yyh2913 179` 提供（引入 `airoha_queue.flushing`，是 B14 的上下文前提）；③ flowtable 侧真源无 `net/xfrm.h`、函数名是 `nft_flow_route()`（非 `nft_flow_route_routing()`）、`nft_dev_forward_path()` 返回 void。19 个补丁逐个按真源重写上下文/锚点（25 另有 2 个 hunk 在"真源+9990"上重锚），逐条处置见 `research-20260912/inner/soe-rebased/*.patch` 顶部 `# Reason:`。**实证**：真实构建顺序（airoha 全序 168 + 179 + 9039/9990 + 9053/9054 内层 + 本 19 个）上**逐个 `patch -p1 --fuzz=0` 全部干净应用、0 FAILED**；仓库级 `audit-patches` **103/103**、`apply-patches --dry-run --oc --experimental` **103/0/0** + 拷贝类 **4/4**。可复现脚本：`research-20260912/inner/soe-basetree.sh`、`gen-25-real9990.py`。**判据（实机）**：① experimental 构建通过；② `ip xfrm state add` 后 devmem/debugfs 看 SOE/PPE 计数增长；③ `authenc(hmac(sha256),cbc(aes))`（硬件）vs `rfc4106(gcm(aes))`（软回退，由 F115/9054 保证）；④ iperf3 over IPsec 吞吐 vs 软件路径；⑤ 开 IPsec 卸载后普通转发/无线不回归。**注意**：干净应用 ≠ 可编译/可跑；SOE 是否真跑起来取决于 NPU/SOE 固件版本与 dtsi SOE 节点，必须实机构建验证 |
| F123 | fanboy `ubi2-oc` 重基（2026-09-12 P4；上游 force-push `f08c3d1e`→`9631414c`，22 commit 中 3 变 1 增） | ① `05` 包 `bridge-flow-offload`→`bridge-hw-offload`（hotplug/apply-rules 收进包 + 新增 `files/etc/init.d/bridge-hw-offload` procd `procd_add_reload_trigger firewall`），且上游 `apply-rules.sh` **已内置** `flow_offloading_hw` 守门；② `06` 内层 `675-02` 的 VLAN/PPPoE encap 由覆写 `info.encap[0]` 改为**追加** `info.encap[num_encaps]` + `NF_FLOW_TABLE_ENCAP_MAX` 越界检查；③ apps pack `00ed581b75`→`0a4f2632de`（flowsense 1.1.7→**1.1.9-r5**、新增 `npu-monitor.settings.air_eff`、status.js 罗盘→CPU-gauge）+ `8bf1c9253c`（删 VLAN/PPPoE 徽章、`set_flow_offload` 发 `config.change`） | 上游 force-push 重基（非 bug）；本仓 `9043` 守门被上游内置 → 冗余；`9030`（1.1.8-r5 bump）被新 pack 超集化 | ① `05`→`2f4fccb1`（**target.mk hunk 改写为末尾追加**：上游以内嵌 `kmod-crypto-hw-eip93` 行为上下文，与本仓 `02`(#EXP) 顺序耦合）；② `06`→`3c8d3e6664`；③ `9024` 重写为 `bridge-hw-offload` 路径 + 表名 `flow_offload`（**保留本仓独立表策略**，避开用户 `bridge fw4`）+ 去 reload `&`；④ `9043` **整条删除**；⑤ `9030` **删除**；⑥ `9017` 按新 pack 重做 19-core 切片（保持无 `root/etc/init.d/fan`＝F67/A4、jitter `223.5.5.5`、F88 RXHWF_FAST 重放）；⑦ `9019`/`9022` 按新 pack 基线重基（保留 pack 的 MLD 多频段 `if/if/if` 语义）；⑧ 静态符号 `bridge-flow-offload`→`bridge-hw-offload`（`scripts/build.sh`、`.github/workflows/build.yml`、`config/seed-config.diff`、MANIFEST/ORDER） | n/a（vendor 漂移吸收） | **实证**：`audit-patches --oc --experimental` **102/102**；`apply-patches --dry-run --oc --experimental` **102/0/0** + 拷贝类 **4/4**；`06` 内层 675-01/02/03 对 6.18.44 真源 `patch -p1 --fuzz=0` 三件全绿（终态 `net/netfilter/nf_flow_table_path.c` 含两处 `info.num_encaps < NF_FLOW_TABLE_ENCAP_MAX` 追加 + `br_vlan_get_offload_info_rcu`）；`9057` 内层对新版 mlo.js `--fuzz=0` 命中（`mode') != 'mesh'` 计数 = 1）。**顺带修复**：原 `9022` 的 `band_unb` awk **多一个 `}`**（awk 语法错误，仅 `$entries_file` 存在时触发；已纠正并复核三个 awk 程序 rc=0）。**实机判据（待）**：开关 `flow_offloading_hw` 后 `nft list table bridge flow_offload` 随之出现/清空、无需 reboot；flowsense 页面为 gauge（无 `◄BND`）、无 VLAN/PPPoE 徽章、`air_eff=80` 生效；F88 CDM1 百分比仍含 `rx_hwf_fast` |
| F124 | 上游**已自带** airoha NPU `924-use-coherent-mailbox-dma`（2026-09-12 P3 前置核对，纠正 P3 handoff 误判） | P3 handoff §1/§5.1 把上游 openwrt main（`f0d3e332`）里**已存在**的 `target/linux/airoha/patches-6.18/924-net-airoha-npu-use-coherent-mailbox-dma.patch` 当成"dryrun 树污染（来自别处）"，并据此要求重建干净树 | 上游 2026-08 已收录 Daniel Pawlik 的同款修复（`6f884eb87a79` 把 `airoha_npu_send_msg()` 改 `DMA_BIDIRECTIONAL` 后 EN7581+MT7996 probe 回退 → 改为 per-core 复用 device-managed coherent bounce buffer；`Fixes: 6f884eb87a79`；patchwork 2026-08-05/08-06 两版），与本仓 `git ls-tree HEAD:target/linux/airoha/patches-6.18/` 逐字相符 | **无需吸收 YYH 的 `924-use-coherent`**（已上游）；P3-A 仍需 YYH `923-recover-stalled-cores`（`airoha_npu_recover()`）+ `924-fix-watchdog-work-lifetime`（`airoha_npu_wait_recovery()`）+ `925`/`926`/`927`/`928`/`929`。**号段冲突**：上游占 `924`，本仓承载这些内层补丁时必须避号（逐号对账后再定号） | merged（openwrt main `f0d3e332`） | 教训：P3 §6.3 第 9 条（跨层先证明符号存在）之外，**还须先核对上游是否已收录同款内层补丁**——一行 `git ls-tree HEAD:target/linux/airoha/patches-6.18/ \| grep <号>` 即可避免重复携带；P3 handoff 的"污染告警"已作废，`research-20260912/dryrun/owrt` 实为干净的上游树（HEAD == 上游 main） |
| F125 | airoha NPU mailbox 可靠性六项（2026-09-12 P3-A 吸收；YYH2913 `xr1710g-6.18-integration` 923..928） | ① NPU watchdog 只抓 coredump、不重启停滞的 firmware core（mailbox 用户只能等满超时，无线卸载不重启整机无法恢复）；② watchdog work 生命周期：devres 解开时先 free IRQ 再 sync work（probe 失败/remove 竞态），且无法等待进行中的 recovery；③ recovery flag 只在 core 0 锁下更新，多 mailbox core 后成数据竞争；④ `airoha_npu_get()`/`airoha_ppe_get_dev()` 用 `DL_FLAG_AUTOREMOVE_SUPPLIER` → Wi-Fi consumer unbind 后 device link 残留；⑤ `wlan_irq_status_get()` 返回整个 `REG_IRQ_STATUS`，mt76 handler 写回 W1C → 一个 ring 清掉另一个 ring 的 pending（丢中断）；⑥ mailbox 先自增 `MBQ0_CTRL2`（固件视作门铃）再写 `CTRL3` → 固件消费上一轮命令，probe 读固件版本 **0.0**、PPE/WLAN 卸载请求被拒 | 上游缺对应修复；⑥ 是 probe 0.1111 的关键之一 | 新增 `patches/root/9061-xr1710g-airoha-npu-mailbox-recovery.patch`（**#EXP**，单包裹 6 内层 `9999-30..35`：recover-stalled-cores / fix-watchdog-work-lifetime / close-recovery-sync-race / fix-offload-provider-device-link-lifetime / acknowledge-only-requested-WLAN-RX-IRQ / publish-mailbox-command-before-doorbell）。**提供 `airoha_npu_recover()` + `airoha_npu_wait_recovery()` —— P3-B（mt76 `0147`/`0155`）的硬前置**。同族 `924-use-coherent` 上游已收录 → 不携带（F124）；YYH `929`（低地址 coherent pool）**未吸收**——它要求 DT `shared-dma-pool` 名为 `mbox`，而本仓/上游均无该 reserved-memory 节点，直接落地会使 probe `of_reserved_mem_device_init_by_name` 失败（详见备注） | n/a（作者自持） | **重基**：本仓真源已含上游 924-use-coherent（`__airoha_npu_send_msg` + per-core coherent buf），故 923 的 `send_msg` 两个 hunk 由旧 `dma_map_single` 上下文重锚到 `__airoha_npu_send_msg`。**实证**：linux 6.18.44 真源 + 本仓 airoha 内层全序（含 9990/9995/9996/9997/9998）之上，6 个内层按序 `patch -p1 --fuzz=0` **全部成功**；`audit-patches`/dry-run 见备注。**实机判据（待）**：probe 时 `WLAN_FUNC_GET_WAIT_NPU_VERSION == 0.1111`；watchdog 触发后无需重启即可恢复；NPU offload 绑定成功。**⚠ 929 的后续条件**：若实机出现“probe 固件版本 0.0 且 928 已修仍复现”，需先按 2GiB DDR 布局在 `en7581.dtsi`/板级 dts 划一块低于 `0xbfffffff` 的 `shared-dma-pool`（名 `mbox`）再吸收 929 |
| F126 | eip93 动态回退的**打包侧配套**修正（2026-09-12 补 `9060` 的 KCONFIG 回归） | 实验档内核编译期 kconfig 报未决符号并中断：`Max skcipher software fallback length (CRYPTO_DEV_EIP93_GENERIC_SW_MAX_LEN) [256] (NEW)` → `scripts/kconfig/Makefile:85: syncconfig Error 1` （CI run 34687410355） | 我先前误判「两个静态阈值已被动态回退取代」而把它们从 `modules/crypto.mk` 的 `KCONFIG` 里删除（照搬作者 commit `96247057` 的改法，但那是按其自身树）。本仓基线上这两个符号是 `int` + 有 prompt，删除即产生未决符号；且驱动 `eip93-cipher.c:186` 仍读取 `GENERIC_SW_MAX_LEN` | `patches/root/9060-…-crypto-eip93-package-deps.patch`：**KCONFIG 原样保留**（4 行），只加 `+kmod-crypto-dynamic-fallback`；注释记明判据 | n/a（本仓打包侧） | 实证：openwrt/openwrt master（blob `c7b782e9`）该段原生即 4 行；对 master 真实 `crypto.mk` `git apply --check` 通过、应用后 KCONFIG 4 行完整。CI run 34687410355 的补丁层已全绿（`Hunk FAILED: 0`），仅此配置项致败 |
| F127 | mt76 NPU 生命周期九补（2026-09-12 P3-B 吸收；YYH2913 0147..0162） | ① NPU hw init / reserved-memory / stop 握手遇 mailbox 超时或 recovery 竞争时直接放弃（probe 静默退化为无 NPU offload；SER 只恢复部分状态）；② NPU RX IRQ 是 device-managed 但绑在 Wi-Fi PCI 设备上，remove 后仍注册而 DMA/NAPI/page pool 已拆 → in-flight completion 重新使能中断线；③ NPU offload 下 `dma_dev` 改为 NPU，`dmam_alloc_coherent()` 把描述符/TXD 挂到 supplier → 重载 Wi-Fi 驱动泄漏；④ NPU RX queue teardown 不完整；⑤ NPU RX 环预留一个空描述符 + 先推 `q->tail` 再 refill，让"已被消费、page 已归 skb"的槽位对 NPU 可见；⑥ NPU TX 无 prefetch 余量、不验 host-owned → 可能覆写 firmware 仍在读的描述符；⑦ host 重启时 MT7990 仍供电发事务 → 下次启动 HIF 枚举失败；⑧ `mt76_dma_init()` 跳过的 WED RRO 队列在 WED 未激活时被 `mt7996_dma_rro_start()` 注册了 NAPI，清理侧只看 WED 标志→ NAPI 实例被留在 `free_netdev()`（上游 revert `332bbe9b2784` 后的真实缺口）；⑨ 原顺序下 NPU stop 握手在 mac80211 teardown 之后 → 超时 + 后续 mailbox 持续无响应 | 上游 mt76 无对应提交；⑧ 是上游 revert 之后留下的缺口（0161 的 `Fixes:` 直指被 revert 的 commit） | 新增 **9 个 `#EXP`**（`patches/packages/mt76-0147/0153/0154/0156/0158/0159/0160/0161/0162-*.patch`，按文件名 glob 序应用＝依赖序）：**0147 = YYH 0147+0155 合并**（`mt7996_npu_prepare_retry()` 为唯一 retry 入口：`-EAGAIN` → `airoha_npu_wait_recovery()`、`-ETIMEDOUT` → `airoha_npu_recover()`，不循环）；0153 NPU RX IRQ teardown 排序 + `mt76_npu_deinit` 的 RCU 顺序；0154 NPU DMA 内存挂 Wi-Fi consumer；0156 完成 NPU RX queue teardown；0158 满环填充 + 发布移到 refill；0159 NPU TX 5 描述符余量 + host-owned 校验；0160 `pci_driver.shutdown` quiesce；0161 `dev->napi_registered` bitmap；0162 unregister 前先 stop NPU。**0157 不吸收**（与本仓 0011 同处且被其覆盖：0011 有实机复现证据 + clamp 容错）；0130/0151 跳过（已由 0014 覆盖）。**硬前置 = root/9061**（`airoha_npu_recover`/`wait_recovery`） | n/a（作者自持） | **本地增补三处（均已落地）**：① **0154 泄漏修复**——`devm_add_action_or_reset()` 失败时补 `dma_free_coherent + put_device + kfree`；② **0161 重基**——上游 `6d1c6a75` revert 后 `mt76_dma_cleanup()` 已改为 `mt76_for_each_q_rx` + 无条件 `netif_napi_del`，按新形态重写 dma.c hunk#2（只对 `test_bit(i)` 为真的队列做 del，正是 revert 所要求）；③ **0159 stub 配对核对**——`mt76_queue_is_npu_tx()` = `q->flags & MT_QFLAG_NPU`，该 flag 仅由 `mt7996_init_tx_queues()` 在 `mt76_npu_device_active()` 为真时设置 → flag 真 ⇒ NPU 代码已编译且活跃 ⇒ 走真实实现；非 NPU 构建 flag 恒 0 → 短路，stub 不被求值。**不存在“非 NPU 构建全丢包”路径**。**实证**：`audit-patches --oc --experimental` 99/99（含 SOE-16..28 停用后的状态）；`apply-patches --dry-run --oc --experimental` 全绿 + **拷贝类 mt76 28 个文件**在真源 `be5ce791` 上按 glob 序全部 `patch -f -p1` 成功（含新增 9 个）；本地 mt76 全序树逐个 fuzz=0。**实机判据（待）**：`rmmod/insmod mt7996e`（NPU 活跃）20 轮无 `NAPI instance ... already registered`/`not been added`；`reboot` 20 次 Wi-Fi 必枚举；NPU 活跃下反复 reload 无 coherent 泄漏；高负载（>1Gbps + 6GHz 320MHz）12h `HOSTADPT_API_Q_FULL` 不增长、RX 环 head/tail 不漂移 |
| F128 | mt76 候选甄别（2026-09-12 P3-C） | YYH 的 7 个 mt76 候选逐条判定：① `0047`（RRO 3.0 RX 数据投递队列）被 handoff 列为"候选·需重基"，**实际 be5ce791 已含同款**（`mac80211.c` 的 `mt76_rx_release_amsdu()` 已有 `mt76_queue_is_wed_rro_data(rxq) && dev->hwrro_mode == MT76_HWRRO_V3 → q = MT_RXQ_RRO_IND`）→ 冗余，不携带；② `0080`（check ddone for tx queues）捕获版本里 `MT_QFLAG_CHECK_DDONE` **只被定义和读取，从未被置位**（`mt7996/dma.c` 仅 `=`→`\|=`，`MT_NPU_Q_TX` 宏不含该位）→ 死代码 + 作者自述"未遇到问题，只是对齐 logan 配置，预防潜在 issue"，且 dma.c hunk 对真源 FAILED → 不携带，待有置位版本再收；③ `0038`（wcid→wcid_idx）改 `struct mt76_rx_status` 语义、4 文件 ~50 行，MTK 私有改法与主线演进易冲突（handoff 自评风险中-高）→ 暂缓；handoff 的"只改 mt76_rx_aggr_reorder 一处"缩小版仍需在 `struct mt76_rx_status` 里加 `wcid_idx` 字段才能重取 wcid，仍动结构体，故本轮不做。**吸收 4 项**：`0018`(=YYH 0010，避号)/`0021`/`0101`/`0145` | ① 已上游（openwrt/mt76 `be5ce791`）；② n/a（作者自持）；③ n/a（MTK 内部树） | 载体：`patches/packages/mt76-0018`（#EXP，避 0010 号冲突）/`mt76-0021`（#EXP）/`mt76-0101`（**default**，上游 next 线已含，pin bump 时复查删除）/`mt76-0145`（#EXP） | ① merged（`be5ce791`） | **实证**：`apply-patches --dry-run --oc --experimental` + 拷贝类 mt76 真源应用全绿（含新 4 项，glob 序 0018<0021<0101<0145<0147）；本地全序（含 vendor/07 内层 0014）逐个 fuzz=0。**教训**：判候选是否可吸收时，必须用**本仓 pin 真源**跑 `patch --dry-run` 并**看 "Reversed (or previously applied)" 提示**（只 grep FAILED 会漏报已上游） |
| F129 | 无线栈缺口：mac80211 8 项吸收 + hostapd 4 项暂缓（2026-09-12 P3-D） | ① mac80211（YYH 40 条）：本仓只携 411 一条；精选 397（STA CSA punct bitmap）/399（bootup CAC 删接口 call trace）/400（non-TX BSS 解析）/405（partner link CSA finalize）/406（CSA 期间不按旧信道 beacon 更新信道）/407（direct CSA mbss finalize）/409（MLD AQL BMC）/410（`nl80211_set_wiphy_radio` 把 RTS 误置 0）；② hostapd（本仓 **0 条**，明显缺口）：603（6GHz 不需 DFS，同 Gilly 015）/905（STA identity 缓冲区溢出，安全）/901（6GHz ACS）/906（HT40 副信道） | ① MTK 内部树/上游风格（未进 backports-7.2）；② hostapd 侧为社区刚需 + 安全修复 | ① **已吸收 8 条 `#EXP`**：`patches/packages/mac80211-397/399/400/405/406/407/409/410-*.patch` → 装到 `package/kernel/mac80211/patches/subsys/`（glob 序 392 之后、411 之前）；410 已按 backports-7.2 重基（去掉作者树多出的 `const struct wiphy_radio *radio;` 上下文行）；② **hostapd 4 项未吸收**（见备注） | n/a | **mac80211 实证**：`audit-patches` **111/111**；`apply-patches --dry-run --oc --experimental` 全绿；拷贝类 mac80211 subsys **29 个文件**（上游 20 + 本仓新增 8 + 411）在 backports-7.2 上按 glob 序全部应用成功。**hostapd 暂缓原因**：本宿主无法获取 pin 源（`PKG_SOURCE_URL=https://w1.fi/hostap.git`，`PKG_SOURCE_VERSION=831364bf02710ad09c2f27d3efa92abeeb5634c0`，2026-08-07）——w1.fi TLS 连接失败、GitHub 无同 SHA 镜像；按 F20/F21 铁律（拷贝类补丁必须真源 `patch -p1` 实证）**不盲上**（未实测的拷贝补丁会在构建期直接红）。**后续执行计划**：① 给 `scripts/verify-copy-patches.sh` 的 `prep_src` 加 hostapd 映射（git 源：按 `PKG_SOURCE_VERSION` 拉 hostap.git）；② 在可联网环境跑 verify；③ 通过后 603/905 倾向 default（6GHz 可用性 + 安全）、901/906 `#EXP`；④ 补 `config/seed-config*` 与 MANIFEST。候选文件位置：`research-20260912/repocmp/suntyrael/rest/package__network__services__hostapd__patches__{603,901,905,906}-*.patch` |
| F130 | 上游守护扩面：远端来源 tip 监视（2026-09-12 P3-F） | `scripts/audit-upstream-watch.sh` 原只监视 **上游 openwrt main 的 6 个 blob + 7 条同址冲突**，而本仓补丁的一大来源是**多个 fork 分支**（YYH/fanboy/Gilly/naoki66/hurryman）——它们 tip 变化时不会让任何本地检查变红，却会让“已覆盖/新增”判定过期（P3 整轮就是因为 suntyrael 只是 YYH 的重传载体才误判追踪对象） | 守护面无“来源分支 tip”维度 | **已实施** `scripts/audit-upstream-watch.sh` 新增 `REMOTE_TIPS` + `remote_report()`：7 个来源（YYH openwrt@xr1710g-6.18-integration / YYH mt76 / YYH http-uboot / fanboy OpenW1700k@ubi2-oc / Gilly Gemtek-W1700K-6.18 / naoki66 ImmortalWrt-for-Gemtek-XR1710G / hurryman OpenW1700k-test@offload-oc）；`git ls-remote` 带 `timeout`（`AUDIT_REMOTE_TIMEOUT`，默认 20s）；**离线/超时只打 `ⓘ` 不计失败**；记录 tip 固定者 tip 前进计 `drift`（`--fail-on-drift` 即 exit 1）；未固定者只打印当前值 | n/a（本仓 CI/脚本自持） | 实证（2026-09-12 17:2x）：`bash -n` 通过；对 `research-20260912/dryrun/owrt` 运行 **exit 0**，6 基线 ✓ / 7 冲突探测 ✓ / 7 远端 **全部 `✓ 未变`**（即 P3/P4 handoff 记录的 tip 快照仍有效）；汇总行新增“远端跳过 N”可区分“真未变”与“未联网” |
| F131 | P3-E 非 mt76 残余对账（2026-09-12；YYH 291 项 → 真残余清单） | P3 handoff 的 §5 分组是**人工启发式**，需一次可复核的**机器对账**产出“到底还剩什么未覆盖” | ① 纯 `git patch-id --stable` 集合比对失败率极高（本仓内层多为重基/重写，上下文不同 → id 不同）；② 即使 id 命中（9 条）也不能回答“未命中的哪些只是重复” | **方法**：提取本仓全部外层补丁的内层（`research-20260912/inner/extract_inner.py`，99 个）→ 与 YYH `rest/` 209 项各算 patch-id（命中 9：本批 mac80211 7 项 + SOE-27 + eip93-9911）；再把 YYH 的 **airoha+generic 147 项**逐条 `patch -p1 --fuzz=0 --dry-run` 打到“本仓 airoha 内层全序树”上，按 `Reversed→COVERED` / `rc=0→APPLIES` / 其它 `CONFLICT` 分类：**COVERED 28 / APPLIES 20 / CONFLICT 99**。**实残余（APPLIES 且真新增）已吸收 3 项**：新增 `patches/root/9062-xr1710g-eip93-reliability.patch`（#EXP，内层 9999-40..42 = YYH backport-6.18 9915/9916/9918）。**已经甄别为“不携带”**：9910（纯注释：13 文件 email 少 `>`）/9912（ECONET 选型）/9990 hack（AES fallback 阈值，已被 9054 动态回退取代）/675-01（= fanboy/06 内层，命名不同）/9991+9999-03 bonding（已被 9053/9999-soe 族覆盖）/9999-07（已被 SOE 19 块取代）/929（需 DT `mbox` pool，F124/F125 已记）。**仍留观察（本批不吸收，原因各自）**：`914/203/220-11/910-02`（PCIe PERST/USB2 SerDes——需与 F56/F116 实机 AER 联动验证）、`624`（PCS TXPLL regmap 字段——与 9029/9038/9044 同域，需 10G 对端实测）、`0401`（pmdomain 回退到 PLL 寄存器——与 OC/`vendor/03` 同域，需实机）、`310-02`（XSI deassert——需 F92 ring 类回归）、`330`（device_set_node 助手，低成本但无实机驱动力）、`920-DRBG`（我们 9058/9059 故意不含其 DRBG 输出改动，避免顺带改熵质量） | n/a（作者自持/MTK 内部树） | **证据留存**：`research-20260912/p3e/kernel_class.tsv`（147 行三态）、`ours.ids`/`yyh.ids`、`residual-applies.txt`（20 项）。**方法学教训**：patch-id 只能判“逐字相同”；对重基过的补丁必须用“在已含本仓补丁的树上跑 `--dry-run`”分类（`Reversed` 是“已覆盖”的强信号）。CONFLICT 99 项中包含大量“因本仓重建/上游漂移而上下文不匹配”的重复项，不应当作“待吸收”（handoff §5 已逐组给出归并结论） |
| F132 | SOE-16 重建损伤：四处 `-Werror` 编译错误（2026-09-12 P6） | CI run `34700107822`（合并后 `e7d1f64`）已越过**整个补丁层**（`Hunk FAILED: 0`）与 **kconfig**（eip93 未决符号已修），红在内核编译：`airoha_eth.c:1234 unused variable 'reason'` / `:1453 implicit conversion to 'enum pkt_hash_types'` / `:1189 'airoha_qdma_rx_check_stall' defined but not used` / `:163 'airoha_qdma_set_rx_irq' defined but not used` | **P5 手工重建 SOE-16 内层时删错行**（作者 `soe16-author.patch` 含同样的 `reason` 缺陷，属沿用）：① 删掉了 `reason = FIELD_GET(AIROHA_RXD4_PPE_CPU_REASON, msg1)` 与其 PPE 回退块，**却保留了 `reason == PPE_CPU_REASON_HIT_UNBIND_RATE_REACHED` 的读取**——不只是 `-Werror`，是**读未初始化变量**；② `skb_set_hash` 第三参数被写成 `false`（应为 `PKT_HASH_TYPE_L4`）；③ 丢掉了 `9045` 的 RX 停滞恢复检测调用 `airoha_qdma_rx_check_stall(q)`，使该 helper 成为未使用静态函数（功能静默退化）；④ `airoha_qdma_set_rx_irq` 唯一调用点位于 `#if IS_REACHABLE(CONFIG_NET_AIROHA_SOE)` 内，非 SOE 配置下必为未使用；⑤ 重建还掉了 `if (!(desc_ctrl & QDMA_DESC_DONE_MASK))` 的花括号，留下孤立 `}` | **修订既有载体**（不改文件名/编号）：`patches/root/9999-net-airoha-soe-16-integrate-soe-eth-datapath.patch`。改法：把被误删的行由 `-` 改回**上下文**（不引入重复逻辑，故 compile 后语义 = vendor 语义），`airoha_qdma_set_rx_irq` 加 `__maybe_unused`，补回花括号；**重算全部 hunk 头**（本 hunk 新侧 +10，其后 23 个 hunk 的新侧起点随之 +10） | n/a（SOE 补丁作者自持；损伤系本仓重建引入） | **实证**：`audit-patches` **113/113**；`apply-patches --dry-run --oc --experimental` 全绿 + 拷贝类 **4/4**（41 文件真实应用）；外层 wrapper 与内层**逐字节一致**（`extract_inner.py` round-trip）；大 hunk 新侧花括号 **29/29 平衡、最大负深度 1**；改法差异仅 **27 行**（逐一为上述修点 + 重算头）。**编译结论以 CI 为准**：修复轮 run `34705859414`（提交 `5f8876a`）。**证据留存**：`research-20260912/p6-soe16/`（`extracted/` 原始内层、`fixed-inner.patch`、`fix_inner.py` 含断言自检、`wrapper-fixed.patch`、`fix/` 参考目标态）；失败日志 `research-20260912/fail-e7d1f64.log`、`logs-e7d1f64/job.log` |
| F133 | fanboy 上游 force-push 后漂移吸收：HSUART 波特率修复 + 远端锚点（2026-09-12 P6） | `sync-upstream` 自 `e7d1f64` 起**每次 cron/push 都红**：`--fail-on-drift` 报 `OpenW1700k.git refs/heads/ubi2-oc` tip 前进（记录 `9631414c` → 实际 `b1f669b68`）。该守卫本身正确（P3-F 建立的 7 来源 tip 监视），是本仓记录的锚点过期——`9631414c` 是 P4 重基时的快照，fanboy **习惯性 force-push 重基**（P4 就是被它逼着重基的） | ① 上游 force-push（旧 tip 不是新 tip 祖先，确认为重基）；② 远端锚点是**静态快照**，没有"重基后重新锚定"的流程，于是第一次真漂移就把 CI 打红 | 新增原料桶 `patches/vendor/fanboy/21-hsuart-baud-calculations-4e2b80ff.patch`（git format-patch 原样，`default` 活动项，紧随 11 号——二者父提交同主题）；`patches/MANIFEST`/`ORDER` 注册；`scripts/audit-upstream-watch.sh` 远端记录 tip 更新为 `b1f669b68` | n/a（vendor 漂移吸收） | **甄别（逐 commit patch-id，两侧各 30 个）**：**29 个纯重基（内容未变）+ 1 个真新内容**。纯重基例证：T20 `20-oc-governor-200mhz-ed7cbc80.patch` 的 patch-id `5724590e` 与新 tip 的 OC commit **完全一致**（仅文件名 hash 过期，内容无需动）。**唯一真新增**：`airoha: fix HSUART baud calculations`（`4e2b80ff`，Matt Merhar）——`an7581/an7583 dtsi` 的 hsuart `clock-frequency` `7372800→20000000`、uart3 `compatible` 改 `en7523-hsuart`，并同步改写 `patches-6.18/886-uart-add-en7523-support.patch`；commit message 实测：修前 ttyS2 请求 460800 实得 921600（HSUART/GPS 串口波特率错乱）。本仓此前**未携带**（`patches/` 全树 0 命中 `en7523-hsuart`/`clock-frequency = <20000000>`，20 支 fanboy 桶无一触及 hsuart/dts）。**实证**：HSUART 补丁对真实上游树 `git apply` 全成功（`an7581.dtsi` hunk 偏移 -10 行）；`audit-patches` **114/114**（113→114）；`apply-patches --dry-run --oc --experimental` 全绿 + 拷贝类 4/4；`audit-upstream-watch --fail-on-drift` **exit 0**（基线漂移 0、路径缺失 0、7 远端全"未变"）。**留下的机制问题**：远端锚点仍是静态快照，下次 fanboy 再 force-push 会**再次打红**——届时应先跑一次 patch-id 比对：若仍为纯重基，只需重新锚定；若含真内容，按本行流程吸收。若该噪音不可接受，可选择把远端 tip 降为 informational，但那是**降级**，须在台账写明 |
| F134 | `airoha_ppe_setup_tc_block_cb` 签名不一致（2026-09-12 P6，SOE-16 之后的新阻断） | run `34705859414`（`5f8876ad`）确认 F132 的四处 `-Werror` **全部消失**（各项 grep 计数为 0）、补丁层仍 0 FAILED，构建推进到下一处：`airoha_ppe.c:1748: error: conflicting types for 'airoha_ppe_setup_tc_block_cb'; have 'int(struct airoha_ppe_dev *, void *)'`，而 `airoha_eth.h:812` 的先前声明是 3 参 `(..., void *type_data, bool is_flowtable)` | **SOE 系列重建只改了一半**：SOE-14 加宽了**声明**（`airoha_eth.h`），SOE-16 改了**调用点**（`airoha_eth.c` 传第 3 参 `is_flowtable`），但**定义**那半在 **SOE-25（`#DISABLED`，未启用）** —— 定义停在 2 参，编译必炸。与 F132 同属"重建/切片后签名只同步了一侧"的缺陷类 | 新增 `patches/root/9999-45-airoha-ppe-setup-tc-block-cb-signature.patch`（**#EXP**，内层同名装到 `target/linux/airoha/patches-6.18/`，置于 SOE-16 之后应用）。把定义加宽为 3 参，参数标 `__maybe_unused` —— 在 SOE-25 接管（把它透传给 `airoha_ppe_flow_offload_cmd` 及其下游）之前，不引入新的未使用类告警。**首版只补了定义 → run `34708868615` 又红在下一层同类错误**：`airoha_ppe.c:1886 assignment to 'int (*)(struct airoha_ppe_dev *, void *)' from incompatible pointer type '...(..., bool)'`（`ppe->dev.ops.setup_tc_block_cb = ...`）。至此根因完整成形：**这个 3 参签名被切在三个补丁里，其中两个是 `#DISABLED`** —— 声明在 `airoha_eth.h`（SOE-14）、调用点在 `airoha_eth.c`（SOE-16）为活动，而**定义在 `airoha_ppe.c`（SOE-25）与 ops 字段在 `include/linux/soc/airoha/airoha_offload.h`（SOE-24）双双停用**。故本补丁最终同时供上两个缺失半边：`9999-45` 扩展为双文件内层（各为单文件补丁）——定义加宽 + ops 字段加宽；**第二版仍不够**：run `34711735097`（`5f07dfc`）确认前两层生效（`airoha_eth.o` 这次编过了），又红在第三层 `airoha_offload.h:39 too few arguments to function dev->ops.setup_tc_block_cb` —— 正是 SOE-24 同一补丁里的 **inline 助手函数体**，我当时判断「无活动调用者、不必改」**判断错了**。最终 `9999-45` = **四组改动 / 双文件**：`airoha_ppe.c` 定义加宽；`airoha_offload.h` 的 ops 字段加宽、inline 助手改传 `false` 并新增 `airoha_ppe_dev_setup_ft_block_cb`（传 `true`）、`#else` 分支补对应桩（否则非 `NET_AIROHA` 配置下调用不一致）。**方法修正**：该文件的三处 hunk 改用 `git diff --no-index` **机器生成**，不再手写 `@@` 头 —— 首版手写的 `@@ -68,6 +74,12 @@` 与真实位置（33 行）不符，SOE-24 自己的那个头同样错，属同一重建血缘 | n/a（SOE 补丁作者自持；损伤系本仓切分引入） | **未改 SOE-14/16 wrapper**：F132 已证明手工改这两个 wrapper 会引入新损伤，故按"一文件一补丁"的本仓约定单独成文（SOE 系列全部是单文件内层补丁）。**实证**：内层补丁对真实 `airoha_ppe.c`（`tmp/openwrt-src/work/A`）`git apply` 成功、落到正确 3 参签名；`audit-patches` **115/115**（114→115）；`apply-patches --dry-run --oc --experimental` 处理 **115、实验档跳过 0、缺失文件 0**，拷贝类真实应用 **4/4、未校验/跳过 0**；`audit-upstream-watch --fail-on-drift` exit 0。**一个真实踩坑**：首版 MANIFEST 把说明写成行尾注释（`... ROOT   # 说明`），被 `verify-copy-patches.sh` 解析成"目标目录 = F134"并**静默跳过本补丁**（报 `目标目录不存在：F134`）——注释必须整行写在条目之前，活动行不允许尾随注释。**编译结论以 CI 为准**：run `34708868615`（提交 `2204ef1d`，同时含 F132 与 F134 两处修复） |
| F135 | 包级补丁 `-p1` 路径约定错：`9056` 四个内层包补丁（2026-09-12 P6） | run `34714547879`（`4949e25`）达成里程碑：**内核与 airoha 驱动全部编过**（`airoha_eth.o`/`airoha_ppe.o`/`airoha_ppe_debugfs.o`/`airoha_npu.o` 全出现，`compile errors: 0`），构建推进到用户态包后红在 `Applying package/network/services/ppp/patches/001-xr1710g-dhcpv6-duplicate-guard.patch: can't find file to patch` （内层 `--- a/package/network/services/ppp/files/lib/netifd/ppp6-up`） | **路径约定错，不是文件缺失**（该文件在真源中存在）：OpenWrt 用 `patch -f -p1 -d <PKG_BUILD_DIR>` 应用包补丁（`KPATCH` = `scripts/patch-kernel.sh`，见 `rules.mk:307`），故内层包补丁必须按**包源相对**寻址；而 `9056` 的四个内层补丁写的是**仓库根相对**路径，经 `-p1` 剥离后成为 `<PKG_BUILD_DIR>/package/network/...`，该路径永远不存在。既有包补丁写法可对照：`105-debian_demand.patch` 用 `a/pppd/demand.c` | **① 首版判断（已作废）**：先把内层 target 由仓库根相对改为包源相对（`a/files/...`），run `34717775692` 证明**仍红**在同一处 —— 改路径方向对但不够。**② 真正机制**（读 OpenWrt 源码得出）：`KPATCH = scripts/patch-kernel.sh` 以 `patch -f -p1 -d <PKG_BUILD_DIR>` 应用（`rules.mk:307`、`Build/Patch/Default` 见 `quilt.mk:87`），而 `files/` 是包 overlay，**在 Build/Prepare 才拷入 PKG_BUILD_DIR，晚于打补丁**；证据：ppp 包目录只有 `files/Makefile/patches/utils`，`ppp6-up` 在 `files/lib/netifd/` 下，且现有 12 支 ppp 包补丁**无一**碰 `files/`（全部指向 `a/pppd/*.c`、`a/configure.ac`）。⇒ **内层包补丁无论怎么写路径都看不到 `files/`**。**③ 最终修法（b）**：新增本仓 overlay 整文件 `overlay/files/lib/netifd/ppp6-up` 与 `.../dhcpv6.sh`（构建期由 workflow 拷进树 `files/`；`sh -n` 通过；逻辑 = 原 001 补丁同一段），`9056` 只保留两支合法的 `Makefile` `PKG_RELEASE` 内层补丁，删掉两支机制上不可能命中的 001，并同步重算外层声明行数与 `diff --git` 头 | n/a（本仓补丁路径约定，非上游项） | 最终形态见 F136（overlay 落错根目录已修）与 F139（9056 整条删除、guard 纯 overlay 投递）；审计/dry-run 数字见那两行 |
| F136 | F135 的 overlay 整文件投递**落错根目录 + 目标路径错**（2026-09-13 P6 复核） | F135 最终修法新增的两支整文件（`ppp6-up`、`dhcpv6.sh`）被放进**仓库根的 `overlay/` 子目录**（`overlay/files/lib/netifd/…`），而 CI 只投递**仓库根 `files/`**（`build.yml:78` / `collect-sources.yml:36`：`cp -rf "$GITHUB_WORKSPACE/overlay/files/." files/`；此处 `$GITHUB_WORKSPACE/overlay` 是 checkout 目录名，来自 `actions/checkout` 的 `path: overlay`）→ 两份文件落进树内 `overlay/files/…` 死目录，**永远进不了 rootfs**：F118 的去重 guard 实际未投递（`xr1710g-dhcpv6-guard.sh` 在 rootfs，但没有调用方）。第二处错：`dhcpv6.sh` 的目标路径——上游装到 `/lib/netifd/proto/dhcpv6.sh`（`odhcp6c/Makefile:43`），overlay 却放在 `files/lib/netifd/dhcpv6.sh`（少 `proto/`），即使根目录正确也覆盖不到 | ① 把 checkout 的动作目录名（`overlay`）当成了本仓的目录约定——本仓定制 overlay 一直在**仓库根 `files/`**（其余 10 个 overlay 文件都在那儿）；② 整文件投递要求 overlay 路径**逐字符等于 rootfs 绝对路径去掉前导 `/`** | `git mv overlay/files/lib/netifd/ppp6-up files/lib/netifd/ppp6-up`；`git mv overlay/files/lib/netifd/dhcpv6.sh files/lib/netifd/proto/dhcpv6.sh`；删空 `overlay/`。`9056` 内层形态未动（**F139 随后整条删除**：那两支 `PKG_RELEASE` 内层同属机制不可能） | n/a（本仓 overlay 路径） | **机制实证（树内可复核）**：`include/image.mk:431` 在 **opkg 安装之后**调用 `prepare_rootfs($(mkfs_cur_target_dir),$(TOPDIR)/files)`；`include/rootfs.mk:71-73` 用 `file_copy` 把 `$(2)/.` 整树拷入 rootfs → **树 `files/` 晚于包、覆盖包文件**，整文件投递方式成立（F135 提交信息里"未实证的假设"至此落实为已证）。目标路径对照：`ppp/Makefile:217` `$(INSTALL_BIN) ./files/lib/netifd/ppp6-up $(1)/lib/netifd/`；`odhcp6c/Makefile:43` `… ./files/dhcpv6.sh $(1)/lib/netifd/proto/dhcpv6.sh`。两份 overlay 对上游 f0d3e332 版逐行 diff = **仅 guard 插入段**（`ppp6-up` +9 行、`dhcpv6.sh` +11 行），`sh -n` 通过。**残留风险（已登记）**：整文件覆盖 = 对上游脚本的快照分叉，上游改这两个脚本不会自动进来 → 每次 pin bump 需 `diff` 复查。**实机判据（待）**：PPP 模式与显式 dhcpv6 并存时 `cmp` 设备上 `/lib/netifd/ppp6-up`、`/lib/netifd/proto/dhcpv6.sh` 含 guard 段；`pidof odhcp6c \| wc -w` = 1 |
| F137 | fanboy 第二次 force-push 漂移：**纯重基**，只需重新锚定（2026-09-13 P6） | `audit-upstream-watch --fail-on-drift` 再次红：`OpenW1700k.git refs/heads/ubi2-oc` tip 前进（记录 `1b7cacf39` → 实际 `414d2098f`）。这正是 F133 预判的复发（fanboy 习惯性 force-push 重基，静态快照锚点必被打红） | 上游 force-push（`1b7cacf39` 不是 `414d2098f` 的祖先，`git merge-base --is-ancestor` 判 NOT ancestor）；锚点无"重基后重新锚定"流程 | 仅改 `scripts/audit-upstream-watch.sh:78` 记录 tip `1b7cacf39` → `414d2098f`（含用途注释里的锚点）；**vendor/fanboy/01..21 原料桶零改动** | n/a（vendor 漂移核对） | **甄别（两侧各 60 commit 逐条 patch-id 集合比对）**：左差 1 条 `mediatek: fix SPI bus-width spelling on the TP-Link F65v1`（=`openwrt/openwrt@0b03b57f`，上游提交）、右差 1 条 `gemini: backport FOTG210 support`（=`openwrt/openwrt@234c308b`，上游提交）——两条都是**上游提交进出 60 窗口**的边界效应，**fanboy 自有内容逐条 patch-id 全等**（含 T20 OC `5724590e`、19 apps pack `8d9c9d2b`、20/21 等）⇒ 纯重基，无真新内容可吸收。**实证**：改后 `audit-upstream-watch --fail-on-drift` **exit 0**（基线漂移 0、7 远端全"未变"）；`audit-patches` 115/115；`apply-patches --dry-run --oc --experimental` 115 处理 / 0 跳过 / 0 缺失 + 拷贝类 4/4。**机制遗留（同 F133）**：锚点仍是静态快照，下次 force-push 会**再次打红**；已连续两轮为纯重基，若第三次仍是，可考虑把该来源降为 informational（须在台账写明是降级） **续（2026-09-13）**：第三次 force-push 已按同法对账并重新锚定，见 **F154**（14/21 逐字相同、2 同 diffstat、5 内容有差）。 |
| F138 | hostapd "0 条补丁"缺口的定案：**4 项候选全部零吸收**（2026-09-13 P6 复核） | F128/F129 把 hostapd 列为明显缺口（本仓 hostapd 补丁 0 条）并给出 4 项候选 `603/901/905/906`，证据路径为 `research-20260912/repocmp/suntyrael/rest/package__network__services__hostapd__patches__{603,901,905,906}-*.patch` | ① **证据源是坏的**：那批文件里除 `603`（767 B 真补丁）外，`602/900/901/902/903/904/905/906` 全是 **14 字节的 `404: Not Found` 桩**——上一轮的下载失败被当成补丁留存，"不盲上"实际变成了"盲判"；② 真源核对：`YYH2913/openwrt@xr1710g-6.18-integration` 的 hostapd `patches/` **只比上游多 603 一条**，其余 8 条由 YYH 自己在 `30d10109 hostapd: drop patches included upstream`（2026-08-19）删除，加入点是 `5bf40fdf` | **零吸收**（本仓 hostapd 补丁保持 0 条）。需要的唯一一条 `603`（6GHz 不需 DFS）**已有等价载体**：`patches/vendor/fanboy/17-cmonroe-stability-8c530f05.patch` 内层含**同一 hunk**（`src/ap/dfs.c @@ -1561,6 +1561,7 @@` 追加 `is_6ghz_freq(iface->freq) \|\|`），`MANIFEST:251` default 活动 ⇒ 6GHz AP 不再走 DFS 要求路径 | n/a（上游已含 / 已由 vendor 桶携带） | **双证（原文取回 + pin 源码）**：从 `5bf40fdf` 取回 8 条原文，对 hostapd pin `831364bf`（2.12，`tmp/hostap-src`）跑 `patch -p1 --fuzz=0 --dry-run`：<br>· `Reversed (or previously applied)` ⇒ **上游已含**：`900/901/902/903` **以及 `905`（STA identity 缓冲区溢出，安全项，无需再带）**；<br>· `rc=0` ⇒ `603` 可应用（但已被 fanboy/17 覆盖，**重复携带会冲突**）；<br>· `FAILED` ⇒ `602/904/906`——逐条读 pin 源码确认**修复已在**：`src/drivers/driver_nl80211.c:6952` 的 `use_existing && if_nametoindex(ifname)` 短路 + `nl80211_use_existing_iface()`（602）、`src/ap/hostapd.c:260 hostapd_remove_hapd_iface()` 指针比较（904）、`src/ap/hw_features.c:1216-1235` 的 `new_sec`/`bw_flag` 换向探测（906）。失败只是上游重构改了上下文（`nl80211_create_iface` 去掉 `radio_mask` 参数、`hostapd_is_usable_chans` 新增 `err2/central` 分支），**非缺失**。<br>**方法教训**：候选清单必须保真下载（`404` 桩必须显式剔除/报错，不能留成文件名）；判"上游是否已含"要 `Reversed` 信号 + **读 pin 真源**双证——本批 3 条（602/904/906）patch 应用失败但修复确实已在上游，只看 `FAILED` 会误判成"待吸收" |
| F139 | 9056 剩下的两支内层**同属机制不可能**（包补丁打不到 OpenWrt 包定义）→ 整条删除（2026-09-13 P6） | run `34735169622`（`f04bd91`，**首次拿到完整构建日志 artifact**）在 F135/F136 之后仍红在 ppp：`Applying package/network/services/ppp/patches/002-xr1710g-dhcpv6-package-revision.patch using plaintext: can't find file to patch at input line 10` → `ERROR: package/network/services/ppp failed to build (build variant: default)`。此轮补丁层 0 FAILED、内核与 airoha 驱动全部编过（`airoha_eth.o`/`airoha_ppe.o`/`airoha_npu.o` 均在），红点已推进到用户态包 | F135 把 001（guard 段）判为"机制不可能"是对的，但把 002（`PKG_RELEASE` bump）判为"合法"**错了**：`KPATCH = scripts/patch-kernel.sh` 以 `patch -f -p1 -d PKG_BUILD_DIR` 应用（`rules.mk:307`），而 `PKG_BUILD_DIR` 是**解包后的上游源码目录**（日志实证：`.../linux-airoha_an7581/ppp-default/ppp-2.5.3`）——002 的目标却写成**仓库根相对** `a/package/network/services/ppp/Makefile`，剥掉 `a/` 后要在 ppp-2.5.3/ 里找 `package/network/...`，永远不存在；即便改成包源相对 `a/Makefile` 也不对，因为 `PKG_RELEASE` 是 **OpenWrt 包定义**变量、不在上游源码里。⇒ 001 与 002 是同一类错，错在两层 | **整条删除** `patches/root/9056-xr1710g-dhcpv6-duplicate-guard.patch`（含 MANIFEST 条目），原位置改为一段注释：本项**无补丁载体，纯 rootfs overlay**。F118 的三份 overlay（`files/lib/netifd/xr1710g-dhcpv6-guard.sh` + `ppp6-up` + `proto/dhcpv6.sh`）由 `prepare_rootfs`（`image.mk:431`）在 opkg 安装后覆盖 rootfs ⇒ 本来就**不依赖包重装**，`PKG_RELEASE` bump 无收益 | n/a（本仓补丁形态） | **实证**：`audit-patches` **114/114**（115→114）；`apply-patches --dry-run --oc --experimental` 处理 **114** / 实验档跳过 0 / 缺失 0 + 拷贝类真实应用 4/4；`audit-upstream-watch --fail-on-drift` exit 0。**若要真的 bump opkg 版本号**：正确形态是 **root 档补丁直接改 `package/network/{services/ppp,ipv6/odhcp6c}/Makefile`**（同 `9017` 的做法），不是包 `patches/`。**教训**：本项连踩三轮（F135 改路径 → F136 改 overlay 根 → F139 删内层），根因都是**没先确认"补丁应用点"（`-p1` 的 `-d` 目标 cwd）就动手**——判"能不能打"要先看 `KPATCH`/`PatchDir` 的 cwd，再谈路径写法；且"看起来合法"的路径（像仓库根的合法路径）最容易骗过静态校验（外层 `git apply --check` 永远看不出内层在包构建期去哪找文件）。**产物归位**：本轮首次落 `buildlog-experimental`（1.9 MB）artifact 于 `tmp/log-f04bd91/`，失败日志从此不再依赖被截断的 Actions 原始日志 |
| F140 | **实验档首次全绿**（run `34737361000` @ `e6f1e26`）+ files/ overlay 进镜像的**端到端实证**（2026-09-13） | 补丁层自 F139 后无 FAILED；本轮构建 `resolve`/`build (experimental)`/`release` 三个 job 全 success，产出 `firmware-experimental`（34.6 MB：sysupgrade.itb + initramfs-recovery.itb + chainload-uboot.itb + manifest）并自动发布 pre-release **`ci-154`**。**遗留的最后一个未证假设**：F136/F139 把 F118 的 guard 全部改成 `files/` overlay 投递，但"树 `files/` 真能覆盖包安装的同名文件"此前只有源码推理（`image.mk:431` + `rootfs.mk:71`） | —（非缺陷，是里程碑 + 假设验证） | 无（无代码改动） | n/a | **端到端实证（可复跑）**：① 取 sysupgrade.itb 内 rootfs —— FIT 里 `rootfs-1` 的 `data-position`/`data-size`（本轮实测 `0x56c000`/`0xbd1000`，compression=none），`tail -c +$((0x56c000+1)) x.itb > rootfs.squashfs`（用 `file` 复核 "Squashfs ... xz compressed"）；② 用 squashfs-tools **4.7.5**（`make LZ4_SUPPORT=0 LZO_SUPPORT=0 ZSTD_SUPPORT=0 GZIP_SUPPORT=1 XZ_SUPPORT=1 LZMA_XZ_SUPPORT=1 unsquashfs`；CI 侧等价物是 OpenWrt 自建的 `staging_dir/host/bin/unsquashfs4`）读内容。**结果**：`lib/netifd/ppp6-up` **1457 B**（= 仓库 overlay，包内原版 1071 B）、`lib/netifd/proto/dhcpv6.sh` **8056 B**（原版 7505 B）、`lib/netifd/xr1710g-dhcpv6-guard.sh` 4542 B，三者 mode `0755`；镜像内两份脚本与 `xr1710g-openwrt/files/...` **`cmp` 逐字节一致**，且各含 guard 标记（`xr_dhcpv6_has_explicit` / `xr_dhcpv6_conflict`）。⇒ **树 `files/` 确实在包之后覆盖 rootfs**，F135 提交信息里的"未实证假设"与 F136/F139 的投递路径全部落地。**纪律**：今后任何改动 `files/` 的提交，应在拿到固件 artifact 后按上面两条命令复核一次（本地三命令，约 1 分钟）；未复核不得声称"已在镜像里"。**证据留存**：`tmp/fw-ci154/`（firmware + `rootfs.squashfs` + 解出的两份脚本）、`tmp/log-e6f1e26/`（本轮完整构建日志 50 MB） |
| F141 | **default（stock）档首次全绿** + 两档镜像内 overlay 双重实证（2026-09-13） | run `34740879225` @ `90bc9cd`：`resolve` / `build (stock)` / `release` 全 success，产出 `firmware-stock`（34.6 MB），自动发布 pre-release **`ci-155`**。stock 档上一次绿是 2026-08-30（`33315438059`），中间经历 P2–P6 大量改动 ⇒ 本轮才第一次证明**档位门本身没错**（F100/F105 那类"`#EXP` 补丁引用了只在实验档成立的上下文"的盲区，只有 stock 真构建能暴露） | —（里程碑，非缺陷） | 无（无代码改动） | n/a | **实证**：`audit-patches` default **58/58**、experimental **114/114**（差 56 条即 `#EXP`/`#OC` 活动项）；按 F140 同款 recipe 校验 **stock 镜像**：FIT `rootfs-1` 的 `data-position` 是 **`0x577000`**（experimental 为 `0x56c000` —— 两档镜像布局独立，**偏移必须现算，不能照抄上一档**），解出 squashfs **1483 inodes**（experimental 1510，差 27 ≈ 实验档多出的包数）；三份 overlay 全在且与仓库 `files/…` **`cmp` 逐字节一致**（`ppp6-up` 1457 B / `proto/dhcpv6.sh` 8056 B / guard 4542 B），`xr_dhcpv6_has_explicit` 标记命中。⇒ `files/` overlay 与档位无关、两档均正确投递；inode 数差异同时证明 **`#EXP` 门真的在过滤**（不是空转）。证据留存：`tmp/fw-ci155/`（含 `rootfs.squashfs`）、`tmp/log-*` |
| F142 | **SOE-17..28 转 #EXP**（12 支，2026-09-13；原 gate 理由证伪）+ SOE-20 按真实真源二次重基 | 自 `9820d1a` 起 SOE-17..28 停在 `#DISABLED`，理由写作"依赖更新上游内核的 nft_flow_route_routing（本仓基线为 nft_flow_route）"；而 F122 又已声称"19 个补丁全部干净应用"——两处结论矛盾，导致 AN7581 硬件 IPsec 卸载整条依赖链只剩 10..16 半截 | ① **判据错**：作者基线的 `nft_flow_route_routing()` / `nft_flow_route_bridging()` 拆分与 int 返回的 7 参 `nft_dev_forward_path()` 都来自**作者侧更新的上游内核**，不是本仓该追的依赖——本仓真源 `tmp/kernel-base@058cfa7` 的 `net/netfilter/nf_flow_table_path.c` 与 vanilla `linux-6.18.44`（sha256 `b9f3776c…`）**逐字节相同**，只有 `nft_flow_route()`；且上游 OpenWrt master 只有 `target/linux/generic/{config,pending,hack}-6.12/6.18` 两档 ⇒ **"升级内核基线"（路线 A）在本仓不可行**，只能按现有 API 重锚（路线 B）。② **20 号初版重基对象错**：hunk #1/#3/#6 对着 kernel-base（**不含** `vendor/fanboy/06` 的 675-01..03）重写，而 675 是 default 活动项、改的正是同一文件的 include 区与 `nft_flow_route()`（引入 `is_bridging` 快路径、`nft_dev_forward_path()` 改 7 参 int 返回）——真实 prepare 内容里这三处必然 FAILED。③ **9999-45 与 24/25 重叠**：F134 那四处签名改动里，24 = `airoha_offload.h` 三处、25 = `airoha_ppe.c` 定义 + `is_flowtable` 透传，24/25 一启用即完全覆盖 9999-45 | ① SOE-20 内层**整份由 `git diff` 机器重生成**（对照文件 `research-20260912/inner/path-post675.c` = kernel-base + 675-01..03，365 行）：include 区随真源、fl4 proto/sport/dport 三行落到 `nft_flow_route()` 的 IPv4/IPv6 分支、XFRM 发现块置于 `skip_l3:` **之前**（只对路由流生效，675 的桥接快路径不受影响）、675 的 `nft_dev_forward_path()` 形态原样留作上下文；外层声明行数 170→167 同步重算。② `patches/MANIFEST`：17..28 十二行由 `#DISABLED` 改 `#EXP`（**去掉尾随注释**——F134 教训：活动行不许尾随注释），9999-45 改 `#DISABLED` 并注明"只有 24/25 再次被 gate 才恢复" | n/a（作者自持；本仓重基） | **实证**：① `audit-patches --oc --experimental` **125/125**（114→125 = +12 SOE −1 9999-45）；`apply-patches --dry-run --oc --experimental` **处理 125 / 实验档跳过 0 / 缺失 0**，拷贝类真实应用 **4/4**（regdb 5 / mt76 32 / mac80211 29 / uboot 41 文件）。② **真实应用序模拟**：`research-20260912/inner/collect_kernel_inners.py`（按 MANIFEST 活动集抽内层，按 `include/quilt.mk:102-105` 的 backport→pending→hack→target 排序）在 `kernel-base@058cfa7` 上重放，`ktest5` 里 **soe-17..24、25、26..28 逐个 `patch -p1 --fuzz=0` 干净应用**（`logs/ktest-seq4.log`）。③ 24/25 启用后 `airoha_ppe_setup_tc_block_cb` 为 3 参、`airoha_offload.h` 有 `setup_ft_block_cb`、`airoha_soe.c` 与 `NET_AIROHA_SOE` 落位。**⚠ 2026-09-13 F150（后续结论）**：转 `#EXP` 后 CI 首次编译 SOE 代码即红——真因是 16 号缺 API 与 25 号结构残缺，**本批曾回退 `#DISABLED`，F150 修完 4 类缺陷后已重新启用**（结构体检：三文件花括号深度 0、异常点 0；编译结论以最终 CI 为准）；详见 F150 与 `research-20260912/inner/MODEL-CHECK-F150.md`。**CI 实证（run `34745558426` @ `36718bf`）**：补丁层 **`Hunk FAILED: 0`**——SOE-17..28 在真实树上全部应用成功（与本地真实序模型一致）；构建红在 **kconfig**：`Airoha SOE ESP offload support (NET_AIROHA_SOE) [N/y/?] (NEW)` → `scripts/kconfig/Makefile:85: syncconfig Error 1` —— 即 SOE 的**配置启用半边缺失**（同轮发现，已由 F149 的 `9064` 修复）；SOE 代码的编译结论见 F149/F150 —— **run `34752142233`（`f09ca19`）experimental 全绿、release `ci-159`**：补丁层 `Hunk FAILED: 0`、`CC [M] drivers/net/ethernet/airoha/airoha_soe.o`、`LD [M] airoha-eth.ko`、`: error:` 计数 0。**未决**：25 的第 12 个 hunk（`airoha_ppe_foe_flow_commit_entry()` 尾部 `hlist_add_head` 改写）在本地模型上 FAILED（该模型对"同名替换上游补丁文件"类（182/992-20）无法重放，14/16 亦假红）——若 CI 报同一 hunk，按真源重锚该处 |
| F143 | **SOE LAG 系列吸收**（作者 `9999-29..38`，10 支，2026-09-13 F142 第二批） | AN7581 SOE 的 LAG（bonding 双口/多口）场景：一个 XFRM state 需同时在多个 lower 上成立（共享序列号/replay 上下文）。该系列是作者 tip `73c3ab3` 的最后一个主题（commit 标题即 bonding LAG XFRM offload infrastructure），此前未吸收 | ① 跨 net/xfrm（29..31）+ flowtable（32）+ bonding（33..35）+ airoha SOE（36..38）；其中 **29/30 的 hunk 上下文依赖 A 系列 9053** 新增的 `xdo_dev_packet_xmit` 与 `struct xfrmdev_ops.flags`，**36..38 依赖 SOE 主体 25/26** 的 `airoha_soe.{c,h}`；② 作者用连续号 `9999-29..38`，与本仓既有 `9999-30..35`（9061 NPU mailbox）、`9999-40..42`（9062 EIP93）**撞号**，且按文件名字典序 `9999-36..38` 会排到 `9999-net-airoha-soe-25/26` **之前** ⇒ 照搬编号必失败 | 新增单个 `#EXP` 捆绑 `patches/root/9063-xr1710g-xfrm-lag-shared-state.patch`（10 个 new-file 段，装到 `target/linux/airoha/patches-6.18/9999-xfrm-lag-NN-<slug>.patch`，前缀 `9999-xfrm-lag-` 同时满足"在 9053 之后"与"在 SOE-25/26 之后"）；`9053` 内层由 `9999-xfrm-packet-offload-framework.patch` 更名 `9999-xfrm-00-packet-offload-framework.patch`（**只改文件名，内容零改动**，另同步 SOE-19 头部注释里的引用） | n/a（作者自持；未提上游 PR） | **实证**：`audit-patches --oc --experimental` **126/126**（125→126）、`--oc` **58/58**（stock 不受影响）；`apply-patches --dry-run --oc --experimental` **处理 126 / 实验档跳过 0 / 缺失 0** + 拷贝类 **4/4**；真实应用序模拟（`collect_kernel_inners.py` + `ktest6`）上 **lag-29..38 逐个 `patch -p1 --fuzz=0` 全绿**（`logs/ktest6.log`）。**⚠ 2026-09-13 F150（后续结论）**：LAG 的 36..38 依赖 SOE 主体（26 号的 `airoha_soe.{c,h}`），随 17..28 一并**回退 `#DISABLED` 后又随修复重新启用**（见 F150）。**编译结论以 CI 为准**：run `34745558426`（`36718bf`）**不含本批**（LAG 在 `69945de` 才接入）；该 run 红在 SOE 的 kconfig（缺 `CONFIG_NET_AIROHA_SOE`，F149 已补）。**LAG 的构建结论：run `34752142233`（`f09ca19`）全绿**（`CC/LD drivers/net/bonding/bonding.ko` 成功、打包为 `kmod-bonding`，镜像 manifest 含 `kmod-bonding` 与 `ip-full`）。**实机判据（待）**：① experimental 构建通过；② 建 `bond0`（两口）后 `ip xfrm state add` 一次、两 lower 均卸载（`xdo_dev_state_lag_compatible` 判定同源）；③ lower 断开或 mode 切换（802.3ad↔active-backup）后 ESP 不中断、计数连续；④ `ip -s xfrm state` 的 replay 计数无跨口错乱 |
| F144 | **802.11r / FT-over-DS 落地**（2026-09-13 P2 §2.8 遗留项；+ Gilly `400` 改号 412） | 本仓 `grep -rln "ft_over_ds\|ieee80211r\|mobility_domain"` 此前为**空**：出厂无线配置既没开 802.11r、也没有 FT-over-DS。orangeyoo v1.6.1 的根因证据很硬——出厂 5GHz 只开 OTA FT（`ft_over_ds='0'`）时，**Apple 客户端惯用 FT-over-DS**，弱信号漫游退回全量 SAE 认证，实机日志 **66 次** `did not acknowledge authentication response` 全部来自同一台 iPhone；置 `ft_over_ds='1'` 后消失。配套同族缺陷：AP 侧 FT 的 PTK 上传竞态（`nl80211: kernel reports: key addition failed`，PMF 下直接打断漫游） | ① 802.11r 完全没开 ⇒ `ft_over_ds` 连被读取的机会都没有（`ap.uc: iface_roaming()` 只在 `ieee80211r` 为真时才下发）；② 现网/已装机器的 `/etc/config/wireless` 是**保留配置**，不会因为换镜像而更新 ⇒ 只改出厂文件修不到存量机器；③ Gilly `400`（Andrea Covelli，PR openwrt/openwrt#23181 / LKML 2026-09-04）**未进上游**：本仓 pin `backports-7.2` 源码里 `KEY_FLAG_DEFERRED_HW_UPLOAD` 与 `ieee80211_upload_deferred_sta_keys()` 均不存在 | ① `files/etc/config/wireless`：四个出厂 AP iface（`default_radio0/1/1_5g/2`）各加 `ieee80211r='1'` + `mobility_domain='6616'` + `ft_over_ds='1'`（密钥/信道/加密/SSID 不动）；② 新增 overlay `files/etc/uci-defaults/98-xr1710g-ft-over-ds`（0755）：只处理**出厂 iface 名且 mode=ap** 的段，逐 option 幂等（全就绪即零写入零 commit），只设上列三项（用户显式设过的 `mobility_domain` 保留），板型 `gemtek,xr1710g-ubi`，失败一律 `exit 0`（oci-defaults 红脸会阻断 boot），可 `uci revert wireless` 回退；③ 新增 `patches/packages/mac80211-412-mac80211-defer-ap-side-ft-key-upload.patch`（**#EXP**；Gilly 原号 400 与本仓 YYH 400 撞号 → 避号 412） | open（PR #23181 未合；pin 无此改动） | **实证**：① `patch -p1 --fuzz=0 --dry-run` 对真源 `backports-7.2` 四文件全过（仅 `sta_info.c` 偏移 -6）；② `audit-patches --oc --experimental` **127/127**（126→127）、`--oc` **58/58**；③ `apply-patches --dry-run --oc --experimental` **处理 127 / 实验档跳过 0 / 缺失 0** + 拷贝类真实应用 **4/4**（mac80211 subsys 现 30 文件：上游 20 + 本仓 10）；④ overlay 脚本 `sh -n` 通过、mode 0755。**⚠ 两档同效**：`files/` overlay 无档位门（F141 已证两档镜像都投递）⇒ 本项的无线默认值对 **stock 同样生效**，属"产品默认"而非"实验补丁"。**实机判据（待，必须做）**：① 三频/双 AP 漫游时 `logread` 无 `did not acknowledge authentication response`；② `iw station dump` 的 `connected time` 连续、无重认证、漫游延迟下降；③ 非 FT 客户端 + PMF + `sae-mixed` 均可正常关联（无 AP 起不来/客户端连不上）；④ `uci set wireless.default_radio1.ieee80211r='0'` + `wifi reload` 后回到改动前行为；⑤ 存量机器升级后 `uci show wireless \| grep -c ft_over_ds=1` 与出厂一致。**未采纳**：Gilly 400 的原号 400 与 YYH 400 同号会造成 glob 序歧义，故避号并保留来源注释 **stock 档产物实证（2026-09-13，run `34756375776` → release `ci-161`）**：stock 镜像（sysupgrade itb 内 squashfs @5730304）里 `etc/config/wireless`（5747 B）与 `etc/uci-defaults/98-xr1710g-ft-over-ds`（3482 B，mode **0755**）**均与仓库逐字节一致**，四个出厂 AP iface 的 `ieee80211r='1'` / `mobility_domain='6616'` / `ft_over_ds='1'` 各 **4 处齐备** ⇒ F141「`files/` overlay 无档位门」由 **stock 产物**（非推断）直接证明，本项默认值对两档同时生效成立。  |
| F145 | **SOE D 系列接线复核**（2026-09-13，handoff_p5 §3.4 遗留项）——结论：**三层一致，无需改动** | handoff_p5 §3.4 把"SOE D 系列接线复核"列为未完成项，但没写清"接线"指哪几层：D 系列（EIP93 动态软回退）跨内核 Kconfig/Makefile、OpenWrt 包定义、target 默认包三层，任一层的名字/依赖对不上就会在构建期或运行期断链 | 逐层核对后发现**三层本已自洽**，真正踩过的坑（F126：删 KCONFIG 导致 `syncconfig` 报未决符号）已修，故本项只需落账而无需再改代码 | 无（纯复核，未改任何文件） | n/a（本仓内部一致性） | **逐层证据**：① **内核层**（9054 内层 `9999-crypto-eip93-dynamic-fallback.patch`）新增 `config CRYPTO_BENCHMARK_LIB`、`config CRYPTO_BENCHMARK`（`select CRYPTO_BENCHMARK_LIB`）、`config CRYPTO_DYNAMIC_FALLBACK`（`select CRYPTO_BENCHMARK_LIB`），并落地 `obj-$(CONFIG_CRYPTO_BENCHMARK_LIB) += crypt-benchmark.o`、`obj-$(CONFIG_CRYPTO_DYNAMIC_FALLBACK) += fallback.o`，EIP93 自身 `select CRYPTO_DYNAMIC_FALLBACK` 且 `crypto-hw-eip93-y += eip93-fallback.o`（grep 行号 52/55/67/70/82-84/615-621/636-639/2132/2150）。② **包定义层**（9060）：`KernelPackage/crypto-benchmark`（FILES `crypto/crypt-benchmark.ko`）、`KernelPackage/crypto-dynamic-fallback`（FILES `crypto/fallback.ko`，`DEPENDS:=+kmod-crypto-benchmark`）、`kmod-crypto-hw-eip93` 加 `+kmod-crypto-dynamic-fallback`，且 **KCONFIG 4 行原样保留**（F126 教训）。③ **默认包层**（`vendor/fanboy/02` 内层）：`target/linux/airoha/an7581/target.mk` 的 `DEFAULT_PACKAGES += airoha-en7581-npu-firmware uboot-envtools kmod-crypto-hw-eip93`。④ **构建实证**：F140/F141 两档全绿即含本链路（`kmod-crypto-hw-eip93` 及其 `fallback.ko`/`crypt-benchmark.ko` 依赖在 experimental 档解析成功）。**运行期判据**见 `docs/DEVICE-VALIDATION.md` V6.1** |
| F146 | **929（NPU 低地址 coherent mailbox pool）不携带的判据落账**（2026-09-13） | F125 备注里写了"929 未吸收"及启用条件，但没有留下可复核的DT证据；handoff_p5 §3.4 也需要这一项闭环 | YYH `929-net-airoha-npu-use-addressable-coherent-mailbox-buffer.patch` 第 115 行调用 `of_reserved_mem_device_init_by_name(dev, dev->of_node, "mbox")`——它要求 DT 里存在**名为 `mbox` 的 `shared-dma-pool`**；而本仓与上游都没有该节点（上游 `an7581.dtsi` 的 `reserved-memory` 子节点是 `atf`/`npu-binary`/`qdma0-buf`/`qdma1-buf`… 且 `npu_binary` 是 `no-map` 不可当 DMA 池用；`grep -rn shared-dma-pool patches/` 与 `9001` 内均无）⇒ 直接落地会让 NPU probe 在 `of_reserved_mem_device_init_by_name` 处失败，比现在（上游 coherent mailbox 路径可用）**更差** | 不携带（保持现状）；若将来必须（实机 probe 报固件版本 0.0 且 928 已修仍复现），先按 2GiB DDR 布局加低于 `0xbfffffff` 的 `shared-dma-pool` 命名 `mbox` | n/a（作者自持；条件未出现） | **复核命令（当前应全部为空）**：`find /proc/device-tree -name mbox`（实机）；`grep -rn "shared-dma-pool" patches/`（仓库）；`sed -n '18,40p' target/linux/airoha/dts/an7581.dtsi`（真源 reserved-memory 子节点）。**前提前置**：929 依赖 928（`9999-35` 发布 mailbox 命令先于 doorbell，本仓已随 9061 落地）|
| F147 | **待实机验证清单成文**（`docs/DEVICE-VALIDATION.md`，2026-09-13）+ br-lan 根因引用逐行更正 | 本仓 CI 只证"补丁层干净 + 编译通过 + 镜像产出"，**不证硬件行为**；而 `#EXP`/`#OC`/overlay 项的"实机判据"散落在 FIXES 各行的正文里（F117/F118/F122/F123/F125/F127/F136/F142/F143/F144…），上机时无法逐项跟踪，也未与 `docs/ACCEPTANCE.md`（known-good 冻结门槛）区分职责 | ① 判据分散 ⇒ 易漏项（尤其"20 轮 reload / 12h 长压 / LAG failover / FT 漫游"这类需要特定动作的项）；② br-lan workaround 的头部注释把 DTS 根因写成 `&gdm2`(lan1) 与 `&gdm4`(lan2) 都用 `<&lan_mac 0>`，而 2026-09-13 对 `patches/root/9001-xr1710g-dts.patch` 逐行复核发现：**`&gdm1`(lan1) 与 `&gdm4`(lan2)** 才都取 `<&lan_mac 0>`，`&gdm2` 取 `<&wan_mac 0>`、Wi-Fi 三频取 `<&lan_mac 1/2/3>` | 新增 `docs/DEVICE-VALIDATION.md`：6 组共 30+ 条可勾选判据（SOE/IPsec 含 LAG · NPU/mt76 生命周期 · PPPoE/DHCPv6 overlay · 网络与桥 · Wi-Fi 含 802.11r/FT · 其他 `#EXP`/`#OC`），每条注明台账来源、档位（E/S/O）、具体命令与通过标准、记录模板；并更正 `98-xr1710g-brlan-mac-unique` 头部注释的 DTS 引用（**仅注释，零行为改动**） | n/a（本仓文档） | **实证**：文档内所有命令/路径均取自台账原文或本仓脚本（`scripts/device-hw-probe.sh`、`device-pcie-aer-probe.sh` 存在性已核）；DTS 引用更正依据 `9001` 第 275/282/289（Wi-Fi 三频）与 315-318（gdm1）/326-333（gdm2）/342-349（gdm4）行；`sh -n` 复核 workaround 脚本仍通过（纯注释改动） |
| F148 | **SOE-17..28 与作者当前版逐条对账**（2026-09-13；handoff_p5 §3.4「旧改版」警告的结论） | handoff_p5 §3.4 警告："我们手上的 SOE-17..28 也是**旧改版**，12 支当前版已下载在 `research-20260912/soe14/author/cur/`"。该警告若成立，则 F142 刚转 `#EXP` 的这批补丁携带的是过期内容（可能缺作者后续修复） | 需要把"重基导致上下文不同"与"内容真的过期"区分开：前者正常，后者必须重取。判据取**新增行集合**（`+` 行、去掉 `+++`）而非整文件——重基只改上下文/行号，`+` 行应当一致 | 无（纯对账，未改任何补丁） | n/a（对账结论） | **方法**：`+` 行集合逐号 `set` 比对（本仓 `research-20260912/inner/repo-inner/9999-net-airoha-soe-NN-*.patch` 对作者 tip `73c3ab3` 的 `hurryman-git/.../9999-NN-*.patch`）。**先验证比对基准**：`soe14/author/cur/`（2026-09-12 17:25 抓取）与 `hurryman-git` 的 20/25/26 **逐字节相同** ⇒ 拿 tip 做基准 == 拿"当前版"做基准。**结果**：19 支里 **16 支 `+` 行集合完全一致**（10/11/12/13/14/15/17/18/19/21/22/23/24/26/27/28）；3 支有差异且**全部有据**：① 16：本仓多 `__maybe_unused`（F132 的 `-Werror` 修复）与恢复的花括号，作者侧多 RX 中断掩码、`error_unregister_netdevs:`、netdev teardown 块——后者在**本仓真源已由 9045（内层 976/980）等提供**（kernel-base 里 `AIROHA_FOE_IB1_BIND_TIMESTAMP` 已 7 处存在）；② 20：作者侧多 5 行 `nft_dev_forward_path(pkt, false, …) < 0` 错误处理——本仓真源是 vendor/fanboy `06` 的 7 参带 `is_bridging` 形态，按真源等价改写（F142 已记）；③ 25：本仓用 `bridge_hwe`（作者 `wdma_hwe`，F122 已记的重命名），作者侧多出的 `AIROHA_FOE_IB1_BIND_TIMESTAMP` / `memcpy(&hwe->d` / `synchronize_rcu()` / `struct airoha_foe_entry hwe;` 在 kernel-base 中计数 7/1/2/1（**真源已有**）。⇒ **结论：我们携带的是作者当前版，不是旧改版**；handoff_p5 §3.4 的该项警告解除。**对账脚本**：`+` 行集合比对（可复跑，见本行方法） |
| F149 | **SOE 与 LAG 的"配置启用半边"缺失**（2026-09-13，复核 SOE-27/28 与 LAG 33..38 时发现） | ① SOE-28 只加了带 prompt 的 `config NET_AIROHA_SOE`（`select XFRM_OFFLOAD`），**没有任何东西 select 它**：不在 target 片段置 `CONFIG_NET_AIROHA_SOE=y` 时，SOE-27 的 `airoha-eth-$(CONFIG_NET_AIROHA_SOE) += airoha_soe.o` 与 14/15/16/25 里所有 `#if IS_REACHABLE(CONFIG_NET_AIROHA_SOE)` 钩子**全部不编译**——功能为零，而且**CI 绿也证不到 SOE 代码能编译**（这正是 17..28 转 #EXP 后最大的验证盲区）。② LAG 侧同理：`generic/config-6.18` 是 `# CONFIG_BONDING is not set`，不选 `kmod-bonding` 则 33..38 号补丁是死代码；且建 bond 需要 `ip link add type bond`，busybox `ip` 与 `ip-tiny` 都不支持 link type | 补丁只提供"代码半边"，"被选择/被编译半边"必须另给：内核符号走 target 配置片段或 kmod 包 KCONFIG，用户态能力走 seed。F15 的教训（kconfig 对未知/未选符号静默）在这里第二次咬人——#EXP 门能保证"不进 stock"，但保证不了"进 experimental 的代码真被编译" | ① 新增 `#EXP patches/root/9064-xr1710g-soe-kconfig-enable.patch`：在 `target/linux/airoha/an7581/config-6.18` 置 `CONFIG_NET_AIROHA_SOE=y`（与 9035 的 FLOW_STATS 同文件不同区，只在 experimental 生效）；② `config/seed-config.experimental.diff` 增 `CONFIG_PACKAGE_kmod-bonding=y` + `CONFIG_PACKAGE_ip-full=y` | n/a（本仓 target 配置/seed） | **实证**：`audit-patches --oc --experimental` **128/128**（127→128）、`--oc` 58/58；`apply-patches --dry-run --oc --experimental` **处理 128 / 实验档跳过 0 / 缺失 0** + 拷贝类 **4/4**。**踩坑留痕**：9064 首版 hunk 手写上下文用 `CONFIG_NET_AIROHA=y`／`..._NPU=y`，与真实累加态冲突——`vendor/fanboy/04` 把它们改成了 `=m`；改为**对累加态用 `git diff` 机器生成**后通过（F134 的同型教训）。**判据（实机）**：`zcat /proc/config.gz \| grep NET_AIROHA_SOE` = y；`lsmod`/`modinfo` 见 airoha-eth 携带 SOE；`ls /lib/modules/*/bonding.ko` 存在且 `ip link add type bond` 可用。**CI 实证（run `34745558426` @ `36718bf`，本行修复前的状态）**：正是本行预判的失败——构建日志尾部 `Airoha SOE ESP offload support (NET_AIROHA_SOE) [N/y/?] (NEW)` `make[8]: *** [scripts/kconfig/Makefile:85: syncconfig] Error 1` ⇒ `target/linux failed to build`；补丁层本身 `Hunk FAILED: 0`。**这是同一类缺陷的第三次出现**（F126 的 EIP93 KCONFIG、本轮 SOE、以及同批 LAG 的 `CONFIG_BONDING`），教训固化：**新增带 prompt 的内核符号，必须同时给"选择它"的那一半**（或改成 `select`/隐藏符号）。启用配置后的编译结论见下一轮 run。 |
| F150 | **SOE 启用后编译真因**：16 号引用缺失 API + 25 号 `airoha_ppe.c` 重基结构残缺 ⇒ **回退 gate**（2026-09-13） | 按 F142 把 SOE-17..28 转 `#EXP`、F149 补上 `CONFIG_NET_AIROHA_SOE=y`（9064）、F143 带上 LAG（9063）后，run `34747460265` 把 SOE 代码**第一次**编进内核，立刻红在 `airoha_ppe.c`（92 处 error）与 `airoha_eth.c`（6 处）：`airoha_ppe_soe_flow_add` 参数类型不兼容（实为 `struct flow_offload_tuple` 未声明）、`q->flushing` / `e->dma_type` / `AIROHA_DMA_MAP_*` / `airoha_unmap_xmit_buf` 未声明、`expected specifier-qualifier-list before 'static'`（残缺 struct）、`conflicting types for 'rcu_read_unlock'`（残缺函数尾） | ① **盲区**：这些代码全在 `#if IS_REACHABLE(CONFIG_NET_AIROHA_SOE)` 内，而该符号此前无人 select（F149）⇒ `#EXP` 绿只证到「补丁可应用 + 其余代码可编译」，**从未证过 SOE 自身可编译**。② **16 号**新增的 `airoha_qdma_xmit_skb()` 写于作者树，引用本仓基线没有的 API：`q->flushing`（yyh2913 `179` **全量版**引入；本仓 deliberately 采用 Gilly 的精简 `957`）、`e->dma_type`/`AIROHA_DMA_MAP_*`/`airoha_unmap_xmit_buf`/`skb_frag_dma_map`（yyh2913 `181` 的 TX 片段映射重构）。③ **25 号** `airoha_ppe.c` 区在 P2/P5 重基时留下**结构性残缺**：hunk 末尾把作者树**上下文**（`/* ---- flush stale PPE flows … */`、`struct airoha_ppe_fdb_work {`）当成新增行（落下一个没有函数体的 struct 开头）；一处**重复函数尾** `rcu_read_unlock(); return err; }`；把本仓基线**需要保留**的 `rcu_read_lock()/npu = rcu_dereference(eth->npu)/if (!npu)…` 块误删；`airoha_ppe_foe_*` 段另有多处多余 `}` 与丢失的函数签名（严格模式模型树体检：终态花括号深度 **-2**、异常点 21~24 处，`airoha_eth.c`/`airoha_soe.c` 为 0） | ① **低成本修点已落地**：16 号按基线等价改写 4 处（去 `flushing` 守卫、去 `dma_type` 记账、`skb_frag_dma_map`→`dma_map_single(skb_frag_address())`、`airoha_unmap_xmit_buf()`→`dma_unmap_single()` 循环；`--fuzz=0` 干净应用、全部 hunk 体完整）；25 号补 `#include <net/netfilter/nf_flow_table.h>`、删掉误加的作者上下文 3 行、删掉重复函数尾、把 npu 块改回上下文。② **先回退 gate、再续修并重新启用**：`patches/MANIFEST` 里 17..28/9063/9064 曾回 `#DISABLED`、9999-45 恢复 `#EXP`（F134）；随后对 25 做**第 4 类残缺修复**——`airoha_ppe_foe_commit_subflow()` 的中间块与 `airoha_ppe_foe_commit_subflow_entry()` 的签名/声明/`kzalloc` 头被旧重基**误当删除行**（实为作者树上下文），逐行改回上下文后：真实应用序模型上 **`airoha_ppe.c`/`airoha_eth.c`/`airoha_soe.c` 花括号深度均 = 0、异常点 0**，于是把 17..28/9063/9064 重新转 `#EXP`、9999-45 交回 `#DISABLED`（24/25 自带签名改动）。③ 修复配方与复现方法落盘 `research-20260912/inner/MODEL-CHECK-F150.md`；体检脚本 `research-20260912/inner/model-check.sh` | n/a（作者自持；损伤系本仓重基引入） | **实证**：`audit-patches` 实验档 **115/115**（`--oc` 口径；build profile 无 `--oc` 口径 114）、stock `--oc` **58/58**（build profile 57）；`apply-patches --dry-run --oc --experimental` 全绿（见随行提交日志）；run `34745558426` 补丁层 `Hunk FAILED: 0`、run `34747460265` 首次编译 SOE 并复现上述全部错误；`model-check.sh` 在真实应用序模型上给出结构体检数字。**续（同日）**：25 的 `airoha_ppe_foe_commit_subflow*` 段误删真源上下文修复后，模型结构体检三文件花括号深度 0/异常 0，17..28/9063/9064 重新启用；**run `34752142233`（`f09ca19`）experimental 全绿、release `ci-159`**（`airoha_soe.o` 编译、`airoha-eth.ko` 链接、`: error:` 0；镜像内 `airoha-eth.ko` 含 65 处 SOE 符号，`etc/config/wireless` 与 `etc/uci-defaults/98-xr1710g-ft-over-ds` 与仓库**逐字节一致**）。**仍留（未完成）**：若将来要保留作者 TX 语义，需对 25 的 `airoha_ppe.c` 段做正经逐 hunk 重基（禁用 `rebase_hunks.py` 式自动回填；本仓 `bridge_*` vs 作者 `wdma_*` 命名必须按本仓）、按需带 yyh2913 `179` 全量版 + `181`（各需重基 1/3 个 hunk），再按 `17..28 → 9064 → 9063` 顺序重启用并逐步 CI。**顺带修复的工具坑**：`fix_hunk_counts.py` 原版只写回 hunk 头、**把 hunk 体整段丢掉**（本轮已修，见文件内 F150 注释）。**残留风险（已登记）→ 已结案（2026-09-14，handoff_p9 §3 第 1 步 / §2.1 第 1 项）**：对 25 的删除集复核，旧口径记「13 处/10 行未复核」。**本轮以三条互锁证据闭合**（报告 `research-20260912/inner/F150-10LINE-REVIEW.md`，判据脚本 `tmp/f150-review2.py`）：**A** 作者原始 `9999-25` 的 38 hunk 拆成（删除集/新增集）后在**本仓终态 kreal3** 逐行查存在性——作者新增去重 **884 行，878 行逐字在终态（99.3%）**，余 6 行全是换行点不同或**本仓主动扩展参数**（`airoha_soe_ppe_flow_add(…, lower_dev, …)`、`airoha_ppe_soe_flow_add(eth, tuple, odev, …)` = LAG 9063 的 `lower_dev`/`odev`，属本仓更强）；**B** `tmp/fb05/soe25-delcheck.txt` 的 10 条候选逐条判定：**8 条在终态原样存在**（前态=终态，本仓根本没删；含 2 条重复的 `{` 与 1 条续行），**2 条（`if (state == AIROHA_FOE_STATE_BIND)`、`rhashtable_lookup(&eth->flow_table, &f->cookie,`）两侧 kpre/kreal3 都不存在 ⇒ 候选集归一化噪声**；另对「作者 D 里有、终态仍存活」的 60+ 行做函数级排查，6 处「疑似存活删除」**全部证伪**（同名行出现在**另一个函数**：`airoha_ppe_get_entry_idle_time`/`flow_l2_entry_update`/`flow_commit_entry` 等）；**C** 与 F150 真源 diff `research-20260912/inner/ppe25-real9990.diff` 交叉核对：该 diff 的「删除」行（`struct airoha_flow_table_entry *e;`、`struct airoha_foe_entry *hwe;`、`bool local = …`）在终态**全都还在**（以合并声明或下移形式）⇒ 作者补丁的「删除」大量是**改写/搬移**，不能用「删除集差集」判语义。**结论：不存在「本仓删掉、作者保留」的语义缺失，10/10 结案为「等价（有据）」**；终态函数级抽查：`hwe_bound`+`airoha_ppe_soe_bind_hold_locked`（`kreal3:1650/1667/1668/1671`）、`remove_flow` 失败恢复（`hwe_data` 快照/`linked` 回挂/`return err`，`kreal3:1375` 起）、`commit_subflow()` 第 5 参（`1815-1816`）、`AIROHA_PPE_SOE_MAGIC_GDM4`（`2074-2077`）、入向 BIND/abort/claim 全链（`1880-1915`）**均逐字在位**。**为何不用「重放作者全序」**：作者原始 25 在本仓真源上 38/38 hunk FAILED（作者树=作者自己的 OpenWrt 树），本仓 bundle 的 `9999-net-airoha-soe-14/16/25` 也无法应用在近似树（缺 yyh2913 `179` 等上下文前提）⇒ 该路在本机不可作判据来源。**SOE 实机验证（未变）**：`docs/DEVICE-VALIDATION.md` V1.x）必须覆盖 `airoha_ppe_foe_*` 绑定/失效路径。 |
| F151 | **上游漂移吸收（9042 → 上游 #24872）+守护失效修复**（2026-09-13） | stock run `34755569091`（`f09ca19` 后派发）第 6 步「应用补丁层」红：`target/linux/airoha/patches-6.18/182-v7.4-…patch：已经存在于工作区中`，随后 `310-10-…:317` / `916-02-…:561` / `920-12-…:599` / `920-13-…:169` 四处 `patch does not apply`（exit 1）| 上游 master 在 10:32Z（run `34752142233` 绿）→ 11:52Z 之间合入 PR #24872，而本仓 9042 正是该 PR 的本地携带版 ⇒ ① **新增型 hunk** 与上游自带文件同址（`already exists`）；② 四处**修改型 hunk** 的上下文/行号已上游化。判据：对干净上游树 `git apply -R --check` 五段**全部干净通过** ⇒ 完整吸收，非部分漂移 | `patches/root/9042-xr1710g-airoha-rx-ring-grow.patch` 转 `#DISABLED`（文件保留作 F92 决策留痕），能力由上游 `182-v7.4-net-airoha-grow-the-small-RX-rings.patch` 承载（与本仓内嵌 182 **逐字节相同**，见备注）；MANIFEST 注释改写为 F92/F151 状态 | **merged**（openwrt `#24872`） | **实证（本机 fresh 上游 `12fa3481` = 12fa348112fe66b1d326e5c3ba2be7d7ecf95881，2026-09-13 11:14Z）**：① 三档补丁层全绿——stock `56 applied / 71 skipped`（audit 56 一致）、experimental `126/1`（audit 126）、`--oc --experimental` `127/0`（audit 127）；`vendor/fanboy/11`（916-02 修改型）与 `vendor/fanboy/21`（886-uart）在新基线上照旧干净应用 ⇒ 9042 之后无链式损伤；② 全局「新增文件 × 上游同址」扫描：83 个新增文件（stock 口径）仅 182 一处命中，即本条；③ **守护失效根因（同轮修）**：sync-upstream 的「上游基线守护」把*远端 fork tip 前进*计为硬失败（run `34755445119` @11:49Z 死在 2 处 fork tip：fanboy `414d2098f→49d20d02e`、Gilly `0ddd9fbf→a57615652`），而该步骤在 CI 里位于 dry-run **之前** ⇒ **真正拦得住补丁链的 dry-run 长期没跑过**，9042 同址冲突遂无人看见，直到手动构建才红（换言之：这次不是 dry-run 没能力，是被上游噪声挡住了）。修法四条：① `audit-upstream-watch.sh` 远端 tip 只报 ⚠ 不计失败（`rdrift` 与 `drift` 分离，退出码只由 漂移/缺失/同址冲突 决定）；② 新增「本仓新增文件 × 上游同址」**自动生成**探测——直接扫 MANIFEST 全部启用条目的 `new file mode` 段取 `+++ b/<path>` 比上游树，命中即硬失败（同时覆盖**拷贝类**盲区：`cp -f` 落位会静默覆盖上游同名文件）；③ `.github/workflows/sync-upstream.yml` 的 dry-run 步骤加 `if: ${{ !cancelled() }}`（守护红也必须跑硬门）；④ 基线锚点刷新 `f0d3e332e→12fa34811`。**回归实证**：新探测对 9042（临时改回启用）报 `⚠ [冲突] 上游已有同名文件：…/182-v7.4-…patch` 且 rc=1；保持 `#DISABLED` 时 `--fail-on-drift` rc=0（`汇总：基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 2（不计失败）`）。证据日志：`tmp/drift/{stock-ok,exp-ok,ocexp-ok,dryrun-ok,guard-ok}.log` **CI 实证（2026-09-13，修复后首次全绿）**：① sync-upstream run `34756325287` **success**——守护输出 `汇总：基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 2（不计失败）`（上游 HEAD `12fa34811`），且 dry-run 硬门**首次真正执行**：`处理：127 / 实验档跳过 0 / 缺失 0` + `拷贝补丁校验：真实应用 4 / 未校验 0`；推送触发的 `34756369462` 同绿（验证 `scripts/**` 触发路径生效）。② **stock 构建 run `34756375776` success**（release `ci-161`，48m33s）——正是此前红在第 6 步的那条路径：`hunk 行数审计 56 一致 / 处理 56 / 实验档跳过 71 / 缺失 0`；内核 prepare `Applying …/182-v7.4-net-airoha-grow-the-small-RX-rings.patch`（**上游 182 真被应用**：patching `drivers/net/ethernet/airoha/airoha_eth.h`）；`Hunk FAILED\|does not apply\|already exists` **0**、`: error:` **0**。③ **experimental 构建 run `34756377107` success**（release `ci-162`）——`审计 126 一致 / 处理 126 / 跳过 1 / 缺失 0`；`182`、`9999-net-airoha-soe-16`、`9999-xfrm-lag-29` 均 `Applying` 成功；`CC [M] airoha_soe.o`、`LD [M] airoha-eth.ko`、`LD [M] bonding.ko` 并打包进 kmod；`Hunk FAILED` **0**、`error:` **0**；镜像级：`airoha-eth.ko` 199520 B、`airoha_soe_` 符号 56 处，`etc/config/wireless` 与 `98-xr1710g-ft-over-ds` 与仓库逐字节一致。日志：`tmp/log-34756375776/full.log`、`tmp/log-34756377107/full.log`。 |
| F152 | **交接复核审计（p2–p6 逐项对物）+证据源 404 桩纠正**（2026-09-13） | 用户问「交接内容全部完成了吗」→ 对五份 handoff 的每个动作项做**对物**核对。动作项本身：p2 §2.1–2.9 载体齐 （9055 / `files/lib/netifd/*`+F118 / 9057 / 9058+9059 / 98-luci-apply-window / scripts/device-pcie-aer-probe.sh / 98-brlan-mac-unique+F147 / F144 / SOE 族），p3 §4.1 九吸+0157 不吸有理由+0155 并入 0147（`#EXP` 清单与 MANIFEST 一致），§4.2 四吸+0146 备选，§4.3 多不吸理由在 F128，§4.4 四条驳回有理由，p4 §0/§1 三项全落地（9017 已是 0a4f2632de 代：`offPct`×4、`TICKS = 90`/`◄BND` 已 0、`air_eff`×9、徽章创建已删且 `_setOffloadBadge` 有 `if(b)` 守卫），p5 §3.1–3.4 与 p6 §2–§4 全部落地/落账 | **本轮复核发现的两类真问题**：① **证据源系统性损坏**——`research-20260912/repocmp/suntyrael/rest/` 209 个文件里 **64 个是 14 字节 `404: Not Found` 桩**（F138 当时只发现 hostapd 那 8 个）；凡引用这些桩的判定都是「盲判」，其中 p3 §4.3/§5.3 与本轮点名的 6 项**全部受影响**；② 逐项结论缺失：`0020`/`9914`/`9919`/`735`/`101`/`fw4-9990`/`rpcd-0001` 在台账里没有最终判决（F131 只写了「CONFLICT 99 项含大量因本仓重建/上游漂移而上下文不匹配的重复项，不应视作待吸收」） | **本行仅台账（不改代码）**。证据：`tmp/drift/stub-404.txt`（64 项清单）、`research-20260912/yyh-refetch/`（重取）、`tmp/pkgtest/{rpcd,iwinfo,firewall4}`（pin 真源克隆） | n/a | **逐条判决（重取真源后，全部为机器判据）**：<br>· `9913`/`9914` **真源里不存在**——YYH2913/suntyrael 两仓 eip93 系列实为 9910/9911/9912/9915/9916/9917/9918/9919（`git/trees` 逐名核对）⇒ 桩名字是历次下载失败的产物，**事项关闭（无物可吸）**；<br>· `735-net-phy-realtek-rtl8261n` 真源只在 **`hack-6.12`**（两仓皆然）⇒ 本仓跑 6.18，且与 `vendor/fanboy/09` 私有 rtl8261ce + `9041` 重复 ⇒ **不吸收**（RTL8261CE 去留仍按 F101 等 #23644/#25092）；<br>· `9915`/`9916`/`9918` 在 kreal3 模型树（6.18.44 + 本仓内层全序）上 `patch -p1 --fuzz=0` 报 **`Reversed (or previously applied)`** ⇒ **已由 9062 覆盖**（强信号，非猜测）；<br>· `9917`（use request-local SA records for cipher requests，9828 B 真文件）**4/7 hunk FAILED** ⇒ 真冲突·**需重基**，本批不吸收；<br>· `9919`（handle request ID exhaustion，7899 B 真文件）**rc=0 全部 hunk 通过**（对同一 kreal3）⇒ **真残余·可吸收**（上游 `Fixes: 9739f5f93b78`、Reported-by Benjamin Larsson、Tested-by Aleksander Jan Bajkowski）——F131 当时把它判 CONFLICT 系树状态/桩所致，**本条是本轮审计最有价值的纠正**；<br>· `101`（iwinfo txpowerlist split wiphy dump，5179 B）对 pin 真源 `66bdd1a` `--fuzz=0` **rc=0（偏移 -28）**；与我们的 `9052`（只改 freqlist，3217+）**函数/行区不重叠** ⇒ 可共存；<br>· `9990-fw4-resolve-bonding-lower-devices`（985 B）对 pin 真源 `b6e5157` **rc=0**，且本仓 `grep resolve_lower_devices` = **无载体** ⇒ **真缺口**（bond0 会留在硬件 flowtable 成员里，直接服务 LAG/offload 特性）；<br>· `0001-exec-observe-EOF-before-invalidating-pipe-fds`（rpcd，1322 B）对 pin 真源 `e37ed9d` **rc=0**，本仓 rpcd **无 patch 载体**（9017/9019–9023 只往 rpcd 装脚本）⇒ **真缺口**；<br>· `0020`（WED off 时 rmmod 放页判据）**点名补记**：`0161` 重基后的 `mt76_dma_cleanup` hunk#2 已把 `page_pool_destroy` 收进 `if (q->page_pool) { … q->page_pool = NULL; }`，语义等价覆盖「只释放自有 pool」；<br>· p2 §2.7 尾注 + §6.3（`mt7996_mac_wtbl_lmac_addr` **RX 路径** WARN）此前 `grep` 零命中 ⇒ **单开 F155** + `docs/DEVICE-VALIDATION.md` V2.9/V2.10。<br>**建议下一批（按价值）**：① `9919`（干净可应用、上游实审、eip93 正是我们启用的 crypto 路径）；② fw4 `9990` + rpcd `0001`（各 ~1 KB，真缺口）；③ iwinfo `101`（需与 9052 同树合测）；④ `9917`（需 4 hunk 重基，风险中等）。 **本批 CI（审计落地验证）**：stock `34765675150`（`ci-163`）/ experimental `34765676609`（`ci-164`）均 success；守护自 F151 修复后**连续 7 次绿**（4 次 push：`34756325287`/`34756369462`/`34765666002`/`34766404084`；3 次 2h 定时：`34767998846`/`34775781707`/`34786661136`），全部零漂移；上一次红是修复前的 `34755445119`。  |
| F153 | **吸收 Gilly `048`：EAPOL TX 路径 NULL sta 守卫**（2026-09-13，`mt76-0022`，**default**） | mac80211 允许「源地址为本机」的 EAPOL 帧发往未授权目的 ⇒ hostapd 可能把**已删除站点**的控制端口帧交给驱动（4 次握手重传与 disconnect 竞态）→ `mt7996_tx()` 传 NULL sta → `mt7996_tx_prepare_skb()` 的软件 MLD 地址翻译**无条件**解引用 `sta->mlo` → TX worker oops | 上游 RFC「guard station-only MLO TX handling」只守卫了一半：同函数的 data-QoS MLO link 选择**已**检查 sta（pin 源 `mt7996/mac.c:1057`），EAPOL 翻译块（1099）无守卫；本仓 pin `be5ce791` 尚无此修复 | 新增 `patches/packages/mt76-0022-wifi-mt76-mt7996-guard-null-sta-on-eapol-tx.patch`（作者 Slavin Liu；来源 Gilly `a57615652`），MANIFEST default 档 | open（上游 RFC 剩余半边；pin bump 时复查可否删） | **实证**：① 对 pin 真源 `be5ce791` `patch -p1 --fuzz=0 --dry-run` **rc=0**（仅 `mt7996/mac.c`）；② 逐行核对上下文：`sta` 可空（1043 `sta ? … : NULL`、1061 wcid 兜底 global_wcid）而 1099 无守卫，与补丁描述一致；③ `apply-patches --dry-run --oc --experimental` rc=0、审计 **128/128**、拷贝类 mt76 真源 **33 个文件**（原 32 + 本项）按 glob 序 `patch -f -p1` 全成功；④ **本批 CI 全绿**：sync-upstream push `34765666002` 与定时 `34767998846` 均 **success**（守护 `汇总：基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 0`——7 个远端来源全部 `✓ 未变`，F154 重锚后首次零漂移；dry-run `处理：128 / 缺失 0` + 拷贝校验 4）；**stock 构建 `34765675150` success（`ci-163`）**：`审计 57 一致 / 处理 57 / 实验档跳过 71 / 缺失 0`、`✓ copied …mt76-0022… → package/kernel/mt76/patches/`、构建期 `Applying …/patches/mt76-0022-…` + `patching file mt7996/mac.c`（`Hunk #1 succeeded at 984 (offset -113 lines)`）、`CC [M] mt7996/mac.o` + `LD [M] mt7996e.ko`（打包 kmod-mt7996e）、`: error:` 0、`Hunk FAILED\|does not apply` 0；**experimental 构建 `34765676609` success（`ci-164`）**：`审计 127 一致 / 处理 127 / 跳过 1 / 缺失 0`、同补丁 `offset -105 lines` 应用成功、`CC [M] airoha_soe.o` + `LD [M] airoha-eth.ko` + `LD [M] mt7996e.o`、`error:` 0、`Hunk FAILED` 0。（offset 为本仓 mt76 系列常态：0009 −64/−65、9991 +343、9990 带 fuzz 1，均由拷贝校验与构建双证。）定档 default 的判据：纯防御性 NULL 守卫——`sta != NULL` 时语义逐字节不变，`sta == NULL` 时只跳过本就不适用的软件 MLD 翻译；stock 亦走该 EAPOL 路径，故两档同效收益。实机判据见 `docs/DEVICE-VALIDATION.md` V2.10。  |
| F154 | **fanboy `ubi2-oc` 第三次 force-push 对账**（2026-09-13；handoff_p4 §1 口径复跑） | 守护报远端 tip 前进 `414d2098f→49d20d02e`（+30 提交），需判断「纯重基」还是「内容真变」 | `git ls-remote` + compare API：`status=diverged`、**ahead 30 / behind 22**、merge base `234c308b9`（09-12T21:09Z）；记录锚点 `414d2098f`（09-12T22:24Z）**已被 force-push 抹掉**（远端/本地均不可达），活 tip `49d20d02e`（09-13T08:35Z）——即 F137「第二次 force-push」之后的**第三次** | 台账；方法可复跑（见备注） | n/a | **方法**：`git clone --filter=blob:none --no-checkout` 取 `ubi2-oc`，对 21 个原料文件**文件名里记录的来源提交**逐个取 `commit/<sha>.patch` 算 `git patch-id --stable`，与新线 30 提交的 patch-id 集合比对（证据 `tmp/pidcmp/{old-*.patch,new30.ids}`）。**结果**：<br>· **14/21 逐字相同（纯重提交）**：01 vermagic→`63b3dc1c4`、02 eip93→`57cfb3993`、03 cpufreq-pmdomain→`f4968f56e`、04 dsa-kmod→`93b880353`、09 hw11/hw21→`addd1562a`、10 pstore→`aeb4302ec`、11 LRO→`ac460c353`、12 lorenzo→`88f85c328`、14 mt76-patches→`b3673d20b`、15 txpower→`8f7e76cee`、17 cmonroe→`2fc7f95c3`、18 smartrg→`8dca4dec0`、20 oc-gov→`49d20d02e`、21 hsuart→`f9476e36c` ⇒ **原料仍有效，无需重取**；<br>· **2/21 同 diffstat（内容同、仅基底变）**：16 txpower-ucode（2 文件 +159/-3 两侧一致）、19 apps-pack（73 文件 +12380 两侧一致）；19 另有新提交 `394d13362`（luci-app-flowsense 兼容 standalone offload，2 文件 +4/-8）——其效果（删 VLAN/PPPoE 徽章 + `config.change` 事件）**已在本仓 9017 内**（P4 §1.3 ④ 已并入）；<br>· **5/21 内容有差（待复核/重取候选）**：**05 bridge-hw-offload**——`51-bridge-hw-offload` `+13→+27`，新版**改为等 `br-lan` 端口集稳定（最多 120 s、连续 3 次一致）再 apply**，注释明写「无线 AP 口可能在 lan ifup 之后很久才入桥（hostapd 启动/DFS CAC）」⇒ **对我们 bridge/offload 场景（F46/F91/9024/9026 同域）有直接价值，建议优先重取**；06 nft-L2 内层 `675-02 +271→+265`、`675-03 +233→+230`（小幅）；13 mt76 Makefile `+4→+3`；08 reliability 新线**删掉** wifi-scripts antenna-memo 6 行（**已由本仓 `9014` 覆盖，无动作**）；07 mt76-rro 的对应提交在新线 30 提交中无 patch-id 命中，且两个 tip 的 `package/kernel/mt76/patches/` 都只有 `0001/0003/0005/0010/0011`（**不含**我们的 `0014-…release-HW_RRO-sessions-on-teardown`）⇒ 该补丁在 fanboy 新线已不存在，我们的 07 属**旧线遗物**，去留由我们自己的判据决定（与我们的 `mt76-0014-…npu-reset-panic` 同号共存已由拷贝校验 33 文件全过证明无碍）。  |
| F155 | **单开：`mt7996_mac_wtbl_lmac_addr` RX 路径 WARNING**（2026-09-13，p2 §2.7 尾注/§6.3 的落账） | `WARNING: CPU: 3 … mt7996_mac_wtbl_lmac_addr+0x60c/0x838 [mt7996e]`，来自 **RX 路径**（`mt7996_queue_rx_skb` ← `mt76_npu_rx_poll`）；同族 issue（naoki66 #17）里与 br-lan MAC 问题并列出现 | 本仓 `mt76-0003` 只移除了 **station statistics 路径**对 `mt7996_mac_wtbl_lmac_addr` 的直读，**RX 路径这条未修**；handoff 明写「属另一项，建议单开」，但此前 `docs/FIXES.md` 里 `grep wtbl_lmac_addr` **零命中** ⇒ 既未修也未登记 | 本行 + `docs/DEVICE-VALIDATION.md` **V2.9**（抓取判据）——**无代码载体**（首要是拿到实机 WARN 现场） | n/a（MTK 私有路径，需现场判读） | **为何不先改**：`WARN_ON` 的具体条件（MLO/MLD link 索引越界 vs 未授权 wcid）必须先看到现场才能判，盲改会把「诊断信号」换成「静默错行为」。**关联线索**：Gilly 新补丁 `048`/`mt76-0022`（F153）修的是**同域 TX 侧** NULL sta，说明该 RX/TX 域确有「站点已删但帧仍在途」的窗口。**下一步（实机）**：按 V2.9 抓 `WARNING:` 全行 + Call Trace + `iw … station dump`；命中后单开重基/修复项（并在 FIXES 补 F 号）。  |
| F156 | **吸收 YYH `9919`：EIP93 request ID 耗尽处理**（2026-09-13 起草 / 2026-09-14 本地校验收口；`9065`，**#EXP**） | eip93 驱动把 async 请求指针存 IDR、把 ID 编进硬件描述符，但旧码用**环深**当 IDR 上限且**不检查分配失败** ⇒ 请求压力下把负错误值当 user ID 写进描述符，result ID 找不到/错配（重负载加密/IPsec 下 descriptor 错配） | F131 曾把 `9919` 判为 CONFLICT，F152 重取真源（`research-20260912/yyh-refetch/`，7899 B）后在 kreal3 模型树复跑 `patch -p1 --fuzz=0` 得 **7/7 hunk 全过** ⇒ 结论纠正：**真残余·可干净应用**（同族 `9915/9916/9918` 已由 9062 覆盖，`9913/9914` 真源不存在）；上游侧有 `Fixes: 9739f5f93b78`、Reported-by Benjamin Larsson、Tested-by Aleksander Jan Bajkowski ⇒ 评审过的真修复 | 新增 `#EXP patches/root/9065-xr1710g-eip93-request-id-exhaustion.patch`（单包裹，内层 `9999-43-crypto-eip93-request-id-exhaustion.patch`：全 user ID 字段范围分配 + IDR 满则等待 + DMA 映射与 ID 都成功后才发布描述符 + 映射回滚 + 中断容忍陈旧/缺失 ID；5 文件 +69/-16） | open（未进 backports；pin bump 时复查可否删本层） | **顺序硬约束**：glob 序 `9999-43` 必在 9062 的内层 `9999-40..42` **之后** ⇒ MANIFEST 里 9065 必须排在 9062 之后；档位 `#EXP` 与 `vendor/02 + 9054 + 9060 + 9062` 同档（缺一不可）。**本地实证（2026-09-14 收口）**：① `audit-patches --oc --experimental` **129/129**、`--oc` **58/58**；② `apply-patches --dry-run --oc --experimental` rc=0 —— **处理 129 / 实验档跳过 0 / 缺失 0** + 拷贝类真实应用 **4/4**（regdb 5 / mt76 33 / mac80211 30 / uboot 41 文件）；③ 内层独立复跑（从包裹中抽出内层后对模型树 `research-20260912/inner/kreal3`（kernel 6.18.44 + 本仓内层全序）`patch -p1 --fuzz=0 --dry-run`）：**5 文件全过、rc=0**（仅 `eip93-main.c` Hunk #1 offset +1），与 F152 的 kreal3 结论一致。**实机判据（待）**：加密/IPsec 压测下 `dmesg` 无 `eip93` 相关告警、`grep eip93 /proc/crypto` 稳定、长时间 `openssl speed`/`iperf3` IPsec 无 descriptor 错配**CI 实证（experimental run `34788303073` → release `ci-165`，HEAD `9163cbd`）**：补丁层 `hunk 行数审计 128 一致 / 处理 128 / 实验档跳过 1 / 缺失 0`，`✓ applied patches/root/9065-…`；**内层在真实构建树上应用**：`Applying …/target/linux/airoha/patches-6.18/9999-43-crypto-eip93-request-id-exhaustion.patch using plaintext`；**eip93 全模块编译链接**：`CC [M] …/eip93/{eip93-main,eip93-common,eip93-cipher,eip93-aead,eip93-hash,eip93-fallback}.o` + `LD [M] …/crypto-hw-eip93.o`（本项改到 `eip93-common.c/hash.c/main.c/h`，全部编过）；同轮 `CC [M] airoha_soe.o`（SOE 未被本项影响）；全局 `: error:` **0**、`Hunk FAILED`/`patch does not apply`/`already exists` **0**；产物 `firmware-experimental.tar.gz`。本地侧：审计 `129/129`（`--oc` 58/58）、dry-run 处理 129/跳过 0/缺失 0 + 拷贝类 4/4、内层对 kreal3 模型树 `--fuzz=0` rc=0（5 文件）。 |
| F157 | **fanboy `ubi2-oc` 第四次 force-push 对账 + 重取 `05`（bridge-hw-offload hotplug 端口集稳定化）**（2026-09-14） | F154 把 21 个原料逐个 patch-id 对账后留了 5 项「内容有差」，其中 **05 bridge-hw-offload**：`51-bridge-hw-offload` `+13→+27`，新版把固定 `sleep 10` 换成「等 `br-lan` 端口集稳定再 apply」，注释明写无线 AP 口可能在 lan ifup 之后很久才入桥（hostapd 启动 / DFS CAC）。旧版在 10 s 后端口集未定型时就会 `apply-rules.sh`，把 bridge-family flowtable 生成在**残缺成员集**上（F46/F91/9024/9026 同域）。另：F156 推送时守护再报该远端 tip 前进（49d20d02e→ac975aa94），需要重新对账 + 重锚 | F154 只判到「有差」未重取；且该线在 F154 之后**再次 force-push**（`b397598dc`→`9604ab5a4`→`ac975aa94`，同一提交被反复重写、时间戳全部相同），说明「上次对账的 tip」从来不是稳定判据——**必须按“文件最后一次变更的提交”取原料**，不能按 tip 取 | ① `patches/vendor/fanboy/05-bridge-offload-22533-9604ab5a.patch`（文件名带来源 SHA；`9604ab5a4` 是该文件在 tip 的**最后一次变更提交**，含 `sleep 5` 版——中间态 `b397598dc` 的 `sleep 2` 版已作废）；**target.mk hunk 保留本仓 2026-09-12 的重基形态**（末尾追加 `DEFAULT_PACKAGES`，与 `02` 顺序无关；上游那半段已在本仓 `02` 的累加态里），文件头描述同步新版语义（原文 “10-second settle time” 已不成立），diffstat 总量按本仓形态重算为 140；② `patches/{MANIFEST,ORDER}` 路径改名 + 注释；③ `scripts/audit-upstream-watch.sh` fanboy 锚点 `49d20d02e`→`ac975aa94`，注释记对账方法 | upstream-open（fanboy 自持分支，无上游 PR） | **对账（22 个自有提交 vs F154 的 21 份原料，按 subject 定位 + `+` 行集合比对）**：**15 项逐字相同**（01/02/03/04/09/10/11/12/14/15/16/17/18/19/20/21 里的 15 项；`old-14`→`f4faedeb0` 亦逐字相同）；**3 项仅上下文/基底变**：`05`（=本项，已重取）、`06`（`675-02`/`675-03`，F154 已记小幅差异）、`08`（`mac80211.sh` 上下文 + `feeds.conf.default` 的 `+`/`-` 集合**与旧原料逐字相同**）；**2 项在新线已不存在**：`07`（fanboy 已删该 HW_RRO teardown 补丁——与本仓自持的 `mt76-0014`/`07` 无关，属旧线遗物，F154 已记）、`13`（`mt76 plus` 提交**换代**：旧 `08-06-26 mt76 plus 20260721 firmware`（`612255dde`，源 `88535934`）→ 新 `09-01-26 mt76 plus 20260721 firmware`（`f59bad1c5`，源 `01367e60`）⇒ **mt76 主源版本换代，属新的重取候选，本轮未动**——需连带复检本仓 mt76 补丁桶 33 文件与 mt76-firmware fork，风险中等，留待专项）。**本项实证**：① `audit-patches --oc --experimental` **129/129**、`--oc` **58/58**；② `apply-patches --dry-run --oc --experimental` rc=0 —— **处理 129 / 实验档跳过 0 / 缺失 0** + 拷贝类真实应用 **4/4**（regdb 5 / mt76 33 / mac80211 30 / uboot 41）；③ 实机应用（stock 档真 apply，非 dry-run）后树内 hotplug 落位为 `sleep 5` 稳定等待版（`grep -n 'br-lan/brif'` 命中、`waited` 上限 120）；④ 守护 `audit-upstream-watch.sh <绝对路径> --fail-on-drift` **exit 0**：`基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 0`，7 源里仅 fanboy 重新锚定（`✓ 未变：OpenW1700k.git refs/heads/ubi2-oc = ac975aa94`）。**实机判据（待）**：`flow_offloading_hw=1` 时 `nft list table bridge flow_offload` 的成员集**完整**（含 `br-lan` 全部端口，含晚入桥的无线 AP 口）；`logread` 无 `bridge-hw-offload` 报错；冷启动 + Wi-Fi 起来前后各查一次成员集。 |
| F158 | **吸收两项包级缺口：firewall4 `9990`（bonding lower devices）+ rpcd `0001`（exec observe EOF）**（2026-09-14；handoff_p7 §2.2 第 2–3 项） | ① **firewall4**：`resolve_lower_devices()` 只展开 `vlan`/`bridge`，**不处理 `bonding`** ⇒ 桥成员是 bond master 时 `bond0` 会留在硬件 offload 的 flowtable device 集里，per-port offload 驱动看不到真实物理口（与 SOE LAG/桥卸载 F143/9043 同域；本仓 `grep resolve_lower_devices` 此前**无载体**）。② **rpcd**：上游 `26dba5206e17`（exec: prevent double close of exec pipe descriptors）把 `c->opipe.fd.fd = -1` **挪到了 `ustream_poll()` 之前** ⇒ poll 看不到 EOF，exec 插件请求一直挂到超时（`ubus call` 执行脚本/命令超时回归；本仓 rpcd 此前**无 patch 载体**，9017/9019–9023 只往 rpcd 装脚本） | 两项都是 F152 重取真源后判定的**真缺口**（不是桩污染）：F131 当年只记「CONFLICT 99 项」，未做最终判决；F152 对 pin 真源 `--fuzz=0` rc=0 并确认本仓无等价载体 | 新增两条 **default** 包补丁（拷进 `package/<pkg>/patches/`，构建期 `patch -f -p1` 应用）：`patches/packages/firewall4-9990-resolve-bonding-lower-devices.patch`（作者 Jihong Min；devtype=`bonding` 或 `/sys/class/net/<dev>/bonding` 存在时沿 `lower_*` 递归展开）、`patches/packages/rpcd-0001-exec-observe-EOF-before-invalidating-pipe-fds.patch`（作者 Yanghan Ye；先 `ustream_poll()` 再置 fd=-1，保留双 close 保护）；`scripts/verify-copy-patches.sh` **登记两项 git 型包源映射**（`$(PROJECT_GIT)/project/<n>.git` 无 `PKG_VERSION` 可拼 tarball ⇒ 用 `PKG_SOURCE_VERSION` 组 codeload 归档 `github.com/openwrt/<n>/archive/<sha>.tar.gz`，与 mt76 同型） | upstream-open（两项均为作者自持补丁；内核/包上游未见同物） | **实证**：① 对 pin 真源 `--fuzz=0` 各自 rc=0（`tmp/pkgtest/{firewall4,rpcd}`，= `b6e5157`/`e37ed9d`）；**并且上游 openwrt main 当前 pin 就是同 SHA**（`tmp/owrt-main` 浅克隆读 Makefile 核对）⇒ 构建期不会因包版本漂移失败；② `audit-patches` 实验档 **131/131**（129+2）、`--oc` **60/60**（58+2）；③ `apply-patches --dry-run --oc --experimental` rc=0 —— **处理 131 / 实验档跳过 0 / 缺失 0**；④ **拷贝类校验升级为 6 目标全过**（regdb 5 / mt76 33 / mac80211 30 / **firewall4 1** / **rpcd 1** / uboot 41 文件）——新登记的两项是**真实下载源码 + 真实应用**通过，不是目录存在性检查；⑤ dry-run 对两个新目标报 `⚠ 目标目录不存在`：这是**预期**（上游树无 `patches/` 目录，真实 apply 走 `mkdir -p`），同一 `⚠` 此前对 mt76 也一直存在，不涉退出码。**实机判据（待）**：① 建 `bond0`（双口）并入 `br-lan` 且开硬件 offload 后，`nft list table bridge flow_offload` 的 device 集应为**物理口**而非 `bond0`，`cat /sys/kernel/debug/...`（或 `flowsense`）见真实物理口计数；② `ubus call file exec '{"command":"/bin/true"}'` 与 LuCI 命令类操作**立即返回**、无超时；③ 回归：无 bond 时 fw4 行为与改动前逐字节等价（`devtype` 匹配不到 bonding）。**CI 实证（stock run `34789005618` → release `ci-166`，HEAD `f1eae29` 含 F157+F158+F159）**：补丁层 `审计 60 一致 / 处理 60 / 实验档跳过 72 / 缺失 0`；三项补丁**真实拷入并应用**——`✓ copied …firewall4-9990-…`、`…rpcd-0001-…`、`…iwinfo-0101-…` 到各自 `patches/`，构建期 `Applying …firewall4-9990-resolve-bonding-lower-devices.patch` → `patching file root/usr/share/ucode/fw4.uc`、`Applying …rpcd-0001-exec-observe-EOF…patch`、`Applying …iwinfo-0101-…patch` → `patching file iwinfo_nl80211.c`；全局 `Hunk #…FAILED` / `patch does not apply` / `already exists` 三类计数 **0**；产物 `firmware-stock.tar.gz`。**iwinfo glob 序实证**：构建日志同目录依次 `100-multi_radio.patch` → `200-freqlist-per-radio.patch`（9052） → `iwinfo-0101-…`（本项），三者均成功。 |
| F159 | **吸收 YYH iwinfo `101`：txpowerlist 按 split wiphy dump + 当前频率匹配**（2026-09-14；handoff_p7 §2.2 第 4 项） | iwinfo `nl80211_get_txpwrlist()` 用非 split 的 `GET_WIPHY` dump，并**只按信道号**（`freq2channel`）匹配 `NL80211_ATTR_WIPHY_BANDS` 里的频率项 ⇒ 多 wiphy/多 band 设备上（本机三频、单 PCI 路径多 radio）信道号跨 band 撞号时，txpower 列表取到**错误 band 的上限**；且 `nl80211_request()` 单次 dump 在 split 模式下拿不全 band。另有 conveyor 所有权瑕疵（本地失败路径未 `nl80211_free`） | F152 已判「真残余·可吸收」，但未落载体；本仓 iwinfo 只有 `9052`（freqlist 按 radio 过滤，Gilly 700，2026-09-12）——**与 txpowerlist 不同函数/行区**（本项 hunk 2529-2650；9052 在 3192+），互不覆盖 | 新增 `patches/packages/iwinfo-0101-nl80211-fix-txpowerlist-wiphy-dump.patch`（作者 YYH2913；**default**）：split wiphy dump 请求 + 优先按当前频率精确匹配、无频率才回落信道匹配 + 取到即 `NL_SKIP` 提前返回 + 失败路径 `nl80211_free`；`scripts/verify-copy-patches.sh` 登记 iwinfo 包源映射（git 型，取 `PKG_SOURCE_VERSION` = 上游 pin `66bdd1a`） | upstream-open（作者自持，未见上游 PR） | **实证**：① 对 pin 真源 `--fuzz=0` rc=0（`tmp/pkgtest/iwinfo`，偏移 −28，三 hunk 全过）；② **与 9052 同树合测**：拷贝校验把 `200-freqlist-per-radio.patch`（9052 内层）+ 本项按 glob 序先后 `patch -f -p1` 应用，**3 个文件全过、零 fuzz/FAILED**（这是「不重叠」的机器判据，不是行号推断）；③ `audit-patches` 实验档 **132/132**、`--oc` **61/61**；④ `apply-patches --dry-run --oc --experimental` rc=0 —— **处理 132 / 实验档跳过 0 / 缺失 0**；⑤ 拷贝类校验升级为 **7 目标全过**（regdb 5 / mt76 33 / mac80211 30 / firewall4 1 / rpcd 1 / **iwinfo 3** / uboot 41 文件）。**实机判据（待）**：三频各自 `iwinfo <iface> txpowerlist` 的上限与该 band 的 `iw phy` 一致（5G/6G 不再互换）；`iwinfo <iface> txpower` 不超上限；LuCI 无线页 txpower 下拉与 `iw` 一致。**CI 实证（stock run `34789005618` → release `ci-166`，HEAD `f1eae29` 含 F157+F158+F159）**：补丁层 `审计 60 一致 / 处理 60 / 实验档跳过 72 / 缺失 0`；三项补丁**真实拷入并应用**——`✓ copied …firewall4-9990-…`、`…rpcd-0001-…`、`…iwinfo-0101-…` 到各自 `patches/`，构建期 `Applying …firewall4-9990-resolve-bonding-lower-devices.patch` → `patching file root/usr/share/ucode/fw4.uc`、`Applying …rpcd-0001-exec-observe-EOF…patch`、`Applying …iwinfo-0101-…patch` → `patching file iwinfo_nl80211.c`；全局 `Hunk #…FAILED` / `patch does not apply` / `already exists` 三类计数 **0**；产物 `firmware-stock.tar.gz`。**iwinfo glob 序实证**：构建日志同目录依次 `100-multi_radio.patch` → `200-freqlist-per-radio.patch`（9052） → `iwinfo-0101-…`（本项），三者均成功。 |
| F160 | **`9917`（eip93 request-local SA records）裁决：不吸收·重基配方落账**（2026-09-14；handoff_p7 §2.2 第 5 项） | F152 判 `9917` 为「4/7 hunk FAILED ⇒ 真冲突·需重基」，但没写清**冲突性质**：是「我们已等价覆盖」还是「代码真缺」。这决定它是待办还是假待办 | 把 9917 拆到文件级在两个模型树上复跑（`patch -p1 --fuzz=0 --dry-run`）：<br>· `eip93-aead.c`（5 hunk）**全部通过**（无 offset 提示）⇒ **我们的树已是 9917 的目标态**（request-local SA record 在 AEAD 路径已存在）；<br>· `eip93-cipher.c` 7 hunk 里 4 FAILED（#3 78、#4 139、#5 159、#7 186），#1/#2/#6 通过（offset +7/+7/+42）；<br>· `eip93-cipher.h` #3 通过（offset +1）；`eip93-common.c` 2 hunk 通过。<br>⇒ 冲突**不是**因为代码缺失（aead 已等价），而是 `cipher.c` 被本仓 `9054`（动态软回退：`eip93_skcipher_is_fallback()`、`crypt(req, bool encrypt)` 签名、fallback 分支）改写，**hunk 上下文对不上但语义位置都在** | **本轮不改代码**（裁决 + 配方落账）。**已由 F162（2026-09-14）取代**：按本配方重基并落地为 `9066`（#EXP），本地三证 + CI 实证见 F162 行。重基配方（下次直接照做）：① `eip93-cipher.h` 的 `eip93_cipher_reqctx` 加 `struct sa_record sa_record;`（现只有 `dma_addr_t sa_record_base`）；② `eip93-cipher.c` 的 `eip93_skcipher_crypt()` 把 `ctx->sa_record_base = dma_map_single(..., ctx->sa_record, ...)` 改为 `memcpy(&rctx->sa_record, ctx->sa_record, sizeof(rctx->sa_record))` + 在**请求本地**按 `encrypt` 置/清 `EIP93_SA_CMD_DIRECTION_IN` + map `&rctx->sa_record`，失败置 `rctx->sa_record_base = 0`；③ 删掉 `eip93_skcipher_decrypt()` 里对 `ctx->sa_record->sa_cmd0_word` 的**就地改写**（当前实现的并发泄漏源）；④ `cra_exit` 去掉 ctx 级 unmap，改为 `send_req` 失败路径与完成路径 unmap `rctx->sa_record_base`（aead.c 已是该形态，可照抄）；⑤ `rctx->sa_record_base = ctx->sa_record_base` 一行删除 | upstream（内核侧，`Fixes: 9739f5f93b78`，作者 Jihong Min，Benjamin Larsson 报告） | **判据/证据**：`tmp/fb05/9917-dryrun.log`（模型树 `research-20260912/inner/kreal3` 全量输出）；两侧 `eip93-aead.c` **逐字节相同**（`tmp/kernel-base` vs kreal3 的 diff 只列出 `cipher.c` 不同）——这是「aead 无需重基」的机器判据。**为何本轮不动**：`9917` 与本轮已吸收的 `9919`（9065）同属 eip93 请求生命周期，改动落在**并发正确的关键路径**上；现有 CI（F156/F158/F159）尚未跑完，不宜在同一路径上叠加未验证的重基（纪律：一次只推进一层可证伪的改动）。**顺带推进 F150 遗留（**已于 2026-09-14 结案，见 F150 行「已结案」段**）（§2.2 第 6 项，13 处未复核删除）**：对 SOE-25 内层做**删除集交叉核对**（本仓内层 vs 作者 `cur` 版；命名 `bridge_*`→`wdma_*` 归一后按行多重集比对）：本仓删除 83 行、作者删除 110 行，其中**本仓删了而作者补丁未删**的候选收敛到 **10 行**（证据 `tmp/fb05/soe25-delcheck.txt`，可复跑）——含 `bool local = skb->pkt_type == PACKET_HOST;`、`if (state == AIROHA_FOE_STATE_BIND)`、`index = airoha_ppe_foe_get_entry_hash(ppe, hwe);`、`u8 dsfield = 0, l4proto = 0;`、`rhashtable_lookup(&eth->flow_table, &f->cookie, …)`、`goto free_entry;` 等。**为何仍不判「等价」**：作者 25 号补丁在**本仓 pre-25 态上 18/38 hunk FAILED**（其前置态与本仓不同代），无法用「应用作者补丁 → 比终态」这条最硬的判据；删除集只能收敛候选、证明不了语义。**下一步（需作者精确前置态）**：取作者 73c3ab3 的 `airoha_ppe.c` **应用后**终态（或按作者补丁序列重放其全序），再与本仓 `kreal3` 终态逐 hunk diff——在此之前该 10 行按「未复核」挂账。**实机判据（若将来吸收）**：`cryptsetup benchmark`/`openssl speed -evp aes-128-cbc` 与 IPsec（`iperf3` over ESP）长压无 `eip93` 告警；并发加解密（同 tfm 多请求）结果与软件实现逐字节一致。 |
| F161 | **fanboy `mt76` 主源换代：落账 → 已实施**（2026-09-14；F157 对账副产品） | F157 按「文件最后一次变更提交」重取 `05` 时顺带发现：fanboy 新线的 `09-01-26 mt76 plus 20260721 firmware` 提交（`f59bad1c5`）把 `package/kernel/mt76/Makefile` 的**主源换代**——`PKG_SOURCE_VERSION` `8853593400…`（2026-08-06，上游 `openwrt/mt76`）→ `01367e60db43…`（2026-09-01，`OpenWRT-fanboy/mt76-firmware` fork），`PKG_SOURCE_URL` 同步指向该 fork（`PKG_MIRROR_HASH:=skip`）。旧提交（`612255dde`/`08-06-26`）在本仓 `vendor/fanboy/13` 内；新提交**未携带** | F154 只按 patch-id 判「13 号有差（mt76 Makefile +4→+3）」未展开，本轮展开后确认是**版本换代**而非上下文漂移——不是笔误，是内容真的不同 | **✅ 已实施（2026-09-14 P14，commit `8abf2f4`，已推 main；远端 = 本地 = `8abf2f4`）**：载体 = **新增 ROOT 补丁** `patches/root/9067-xr1710g-mt76-source-01367e60.patch`（**3 行**：`PKG_SOURCE_URL` → fork、`PKG_SOURCE_VERSION` → `01367e60…`、`PKG_MIRROR_HASH` → **真实 sha256 `983ba810…`**；`PKG_SOURCE_PROTO`/`PKG_SOURCE_DATE` 不变）+ `patches/MANIFEST` 一条 **default 档** + `patches/ORDER` 随 `gen-order.py` 重生成（评审注释登记在 `NEW_COMMENTS`）。**原「本轮不吸收」的两条理由均已消解**：① 三步只读证据证明这是**快进式固件提交**（`be5ce791` 是 `01367e60` 的直接祖先、恰 1 个提交、`22 files changed, 0 insertions(+), 0 deletions(-)`、22 文件全 `firmware/mt7996/*.bin`）、且 **32/32 包补丁零改动适用**（逐条同偏移/同模糊命中）⇒ **不存在**"33 个补丁整体重基"这件事；② 决策点① 已解 —— **填真实 sha256 替掉 fanboy 的 `skip`**，锁源铁律成立，并顺带把下载路径从 `rawgit` 切回 `github_archive`。**（原决定留痕，不删）本轮不吸收**（落账 + 判据）。理由：① 本仓 33 个 mt76 包补丁（`patches/packages/mt76-*`）全部对着 `be5ce791` 基线重基过并有两档构建实证，换代 = 33 个补丁整体重基 + `mt76-firmware` 固件版本变动，属**独立专项**，不应搭在 F156/F158/F159 这批上；② `01367e60` 在 fanboy 自己的 fork（`PKG_MIRROR_HASH:=skip`），可用性/可复现性需先评估（本仓纪律：拷来源必须自带 sha + 可下载自证） | upstream-open（fanboy 自持 fork） | **待办判据（吸收前必须做；**①②③ 已于 2026-09-14 落地为只读证据，见行尾**）**：① `git ls-remote`/archive 确认 `01367e60db43` 可下载且 `tar -tzf` 顶层目录 = `mt76-01367e60…`（与 `be5ce791` 同型）〔**F163 实测更正（2026-09-14）**：顶层实为 **`mt76-firmware-01367e60…`**——codeload 归档顶层 = **源 URL 的 repo 名**-<全 sha>，而 fanboy fork 的仓库名是 `mt76-firmware`；此条「与 `be5ce791` 同型」的预测**不成立**，按仓库名取才是对的〕；② 对 `01367e60` 源码逐补丁 `patch -f -p1 --fuzz=0` 复跑 33 文件（本仓已有机制：`verify-copy-patches.sh` 的 mt76 映射 + `apply-patches.sh --dry-run`）；③ 差异面先出清：`be5ce791..01367e60` 的 `mt7996/mac.c`/`dma.c`/`mt76.h` 等改动清单；④ 只有 ①②③ 全绿才换代并派发两档构建。**风险**：MT7996/NPU 是本机 Wi-Fi 主路径（`mt76-0003/0013/0014/0022` 等都在此），换代需与 `docs/DEVICE-VALIDATION.md` V2.x 一起回归。 **三步只读证据（2026-09-14 落地；只读、不改仓库、不派 CI；报告 `research-20260912/inner/F161-EVIDENCE.md`，证据 `tmp/f161/`）**：**① 可下载自证 ✅**——`git ls-remote` 命中 `refs/heads/260901`（非悬空 sha），且 **`be5ce791` 是 `01367e60` 的直接祖先**、`rev-list --count` **恰 1 个提交**（`01367e6 new mt7996 family firmware`），**零源码改动** ⇒ 换代是**快进式固件提交**，不是分叉重基；**② 补丁复跑 ✅**——按构建语义（`patch -f -p1`，与 `scripts/patch-kernel.sh` 同口径）对 `01367e60` 源码**32/32 成功、0 FAILED、0 `.rej`**，且与 `be5ce791` 对照**逐条同偏移/同模糊命中**（各 40 条 offset/fuzz 记录；非 hunk 输出 `diff` **无差异**；两个本仓常态 fuzz 补丁 `mt76-0015`/`mt76-9990` 在两棵树上同为 `fuzz 1`，**非本轮引入**）⇒ **换代不改任何一个补丁的应用结果**；**③ 差异面 ✅**——`be5ce791..01367e60` **22 文件 / 0 行源码增删 / 共 +9 069 388 B**，**全部是 `firmware/mt7996/*.bin`**：**生产固件只 4 个 `_wm` 合计 +27 656 B**（另 `mt7990_wm.bin` −92 916 B、9 个「同大小内容变」），**≈8.8 MB 是驱动不加载的测试模式 `*_wm_tm*.bin`**（机器判据：`mt7996/mt7996.h:47-60` 的 `MT7996/MT7992/MT7990_FIRMWARE_*` 常量**全部指向非 `_tm` 文件**，全树 `grep '_tm\.bin'` **零引用**）。**顺带发现守卫盲区**：`scripts/verify-copy-patches.sh` 的 mt76 分支把源码 URL **硬编码为 `github.com/openwrt/mt76`** ⇒ 换到 fork-only 的 `01367e60` 后该 URL 404，而脚本设计为「下载失败 = ⚠ 未校验、不红」⇒ **32 个 mt76 补丁会长期静默失去校验**；修法：mt76 分支改为从包 Makefile 读 `PKG_SOURCE_URL`（同 F158 给 firewall4/rpcd/iwinfo 登记 codeload 映射）。**换代前剩两个决策点（非补丁工程）**：④ **锁源铁律**——候选同时命中本仓 F13 曾据此**否决** fanboy 13 号的两条（fork URL + `PKG_MIRROR_HASH:=skip`）；缓解：sha 为完整 40 位 + `PROTO:=git` 内容寻址（不可变），可选把 `git archive 01367e60` 的 sha256 **算回 `PKG_MIRROR_HASH`** 替掉 `skip`；⑤ **实机固件回归**（CI 绿不能代替）——固件二进制变了，须走 `docs/DEVICE-VALIDATION.md` V2.x：dmesg 固件版本行、三频吞吐基线、NPU 卸载长稳、冷启动 ×N 无固件超时。**⑥ 决策点①（锁源铁律）已可解（2026-09-14，只读；报告 `research-20260912/inner/F161-EVIDENCE.md` §7）**：§4.1 的缓解前提「算 `git archive 01367e60` 的 tar hash 填回 `PKG_MIRROR_HASH`」**不成立**——GitHub 型源走 `DownloadMethod/github_archive`（`dl_method_git` 按 URL 分派），其产物是**重打包**的 `<PKG_NAME>-<PKG_SOURCE_DATE 点化>~<sha 前 8 位>.tar.zst`（`tar --numeric-owner --owner=0 --group=0 --sort=name --mode=a-s --mtime=@<commit_ts> -I 'zstd -T0 --ultra -20'`，`SUBDIR` 由 `include/download.mk:13-16` 定），既非 codeload 归档、也非 `git archive` 单独产物。**方法先对已知答案复现（falsifiable）**：复刻现行 pin（`openwrt/mt76@be5ce791`、`PKG_SOURCE_DATE=2026-09-01`）得 `d1d0f7588c5b9ceafcac341ce19dd206ed9ec106847e672ab77e48bacb57f81a`，与 Makefile 现行值**逐字符相同**。**候选实测值**：`PKG_MIRROR_HASH := 983ba810fd6947c9c05bc3ce79ff8e383255115cd0273837d9f4a93f3cbd04da`（`SUBDIR=mt76-2026.09.01~01367e60`、`commit_ts=1788275278`、产物 26 878 455 B）；**用 OpenWrt 官方 `scripts/dl_github_archive.py` 端到端复核 exit 0**（其内部 `_hash_check` 通过），**负对照**（换用现行 pin 的 hash）`exit 1` + `Wrong hash (probably caused by .gitattributes), expecting d1d0f758…, got 983ba810…` + **不落盘** ⇒ 校验非空过，且官方工具**独立复算**出同一个值（两条互不依赖的路径同值）。**顺带查明**：`skip` 不只是「不校验」——`_init_hasher()` 在 `__init__` 即按长度硬校验，`skip` 令 `github_archive` **构造即失败**、构建静默回落 `rawgit`（整仓 clone、无哈希校验）⇒ 填真实 sha256 同时把下载路径切到「GitHub 归档 + 提交前后 sha256 校验」。**注意**：该 hash 经 `SUBDIR` 依赖 `PKG_SOURCE_DATE` 与 `PKG_NAME`，换代若改日期必须重算。复跑：`tmp/f161hash/repro.sh`（先用现行 pin 自检已知答案）。**⑦ 决策点②（实机固件回归）已定稿（2026-09-14；报告 §8 + 清单 `docs/DEVICE-VALIDATION.md` §7）**：判据分两层——**第一层字节**（设备上 `/lib/firmware/mediatek/mt7996/*.bin` 的 sha256 与指纹表 `docs/device-validation-f161-firmware-sha256.txt` 的 **new 列逐字符相同**、`*_tm*.bin` **不存在**）、**第二层行为**（版本行→三频→吞吐→NPU 长稳→冷启动→回退）；**最硬的行为判据是固件内嵌 `build_date` 的字面匹配**（blob 尾部 `struct mt76_connac2_fw_trailer`，`mt76_connac_mcu.h:176-188`；驱动打印点 `mt7996/mcu.c:3708`）：`mt7996_wm.bin` `20260311120504`→**`20260721003035`**、`mt7996_wm_233.bin` `20260311120702`→**`20260721003609`**（新固件日期 `20260721` 与 fanboy 提交名 `…plus 20260721 firmware` 对得上）⇒ 设备上以 `dmesg` 过滤 `Firmware Version` 一行同时判定「换没换」与「本机走 444/233 哪个变体」。**机器判据修正 §3.1 措辞**：驱动会加载的 **7 个 MT7996 生产固件全部内容变**（不只是 4 个 `_wm`——`dsp`/`rom_patch`×2/`wa`×2 属「同大小或 +64 B、内容变」，被「只 +27 656 B」的总量表述掩盖）；5 个新增 `*_tm*.bin`（9 134 512 B）**驱动源码与包 Makefile 双零引用**。**执行顺序（基线必须在刷机前采）**：① 刷机前采 old 基线（指纹/版本行/三频吞吐）→ ② 换代实施（另起一轮 + 两档 CI）→ ③ 刷机后跑 V7.0–V7.6 → ④ 回填本行。**DoD**：指纹 + 版本行 + 三频吞吐 + NPU 长稳 + 冷启动 ×N + **回退证据**六项齐全才可改判「已换代」；不允许无实机记录写「已验证」。**⑧ 换代实施落地（2026-09-14 P14；commit `8abf2f4`，已推 main；远端 = 本地 = `8abf2f4`）**：① **本地三证**——静态审计 `--oc --experimental` **134** 一致 + 60 包裹 ROOT、`--oc` **62** 一致 + 20 ROOT、无档 **61**（MANIFEST **136→137** = default 61 / oc 1 / experimental 72 / disabled 3）、`gen-order --check` 与 `audit-order.sh` 双 rc=0；CI 同款 dry-run（`tmp/drift/run.sh --dry-run --oc --experimental`）**rc=0**：处理 134 / 实验档跳过 0 / 缺失 0、拷贝类「真实应用 7 / 未校验 0」，且 **mt76 校验命中新源**（`src-1/mt76-firmware-01367e60db43…` + 「全部补丁真实应用成功（patch -f -p1，glob 顺序，**39 个文件**）」）⇒ **F163 的派生 URL 在换代后确实生效**（旧硬编码脚本此处会静默跳过 ⇒ 假绿）；载体补丁**非 dry-run** 真实应用后三字段如预期、随后 `git checkout --` 还原逐字节一致。② **CI 免费门禁（push 触发）**：`sync-upstream` run `34829153005` **success**，独立复现「134 一致 / 60 包裹 ROOT / MANIFEST 137 条」+ mt76 新源校验（同上 39 文件）；同轮上游守护自报 `上游 HEAD: b4674004e`（**p13 记的 `e8d1857` 已前进**）、**基线漂移 0 / 路径缺失 0 / 同址冲突 0**、锚点仍 `12fa34811`。③ **两档构建全绿**：run `34829174800`（**ci-170**，stock）+ run `34834500124`（**ci-171**，experimental），均 **success**、均 @ `8abf2f4`。④ **构建态实证（真源日志，两档各 1 条）**：下载配方 = `dl_github_archive.py --url="https://github.com/OpenWRT-fanboy/mt76-firmware" --version="01367e60db43…" --subdir="mt76-2026.09.01~01367e60" --hash="983ba810…"` ⇒ **确认走 `github_archive`（非 `rawgit`）**、`SUBDIR` 与 §7.3 预测**逐字符相同**、真实 hash 参与校验（构建成功 ⇒ 校验通过）。⑤ **F165 回归**：同一新源日志上 `tmp/f165/extract.sh` 仍 **7/7 内层 `Applying` + 死文件 0 + wrapper ROOT 1** ⇒ 换代未回退 F165。⑥ **新坑（本轮实证，纠正 p13 §0.2 坑 21）**：`ci-N` tag 的 sha **不是**该 run 的 commit，而是**发布时刻默认分支 HEAD**——`build.yml` 用 `softprops/action-gh-release@v2` + `tag_name: ci-N` 且**未给 `target_commitish`** ⇒ GitHub 按当时默认分支 HEAD 建 tag。本轮三例 `ci-169`/`ci-170`/`ci-171` 的 tag **全 = `8abf2f4`**（而 run 169 的 headSha = `983a58f`）；旧例 **`ci-166`** tag `8d337d4e` ≠ run `34789005618` headSha `f1eae297`（ci-167/ci-168 只是恰好 main 未前进才相等）⇒ **判 run↔commit 归属一律用 `gh run view <id> --json headSha`**，tag sha 仅在「发布前 main 未前进」时等于 run commit。**仍待**：实机 V7.0–V7.6（用户侧硬件与档期）——CI 绿不能代替（固件二进制变了）。 |
| F162 | **EIP93 请求本地 SA record**（YYH/backport-6.18 **9917**，作者 Jihong Min/hurryman2212，2026-05-25；2026-09-14 重基吸收，`9066`，**#EXP**） | cipher/AEAD 请求把**可变的** direction/copy 标志直接写在 **tfm 级** SA record 上——`eip93_skcipher_decrypt()` 里 `ctx->sa_record->sa_cmd0_word \|= EIP93_SA_CMD_DIRECTION_IN`，`eip93_aead_decrypt()` 同型。`ctx->sa_record` 是 **tfm 级**（`cra_init` 分配一次），而 `ctx->sa_record_base = dma_map_single(..., ctx->sa_record, ...)` 也是 tfm 级映射 ⇒ 同一 tfm 上的**并发请求**里，decrypt 请求把 direction/copy-pad 标志留在共享 SA record 上，正在跑的 encrypt 请求会读到被改写的方向位（AEAD 侧连 `COPY_PAD`/`COPY_DIGEST` 一起被清）⇒ 并发加解密结果错误/解密失败。**这正是 F160 记的「`eip93_skcipher_decrypt()` 里对 `ctx->sa_record` 的就地改写 = 并发泄漏源」** |  | ① 把准备好的 SA record **复制进请求上下文**（`rctx->sa_record __aligned(CRYPTO_DMA_ALIGN)`），请求特定标志（direction）只改这份副本；② 按**请求**做 `dma_map_single(&rctx->sa_record)`，映射失败时 `rctx->sa_record_base = 0`；③ 在 `eip93_send_req()` 的 `free_sa_state` 失败路径与 `eip93_handle_result()` 的完成路径 unmap（`if (rctx->sa_record_base)` 守卫）；④ `send_req()` 的 `check_valid_request` 失败分支补 unmap + 置 0；⑤ `cra_exit` 去掉 ctx 级 unmap；⑥ 删除 `struct eip93_crypto_ctx` 的 `sa_record_base` 字段（上下文里不再需要）；⑦ `cipher.h` 补 `#include "eip93-regs.h"`（`struct sa_record` 定义处） | open（内核侧 `Fixes: 9739f5f93b78`，未进 backports；pin bump 时复查可否删本层） | **原补丁只对 vanilla 6.18.44 成立**：对模型树 kreal3 复跑 7 hunk 里 **4 FAILED**（`eip93-cipher.c` #3/#4/#5/#7）。**冲突面只是上下文、不是缺码**（F160 裁决）：本仓基线（kernel-base = vanilla + openwrt 内核补丁集）自带 EIP93 **动态软回退**（`eip93_skcipher_is_fallback()`、`crypt(req, bool encrypt)`、fallback 早返回分支），fallback 分支插在 `ctx->sa_record_base = dma_map_single(...)` 之前使 #4/#5 错位、`decrypt()` 里多出的 `ctx` 变量使 #7 错位；`aead.c` 5 hunk / `cipher.h` 3 hunk / `common.c` 2 hunk 原样通过。**重基做法（机器可复核）**：逐行移植到 kreal3 终态，**逐行保留**软回退分支（fallback 命中仍直接走软件实现、**不触碰任何 SA record**、也不设 `rctx->sa_record_base`），其余照 9917 原样；`aead.c` 侧 9917 目标态本已存在 ⇒ 本项只改 **cipher 路径 + 公共 unmap 路径**。**本地三证（2026-09-14）**：① `audit-patches --oc --experimental` **133/133**（132→133）、`--oc` **61/61**（stock+OC 不含 #EXP）；② `apply-patches --dry-run --oc --experimental` rc=0 —— **处理 133 / 实验档跳过 0 / 缺失 0**，`✓ [dry-run] patches/root/9066-…` 在列，拷贝类 **7 目标真实下载+应用全过**（regdb 5 / mt76 33 / mac80211 30 / firewall4 1 / rpcd 1 / iwinfo 3 / uboot 41）；③ 内层 `9999-44-…` 对模型树 `research-20260912/inner/kreal3` `patch -p1 --fuzz=0` **16/16 hunk 干净应用、零 offset、零 fuzz**，应用后四文件与逐行移植终态 **`cmp` 逐字节相同**（证据 `tmp/9917/`：`pre/`+`post/`+`ported2.diff`+`9999-44-…patch`）。**顺序/档位**：glob 序 `9999-44 > 9999-43`（在 9065 之后），与 9999-40..43 改的文件不重叠；**#EXP 与 vendor/02 + 9054 + 9060 + 9062 + 9065 同档**（缺一不可）。**ABI/结构影响**：`struct eip93_cipher_reqctx` 增 `struct sa_record sa_record`（108 B，`__packed` 结构 + `CRYPTO_DMA_ALIGN` 对齐）⇒ `skcipher/aead_reqsize` 相应变化，ask size 取自 `sizeof(struct eip93_cipher_reqctx)` 无需手改。**实机判据（待）**：① 并发加解密（同 tfm 多请求，`cryptsetup benchmark`/`openssl speed -evp aes-128-cbc` 与 `iperf3` over ESP 同时跑）结果与软件实现逐字节一致；② 长压无 `eip93` dmesg 告警、无 `DMA-API` unmap 警告（映射/解映射配对）；③ `grep eip93 /proc/crypto` 稳定。**CI 实证（experimental run `34793041395`，HEAD `fd769c9`，2026-09-14T00:32:59Z 派发，build 57m7s 绿 → release `ci-167`，2026-09-14T01:30:26Z）**：补丁层 `✓ hunk 行数审计：132 个补丁全部一致` / `处理：132  实验档跳过：1  缺失文件：0`——**132 = 60 default + 72 `#EXP`**（experimental 档**不含 `#OC`**，那个 `1` 就是 `#OC` 行；`apply-patches.sh` 的 `skipped` 计数器把 `#OC` 跳过也并入「实验档跳过」标签，属既有措辞、非异常；本地 `--oc --experimental` 口径才是 **133/0**）；`✓ applied  patches/root/9066-xr1710g-eip93-request-local-sa-records.patch` 在列；**内层 `9999-44-…` 构建期真实应用**——`patching file` `eip93-aead.c` / `eip93-cipher.c` / `eip93-cipher.h` / `eip93-common.c` **四文件全过**，hunk 全 succeeded（`eip93-common.c` #1/#2 offset 30 lines），**全局 `Hunk #…FAILED` / `patch does not apply` / `already exists` 三类计数均为 0**；产物 `firmware-experimental.tar.gz`（artifact `firmware-experimental` + `buildlog-experimental`）。**CI 绿只证「补丁应用 + 编译」通**：上列「实机判据（待）」三项（同 tfm 并发加解密逐字节一致 / 长压无 `eip93`+`DMA-API` unmap 告警 / `/proc/crypto` 稳定）**仍未回填**，不得以 CI 绿代替。 |
| F163 | **verify-copy-patches 守卫盲区：mt76 源码 URL 硬编码 ⇒ 换代后 32 个补丁静默失去校验**（2026-09-14；F161 §5 顺带发现，handoff_p10 §3 第 1 步） | `scripts/verify-copy-patches.sh` 的 `*/mt76/patches)` 分支把源码归档 URL **硬编码为上游** `https://github.com/openwrt/mt76/archive/$sv.tar.gz`。F161 已证 mt76 换代候选是 fork-only 提交（`01367e60` @ `OpenWRT-fanboy/mt76-firmware`）⇒ 换代后硬编码 URL **必 404**，而脚本策略是「下载失败 = ⚠ 未校验（警告不红）」⇒ **32 个 mt76 包补丁从此长期不被校验，而 sync-upstream 2h cron 仍全绿**（假绿）。F20/F21/F22/F25 同型盲区第五次，且这次是**守卫自身**随源换代而静默失效 | ① **源码 URL 未从被检对象自身派生**——硬编码上游 = 第二真相源，与「单一事实源 = 包 Makefile」的设计前提自相矛盾；② **「下载失败」把两类故障混为一谈**：瞬时网络故障（F19，不应假红）与**源不存在/写错**（确定性配置错误，必须红）——后者被前者的话术吞掉 | `scripts/verify-copy-patches.sh`（**只改守卫，不碰任何补丁内容**）：① mt76 分支改为从包 Makefile 读 `PKG_SOURCE_URL` + `PKG_SOURCE_VERSION` 组 codeload 归档；**归档顶层目录取源 URL 的 repo 名**（不是 PKG_NAME/包目录名——fanboy fork 的仓库名是 `mt76-firmware`，归档顶层实测 = `mt76-firmware-<全 sha>`）；源 URL 不可推导（非 `https://github.com/<owner>/<repo>`）⇒ **直接报红**（无法校验 ≠ 可以静默跳过）；② 共享下载路径用 `curl -w '%{http_code}'` 取状态码：**404/410 = 红**（源不存在 = 配置错误），其余下载失败仍按 F19 保持 ⚠ 不红；页眉「策略」段补两条规则 | n/a（自持工程化守卫） | **A/B 实证（同一棵 drift 树、同一 bogus 条件）**：旧脚本（HEAD `7e35f1e`）→ `⤓ 下载 https://github.com/openwrt/mt76/archive/0000…`（**根本没读 Makefile**）+ `⚠ 下载失败…跳过（未校验）` + `未校验/跳过 1` + **exit 0（绿）**；新脚本 → `✗ 源不存在（HTTP 404）` + `拷贝类补丁校验失败` + **exit 1（红）**。**三测**：① **回归**——现行 pin（`PKG_SOURCE_URL=https://github.com/openwrt/mt76` + `be5ce791…`）派生结果与旧硬编码**逐字符相同**（归档名仍 `mt76-be5ce791…tar.gz` ⇒ 缓存命中、零重下），`apply-patches.sh --dry-run --oc --experimental` rc=0、**处理 133 / 实验档跳过 0 / 缺失 0**、拷贝类 **7 目标真实应用全过且逐项计数与 F158/F159 记录一致**（regdb 5 / mt76 33 / mac80211 30 / firewall4 1 / rpcd 1 / iwinfo 3 / uboot 41）；② **F161 fork 场景**——把 drift 树 Makefile 换成候选（fork URL + `01367e60`）后，脚本自动派生 `https://github.com/OpenWRT-fanboy/mt76-firmware/archive/01367e60….tar.gz`，**真实下载（31 024 047 B）+ 解包顶层 `mt76-firmware-01367e60…` + 32 个补丁按构建语义 `patch-kernel.sh` 全部应用、0 FAILED**（= 用**构建口径**独立复证 F161 判据②）；③ **负例**——不可推导源型（`$(PROJECT_GIT)/project/mt76.git`）⇒ `✗ 无法校验必须报红` + exit 1。**未改**：`*/firewall4` / `*/rpcd` / `*/iwinfo` 三个分支（其源为上游 `$(PROJECT_GIT)/project/<n>.git`，非 GitHub 型，硬编码映射是 F158 登记的有意映射；未来迁 fork 同样由 404→红 兜住）。**证据**：`tmp/oldver/`（旧脚本 A/B 基线）、`tmp/mt76-Makefile.bak`（drift 树 Makefile 还原基线，还原后 `git diff` 0 行）、`tmp/copy-patch-verify/mt76-firmware-01367e60….tar.gz`（fork 归档缓存）。 |
| F164 | **ORDER↔MANIFEST 档位视图失同步 + `audit-config.sh` 取值盲区**（2026-09-14） | ① `patches/ORDER` 长期被当"评审视图"却**无任何机器校验**：实测 **96 条 vs MANIFEST 136 条**，MANIFEST-only **49 条**（整条 SOE `9999-net-airoha-soe-10..28` 共 19、`9044/9045/9046/9050~9066`、mt76 `0013~0022`、`firewall4-9990`/`rpcd-0001`/`iwinfo-0101`、`9047/9048/9049/9052/9057/9058/9059`、`9064`），且 `#` 前缀**同时**表示"说明"与"未启用条目"（`# experimental root/9037-...`）——单看前缀无法还原档位。② `scripts/audit-config.sh` 的判据是 `grep -q "^${sym}="`：**只查存在性、不查取值** ⇒ seed 写 `=y` 而被 kconfig 解析成 `=m`（只编成模块、**不装进镜像**）时照样打 ✓ —— 能力静默丢失。活例：seed 的 `CONFIG_PACKAGE_wpad-openssl=y` 实为 `=m`，镜像里装的是同组 `DEFAULT_VARIANT` 的 `wpad-basic-mbedtls`。 | ① ORDER 没有守卫机制（F20/F21/F25 守的都是 MANIFEST/补丁本体，没人守"视图"）；② seed 审计把"符号进了 .config"当成"配置生效"，而 OpenWrt 里 `=y` 与 `=m` 语义完全不同（`=m` = 编译但不进 rootfs）；③ `make defconfig` 会把显式 `=y` 按 kconfig 规则压低——`depends on` 是上限，wpad 各变体之间更有 `CONFLICTS` 生成的 `depends on m \|\| (PACKAGE_wpad-X != y)`，而旧审计完全看不见这一层。（实测：`wpad-openssl` 依赖的 `libopenssl`/`libopenssl-legacy` 均非瓶颈，真因是 CONFLICTS 上限。） | ① `patches/ORDER` 按新口径**重写**为 MANIFEST 的**纯投影**：档位词汇 `default\|oc\|experimental\|disabled\|pending` 与 MANIFEST 前缀一一对应，`#` 行只作说明（废止双重语义），49 条缺失全部补齐（行尾评审注释按路径逐字继承）。② 新增 `scripts/audit-order.sh`（集合 + 档位对账）并**挂进 `scripts/apply-patches.sh`**（dry-run 与真实 apply 都先跑 ⇒ 改 MANIFEST 不同步 ORDER 即红）。③ 新增 `scripts/gen-order.py`（按不变式重生成；`--check` 只校验不改写）。④ `scripts/audit-config.sh` 改为**逐符号比对取值**：`want=y & got=m` ⇒ ✗（唯一豁免 = 符号紧邻上一行注释里的 `F164-ALLOW-RESOLVED-M: <理由>`，命中时打 ⚠ 并计数，**绝不静默**）；`want=m & got∈{m,y}` ⇒ ✓；`want=n & got≠n` ⇒ ✗；字符串/整数逐字符比对；seed 行尾注释截断后比对并打 ⚠。⑤ `config/seed-config.diff` 的 **9 处尾随注释**移到独立注释行（脆弱写法，见背景②；已证活动符号取值集合逐字节不变），头部写入写法铁律；wpad 行上方写入已登记豁免 + 修法。⑥ `patches/README.md` 更正桶表（`patches/kernel/`、`patches/uboot/` **不存在**）与 ORDER 口径。 | n/a（本仓工程化/配置） | **实证（全部本地可复跑）**：① `scripts/audit-order.sh .` **rc=0**——`MANIFEST 136 条（default 60 / oc 1 / experimental 72 / disabled 3）；ORDER pending 9 条`；**负向**把 `root/9000` 改标 `pending` ⇒ **rc=1** 且报 2 条（档位不一致 + pending 存在于 MANIFEST），还原后 rc=0。② `scripts/gen-order.py . --check` **rc=0 且幂等**（重生成前后 md5 一致 = 已是规范投影）。③ `scripts/audit-config.sh` fixture **6 例**行为符合预期（正向 rc=0；缺失 / `m→n` / 字符串不符 ⇒ rc=1；尾随注释 ⇒ ⚠ 且不误判；`y→m` 无标记 ⇒ **rc=1**（=旧版盲区现被抓住）；`y→m` 带标记 ⇒ rc=0 + ⚠ + 计数 1）。④ **真实 main 档 `.config` 端到端实证**（`tmp/test-scratch/verify-audit-config.sh`：scratch 上游树 `run.sh --experimental` 叠加 → `feeds update/install` → 追加两段 seed → `make defconfig`；**不构建**）：`✓ XR1710G 目标已选中`；共享 seed **rc=0**（43 个活动符号取值全部命中，**已登记降级 1**、脆弱写法警告 **0**）、实验档 seed **rc=0**（2/2）；实测 `wpad-openssl=m` + `wpad-basic-mbedtls=y` + `libopenssl-legacy=m`，与 CONFLICTS 上限判读一致；`bridge-hw-offload=y`（跨树假阳性在真实树上排除）、`kmod-bonding=y`、`ip-full=y` 全部命中。（跨分支参考跑 `tmp/owrt-master-tailscale/.config` 时 wpad 同样转 ⚠ 已登记。）⑤ seed 规范化等价性：`(符号=取值)` 集合 **43 条 diff 为空**。⑥ **新增硬门不破坏流水线**：`tmp/drift/run.sh` 四档实跑全部 **rc=0** 且每档都执行了 `audit-order.sh`——stock `处理 60 / 跳过 73 / 缺失 0`、`--oc` `61/72/0`、`--experimental` `132/1/0`、`--dry-run --oc --experimental` `133/0/0`（与 F162 口径一致）。⑦ CI 实证（待）：**两档 dispatch**，`apply-patches.sh` 第 1 步即跑 `audit-order.sh`（新增硬门）。**口径澄清（2026-09-14 F165 修正本行原文「实测为 32 / 2」）**：F159/F162/F163 记的"拷贝类 mt76 **33** / iwinfo **3** / uboot **41**"是**构建态**计数，**正确**；**32 / 2 / 40** 只是「干净树 + 单独跑 verify」（ROOT 补丁未应用）时的量，不构成更正。`verify-copy-patches.sh` 按设计数的是**应用集合** = MANIFEST 拷贝条目 + 树内既有补丁 + ROOT 补丁**派生**的包补丁（见该脚本 `:250-262` 树内既有、`:86-94` 派生目标），实测分解：mt76 32+1（`vendor/fanboy/07` 派生 `0014-…release-HW_RRO…`）=**33**；iwinfo 1+1（上游自带 `100-multi_radio.patch`）+1（`root/9052` 派生 `200-freqlist-per-radio.patch`）=**3**；uboot 40+1（`root/9002` 派生）=**41**。分解已由 CI `ci-167` 日志逐条复核（`Applying …/0014-…release-HW_RRO…`、`Applying …/200-freqlist-per-radio.patch` 均在列）。**故本行原结论「33 / 3 实测为 32 / 2」作废**——`ls patches/packages/mt76-*.patch` 数的是**本仓自带文件数**，与 verify 的**应用集合**不是同一个量（两者都真，但不能互相"更正"）。`audit-patches.sh --oc --experimental` 的 **133** 与 `audit-order.sh` 的 **136/9** 互不冲突（前者数活动补丁条目、后者含 #DISABLED）。**残留风险**：① `F164-ALLOW-RESOLVED-M` 豁免当前**仅 1 处**，移除它需要产品决策（改 `wpad-mesh-openssl`/`wpad-mesh-mbedtls` + 显式置 `CONFIG_PACKAGE_wpad-basic-mbedtls=n` 解 CONFLICTS）+ 构建 + 实机 V5.x 回归；当前镜像**丢 802.11s mesh**（SAE 未丢）。② `audit-order.sh` 的桶前缀映射 `BUCKETS` 需与未来新增桶同步（新桶须同步登记，否则报 MANIFEST-only）。③ 本次未跑构建，`=m` 的"不装进镜像"结论由**产物 manifest 实证**支撑（ci-154/155/159/161/162 五份 manifest 的 wpad 只有 `wpad-basic-mbedtls`）。|
| F165 | **包裹补丁载体错位 ⇒ `mt76-0014` 的 7 个内层补丁从未生效（静默）**（2026-09-14；F163 守卫复核顺带发现） | `patches/packages/mt76-0014-wifi-mt76-npu-reset-panic-race-hardening.patch`（**#EXP**，2026-09-10 吸收）是**包裹补丁**：本体不修任何驱动代码，只在树内**新建** `package/kernel/mt76/patches/0{26,34,35,36,42,44,47}-*.patch` **7 个内层补丁**（NPU 整机复位后 host TX 永死与 NAPI NULL oops / `reg_lock` 改 irqsave 防 AP panic / wcid 竞态 / ALL_STA_INFO 与 ie_countdown 越界校验）。但 MANIFEST 的 dest 写成了**拷贝目录** `package/kernel/mt76/patches/` ⇒ 包裹补丁被拷进该目录后，由构建施加于 **mt76 源码树**（`patch -p1` 的 cwd），7 个内层文件落在 `<mt76 源码>/package/kernel/mt76/patches/` 里，**永不被构建 glob 应用**；而包裹补丁本体应用"成功" ⇒ 全链路零报错，7 项加固静默失效（#EXP 档自吸收起一直带着"已落地"的错觉）。 | ① 包裹补丁的**唯一正确载体是 ROOT**（在树根 `git apply`，内层补丁才落进真正的 `package/.../patches/` 并被构建 glob）；dest 写成目录时是**假成功**——它成功创建了一个没人读的文件。② 无任何守卫校"包裹补丁 dest 是否 ROOT"：`audit-patches.sh` 只审 hunk 行数、`audit-order.sh` 只审档位与集合，两者都看不见载体。③ 文字面处处指向 ROOT（补丁头自述"构建时由 `patch-kernel.sh` glob 应用"、同族包裹补丁均为 ROOT），唯机器不校 ⇒ 人读三遍也过。 | ① `patches/MANIFEST`：`mt76-0014` 的 dest `package/kernel/mt76/patches/` → **ROOT**（**一行**，不碰任何补丁内容；另加注释写明"包裹补丁 ⇒ dest 必须 ROOT"与该错位的后果）；② `scripts/audit-patches.sh` 新增**包裹补丁载体审计**：对活动条目扫描"在树内新建 `+++ b/**.patch`"的补丁，`dest != ROOT` 即**红**（错误文案内嵌 F165 教训）；③ 同轮改正 F164 行的口径错误（见右列⑦）。 | n/a（本仓工程化/载体纪律） | ① **主证（同一命令，修前→修后）**：`tmp/drift/run.sh --dry-run --oc --experimental` —— `✓ [dry-run] patches/packages/mt76-0014-…` 由修前的"拷贝（⚠ 目标目录不存在）"变为 **ROOT 应用**；mt76 的 verify 行由 `33 个文件` 变 **`39 个文件`**（31 本仓条目 + 1 由 `vendor/fanboy/07` 派生 + **7 本项新携带**），**39 个全部 `patch -f -p1` 真实应用成功、0 FAILED**；总账仍 `处理 133 / 实验档跳过 0 / 缺失 0`、rc=0（⇒ 内层 7 补丁对 `be5ce791` 真源同样干净）。② **定位证据（插桩保留 WORK）**：内层 7 文件实测落在 `src-1/mt76-be5ce791…/package/kernel/mt76/patches/`，而构建实际 glob 的 `patch-package_kernel_mt76_patches/` 内**0 个**。③ **构建侧反证（CI `ci-167` 日志）**：wrapper 本体有 `patching file package/kernel/mt76/patches/047-…`（= 在源码树里创建死文件），但全日志**无** `Applying …/047-…`；对照 `vendor/fanboy/07`（dest ROOT）派生的 `0014-…release-HW_RRO…` **有** `Applying` 行（日志 176361 行）⇒ 机制差异确凿。④ **横向普查**：`--oc --experimental` 档 **60 个包裹补丁**中，修前**仅 `mt76-0014`** dest ≠ ROOT（其余 59 个，含同样带 `#… diff --git` 前缀的 `root/9045`，均为 ROOT 且生产可用）⇒ 单点错位，非系统性。⑤ **守卫双向（负对照）**：临时把 dest 改回拷贝目录 ⇒ 守卫 **rc=1** 并报"在树内新建 7 个 .patch 文件，但 MANIFEST dest = …（必须 ROOT）…"（随后还原，`cmp` 逐字节校验一致）；修后 `--oc --experimental` **rc=0**（"60 个包裹补丁均为 ROOT"）、`--oc` **rc=0**（20 个）。⑥ **静态面不变**：`audit-order.sh .` rc=0（136 = 60/1/72/3）、`gen-order.py . --check` rc=0、`audit-patches --oc --experimental` 133 一致、`--oc` 61 一致；ORDER 只有"档位 路径"两列、**不含 dest** ⇒ 本修**不需要改 ORDER**。⑦ **同轮修正 F164 行**：「拷贝类 mt76 33 / iwinfo 3 实测为 32 / 2」**作废**——33 / 3 / 41 是**构建态应用集合**（= MANIFEST 条目 + 树内既有补丁 + ROOT 派生包补丁），32 / 2 / 40 是**干净树单独跑 verify** 的量；分解 mt76 32+1(`fanboy/07`)=33、iwinfo 1+1(上游自带 `100-multi_radio`)+1(`root/9052`)=3、uboot 40+1(`root/9002`)=41，已由 CI 日志逐条复核。⑧ **台账自查口径细化**：以 `awk -F` 指定竖线分词的自检会把**已转义**的竖线也当分隔符 ⇒ 会把"已正确转义"的行误判成破表（F164 行 NF=15 即含 6 个转义竖线，而其**未转义**竖线恰为 8 = 正常）；精确判据应数**未转义的**竖线。⑨ **CI 补丁层实证（2026-09-14，push `ca9f05b` 后；run `34802990547`，success 37s，全新 CI runner）**：CI 自跑 `audit-patches.sh` ⇒「✓ 包裹补丁载体审计：60 个包裹补丁均为 ROOT」（本项新守卫**首次在 CI 生效**）+「✓ ORDER ↔ MANIFEST 一致：136 条（60/1/72/3）」（F164 硬门首绿 ⇒ 原先担心的"硬门若红则 `fd769c9` 起每次构建皆红"被证伪）；`mt76-0014` 的 dry-run 行由修前的「⚠ 目标目录不存在」变为 **`✓ [dry-run] patches/packages/mt76-0014-…`**，且 `▶ [verify] package/kernel/mt76/patches` 报 **`✓ …39 个文件`**（33→39 在 CI 侧独立复现；其余 dest 计数 regdb 5 / mac80211 30 / firewall4 1 / rpcd 1 / iwinfo 3 / uboot 41 与修前同）。⑩ **机制证明（读 openwrt 真源，独立于任何日志）**：`include/quilt.mk:37` `PATCH_DIR?=$(CURDIR)/patches` + `:89` `$(call PatchDir,$(PKG_BUILD_DIR),$(PATCH_DIR),)`，且 `package/kernel/mt76/Makefile` **未设** `PKG_PATCH_DIRS` ⇒ quilt glob 的正是 `<openwrt>/package/kernel/mt76/patches/*.patch`；`scripts/apply-patches.sh:81` 的 ROOT 真实模式 `git apply` 落盘且**不回滚**，`:89-91` 的拷贝模式才把 wrapper 本体送进 `PATCH_DIR` 并被 quilt 施加于 mt76 **源码树** ⇒ 完整解释 ci-167 的「有 `patching file …/047-…`、无 `Applying …/047-…`」。⑪ **守卫负对照本轮复跑**：MANIFEST 现已**提交** ⇒ `git checkout --` 恢复精确——改回拷贝目录 ⇒ **`rc=1`** 且点名"在树内新建 7 个 .patch 文件，但 MANIFEST dest = package/kernel/mt76/patches/（必须 ROOT）"；还原后 `cmp` 逐字节一致、`git status` 空；正对照 `rc=0`。⑫ **构建态实证（2026-09-14，run `34803022939`，`profile=experimental` @ `ca9f05b`，结论 **success**，约 75 min（`03:33:45Z→04:48:48Z`），release `ci-168`）**：真实构建日志里 7 个内层补丁**全部**出现 `Applying …/openwrt/package/kernel/mt76/patches/0XX-… using plaintext:` —— `026 / 034 / 035 / 036 / 042 / 044 / 047` 各 **1 次**（同一秒 `04:47:11`，与正对照 `0014-…release-HW_RRO…` 同批）⇒ **7 项加固首次真正进入构建**。**负判据归零**：源码树死文件行 `patching file package/kernel/mt76/patches/0XX-…` 由修前 **7 条 → 0 条**（修前 7 条见本行③）；wrapper 本体 `✓ applied  patches/packages/mt76-0014-…`（ROOT 落盘）**1 条**。构建态守卫同绿：`✓ 包裹补丁载体审计：60 个包裹补丁均为 ROOT`、`✓ ORDER ↔ MANIFEST 一致：136 条（60/1/72/3）`。⚠ **口径**：真实构建走 `apply-patches.sh . --experimental`（**不带** `--oc`）⇒ 报 `✓ hunk 行数审计：132` 与 `处理：132  实验档跳过：1`（跳过的 1 即 `#OC` 行），**不是**本地 `--oc --experimental` 的 `133/0`；`verify-copy-patches` 的 `39 个文件` 那行**只在 dry-run 模式跑**，构建态无此行（39 的构建态等价证据即上述 7 条 `Applying`）。⑬ **仍未做**：实机判据（047 = 整机复位后 host TX 恢复、`dmesg` 无 NAPI NULL oops）——**不得以本地 dry-run 绿代替**。 **⑭ 实机首测（2026-09-15，ci-171）未通过**：单次 full chip reset 即 `chip full reset failed`（NPU stop 三阶段 -110）⇒ 见 **F170**。 |
| F166 | **CI 构建缓存实测零效果：ccache 从未被调用（构建 100% 全量重编）**（2026-09-14；用户点名"配置 GitHub 构建缓存加快 build"） | `build.yml` 的 ccache 步（`hendrikmuhs/ccache-action@v1.2`）自加入起**从未产生任何有效缓存**，构建全程全量重编：ci-168（run `34803022939`）post 步 `ccache -s` = `Cache size (GB): 0.0 / 0.5 ( 0.00%)`，且 post 步原文 `Not saving cache because no objects are cached.`（`save.ts:120`，闸门 `ccacheIsEmpty` = `save.ts:8`/`:11`）；仓库唯一存档 `ccache-openwrt-stock-2026-09-11T18:15:48.685Z` 仅 **25,960 B** 且 `last_accessed_at == created_at`（**从未被恢复过**） | 三层**同时**坏：① **`CONFIG_CCACHE` 未设置** ⇒ `rules.mk:347` `ifneq ($(CONFIG_CCACHE),)` 不成立 ⇒ 编译命令行里根本没有 ccache（`TARGET_CC`/`HOSTCC`/`KERNEL_CC` 均未前缀）；② **缓存目录错位** —— action 固定用 `$GITHUB_WORKSPACE/.ccache`（`common.ts:70`），而 OpenWrt 缺省写 `$(TOPDIR)/.ccache`（`rules.mk:354`）⇒ 即便启用也存到快照之外、快照恒空；③ **恢复键缺失** —— `append-timestamp=true`（默认）只给**保存**键加时间戳，恢复侧主键是裸前缀 `ccache-<key>-`，不给 `restore-keys` 就永远 miss（实证：ci-166 stock 在 09-11 存档**之后**仍报 `No cache found.`） | `.github/workflows/build.yml`（**3 处，+17 行；纯 CI 配置，不碰补丁层**）：① ccache 步补 `restore-keys: openwrt-${{ env.PROFILE }}`；② `max-size` 500MB→**3G**（仍远低于仓库 10GB 额度）；③ 配置步**在最后一次 defconfig 之后**追加 `CONFIG_CCACHE=y` + `CONFIG_CCACHE_DIR="$GITHUB_WORKSPACE/.ccache"` + 防静默失效守卫 `grep -q '^CONFIG_CCACHE=y$' .config` | n/a（本仓 CI 配置，非补丁层；上游同款做法 `openwrt/actions-shared-workflows` → `reusable_build.yml:566` `echo CONFIG_CCACHE=y >> .config`） | **提交 `983a58f`**（已推 main；`build.yml` **不在** `sync-upstream.yml` 的 `on.push.paths` ⇒ push 不触发任何 workflow）。**A/B 实证（2026-09-14 P14；同一 experimental 档、同一抽取器 `tmp/cache/ab.sh`，判据**锚定 step 列**防坑 19）**：**填充轮** = run `34827209505`（ci-169 @ `983a58f`）—— 恢复判据 `No cache found.`（**MISS**，符合填充轮预期）、`Cacheable calls: 25434 / 32646 (77.91%)`、`Hits: 2778 / 25434 (10.92%)`、`Cache size (GB): 1.0 / 3.0 (33.89%)`、存档 **SAVED** = `ccache-openwrt-experimental-2026-09-14T10:41:01.180Z`（**267,967,272 B**）；**命中轮** = run `34834500124`（ci-171 @ `8abf2f4`，即 F161 的 experimental 构建）—— 恢复判据 `Restored from cache key "ccache-openwrt-experimental-2026-09-14T10:41:01.180Z"`（**HIT**）、`Hits: 9790 / 25434 (38.49%)`、`Cache size 1.8 / 3.0 GB (60.90%)`；**构建步 80.2 → 73.9 min（−6.3 min / −7.9%）**。**stock 侧独立佐证**：run `34829174800`（ci-170）post 步 **SAVED** `ccache-openwrt-stock-2026-09-14T11:01:11.225Z` = **265,422,745 B**（对照旧存档 25,960 B、从未恢复）⇒ 两档恢复/存档通道都已打通。**口径纪律（必须标明）**：命中轮与填充轮**不是同一 commit**（`983a58f` vs `8abf2f4`，差异仅 mt76 固件），缓存键只按 `profile` ⇒ 跨 commit 命中成立，但 **"−6.3 min" 是跨 commit 观测，不是严格同 commit A/B**；且 ccache 只覆盖 C/C++ 编译阶段，下载/feeds/打包/镜像生成不受益，故加速比不等于整 run 加速比。**残留风险 R1 已当场判否**：曾疑 action 的 `ccacheIsEmpty()`（`save.ts:8-11`，读 PATH 里 apt `/usr/bin/ccache` 4.9.1）会把 OpenWrt 自建 ccache 4.13.3 写的目录误判为空 ⇒ 静默不存档；**实测两轮均 SAVED**，R1 不成立（退路 `actions/cache@v4` 未启用，留作备选）。**仍未做**：严格同 commit 二次构建的加速比（可选，非阻塞）。 |
| F167 | **上游漂移（Q1000K）致补丁层红：`9000` 的 `platform.sh` 上下文冲突 + 漂移守护盲区**（2026-09-15；由 `schedule` cron 报红发现） | 2026-09-14 12:43Z 起**连续三次** `schedule` 的 `sync-upstream` 红（run `34844969061` / `34886296687` / `34907333941`，三者同因：`汇总：基线漂移 1` + `✗ 冲突：patches/root/9000-xr1710g-common.patch` + `patch failed: …/platform.sh:33`）：上游 main 前进到 **`0d7bfcb7e`「airoha: add support for Quantum Fiber Q1000K」**（5 files / +309 −2；`01_leds`+4 / `02_network`+4 / `platform.sh`6± / 新 `an7581-q1000k.dts` 266 / `an7581.mk`+31）⇒ 把 9000 hunk 的上下文行 `nokia,xg-040g-md-ubi)` 改成 `nokia,xg-040g-md-ubi\` + `quantum,q1000k-ubi)` ⇒ **上下文真变** ⇒ 补丁层红、**此后任何新构建都会失败**（与 F161/F166 无关：drift 列表里 `package/kernel/mt76/Makefile` 明确 ✓ 未变） | ① `git apply` **容忍行号偏移、不容忍上下文变化** ⇒ "上游在相邻行加了个 board"就足以让 9000 死（而行号本身也因上游前一处 +1 而漂移，两个效应叠加，靠偏移救不回来）；② **漂移守护有盲区**：它监视 `an7581.mk`，却**没有**监视同样被 9000 修改的 `platform.sh` 与 `02_network` ⇒ 本次是 dry-run 硬门拦住的，**守护没预警**（F163 同类：守卫自身覆盖面不全 ⇒ 凡被本仓补丁修改的 airoha base-files 文件都应登记） | ① `patches/root/9000-xr1710g-common.patch`：**只重写 `platform.sh` 那一个 hunk 的上下文**（`@@ -33,6 +33,7 @@` → `@@ -34,6 +34,7 @@ platform_do_upgrade() {`；尾部上下文 `nokia,xg-040g-md-ubi)` → `nokia,xg-040g-md-ubi\` + `quantum,q1000k-ubi)`；`index 8584b84..cd0b5e3` → `d8cf5e2..9b64787`）。**由 `origin/main` 的该文件程序化插入 `gemtek,xr1710g-ubi\` 后经 diff 生成、非手写**；**其余 hunk 一行未动**（本仓新增的那一行不变）② `scripts/audit-upstream-watch.sh`：锚点 `12fa348112…` → **`0d7bfcb7e31e…`**、`BASELINE_DATE` `2026-09-13` → `2026-09-14`、`an7581.mk` blob `14e28682…` → `efc1b44a…`，并**新增 `platform.sh`/`02_network` 两条监视项**（补盲，带 F167 缘由注释） | n/a（本仓工程化 / 补丁层重基） | **受影响面（逐 hunk 判过，非猜）**：真冲突**仅** `platform.sh`×`9000`；`02_network`×`9000`（上游插入点 `@@ -24,6 +24,10 @@` 在我们 hunk 旧行 17–22 **之后**）、`an7581.mk`×`9000`（上游插入点在**文件末尾** `@@ -188,3 +188,34 @@`，我们 hunk 旧行 127–132）、`an7581.mk`×`vendor/fanboy/09`（旧行 110–116）**均不冲突**；`01_leds`/新 `an7581-q1000k.dts` 本仓无补丁触及。**本地复现（已知阳性/阴性双对照）**：同一 hunk 对**旧**基线（`12fa34811`）`patch -p1 --dry-run` **clean**、对**新**基线（`0d7bfcb7e`）**`Hunk #1 FAILED at 33`**（复现 CI）；重基后对**新**基线 **clean 且结果逐字节相同**，对**旧**基线则正确 **`Hunk #1 FAILED at 34`** ⇒ 是"面向新基线的真重基"，**不是把判据放宽**。**收口判据（本地全绿，命令可复跑）**：① `audit-upstream-watch.sh <tree> --fail-on-drift` **rc=0**（`汇总：基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 2（不计失败），基线锚点 0d7bfcb7e`）；② `tmp/drift/run.sh --dry-run --oc --experimental` **rc=0**（`✓ [dry-run] patches/root/9000-xr1710g-common.patch`、`处理 134 / 实验档跳过 0 / 缺失文件 0`、`拷贝补丁校验：真实应用 7 / 未校验 0`、`ORDER ↔ MANIFEST 一致：MANIFEST 137 条`、mt76 仍命中 fork 源 `mt76-firmware-01367e60db43…` 39 文件）；③ 静态审计 `--oc --experimental` **134 一致** + 60 包裹 ROOT、`--oc` **62 一致** + 20 ROOT、`gen-order --check` rc=0；④ **负对照 B（新监视项真的会咬）**：把 `platform.sh` 的登记 blob 临时改错 ⇒ 守护报 `⚠ [基线] 漂移：…platform.sh` + `基线漂移 1` + **rc=1**，恢复后 `cmp` **逐字节一致**；⑤ 本地 drift 树已同步到新基线（`tmp/drift/owrt` HEAD = `0d7bfcb7e`）。**CI 实证（push `0fe65bf` 触发，run `34925537604`，success）**：`汇总：基线漂移 0，路径缺失 0，同址冲突 0（含新增文件同址 0），远端 tip 前进 2（不计失败），基线锚点 0d7bfcb7e` + `hunk 行数审计 134 一致` + `60 个包裹补丁均为 ROOT` + `MANIFEST 137 条` + `处理：134 / 实验档跳过 0 / 缺失文件 0` + `拷贝补丁校验：真实应用 7 / 未校验 0`；**且该次 CI 的上游 HEAD 已是 `11c64908c`（比锚点又前进一次）仍全绿** ⇒ 收口在更新的上游上依然成立。**连续红窗口**：12:43Z → 23:07Z 三次 cron 同因失败，直到本项落地才恢复绿。**顺带（非失败项，留待对账轮）**：远端 tip 前进 2 处 —— `OpenW1700k.git ubi2-oc` `ac975aa94 → 37aa0bcd3`（3 小时内第二次动）、`ImmortalWrt-for-Gemtek-XR1710G.git HEAD` `38148509 → bb34bbe6e`（naoki66 线，**首次出现**）⇒ 按 F151/F154/F157 惯例择机重新对账 `vendor/fanboy/01..21`。**仍未做**：实机（不受本项影响）。 |
| F168 | **fork 线 tip 前进对账：fanboy `ubi2-oc` + naoki66 线（结论：两处均无需重取）**（2026-09-15；F167 收尾后守护报 2 处 tip 前进） | 守护 `audit-upstream-watch.sh` 报两处远端 tip 前进：`OpenWRT-fanboy/OpenW1700k.git refs/heads/ubi2-oc` `ac975aa94→37aa0bcd3b`、`naoki66/ImmortalWrt-for-Gemtek-XR1710G.git HEAD` `38148509→bb34bbe6e` ⇒ 必须判「纯重基」还是「内容真变」，据此决定 21 份 `patches/vendor/fanboy/*` 原料与 naoki66 来源补丁是否需重取（F151/F154/F157 惯例） | 两条线都是作者自持分支、持续 rebase（fanboy 线同一提交被反复重写且时间戳相同，F157 已记）⇒ **tip 本身不是稳定判据**，判据必须是「提交内容」而不是「SHA/时间」 | `scripts/audit-upstream-watch.sh`：两条记录 tip 重锚（`ac975aa94→37aa0bcd3b`、`38148509→bb34bbe6e`）并各自附对账结论；**零补丁改动、零 MANIFEST/ORDER 改动**（结论 = 无需重取） | n/a（fork 漂移，非上游） | **fanboy 线 = 纯重基**：`git log -p origin/main..<tip> \| git patch-id --stable` 两侧各 22 个自有提交（subject 一一对应）⇒ **22/22 patch-id 全等、零内容变化** ⇒ 21 份原料仍有效（证据 `tmp/recon-p15/{old,new}.ids`）。**naoki66 线 = 快进 19 提交 / 29 文件**（compare API `status=ahead`、`behind=0` ⇒ 非 force-push），变更面三类且**均不影响本仓**：① 自有 LuCI 应用（改 `luci-app-airoha-factory`/`-recovery`，新增 `luci-app-netmode`/`luci-app-mesh-conf`）——本仓**一条都不携**（`grep -rl` 全 0 命中）；② 其自有的 upstream 内核补丁副本（`0403` 删 / `303-01`+`303-02` 删 / `182-v7.4` 增 / `310-10`+`916-02`+`920-12`+`920-13` 各 ±1~3 行）——本仓对应项取自**上上游** openwrt 与 fanboy（`9042` 已因上游 #24872 吸收而 `#DISABLED`）；③ `config.seed`、`README.md`、`CHANGELOG.md`、`.gitattributes`——非本仓来源（本仓用 `config/seed-config*.diff`）。本仓实际取用的 naoki66 源文件 **`622-`/`625-`/`743-`/`744-` 在新 tip 未变且仍在**（contents API 逐条核对）⇒ 无重取。**判据**：守护 `--fail-on-drift` **rc=0 / 基线漂移 0 / 路径缺失 0 / 同址冲突 0**，两处由 `⚠ 前进` 转 `✓ 未变`；静态审计 `--oc --experimental` 134 + 60 包裹 ROOT、`--oc` 62 + 20、`audit-order` + `gen-order --check` 双 rc=0（对账未动补丁层，两处独立复核） |
| F169 | **fanboy `06` 重取：`nf_ct_bridge_in` 的 drop 分支双重释放（活跃 default 档，两档镜像都带）**（2026-09-15；P15「06 逐 hunk 读」顺带定性） | `patches/vendor/fanboy/06-nft-flow-offload-l2-22533-3c8d3e6664.patch` 内层 `675-03` 把 `nf_ct_bridge_in()` 的 `drop:` 分支写成 `kfree_skb(skb); return NF_DROP;`。而该函数以 `.hook` 注册在 `nf_ct_bridge_hook_ops[]`，是 netfilter hook：返回 `NF_DROP` 时 **由 `nf_hook_slow()` 统一 `kfree_skb_reason()` 释放**（6.18.44 `net/netfilter/core.c` 原文）⇒ 同一 skb 释放两次（双重释放 / slab 破坏，可 panic）。可达：`BR_INPUT_SKB_CB(skb)->frag_max_size` 非 0（碎片）且 `nf_ct_bridge_inner()` 返回 <0（截断的 VLAN/PPPoE 封装） | 该版取自 fanboy 的**瞬时提交 `3c8d3e6664`**（F123 重取），而该提交随后被 force-push 抹掉（F168 已证：本地可读、非任何分支祖先）；fanboy **现行线 `ceffe6acb5` 已去掉**这个 `kfree_skb` | ① `patches/vendor/fanboy/06-nft-flow-offload-l2-22533-3c8d3e6664.patch` → **重取并改名为 `06-nft-flow-offload-l2-22533-ceffe6acb5.patch`**（配方 `git format-patch -1 --stdout ceffe6acb5` 去尾部 3 行 `-- `+版本号；已用**旧文件逐字节自证**该配方）；② `patches/MANIFEST`（路径 + 缘由注释）、`patches/ORDER`（路径 + 评审注释），`gen-order --check` rc=0 | n/a（fanboy 自持分支。上游 openwrt PR **#22533 是过期的 pending-6.12 系列**（head `8c4f35eb96`，2026-08-02 后未动，文件名 `675-01-net-export-…`/`675-02-nft_flow_offload-add-bridging-support`），**不是**本 6.18 系列的真源 ⇒ 只能以 fanboy 线为准） | **8 处 hunk 差异逐条定性**（`tmp/recon-p15/{our06,fb06}.diff`）：**5 处机械**（外层 `index` blob + 内层 `@@ -0,0 +1,N @@` 与 hunk 行数随内容联动）；**2 处等价**：`675-02` encap 由「追加 `info.encap[info.num_encaps]` + `NF_FLOW_TABLE_ENCAP_MAX` 越界检查」回到覆写 `encap[0]`/`num_encaps = 1`（`info` 在 `nf_flow_route` 内 `= {}` 零初始化，且该段写在 `nft_dev_path_info()` **之前** ⇒ 此处 `num_encaps` 恒为 0，两形式结果相同）、ipv6 `offset` 守卫由两层 `if` 合并为 `if (offset && …)`（语义同）；**1 处真缺陷 = 本项**。**三证（相互独立）**：① `nf_hook_slow()` 的 `case NF_DROP: kfree_skb_reason(…)`；② **全 `net/` 树零例** `kfree_skb(skb);` 紧邻 `return NF_DROP;`（而紧邻 `return 0`/`-ENOMEM`/`-EMSGSIZE` 的 8 处均有）⇒ 「hook 不得自行释放 NF_DROP 的 skb」约定确凿；③ 上游 pristine `nf_ct_bridge_in` 同类分支本就是裸 `return NF_DROP;`。**真源 A/B（对 6.18.44 pristine `patch -p1`，`tmp/recon-p15/ktest{,-old}`）**：旧版落树 `drop:` 下 = `kfree_skb(skb); return NF_DROP;`（双重释放处数 **1**）、新版 = 裸 `return NF_DROP;`（**0**），两者 `--dry-run` 均 rc=0。**全量判据**：`audit-patches --oc --experimental` **134 一致** + 60 包裹 ROOT、`--oc` **62** + 20、`audit-order` + `gen-order --check` 双 rc=0；`tmp/drift/run.sh --dry-run --oc --experimental` **rc=0**（`处理 134 / 实验档跳过 0 / 缺失 0`、拷贝类 `真实应用 7 / 未校验 0`，且日志第 80 行确为新 06 `✓ [dry-run] …ceffe6acb5.patch`）。**实机判据（待）**：桥 + conntrack 下 IPv4/IPv6 分片（含截断封装）压测，`dmesg` 无 double free / slab 报错。**注**：本项是内存安全修复。**CI 实证**：push `9d35ed8` 触发的 `sync-upstream`（run `34936165413`/`34936165412`，两个 run 同因、均 **success**）独立复现 —— `基线漂移 0 / 路径缺失 0 / 同址冲突 0 / 远端 tip 前进 0`、`hunk 134 一致` + `60 包裹 ROOT`、`MANIFEST 137（61/1/72/3）`、`处理 134 / 跳过 0 / 缺失 0`、`拷贝类 真实应用 7 / 未校验 0`，日志中确为新 06（`✓ [dry-run] …ceffe6acb5.patch`）。**实机口径（2026-09-15 用户决策）**：已构建并到手的 `ci-170`/`ci-171`（commit `8abf2f4`）**早于本项**、带该双重释放；用户明确选择**不派新构建**、用 `ci-171` 完成 F161 的 V7.0–V7.6 ⇒ 该轮实机结果**不得**用作 F169 的判据（F169 的实机项另排） |
| F170 | **单开：full chip reset 前置 NPU stop 超时 ⇒ 整机复位失败（`mt76-0014` 内层 047 × `mt76-0147`/`0162` 交互）——A 修法实机否证，真根因转 PCIe AER（见 F172）**（2026-09-15；F165 实机判据首测；2026-09-15 P18 ci-172 否证 A） | 用户 2026-09-15 刷 ci-171（experimental）后，按 F165 判据**单次**触发 `echo '0,8' > /sys/kernel/debug/ieee80211/*/mt76/sys_recovery`（→ `mt7996_reset()` 置 `MT_MCU_CMD_WDT_MASK` → `recovery.restart=true` → `mt7996_mac_full_reset()`）：`NPU detach request failed: -110` + `NPU stop timed out, recovering core 0` + `NPU pause request failed: -110`（三阶段全超时）→ `Timeout for initializing firmware` / `Firmware is not ready for download` 反复 → `chip full reset failed`；两次触发累计 `chip full reset failed` ×12、`NPU pause request failed` ×24、firmware-timeout ×**254**；Wi-Fi 死到必须 reboot。mac80211 侧另有 `__ieee80211_stop_tx_ba_session`（+0x2a4/+0x22c）、`ieee80211_sta_eosp`（附文 `Driver is not allowed to fail if the sta_state is transitioning down the list: -5`）、`sta_set_sinfo` WARNING 与 `phy0.0-ap0: failed to remove key ... (-5)` | 047 在 `mt7996_mac_full_reset()` 开头新增 `mt7996_npu_hw_stop(dev)`；该函数实现在 #EXP `mt76-0162`（被 `mt76-0147` 改过）里是三阶段握手，本机**首跳即 -110** ⇒ NPU 从未静默 ⇒ 固件重下载连续失败 ⇒ `mt7996_mac_restart()` 10 次重试全灭（047 自己加的 `if (ret) goto out` 把失败暴露成 `chip full reset failed`）。即 F165/047 声称的「full chip reset 后 host TX 恢复」**未兑现**，症状只是由「静默失联」变为「显式复位失败」；上述 mac80211 WARNING 属「固件已死、驱动 teardown 返回 -EIO(-5)」的下游症状 | **A 修法已实机否证（P17 出法 → 2026-09-15 P18 ci-172 实机）**：`patches/packages/mt76-0014-…` 内层 047 在 full-reset 里的停 NPU 调用由 `mt7996_npu_hw_stop()`（0147 下 `-ETIMEDOUT ⇒ airoha_npu_recover` 重启 core 0）改为 **`mt7996_npu_hw_stop_teardown()`**（0162 的非 recover 变体，unregister 同款）——不再在复位中途重启 NPU；047 的 NAPI 跳过与 `__mt7996_npu_hw_init` 重入不动。`--oc --experimental` 审计 134/60 绿。备选 B：删停 NPU 调用但保留 NAPI 跳过与重入。**实机结论（ci-172，内核 `Tue Sep 15 08:07:52 2026`）：A 生效但未修好**——整段日志**无** `NPU stop timed out, recovering core 0`（teardown 的 `recover=false` 短路确实执行），但 `chip full reset failed` 照旧（836.076086，距首次 stop +41.4 s = 10 次重启全失败）⇒ **「整机复位中途 recover NPU core 0」不是根因，A 的整套 NPU-recover 假说被实机否证**；真根因 = PCIe `[14] CmpltTO` + 缺 AER 回调，见 **F172**。B 已按 handoff 备好并过审计（134/60 + 62/20 + dry-run ROOT 134/0/0），用户 2026-09-15 裁定「选 2 真修」⇒ **B 回退**（工作区干净）。**[P19 重做并落地]**：F172 实机判「PCIe 层通、设备层未通」后，B 作为**隔离实验**重做（commit `774cf55`）——删内层 047 的 `@@ -2472,6 +2497,11 @@` hunk（13 行 = `mt7996_mac_full_reset()` 开头的 `mt7996_npu_hw_stop_teardown(dev)` 及其注释），外层 `@@ -0,0 +1,85 @@ → +1,72`，内层 subject 改 `re-init the NPU after a full chip reset`，保留 NAPI 跳过与 `__mt7996_npu_hw_init()` 重入，wrapper 头加 `!!! P19/B 隔离实验` 注。本轮门禁全绿（`--oc --experimental` 135/60 + `--oc` 62/20 + ORDER 138 + 离线 dry-run `处理 135/跳过 0/缺失 0`、mt76 verify 40 文件）；ci-174（run `34979326215` @ `774cf55`）success，构建日志内层 047 = **3 hunk**（ci-173 = 4），产物 `mt7996e.ko` 239,112 B（ci-173 = 239,168 B）。证据 `tmp/p16/results-ci171.md` + `handoff/handoff_p18.md` §1.2/§1.3 + `handoff/handoff_p19.md` §1.8 | n/a（047/0147/0162 均作者自持、无上游提交；**用户 2026-09-15 决定不回给作者 Gilly**） | **实机判据（P18 判定 = FAIL）**：单次 `echo '0,8'`（ci-172，内核 `Tue Sep 15 08:07:52 2026`）后 `SYS_RESET_COUNT` WM 0/WA 0 → WM 1/WA 1；**A 生效**（整段无 `recovering core 0`）但 **未修好**（`chip full reset failed` 照旧），Wi-Fi 死到必须 reboot。真根因只在**未过滤 dmesg**：`mt7996e: AER: can't recover (no error_detected callback)` + `pcieport: [14] CmpltTO (First)`（Uncorrectable **Non-Fatal**）+ `AER: device recovery failed`；设备仍在 `/sys/bus/pci/devices`（`0000:01:00.0` = PRESENT）、**无 `Root Port link has been reset`** ⇒ 按 F116 口径属「原则上可恢复」，阻塞点 = 驱动缺 AER 回调（**F172**）。⚠ **坑 39**：p17 的 `grep -iE 'NPU\|napi\|chip full reset\|NULL pointer\|Call trace'` 把 AER/pcieport/CmpltTO 行全滤掉 ⇒ 误判根因；复跑必须看未过滤 dmesg。**[ci-173/F172 实机回填]**：刷 ci-173（含 F172）单次 `0,8` 仍 `chip full reset failed`（792.893）——F172 把 AER 恢复做成（`Root Port link has been reset (0)` + `device recovery successful`），但固件重下仍 `Failed to get patch semaphore` ⇒ 设备层未恢复；详见 **F172** 行实机回填。**[B 隔离实验实机判定（ci-174，2026-09-16，设备 `/lib/modules/6.18.44/mt7996e.ko` sha256 = `2106f42faf8e74a695825701cc49c5332fb089bbf467c233c6997447b2b714d7` 已核）= FAIL（分支②）]**：刷 ci-174（sysupgrade sha256 `dc134f8b61dae73305db25ade7da9f64c28659be5bbccd46895385834cf64289`）→ ⚠ **不要用 `uname -a` 核版本**（与 ci-173 的 kernel 段逐字节相同，见 F172 行口径修正）→ 改用 `sha256sum /lib/modules/6.18.44/mt7996e.ko`，应为 `2106f42faf8e74a695825701cc49c5332fb089bbf467c233c6997447b2b714d7` → 单次 `echo '0,8' > /sys/kernel/debug/ieee80211/*/mt76/sys_recovery` → **未过滤 dmesg**：① 若 `[14] CmpltTO` 与 `chip full reset failed` **都消失** ⇒ NPU stop 即触发器，B 是修法方向；② 若仍 `chip full reset failed` / `Failed to get patch semaphore` ⇒ NPU stop 被排除，根因在**固件下载 / WFSYS 复位路径**（PERST 后 MCU 未真复位）。**[实机结果（ci-174，2026-09-16）]**：未过滤 dmesg（`tmp/p21/b-dmesg.log`）＝ `phy0 indicated firmware crash` → `[14] CmpltTO (First)` → `PCIE error detected, state: 1` → `Root Port link has been reset (0)` + `device recovery successful`（PCIe 层与 ci-173 完全一致地成功）→ `Message 00000010 (seq 8..15,1..2) timeout` ×10 ≈48 s（`0x10` = `MCU_CMD_PATCH_SEM_CONTROL`，固件重下**第一条** MCU 命令）→ `chip full reset failed`（163.95）→ `ieee80211_reconfig`「Hardware became unavailable」WARNING + 四 AP `failed to remove key (-5)`，Wi-Fi 死待 reboot。**全段零 NPU 行**（ci-173 首行 `NPU detach -110` 消失）⇒ B 生效且 **NPU stop 被排除**：CmpltTO 是 MCU 死的下游表征（`0,8` ⇒ WDT ⇒ MCU 死 ⇒ PCIe completions 停）。**代码级定位（真源 `be5ce791`，`tmp/mt76_clone`）**：① `mt7996_mac_restart()` 每次尝试都 `mt7996_dma_reset(dev, true)` → **`mt7996_wfsys_reset()`**（非 mt7990 分支 = `MT_WF_SUBSYS_RST` bit0 打摆 20 ms×2）——同一摆位 10 次全灭 ⇒ **B 并没有去掉 WFSYS 复位**，被删的只是 047 注入的 NPU stop；② 该分支**无 poll 校验、失败静默**（`wfsys reset fail` 报错只在 mt7990 分支）；③ `driver_own`（纯寄存器）能过、`PATCH_SEM(0x10)`（MCU）全灭 ⇒ **死的只有 MCU**；PCIe 平台的固件重下机制里**不存在 MCU 冷复位**（probe 路径 pci.c:139 也是同一 poke）⇒ **A（teardown stop）/B（无 stop）/stock（recover stop）三版同灭 =「MCU 硬死不可软件恢复」三证**。候选方向：a) fail-fast；b) `remove`+`rescan`（**须在死芯片上重做**）；c) fork 树冷复位。**[c 结果（2026-09-16）]**：**零先例**——mt76 全 fork 的 `wfsys_reset` 与上游同款 poke、宿主 `drivers/pci/controller/`（含 airoha）**无 `perst` 引用**、naoki 树 `WFSYS_AON` 映射只是地址翻译 ⇒ 「更硬复位」无现成代码可抄。**[b 第一次实验（2026-09-16）＝无效，作废]**：用户先 reboot（设备健康）后才跑 `remove`+`rescan` ⇒ log 中固件重下成功（`WM/DSP/WA Firmware Version` 横幅）只是**健康芯片的正常 probe**，证不了「rescan 能复活死 MCU」；台账一度记为「能救/MCU 复活」的判读**当场作废**（未提交即改正）。**[b 重做（2026-09-16 P22）＝成功，语义精化「复活 ≠ 可服务」]**（P21 读码预测「rescan 走 probe 同款 poke ⇒ b 必败」被实机**证伪**）：单次 `0,8` @02:55:24（死芯片、53.8 s 后 `chip full reset failed`、WA 0→1、**零 NPU 行** = 047/B 生效）→ **不 reboot** 直接 `remove`+`rescan` ⇒ 02:57:58 两 endpoint 重枚举 + `WM/DSP/WA Firmware Version` 横幅/固件全量重下、`SYS_RESET_COUNT` 清零 = **rescan 能复活死 MCU**；但复活后 **beacon 未上空**（monitor 8 s：邻居 200 帧 / 己方 0 帧；`wifi down/up` 无效；用户实连判「不正常」）⇒ **不能免重启修好**（beacon/RF 状态机只有冷 boot 才完整初始化）⇒ b 降级为「**死活判定探针**」，完整恢复手段仍 = reboot、**a（fail-fast + 受控 reboot）为唯一务实收口**（详见 F173）。顺带记录：该次 fresh boot 在 340 s 出现一次 **L1 SER**（NPU detach/stop/pause/init 全 `-110`、`L1 SER recovery completed`、Wi-Fi 存活）——NPU 链路该 boot 早期即不健康（57 s `NPU flow stats unavailable (-22)`），主 Wi-Fi 链路不受影响。关联 F165 行、`docs/DEVICE-VALIDATION.md` |
| F171 | **P17 上游锚点刷新 + 两条 fork 线 tip 对账（结论：均无需重取）**（2026-09-15；承接 F167/F168） | ① 漂移锚点仍 `0d7bfcb7e`，而实测上游 main 已到 `7f4f82469`（11 提交）；② 守护报 naoki66 `bb34bbe6e→c31b73423`；③ 本轮复跑又见 Gilly `a57615652→3d4ff76d3` | ① 上游 `0d7bfcb7e→7f4f82469` 的 11 提交中，8 个监视 blob **全未变**（唯一涉 airoha/generic 者为新增 `hack-6.18/710-sch_cake…`，非 744/745 冲突面）；② naoki66 `bb34bbe6e→c31b73423` = **快进 1 提交/4 文件**，全在 `package/luci-app-mesh-conf/`（本仓不携带），8 个来料源文件（mac80211-411、mt76-0010/0012、622/625/628/743/744）blob+sha256 全等且仍在；③ Gilly `a57615652→3d4ff76d3` = **快进 1 提交**「Bump Kernel-6.18.52 and rebase」，960/961/982 从 `openwrt-patches/` **移除**（随 6.18.52 上游化），本仓 9045/9042 内层因 pin 仍 6.18.44 不受影响 | `scripts/audit-upstream-watch.sh`：`BASELINE_UPSTREAM`→`7f4f82469…`、`BASELINE_DATE=2026-09-15`；`REMOTE_TIPS` naoki66→`c31b73423`、Gilly→`3d4ff76d3`（各补本轮结论）；`tmp/drift/owrt` checkout 至 `7f4f82469` | n/a（记账/守护）；上游 `7f4f82469` 未涉监视面 | 复跑 `audit-upstream-watch.sh $W/tmp/drift/owrt --fail-on-drift` rc=0；方法承 F151/F168「**按内容不按 SHA**」（blob + sha256 + GitHub compare API）。**留观察**：内核 bump ≥6.18.52 时须删 9042/9045 内层 982/960/961 |
| F172 | **mt7996 PCIe AER error handling：补 `error_detected`/`slot_reset`**（2026-09-15/16 P19；F170 实机否证 A 后的真根因）——**ci-173 实机：PCIe 层通、设备层未通（F172 必要不充分）** | 未过滤 dmesg（ci-172）先发链：`mt7996e 0000:01:00.0: AER: can't recover (no error_detected callback)` → `pcieport 0000:00:00.0: PCIe Bus Error: severity=Uncorrectable (Non-Fatal), type=Transaction Layer, (Requester ID)` → `device [14c3:6899] error status/mask=00004000/00400000` → `[14] CmpltTO (First)` → `AER: device recovery failed`（×3 批）；其后 `Timeout for initializing firmware` / `Firmware is not ready for download`（797.17–875.07）/ `NPU detach request failed: -110` / `chip full reset failed`（836.076086）**全是下游症状**。设备仍在总线、**无** `Root Port link has been reset` ⇒ 恢复从未被尝试（不是硬件死亡）。 | 内核语义（`tmp/kernel-base/drivers/pci/pcie/err.c`，本仓 6.18.44 实读）：AER **Non-Fatal** → `pcie_do_recovery(dev, pci_channel_io_normal, aer_root_reset)`；`report_error_detected()` 对「无 error_detected 回调」的非桥设备投 `PCI_ERS_RESULT_NO_AER_DRIVER` ⇒ 既不 mmio_enabled、也不 NEED_RESET/slot_reset，直接走 `failed` → `device recovery failed`（**设备不摘除、链路不重置**）。`mt7921/pci.c` 有现成 `pci_error_handlers`，但**不能照抄**：它对任何非 0 uncorrectable 一律 `PCI_ERS_RESULT_DISCONNECT`，而我们的 uncorrectable status 恰为 `0x4000`（bit14 CmpltTO）⇒ 照抄 = 摘设备、不恢复。参考 `tmp/f161/src/mt7921/pci.c:610-710`。 | 新增 `patches/packages/mt76-0023-wifi-mt76-mt7996-add-pcie-aer-error-handling.patch`（**#EXP**，dest `package/kernel/mt76/patches/`，1 文件 **3 hunk / +84 行**；P19 初版为 2 hunk / +62 行，P22/a 重写 slot_reset）给 `mt7996_pci_driver` 补 `.err_handler`：① `error_detected`：`pci_channel_io_perm_failure ⇒ DISCONNECT`；其余（含 Non-Fatal CmpltTO 的 `pci_channel_io_normal`）`⇒ PCI_ERS_RESULT_NEED_RESET` ⇒ PCI 核执行 `aer_root_reset()` 的 **secondary bus reset**；② `slot_reset`：`pci_enable_device_mem + pci_set_master + pci_restore_state/pci_save_state` 之后——**P19 原版**把 full chip reset 交驱动 `reset_work`（`recovery.restart=true` + `queue_work(… &dev->reset_work)`，由 `mt7996_mac_full_reset()` 做 WFSYS reset + 重下固件 + 047 的 NPU 重入，不在 AER `device_lock` 内联跑 mac80211 teardown）；**P22/a（P23 实施）改为 fail-fast**：不再排 `reset_work`（ci-173/ci-174 实测该路径 = `Message 00000010`（PATCH_SEM）×10 ≈54 s 全超时 ⇒ `chip full reset failed` + ieee80211 WARNING 风暴），`dev_crit` 报「MCU 不可 in-driver 恢复」后堆上 `delayed_work` **10 s 触发 `kernel_restart("mt7996: unrecoverable MCU fw (PCIe CmpltTO)")`**（受控重启 = 自愈闭环；`kzalloc` 失败降级为打印手动重启提示）。不改 047（F170-A 的非 recover teardown 仍在 full-reset 路径）；不动 `mt7996.h`（避坑 45 同文件 hunk 上下文）。**排序**：同文件 glob 序 0023 < 0153 < 0160，三补丁逐 hunk 干净（`.err_handler` 插入点避开 0160 的 hunk 上下文）。 | open（模板在树内 mt7921；mt7996 上游 pin `be5ce791` 无，pin bump 时复查可否删）；是否回给作者/上游待定 | **本地（可复跑）**：对 mt76 pin 真源 `be5ce791` `patch -p1 --fuzz=0` rc=0；其后 `mt76-0153`/`mt76-0160` 仍逐 hunk 成功；`audit-patches --oc --experimental` **135/60** ✓、`--oc` **62/20** ✓；MANIFEST **138**（61/1/73/3）+ `audit-order.sh`/`gen-order.py --check` 双 rc=0；`audit-upstream-watch.sh --fail-on-drift` rc=0（漂移 0/缺失 0/同址 0，远端 tip 前进 2 = 非失败项）。**实机判据（待，用户侧）**：刷含 F172 的 experimental → 先 `uname -a` 核版本 → 单次 `echo '0,8'` → **未过滤 dmesg** 应出现 `Root Port link has been reset` + `device recovery successful`，且**无** `chip full reset failed`、**无** `CmpltTO` 悬死；Wi-Fi 恢复无需 reboot。失败则用 B 隔离实验（F170 行）判 CmpltTO 是否 047 引起。**[CI 回填（run `34967804641` @ `18733e5`，2026-09-15）]**：**success ⇒ release `ci-173`**（build job 1h17m14s + release job 绿）；`apply-patches.sh . --experimental`（真实构建**不带** `--oc`）报 `hunk 134` + `处理：134 实验档跳过：1`（跳过 = `#OC` 行）`缺失 0`——134 = default 61 + `#EXP` 73，较 ci-172 的 133 **+1 = 本补丁**；同轮 `ORDER ↔ MANIFEST 一致：138 条（61/1/73/3）`；构建日志 **line 176875** `Applying …/package/kernel/mt76/patches/mt76-0023-…patch using plaintext:` + `patching file mt7996/pci.c`（0153/0160 同文件先后各一次，glob 序 0023<0153<0160 实证）；全局 `Hunk FAILED`/`patch does not apply`/`already exists` **0**（仅 4 处文件名含 `FAILED` 的假阳性）。资产 `firmware-experimental.tar.gz` 35,689,548 B；sysupgrade sha256 `762b8ae25ed9424961b95f1978f938e213c0576148cd146ffbbe147efa4cc441`。**实机判据 = FAIL（部分）**。**[实机回填（ci-173，2026-09-15，用户串口）]：PCIe 层通、设备层未通**——单次 `echo '0,8' > /sys/kernel/debug/ieee80211/*/mt76/sys_recovery` 后 `SYS_RESET_COUNT` WM0/WA0 → WM1/WA1；未过滤 dmesg 关键链：`pcieport 0000:00:00.0: AER: Multiple Uncorrectable (Non-Fatal)` + `error status/mask=00004000/00400000` + `[14] CmpltTO (First)` → `mt7996e 0000:01:00.0: PCIE error detected, state: 1`（**本补丁 `error_detected` 确已生效**；state 1 = `pci_channel_io_normal`，正是 Non-Fatal 语义）→ `AER: Root Port link has been reset (0)`（`NEED_RESET` ⇒ `aer_root_reset()` secondary bus reset）→ `AER: device recovery successful`（**ci-172 的 `can't recover (no error_detected callback)` 消失**）；但随后 `mt7996e: Message 00000010 (seq 6..15) timeout` / `Failed to get patch semaphore`×10 → `792.893677 chip full reset failed` → `ieee80211_reconfig` WARNING「Hardware became unavailable during restart.」+ `failed to remove key (-5)` ⇒ **设备层未恢复**（Wi-Fi 仍死，需 reboot）。因果：`NPU detach request failed: -110`（738.662390）**早于** CmpltTO（738.851952）**0.19 s** ⇒ CmpltTO 更像 MCU 卡死的**下游表征**。**判定 = 必要不充分**：修好了「缺 AER 回调 ⇒ 恢复被放弃」，未修好固件重下/MCU（**保留，不劣化**）；下一步转 B 隔离实验（F170 行）。**[ci-174 CI 回填（run `34979326215` @ `774cf55` = B 隔离实验，2026-09-15）]**：**success ⇒ release `ci-174`**；构建日志 `hunk 134` + `处理：134 实验档跳过：1 缺失 0`（与 ci-173 同数——B 不改条数）；`ORDER ↔ MANIFEST 一致：138 条（61/1/73/3）`；本补丁仍在（log **line 177149** `Applying …/mt76-0023-…patch`）；全局 `Hunk FAILED`/`patch does not apply` **0**（4 处为文件名含 `FAILED` 的假阳性）。**B 的构建层 A/B 对照**：内层 `047-wifi-mt76-mt7996-restart-npu-on-full-chip-reset.patch` 在 ci-173 应用 **4 hunk**（#1@2321/#2@2344/#3@2404/**#4@2493**），ci-174 只 **3 hunk**（#4 消失）⇒ P19/B 删「full-reset 停 NPU」hunk 确已进构建。**B 的产物层实证（解镜像，无需设备）**：从 itb 的 rootfs squashfs（xz）取 `/lib/modules/6.18.44/mt7996e.ko`——ci-173 **239,168 B / sha256 `0662d6c271ea5daa25c0548db66f4cc78bc40095eae1fc4cf1f67e75878d5aa3`**，ci-174 **239,112 B / sha256 `2106f42faf8e74a695825701cc49c5332fb089bbf467c233c6997447b2b714d7`（−56 B）**；同镜像其余 13 个 `.ko` 片段哈希两版**逐一相同** ⇒ 两版唯一变量就是 mt7996e.ko。资产 `firmware-experimental.tar.gz` **35,689,375 B**（较 ci-173 −173 B，sha256 `02008936b33bd5bd5338ae0af870b3f0b51694bf5894e8b7e4bcd0fee7ae8a52`）；sysupgrade sha256 **`dc134f8b61dae73305db25ade7da9f64c28659be5bbccd46895385834cf64289`**（18,600,780 B）。⚠ **版本核对口径修正**：ci-174 与 ci-173 的 **kernel 段逐字节相同**（sha256 `dd8bf4da758251f52e9be059…`，FDT 与 squashfs `mkfs_time` 亦同；两版 `/etc/openwrt_release` 同为 `OpenWrt experimental SNAPSHOT r0-1ff2a3e`）⇒ **`uname -a` 两版都报 `6.18.44 #0 SMP Tue Sep 15 11:43:19 2026`，不能用它确认「已刷 ci-174」**；唯一运行时判据 = `sha256sum /lib/modules/6.18.44/mt7996e.ko`（ci-174 = `2106f42f…b714d7`，ci-173 = `0662d6c2…d5aa3`）或刷机前核镜像 sha256。**B 实机判定 = FAIL（分支②，2026-09-16）：NPU stop 被排除，CmpltTO/MCU 死与 NPU 无关；见 F170 行实机结果 + 代码级定位。** **[P23/a CI 回填（run `35014585319` @ `df8c7ad`，2026-09-16）]**：**success ⇒ release `ci-176`**（build job 1h24m + release 绿；`hunk 134` + `处理：134 实验档跳过：1 缺失 0`；`ORDER ↔ MANIFEST 138（61/1/73/3）`；log line 177308 `Applying …/mt76-0023-…patch using plaintext:` → line 177411 `CC [M] mt7996/pci.o` 无 warning/error；全局 `Hunk FAILED`/`patch does not apply` **0**）。**产物层（解 itb 的 rootfs squashfs，二进制安全提取）**：`mt7996e.ko` **240,296 B / sha256 `d5fc13179760f2b04a8b4b03b6db5e61b7db44031a1f5169f95d1261140aa147`**（ci-174 = 239,112 B / `2106f42f…`，**+1,184 B**），三个 a 标记字符串（`controlled reboot after unrecoverable MCU fw crash`／`MT7996 MCU unrecoverable via in-driver reset`／`unrecoverable MCU fw (PCIe CmpltTO)`）各命中 1 次，ci-174 的 ko 0 命中；sysupgrade itb sha256 `6f1e6a2314a1f94f957cd2ddd9dcddbfc93555bd29c4c9fd150636426b421c4f`（18,600,780 B）；内核横幅 `6.18.44 #0 SMP Tue Sep 15 18:58:04 2026`。**[P23/a 实机回填（2026-09-16 05:27，agent 直连 + `sysupgrade` 刷 ci-176，配置保留）]**：单次 `echo '0,8'` ⇒ **未过滤 `/dev/kmsg` 全链**（内核 µs 时戳）：`289.778 [14] CmpltTO (First)` → `289.785 PCIE error detected, state: 1`（本补丁）→ `289.789 AER uncorrectable 0x00000000, correctable 0x00000000`（本补丁；AER 核已清状态位）→ `289.989 AER: Root Port link has been reset (0)` → **`289.996 MT7996 MCU unrecoverable via in-driver reset; controlled reboot in 10s`**（本补丁 dev_crit）→ `290.006 AER: device recovery successful` → `295.529 Message 00000010 (seq 10) timeout` ×**1** + `295.535 Failed to get patch semaphore` ×**1** → **`300.089 mt7996: controlled reboot after unrecoverable MCU fw crash (PCIe CmpltTO, F170/P22-a)`** ⇒ **受控重启在 dev_crit 后 10.09 s 触发**（判据 ≤15 s **命中**）；**无 `chip full reset failed`**、**无 `Message 00000010` ×10 风暴**、**无 `Hardware became unavailable`/`failed to remove key` WARNING 风暴**（唯二单发 = AER 恢复成功后 mac80211 自身重启路径的一次固件下载尝试被 10 s 受控重启截断；`reset_work` 确未再排）。设备 **~25-60 s 内回网**（ping 于 05:27:56 起无响应 + 回网后 `uptime` = 05:28:20 boot）；重启后 `/sys/fs/pstore` **无新记录**（`dmesg-ramoops-0/1` 仍为 09-15 18:24）⇒ **非 panic 的受控重启**；boot 后 4 AP 全起（SSID/信道正确：6G ch37 320MHz / 5G ch149 ×2 / 2.4G ch1）、`SYS_RESET_COUNT` 清零、`dmesg` 零 `CmpltTO`/`chip full reset failed`。⚠ **空口判据的局限（本轮实证，修正坑 58）**：与 AP 同 radio 的 monitor **看不到自身 beacon**（P23 复测：moni0 独立 MAC，`ip link set moni0 down` 后 `iw dev moni0 set channel` 仍 `-16 busy`；健康态 ch6/149 各抓 8 s **只见邻居数十帧（含 LAA 地址）／己方 `58:28:d7:44:` 0 帧**，未见自身 TX 环回）⇒ 空口侧只能用**客户端实连 + rx/tx bytes delta** 或**外部独立嗅探设备**；本轮服务级证据**仅 5G**（刷 ci-176 后 ~7-9 min 3 站点回归 = `phy0.1-ap0` 2 站 + `phy0.1-ap1` 1 站且有 8 s ≈39 KB/s 流量；**当时 2.4G ch6 与 6G ch37 均 sta=0**）；受控重启后 11 min 内四 AP 均 sta=0，且 DHCP 租约仅本机、`ip neigh` 无其他 LAN 设备 ⇒ **客户端当时不在线**（非「连不上」）⇒ 该次重启的判据 C 记 **未验证/证据缺口**（既非通过也非 FAIL；同镜像冷 boot 的正参照 = 05:12 刷机后 ~9 min 内 3 个 5G 站点回归且流量流动），6G 侧因**无 6G 客户端**始终为空口验证缺口。**[P24 §2.1 补证（2026-09-16 08:33 采集；该 boot = 08:19:55 **用户手动热重启**后的 ci-176，非刷机；agent 只读）]**：**2.4G 服务级空口判据命中** —— `phy0.0-ap0`（ch6/20MHz）2 站点实连（`42:72:ce:67:2e:ad` = LIO-AN00、MFP yes，`38:21:87:8e:f0:db` = QCA4002、MFP no；均 authorized+authenticated+associated，`connected time` 161 s / 27 s，`associated at [boottime]` 663.6 s / 797.1 s = **本次 ci-176 启动后关联**）且 8 s delta **rx +1194 B / tx +102 B**（累计 rx 172 KB / tx 268 KB）；同轮 5G `phy0.1-ap0` 2 站点 8 s delta rx +9728 B / tx +20736 B（≈38 KB/s）；**6G `phy0.2-ap0`（ch37/320MHz）仍 sta=0 ⇒ 唯一残余缺口（无 6G 客户端）**。证据 `tmp/p24/ota/ota-083339.txt`（含逐站点 dump + 8 s delta；采集脚本 `tmp/p24/ota/sta-capture.sh`）；**独立佐证**：并发另一会话的 `tmp/p24/2g-rootcause.md`（08:2x–08:37，以 `hostapd` 事件 + `tx_stats` band0 attempts +~11/s）独立得出同一结论，并定位「用户 08:22 看到的 2.4G sta=0」= 08:19:55 热重启后的**重连空窗**（08:30:10/08:30:58/08:33:12 三台上线）。** |
| F173 | **P22：b 重做判定精化（复活 ≠ 可服务）+ 今晨「重启好几次都不行」的 Wi-Fi 全灭配置根因闭环 + 三条诊断纪律（坑 56–58）**（2026-09-16；用户令「wifi 寄了排查解决」→「重做 b」） | ① 用户报今晨三频全灭且**重启多次无效**（`iw dev` 空、`logread \| grep hostapd` = `Invalid country_code` + `add_iface failed`，**dmesg 干净**）；② b（`0,8` 死芯片后 `remove`+`rescan`）须在**真死芯片**上重做，判「能否复活 MCU」与「能否免重启修好」两问 | ① **配置层故障**（非 MCU 死）：`/etc/config/wireless` 同时存在真段 radio0/1/2（`option phy 'phy0'`，US/K2P）与**幽灵段 radio3/4/5**（netifd/wifi-scripts 对「无配置段匹配的 wiphy」自动生成的默认段：SSID OpenWrt、disabled、radio5 `country '00'`），两套**抢占同一 phy0 的三个 radio 槽** ⇒ 合并 inline 配置 `country_code=00` ⇒ hostapd `Invalid country_code` ⇒ add_iface 全灭。**幽灵段来源 = b 第一次实验自己埋的祸根**：rescan 后 wiphy 改名 `phy0→phy1` ⇒ `option phy` 段失配 ⇒ 自动生成默认段（mtime 法证 02:04，与当天首次重启同时）⇒ 之后重启 wiphy 回 `phy0`、幽灵段按 path 仍匹配同芯片 ⇒ 冲突持久（「重启好几次都不行」完全解释）。② b 重做：rescan ⇒ 固件全量重下（**能复活**）、但 beacon 不上空（**不能免重启**）⇒ 「复活 ≠ 可服务」（实机链见 F170 行） | ① 设备侧修复（备份 `/root/wireless.bak-20260916-023426`，md5 `40cf5e45…`）：删幽灵段 ×2 + radio0/1/2 **删 `option phy` 改 `option path 'soc/1fc00000.pcie/pci0000:00/0000:00:00.0/0000:01:00.0'`**（rescan 改名/重启均稳）+ 修引号损坏（`ieee80211r`/`mobility_domain`/`ft_over_ds` 值带字面引号，重设为 1/6616/1）；② **本仓补丁层零变更**（纯诊断/纪律轮）。证据 `tmp/p22-wifi-fix/{wifi-fix-report.md,takeover-reconciliation.md}`、`tmp/p21/b-redo/{VERDICT.md,b-redo-kill.log,b-redo-rescan.log}` | n/a（自持诊断结论与纪律） | **纪律三条（新增；P2–P22 全部仍有效）**：**坑 56** 判「Wi-Fi 死」必须先**分层**——**配置死**（`logread \| grep hostapd` 见 `Invalid country_code`/`add_iface failed`，dmesg 干净）/ **驱动死**（CmpltTO、`chip full reset failed`）/ **beacon 死**（主机侧 `AP-ENABLED` + ubus `ENABLED` 全报好，但空口无己方 beacon），三者处置与战役归因完全不同（本轮两实例：配置死被当驱动死、beacon 死被主机侧 ENABLED 掩盖）；**坑 57** 任何 PCIe remove/rescan 实验后必查 **wiphy 改名**（`phy0→phy1` 致段失配）与**幽灵段再生**（`uci show wireless` 对段数，2026-09-16 02:59 当场抓到）；**坑 58** 判服务级正常必须**空口验证**，**主机侧 ENABLED 不算数**；**方法学更正（P23 实机实证）**：与 AP **同 radio** 的 monitor **看不到自身 beacon**——健康态下同样「邻居数十帧 / 己方 0 帧」（`iw dev moniX set channel` 于同一 phy 报 `-16 busy`；moni0 默认继承 AP 的 MAC 时更被自身帧过滤）⇒ 该法**只能用来确认邻居存在**，**不能**当「己方 beacon 是否上空」的判据（见下「连带更正」）⇒ 可用判据 = **客户端实连 + rx/tx bytes delta** 或**外部独立嗅探设备**；**自扫法无效**（AP 扫描时本 radio 离开工作信道，永远扫不到自己）。**连带更正**：P22 的 b-redo「beacon 未上空」按此口径**方法学已不成立、需重验**——`tmp/p21/b-redo/VERDICT.md` §6 原文即**以 monitor 抓帧为主证据**（ch1 8 s：邻居 200 帧/己方 0 帧）＋旁证（passive-scan 客户端看不到网络、`wifi down/up` 重建无效、用户实测「不正常」）；该轮**并无「客户端实连失败」记录**（§6 反而记「可关联（主动 probe）」）⇒ 该结论只剩旁证、应标「待重验」。**访问面变更**：agent 现可用密码直连设备（`sshpass -p … ssh -F /dev/null -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no root@192.168.123.1`）⇒ p16–p21「agent 无法 SSH、设备命令由用户手跑」**作废**；b 重做已由 agent 端到端执行验证。**免密路径不可用（P23 实测，勿再找）**：设备**无 `/etc/dropbear/authorized_keys`**（现网实读；`dropbear.main.PasswordAuth='on'`、仅 Port 22 一处配置）⇒ 仓库 `.ssh/ssh-device`（`-i .ssh/id_ed25519`，注释 `dsh-host-20260907`）与 `~/.ssh/id_ed25519_xr1710g`（`zcode-agent@xr1710g`）**两把均被拒**（`Permission denied (publickey,password)`；二者都不是默认 IdentityFile 名，只有显式 `-i` 才会被 offer，故「没 offer」不是原因）⇒ **密码 `password` 是唯一可用路径**（`HANDOFF.md` 原「优先免密」一句据此更正）；要恢复免密须先在设备上落 `authorized_keys`。**[已修复（2026-09-16 08:13，用户裁定后 agent 执行）]**：把 `xr1710g-openwrt/.ssh/id_ed25519.pub` 追加进设备 `/etc/dropbear/authorized_keys`（`chmod 600`；实际落在 `/overlay/upper/etc/dropbear/` = UBI overlay ⇒ **重启不丢**），实测仓库 `.ssh/ssh-device` 免密直连 OK、`IdentitiesOnly+BatchMode` 纯公钥亦 OK、密码路径仍可用；`tmp/p23/{flash,a-experiment}.sh` 已改为**免密优先 + 密码回退**（新设备/新宿主若未装 key 会自动回落密码）。**另**：本轮 03:45–05:08 密码一度被改（SSH/LuCI 同拒、主机密钥未变=同一系统未重刷；`pstore` 亦无新记录），用户 05:08 改回 —— 遇到「凭据被拒」应与用户确认而非反复重试（dropbear 会限速）。**顺带复核坑 35（安全面）**：`ttyd` 仍监听 `192.168.123.1:7681`（`option interface '@lan'` + `command /bin/login`，HTTP 200）——因 root **已设密码**，ttyd 只是**需密码的登录页**（非裸 shell）；一旦 root 密码被清空，ttyd 立即退化为 **LAN 内裸 root 通道**（坑 35 条件即成立）⇒ 改动设备凭据时勿留空。**验证与设备态**：03:08 受控 reboot 后三频 4 AP 全起、两客户端自动重连、5G 站点 rx/tx bytes 8 s delta 实测流动（2.4G 站点空闲 ≠ 断流，doze）；内核 `6.18.44 #0 SMP Tue Sep 15 11:43:19 2026`（= ci-174）。**P22 轮 0 commit**，本行 + F170 b 段回填由 P23 落账（commit `86e5c1c`）；a（fail-fast + 受控 reboot）实施见 F172 行。**坑 59（P23 工具面，三条实测边界）**：① 设备**无 `/usr/libexec/sftp-server`** ⇒ `scp` 直接失败（现代 scp 默认走 SFTP），改用 **`ssh 'cat > /tmp/fw.itb' < itb`** 管道；② 本环境**无 moreutils `ts`**，`... \| ts '%.s'` 会**立刻断流并静默产出空文件**——P23 第一次 `0,8` 实验的全部内核日志因此丢失（飞机残骸=空文件），改用 **`ping -D`** 与 **`/dev/kmsg` 自带 `<pri>,<seq>,<usec>` 时戳**；③ **`dmesg` 跨重启不保留、`/sys/fs/pstore`（ramoops）只记 panic 不记 `kernel_restart`** ⇒ 受控重启类判据的证据必须**在触发前**开始流式落盘（本轮 `/dev/kmsg` 流中断点=重启瞬间，反而成为重启时刻的天然时标）。 |
| F174 | **P23：双线 fork tip 对账（fanboy `ubi2-oc` 又一次 force-push 重基 + naoki66 快进）——结论：21 料 20 份零变化、无重取；07 转待评估**（2026-09-16） | 守护 `audit-upstream-watch.sh` 报两处 tip 前进：fanboy `37aa0bcd3b→ca46e0a4e`、naoki66 `c31b73423→5a71e6a53` ⇒ 必须判「纯重基」还是「内容真变」，据此决定 21 份 `patches/vendor/fanboy/*` 原料与 naoki66 来料是否需重取（F151/F154/F157/F168/F171 惯例） | fanboy 线**又是 force-push**（GitHub compare = **diverged**：新线自有 54 提交 / 238 文件，记录侧 22 提交**全部不在**新线祖先链）⇒ tip 不是判据、**必须按提交内容判**；naoki66 线是**快进**（非 force-push）：10 提交 / 37 文件，全部落在本仓不携带的面 | `scripts/audit-upstream-watch.sh`：`REMOTE_TIPS` fanboy→`ca46e0a4e`、naoki66→`5a71e6a53`（各补本轮逐料结论）；**零补丁改动、零 MANIFEST/ORDER 改动** | n/a（fork 漂移，非上游） | **fanboy 21 料逐一核对**（方法承 F151/F168「按内容不按 SHA」：`git patch-id --stable` 集合比对 + GitHub API 逐文件 patch/blob 比对；`git clone --filter=blob:none` @ `ca46e0a4e`）⇒ **20 料零内容变化**：① **13 料**（01/02/03/06/09/10/11/12/14/15/17/20/21）本仓文件 patch-id 与新线某提交**完全相同**；② **5 料**（04/05/08/18/19）本仓文件含**本地编辑**（F13 定档/切片/重 diff）故 patch-id 不等，但 API 逐文件比对证明 fanboy **旧提交与新提交内容完全一致**（04/05/18/19 全同；08 仅少 `package/network/config/wifi-scripts/files-ucode/lib/netifd/wireless/mac80211.sh`）⇒ 差异全在本仓侧、原料有效。**07（`mt76: mt7996: release HW_RRO session on BlockAck teardown`，源 `5b917d4b`）在新线消失**：全history 无同 subject 提交，其新增的 in-tree `package/kernel/mt76/patches/0014-wifi-mt76-mt7996-release-HW_RRO-sessions-on-teardown.patch` 亦不在新 tip 的 `package/kernel/mt76/patches/`（新 tip 仅 0001/0003/0005/0010/0011）⇒ 转**待评估**（fanboy 弃用/换法未知）；本仓材料仍自洽（`apply-patches` dry-run 全绿、目标 mt76 源 pin `be5ce791`/`01367e60` 无该修复）故**仍按现状应用**。**观察项**（本仓未采用，仅记账）：13 的 `package/kernel/mt76/Makefile`（mt76 源 pin）与 16 的 `wifi-scripts/…/mac80211.sh` 新旧**确有真内容变化**（16 本仓早已自行重 diff 为 `root/9010-wifi-scripts-txpower-ucode.patch`）。**naoki66 侧**：10 提交/37 文件全在本仓不携带的面（自有 LuCI 应用 mesh-conf/mlo/netmode + feed 补丁脚本 + airoha `board.d/02_network`/`uci-defaults` 迁移）；本仓实取的 7 个文件（mac80211 subsys-411、622/625/628/743/744）blob 两侧全等且仍在、`mt76-0012` 源提交 `2d3aa30` 仍是新 tip 祖先、`luci-app-airoha-recovery`（F12 来源）未被触碰 ⇒ **无重取**。资产 `tmp/p23/fanboy/`（blobless clone @ `ca46e0a4e`）、`tmp/p23/fanboy-{changed,ourpaths,intersect}.txt`、`tmp/p23/fanboy-sha-status.txt`。**加强核对（P23 收尾，GitHub API blob 级）**：① **19 号 73/73 文件 blob 全等（含 20 个二进制文件）⇒ 全量核对通过**——此前「patch 文本比对」对**二进制文件是空比对**（`patch` 字段缺失 ⇒ `None==None`），不能算证据，已补上；② **8 料**（04/08/09/10/11/13/16/20）存在**文件终态 blob 不同**者（多为 `config-6.18`/`netdevices.mk`/`an7581.mk`/`dtsi` 等基线文件），但**逐文件 delta（归一化 `@@` 行号后的 patch 文本）全部相同**、且 08 仅**少一个文件**（`wifi-scripts/…/mac80211.sh`）⇒ 差异 = **fanboy 基线前进导致的文件终态变化**，不是原料 delta 变化（**唯一例外**：13 的 `package/kernel/mt76/Makefile` 与 16 的 `wifi-scripts/…/mac80211.sh` 的 **patch 文本本身有变化** = 已列的观察项；本仓 13 未采用、16 早已自行重 diff 为 `root/9010`）。**结论不变：无重取**。**方法论**：「blob 不同」**不能**推出「原料变」（会把基座移动误判为原料变化）；判据 = **delta + 新增/删除文件集**，blob 全等则足以证明「零变化」。**另**：`compare/<old>...<new>` 的 `files` 数组（本轮 238 文件 / 与本仓 123 路径交集 122）是**两 tip 的树差异**，其中含**本仓旧侧 22 个提交自身**的改动 ⇒ **交集大小不能当「新 tip 动了这些文件」的证据**，本轮判定**未**使用该交集。**07 已由 F175 收口（保留 default 档，删除门槛见 F175）**。 |
| F175 | **fanboy `07`（mt7996 HW_RRO 会话释放）在当前新线缺席的定性 —— 非「上游已修」、非「弃用/换法」，且**仍在 fanboy archive 线上** ⇒ 本仓 default 档保留，不可删**（2026-09-16；F174「07 转待评估」收口） | F174 对账发现 fanboy 新线（`ubi2-oc` @ `ca46e0a4ec`）既无同主题提交、也无其内层 `0014-…-release-HW_RRO-sessions-on-teardown.patch`（新线 mt76 patch 仅 0001/0003/0005/0010/0011）⇒ 必须判「其弃用（上游已修？）」还是「换法」，以决定本仓 `patches/vendor/fanboy/07-…-5b917d4b.patch`（default 档）能否删 | ① **不是上游已修**：新线 mt76 pin 与本仓**同一提交**（`01367e60`，见 `patches/root/9067-…`），且该 pin 的 `mt7996/mcu.c` 与 `openwrt/mt76` **master 逐字节相同**（sha256 `17d19d23…`、diff 0 行）——两者均**无** `rro_id`、无 BA teardown 释放（`mt7996_ampdu_action` 的 `IEEE80211_AMPDU_RX_STOP` 分支仍不取会话 id），也**无**亚 64 旁路（`ba_rdd_rro = !tx && enable && mt7996_has_hwrro(dev)` 无条件开）⇒ trigger（Apple 亚 64 窗口 + wcid 复用 ⇒ FOE_ID=0 停摆）依旧存在。② **不是弃用（推翻 F174 的「消失」判读）**：`ubi2-oc-archive-260830` 的 tree **仍有** `0014-wifi-mt76-mt7996-release-HW_RRO-sessions-on-teardown.patch`，与本仓 07 的内层补丁**逐行等同**（仅 `@@` 上下文行号随各自基线不同：`main.c` 1713↔1653、`mcu.c` 1043↔779 / 1055↔791、`mt7996.h` 300↔301）⇒ 07 仍在 fanboy **活跃线体系**（archive）上，当前 `ubi2-oc` 只是又一次重基线、尚未搬回；且**不是换法**：当前线 5 份 patch 无等价物（**精确模式** `grep -inE '\brro\b\|hw_rro\|hwrro\|wed_rro\|RRO_BA_SESSION\|RRO_DEL\|blockack'` 只命中 0003 里**既有** `mt7996_mcu_wed_rro_reset_sessions` 的两行定义/声明上下文；⚠ 宽松 `-i rro` 会被 **error** 命中，不可作判据）。本仓 21 料 patch-id 与当前线「触 `package/kernel/mt76` 的 15 个提交」交集仅 `14`（=`0c882157e5 mt76 patches`）与 `15`（=`4f8ce8cbcd txpower control`），**07 的 `16bb605f…` 零命中**；当前线 first-parent 顶部 60 提交（精确模式）无 RRO/BlockAck 主题。③ **`compare/behind_by` 不可作祖先/来源判据**（方法更正）：老线 SHA 在 force-push 后是孤儿对象——`5b917d4b`→`behind_by=7`，而本仓 **15 料 `d7aa0235`→15**、**21 料 `4e2b80ff`→245**（自证不一致）⇒ 判「料是否还在某条线」只能查 **tree/内容**（`contents/package/kernel/mt76/patches?ref=<branch>` + 逐行/patch-id 比对）。④ **附带发现（provenance）**：本仓 `#EXP mt76-0017` 的 patch 体与 `ubi2-oc-archive-260818` 的 `0014-…bypass-hwrro-for-sub-64-window-ba.patch` **逐行等同**（仅头部注释不同）⇒ 该料实为 fanboy 线产物（F13/P2 记的「Gilly 032」为其载体）；同线 `0015-…avoid-stale-npu-wcid-reuse.patch`（**本仓未携带**；语义 = 重关联多 TID 站点不复用带陈旧 NPU 上下文的 wcid）⇒ 转 P25 评估；**P25 已判定它与 07 互补而非替代**（F176 行：机制不同路、可共存，`--fuzz=3` 双应用后两处防复用点不重叠）。⑤ **档位不对称（保留的独立理由，与 fanboy 意图无关）**：07 = **default**（MANIFEST 第 320 行无 `#EXP`、ORDER 83；2026-08-31 ci-74 毕业后 **stock+experimental 都带**），`mt76-0017` = **#EXP 仅实验档**（MANIFEST 72 / ORDER 105）；全量 grep 本仓补丁仅此两条触及 `hwrro/ba_rdd_rro` ⇒ **删 07 = stock 档失去唯一 RRO 缓解** | `patches/vendor/fanboy/07-mt76-release-hw-rro-session-5b917d4b.patch` **保留**（default，ORDER 83）；`scripts/audit-upstream-watch.sh` `REMOTE_TIPS` 结论补注「当前 `ubi2-oc` 缺席 07（重基线未搬回），`archive-260830` 仍带 ⇒ **不删、不重取**」 | carried（未上游；pin 与 master 现均无等价修复；未见公开提交/邮件列表讨论） | **删除门槛（可核；二者任一出现才可删）**：① pin/上游 driver 在 BA teardown 路径释放 RRO 会话（`RX_STOP` 分支带会话 id + `UNI_RRO_DEL_BA_SESSION`）；② 亚 64 窗口不再启用 HW_RRO（`ba_rdd_rro` 带 `buf_size >= 64`）。⚠ 与 07 互补的本仓 `mt76-0017`（#EXP，亚 64 旁路）**同样未上游**（openwrt-devel 有投递、master 未合），且 07 是 **stock 档唯一** RRO 缓解（见 ⑤）⇒ 两条都不能随「当前新线缺席」而删。**P25 评估项**：`ubi2-oc-archive-260818` 的 `0015-…avoid-stale-npu-wcid-reuse.patch`（本仓未携带的第三条思路：不给重关联的多 TID 站点复用陈旧 NPU 上下文的 wcid）。证据：`tmp/p24/fanboy07/`（pin 与 master 的 `mcu.c` 双份 + 零 diff、GitHub 提交 patch、两份 patch-id 表、`contents` 全分支扫描结果、`archive-260830` 的 in-tree 0014 与本仓 07 内层补丁的逐行比对） |
| F176 | **P25：fanboy `archive-260818` 的 `0015-…avoid-stale-npu-wcid-reuse`（不让重关联多 TID 站点复用陈旧 NPU wcid）与 07 **互补不替代** ⇒ 本仓不吸收（firmware/NPU 假设不可核 + 副作用不可控）**（2026-09-16；F175「转 P25」收口） | 07 治「RRO 会话未释放 ⇒ 复用同 wcid 时继承死上下文」；0015 治「NPU 不清陈旧 per-wcid 上下文 ⇒ 复用任何旧 wcid 都有风险」，且只对多 TID（iPhone 类）站点启用 | ① **非替代**：0015 不重写 07 的 `RX_STOP` 分支，也不动 `hwrro/ba_rdd_rro`（属 wcid 分配器/WTBL 生命周期面）；对 pin `01367e60` 单独 `--fuzz=3` 可应用、且 07+0015 **双应用成功**（`main.c` 两处防复用点不重叠：1229 WTBL clear / 1296-1302 `mt76_wcid_mask_clear` + `npu_stale`）⇒ 二者可共存。② **不吸收**：核心假设（「一次轮转的中间关联流量足以冲掉陈旧 NPU 上下文」）在驱动侧**不可核**——依赖未公开的 NPU 固件内部状态；副作用面（`MT76_N_WCIDS` 1088 索引的 `npu_stale` 位图只增不清，外加「fresh 池耗尽后轮转复用」的启发式）**不可控**且无法在本仓实机判据下验证。③ **档位**：`patches/vendor/fanboy/07-…-5b917d4b.patch` **保持 default 不变**（ORDER 83） | `vendor/fanboy/07` 保持原样（**不新增** 0015 补丁） | n/a | 复跑（本仓已落 `tmp/p25/verify-0015-07-complementary.sh`，实测绿）：`bash tmp/p25/verify-0015-07-complementary.sh`。⚠ 对 pin 源码 **`git apply`/`patch --fuzz=0` 单跑任一料都干净**，但**双跑必冲突**（正反序都冲突：07→0015 挂在 `mt7996.h:301`、0015→07 挂在 `main.c:1713`）——因为 0015 的 hunk 上下文里有 07 尚未加入的 `rro_id[]`/`mt76_rx_aggr_start` 改动，**与 07 改的是同一区域**；改用 `patch -p1 -l --fuzz=3` 双跑即成功（`c1`/`c2` 均实测）。该冲突是**上下文耦合**、非语义替代：双应用后 `main.c` 的 `rro_id`（07，WTBL 面）与 `npu_stale`/`rx_ba_tids`（0015，wcid 分配器面）两处防复用点并存。承 F175 的删除门槛不变：07 的保留与 0015 无关。 |
| F177 | **P26：门禁脚本回归修复 —— `scripts/audit-upstream-watch.sh` 的 `REMOTE_TIPS` 元素未转义反引号 → bash 命令替换 → rc=127（F176 起 CI 守卫无条件红）**（2026-09-16；F176 落账时引入） | F176 的 docs commit 往 `REMOTE_TIPS` 的 fanboy/Gilly 双引号字符串里写了行内代码反引号（如 `--fuzz=3`、`3d4ff76d3→38806db2d`）；bash 在**数组赋值**时对该元素做命令替换，把这些词当命令执行 → `未找到命令`（rc=127）。该脚本被 `.github/workflows/sync-upstream.yml:43` 以 `--fail-on-drift` 调用，rc=127 = 守门步骤**无条件失败**，与是否真漂移无关 | 双引号字符串中的未转义反引号（bash 命令替换）；`bash -n` **不报**（这正是它能过本地检查的原因） | 修正 `scripts/audit-upstream-watch.sh`（`tmp/p26/fixbacktick.py`：REMOTE_TIPS 双引号行内 18 处反引号全替换为单引号）；不改语义、不动任何补丁 | n/a（本仓脚本修复） | 逐版本实证（同树）：`a4688dd`/`4fbbae1` 反引号 0、rc=1（脚本正常执行、树被 dry-run 污染）；**`ae3caae` 反引号 2、rc=127**（回归起点）；`0996375` 反引号 2、rc=127；`f5971df` 反引号 18、rc=127。修复后 `bash -n` 通过、干净树 rc=0。同型全仓扫描（`*.sh` 双引号行）空。证据 `tmp/p26/regress/evidence.md`；复跑 `python3 tmp/p26/fixbacktick.py scripts/audit-upstream-watch.sh && bash scripts/audit-upstream-watch.sh $W/tmp/drift/owrt --fail-on-drift`（期望 rc=0）。**教训（坑 69）**：往 shell 数组/字符串里写散文（含反引号、`$`）必先判引号语义；`bash -n` 查不出命令替换。 |
| F178 | **P26：Gilly `38806db2d` 完整逐料对账 —— 81 份按 blob sha 全核（79 份零变化、+2 新料）；两个新料均不吸收（985 PCIe 端口 IRQ 亲和性 / netifd-hotplug-20-packet-steering）**（2026-09-16；F176「转 P26」收口，坑 67 方法） | 守护报 Gilly tip `3d4ff76d3→38806db2d`（1 提交）；F176 只做了「新增/删除」初判，未逐一核对其余 79 份 | compare=**ahead 1**（非 force-push）：`38806db2d`「Update Kernel-6.18.52 SSH and rebase」= 4 文件（+985.patch、+netifd-hotplug-20-packet-steering、改 `Openwrt_Gemtek_w1700k.sh`、+一个 itb 产物）；逐 blob sha 比对两侧 `contents`：**79/81 逐字节相同（同 sha）**，`048` 在旧 tip 缺失（= F153 已吸收、随后被 6.18.52 重基移除）、新增 2 份即上述文件；`openwrt-add-patch` 映射未变 | **不新增补丁**；锚点 `3d4ff76d3→38806db2d`（REMOTE_TIPS 重锚） | ① **985（PCIe 端口 IRQ 亲和性）= 不吸收**：机制与设备实测**双证**——`pcie-mediatek-gen3.c` 的 `MSI_FLAG_NO_AFFINITY` + chained parent ⇒ 每 MSI 亲和性只读、端口 IRQ 无 `/proc/irq` 条目；本机 `/proc/interrupts` **irq 52/53（mt7996e / mt7996e-hif）100% 落 CPU0**，且 52/53/6/7/19/22 的 `smp_affinity_list` **写 rc=1（只读）**、而 GIC SPI 40/34 写 rc=0（可调）⇒ 诊断成立、与社区 t/222776 #3902「mt7996e 绑死 CPU0 改不了」一致。**但不吸收**：本机 CPU0 实测 `top` 97% idle / load 0.13、irq 52 约 190/s、`/proc/interrupts` CPU0 列 IRQ 占比 0% ⇒ **无实测饱和症状**，属 PCIe 控制器级改动、revert 成本高于当前收益；上游 mainline（`NO_AFFINITY` 第 604 行）与本仓 6.18.44 树**均仍 chained**（OpenWrt 侧只有 `966-…WIFI-HW-reset`，不涉 IRQ）⇒ 留作**可评估项**，触发条件 = 出现 CPU0 中断延迟/饱和症状或 F172 复现需要。② **netifd-hotplug-20-packet-steering = 不吸收（且本仓路线不宜）**：该脚本在接口 `add` 后延时重跑 `packet_steering`（补 WiFi 接口晚于 netifd 事件、`rps_cpus` 停留 0 的缺口）。本机实测 WiFi 接口 `rps_cpus` **已是 8**（phy0.0/0.1/0.1ap1/0.2 全 8）、`packet_steering` 服务 enabled、无 `packet_steering` uci 项 ⇒ 所述缺口**当前未咬人**；且本仓走 NPU offload，社区与 F101/B4 研究（t/222776 #3902）明确「跑 NPU 就不该开 packet steering」⇒ 让 steering 更主动地追 WiFi 接口**与路线相悖**。③ **非新料复核（坑 67 口径）**：`033`（release-hwrro-sessions-on-teardown）与新 tip 的 hunk 体**逐行等同**（57 行，仅 5 处行尾空白差异）⇒ = 本仓 07；`037` 的 4 字节结构修正 pin `01367e60` 已有。 | 证据 `tmp/p26/gilly/{old,new}.tsv` + `985.patch`/`ps.uc`/`g033.patch`/`g037.patch`；985 对**本仓 6.18.44 裸源 `patch -p1` 实测干净**（rc=0，`request_irq`/`free_irq` 落位）；设备只读取证 `tmp/p26/affinity-probe.sh`（写测试后已还原 0-3）。**注**：985 与本仓 `mt76-0023`（`mt7996/pci.c`）**不同文件、不冲突**。 |
