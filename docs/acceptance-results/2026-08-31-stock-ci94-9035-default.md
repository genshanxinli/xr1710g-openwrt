# 2026-08-31 stock ci-94（9035 转 default 后）fresh flash 复验

> 审查对象：build run **#94**（workflow_dispatch all，main `a820ea0`）stock 档 Artifact `firmware-stock`。
> 固件：`openwrt-stock-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade.itb`，sha256 `7a3d9e1e1ea8913e1bdd664d6c93d6f396f42491be7cdca03aa333d1be6d6522`。
> 说明：#94 为 all（stock/oc-1.3/oc-1.4），stock 档与修复后 main `aba19bf` 等价（aba19bf 仅重建 #EXP 04 的 config 上下文，stock/oc 不应用 04）。
> 基线：openwrt master `93cf01b`，kernel 6.18.44，`DISTRIB_DESCRIPTION='OpenWrt stock SNAPSHOT r0-93cf01b'`。
> 实机：`root@192.168.123.1`，2026-08-31 以 `sysupgrade -n` 清空 overlay 全新刷入。

## 0. 结论

- **9035 default 在 stock 档通过**：`CONFIG_NET_AIROHA_FLOW_STATS=y` 生效；dmesg 出现预期 `NPU flow stats unavailable (-22), continue without per-flow hw stats`；NPU offload 存活（`offload_bound=24`、`offload_total=131`、conntrack `[HW_OFFLOAD]`=55）。
- **LED 首启探测**：`98-xr1710g-led-sysfs-prefix` 自动改写为 `mt7530-0:*`；`led start` rc=0，6 LED `offloaded=1`。
- **bridge-flow-offload**：已安装，`nft list table bridge flow_offload` 存在且 `flags offload`。
- compat_version 2.0、fw4 flow_offload 默认、三频、device-hw-probe 全绿。
- F66 仍为已知良性 `rdinit=/init failed: -2`。

## 1. 通过项

- [x] **9035 FLOW_STATS=y 共存**：dmesg `airoha_eth ...: NPU flow stats unavailable (-22)`；NPU offload 存活
- [x] **E2**：`OpenWrt stock SNAPSHOT r0-93cf01b`
- [x] **compat_version 2.0**：/etc 与 /rom
- [x] **fw4 flow_offload 默认**：`flow_offloading='1'`、`flow_offloading_hw='1'`
- [x] **LED**：首启探测改写 `mt7530-0`；`led start` rc=0，6 LED `offloaded=1`
- [x] **bridge-flow-offload**：已安装；`nft list table bridge flow_offload` 有 `flowtable br_offload` `flags offload`
- [x] **NPU offload**：`npu_loaded=true`、`offload_bound=24`、`offload_total=131`；conntrack `[HW_OFFLOAD]`=55
- [x] **三频**：2.4G ch6 20MHz 30dBm；5G ch149 HE80 30dBm；6G ch37 EHT320 29dBm
- [x] **F75/A12 device-hw-probe**：route_a-d rc=0；B7 EFR32 absent
- [x] **包集合**：207 包，含 `bridge-flow-offload-1.0-r1`

## 2. 未闭环（延续既有口径）

- [ ] F66 rdinit 假警告
- [ ] C2/C3/F69/F71/B2/D3 物理对端/客户端项
- [ ] oc-1.3/oc-1.4 实机复验（#94 构建已绿，产物未刷）
