# 2026-09-07 D3 长稳检查（experimental r0-93cf01b 连续运行 7 天）

> 检查方式：SSH 只读巡检 + `scripts/device-hw-probe.sh`（F75/A12 全量）+ `scripts/device-npu-probe.sh`。
> 检查时间：2026-09-07 17:00–17:30 CST。本记录同时作为新宿主（`~/项目/xr1710g_openwrt`）ssh 通道重建后的首轮实机验证。

## 1. 固件与运行时长

| 项 | 值 |
|---|---|
| 固件 | `OpenWrt experimental SNAPSHOT r0-93cf01b`（E2 档位标识 ✓） |
| 内核 | 6.18.44（airoha/an7581，`gemtek,xr1710g-ubi`，squashfs） |
| 批次 | 2026-08-31 experimental（含 9035 default：dmesg `NPU flow stats unavailable (-22)` 标志线 ✓） |
| 系统 uptime | **7 天 0 小时 29 分**（606551 s，检查时点）——**零重启** |
| PPPoE 会话 uptime | 337213 s（≈3.9 天，09-03 前后 ISP 侧会话重建一次；设备本体未重启） |
| pstore/ramoops | 空——7 天无 crash 记录 |

## 2. D3 判定

- **时长达标**：7 天 ≫ 72 h。
- **零内核异常**：dmesg 全缓冲 413 行，error 模式匹配 5 条全部为良性（3× ramoops 命名、1× ubi `corrupted PEBs: 0`、1× 已知良性 `rdinit=/init failed` 仅启动 1 次）；logread 仅 3 条（1× 某 STA 的 WPA group key 握手失败——客户端侧；2× dnsmasq DNS-rebind 拦截——预期防护行为）。
- **无内存泄漏迹象**：used 191 MB / 1.87 GB，MemAvailable 1.63 GB（无 day-0 基线，弱证据，仅记录）。
- **负载条件未达标**：本周 WAN 收发仅 0.95 GB / 1.85 GB，loadavg ≈ 0——属"轻负载家用长稳"，`2×10G 对打 + 三频高负载` 压力条件仍未满足，**D3 整项保持 open**，本记录作为时长维度的强证据。
- **issue #7 关联**：本周未执行 `flow_offloading_hw 1→0→1` / fw4 reload 触发序列，未见整机无声重启（uptime 连续）；9036/9037 防御补丁的实机触发验证仍待做。

## 3. NPU / offload 健康（B5）

- NPU loaded：`TLB7.7.0.0_v03`，8 cores @ 800 MHz；`getStatus` RPC 正常（#22），5 个 memory regions；`offload_bound=17 / total=85`。
- PPE：`ppe_BND=40–42`、`ppe_entries≈128`、`conntrack_hw=52–58`（巡检期间多次采样均有活跃 HW offload 流）。
- WiFi 客户端硬件卸载活跃：如 `LOK-360`（5G）`hw_offload=50/28` 条流。
- fw4 flowtable `inet fw4/ft` `flags offload` 在位（wan/lan1–3/pppoe-wan/全部 AP）；`flow_offloading=1`、`flow_offloading_hw=1`、`compat_version '2.0'`（P2 修复与 F65 项在位）。

## 4. F75/A12 硬件探针（7 天后复跑，4 路 rc=0）

- LED 子系统：状态 LED 读写恢复正常，PHY LED hw-offload 生效、无 EINVAL；C5 恢复后清单正常。
- B2.1：双 10G PHY driver=`RTL8261BE 10Gbps PHY`；VEND1 `0x103=0x8261`、`0x104=0x1141`（lan1 phy0x08 / lan2 phy0x05 双双命中）→ RTL8261BE 判据 ✓。注：本镜像重刷后 phytool 丢失，本轮已 `apk add phytool` 补齐。
- B7：EFR32 缺席断言 OK（uart2/hsuart3 absent）。
- 端口计数：全部端口 `rx_errors=0 tx_errors=0`；`lan2/wan carrier_changes=13（up7/down6）`——外部链路重协商（下游设备 2.5G 链路通断），非设备故障。
- 温度/风扇：单点采样 60.3 °C，pwm1=79 且仅 1 个写入者（S99fan）。

## 5. WiFi

三频在位：2.4G ch6/20MHz、5G ch149/80MHz（双 BSS）、**6G ch37/320MHz（EHT320）**；检查时 5 个站点在线。6G 仍无客户端（C2 客户端侧判据继续 pending）。

## 6. 环境备注（新宿主首验）

- 新宿主无 `.ssh/`：本轮重建 `id_ed25519` 并装入设备 `authorized_keys`；新增 `.ssh/ssh-device` wrapper——本宿主 `/etc/ssh/ssh_config.d/20-systemd-ssh-proxy.conf` 属主损坏导致系统级 ssh 启动失败，wrapper 固定 `-F /dev/null` 绕过，并按"首参含空白即远程脚本体"自动补 `DEVICE_HOST`（默认 `root@192.168.123.1`），兼容 `device-*probe.sh` 的 `$SSH_CMD 'body'` 与手工 `ssh-device user@host 'cmd'` 两种用法。
- 踩坑记录：本沙箱每次 bash 调用的 `/tmp` 相互隔离，probe 的 `OUT_PREFIX` 必须指向工作区（本轮用 `tmp/device-hw-probe.route_*.log`）。

## 7. 结论

experimental `r0-93cf01b`（#88/#96 批次）作为日常固件连续运行 7 天：零重启、零内核报错、零 crash 记录、NPU/offload/三频/风扇/LED/双 10G PHY 全部健康——**D3 时长维度达标**；压力负载与 6G/10G 物理对端项（C2/C3/B2/F69/F71）维持原有延后口径。`02 EIP93`、`04 DSA`、`9029 JCPLL`、`mt76-0010` 四条 `#EXP` 在本 7 天窗口内随镜像无故障运行，可作毕业评审的旁证（正式毕业仍按各自实机判据）。
