# 实机验收记录 — stock ci-69（commit 46600b2 / workflow run 32621215717）

- 日期：2026-08-30（UTC）；设备本地 CST 2026-08-30
- 设备：Gemtek XR1710G（`gemtek,xr1710g-ubi`），HTTP U-Boot 2026.07-00766-g53b73174c0fc
- 固件：pre-release `ci-69` = `firmware-stock.tar.gz`（all run，commit `46600b2`）
  - `DISTRIB_REVISION='r0-3d1645e'`，kernel `6.18.44`，builddate 2026-08-21 18:07 UTC
  - sysupgrade.itb sha256 `79ed39c062aaf1a7670221854008cf7f3098777aad4d22fe8de396e2cfedff21`
- 访问：ssh root@192.168.123.1（本仓库 `.ssh/ssh-device`）
- 结论：**远程可验 stock 项全部通过，除 F66（已知良性假警告）与 E2（旧构建无档位标识）两项外；物理项（C2 6G 客户端、C3 外部对端 iperf3、B2 双 10G 对打、A3 恢复页、D3 72h）留待物理条件。** 详细记录见下。

## 一、HANDOFF 7.1 stock 清单逐项

| 项 | 结果 | 证据/说明 |
|---|---|---|
| 冷启动无 `rdinit=/init failed`（F66） | ❌ 已知良性 | `dmesg` 每次启动仍见 `check access for rdinit=/init failed: -2, ignoring`；`/proc/device-tree/chosen/bootargs` 无 `rdinit=`（HTTP U-Boot 默认 env 覆盖 DTS chosen）。见 FIXES F66。 |
| `/proc/mtd` 布局：ubi=0x1b700000、reserved_bmt=0x04200000（F64） | ✅ | `mtd2: 1b700000 "ubi"`，`mtd3: 04200000 "reserved_bmt"`；`/proc/partitions` mtdblock2=449536KiB、mtdblock3=67584KiB。 |
| `ubinfo -a` 无坏块；多轮重启不新增（F64） | ✅ | bad PEBs=0；max/mean erase counter=2/1；2 轮 reboot 后 bad PEBs 仍 0、max erase counter 仍 2。 |
| fan 单写入者；曲线随温切换；fancontrol 可读可设（F67/A4） | ✅ | `/etc/init.d/fan start` rc=0；`ps` 仅一个 `S99fan`；nct7802 pwm1=74、fan1_input≈1210；`ubus call luci.fan getStatus` 可读（temp_board=51、fan_rpm=1212、fan_pwm=74、uci_mode=auto）。RPC 可设：`setManualPwm pwm=80` 返回 success 且 `uci_manual_pwm` 127→80，`setMode auto` 返回 success，随后已恢复 `uci_manual_pwm=127`。 |
| `led start` rc=0；PHY LED 无 EINVAL（#14/9031） | ✅ | **本机修复**：ci-69 stock 默认 `/etc/config/system` 的 sysfs 名为 `mt7530_dsa-0:*`（适配 eb7a45b 新内核），但本 stock 内核（3d1645e）实际 LED 为 `mt7530-0:*`。已将设备 UCI 4 个 1G LED 改 `mt7530-0:09/0a:*`，删除 10G LED（本构建无 phy5/phy8 LED）。`led start` rc=0；4 个 PHY LED `offloaded=1`、`device_name` 正确；`logread` 无 EINVAL。 |
| `getStatus` RPC 5 个 NPU memory regions（#22/F63） | ✅ | `ubus call luci.airoha_npu getStatus` 返回 `npu-ba/npu-binary/npu-pkt/npu-txbufid/npu-txpkt` 共 5 个。 |
| 有损链路 `tx_retries>0` 时 `tx_failed≈0`（F68/A5） | ✅ | 5G `phy0.1-ap1`：tx_packets=78995、tx_retries=74027、tx_failed=56（0.076%）；5G `phy0.1-ap0`：tx_retries=12937/1693，tx_failed=1/2；2.4G：tx_retries=397/106，tx_failed=0/0。终端失败计数远小于重试计数，mt76-0009 生效。 |
| 三频 AP 正常（C1/F72） | ✅ | radio0 2.4G ch6/20MHz（auto，配置 EHT40）、radio1 5G ch149/HE80（iQOO 兼容默认）、radio2 6G ch37/EHT320/29dBm；`wifi status` 三 radio `up=true`，4 个 BSS `AP-ENABLED`。 |
| 6GHz C2 双侧国家码判据（F72/A9） | ⚠️ 仅 AP 侧 | AP 侧 US regdb + 6G EHT320 + 29dBm up；无 6G 客户端，客户端国家码/可见可连未验。 |
| C3 无线速率外部对端 iperf3（F74/A11） | ⚠️ 未测 | 需外部对端 + 160/320MHz 客户端；本会话不具备。 |
| 管理面改址回连 B6（F74/A11） | ➖ 按用户要求取消 | 见 HANDOFF 7.4（B6 已取消）。 |
| `device-hw-probe.sh` 全绿（F75/A12） | ✅ | 脚本增加 DSA 前缀自动探测（`mt7530_dsa-0` / `mt7530-0`）后，4 路 rc=0；B2.1 10G PHY VEND1 `0x103=0x8261`、`0x104=0x1141` → RTL8261BE 10Gbps PHY（已 `apk add phytool`）。B6 MDIO errors 全 0；B7 EFR32 缺失 OK。 |

## 二、ACCEPTANCE 其他远程可验项

| 项 | 结果 | 证据/说明 |
|---|---|---|
| A2 `sysupgrade -T` 兼容（F65） | ✅ | ci-69 `*-squashfs-sysupgrade.itb` 上传设备 `/tmp`，`sysupgrade -T` rc=0。 |
| A2 实际 sysupgrade 往返 | ⚠️ 未做 | 同版重刷会重置/保留配置且本轮无需；以 `sysupgrade -T` 为 stock 放行判据。 |
| B1 WAN | ✅ | PPPoE `pppoe-wan` up，IPv4 172.27.26.33；ping 8.8.8.8 3/3 通；DNS 解析 openwrt.org 正常。 |
| B2 双 10G 口 | ⚠️ 未测 | lan1 NO-CARRIER（无对端）、lan2 2500/Full（对端 2.5G，非 10G）；eth0 CPU 侧 10G/Full。需 2×10G 对端。 |
| B3 剩余 1G 口 | ✅ | wan 1000/Full、lan3 1000/Full，SSH/网关正常。 |
| B4 LAN 网关 | ✅ | br-lan 192.168.123.1/24；dnsmasq 4 租约；LAN 客户端在线。 |
| B5 NPU offload | ✅ | `getStatus` npu_loaded=true，offload_bound=2、offload_total=74；PPE entries 有 BND/UNB；`device-npu-probe.sh` rc=0，conntrack `[HW_OFFLOAD]` 计数 10+；IPv6 专项 rc=0，采样出现 `conntrack_v6_hw=4`、`ppe_BND IPv6=2`（TCP 打流端点 speed.cloudflare.com 不可达，见下）。 |
| C1 三频 | ✅ | 见上。 |
| D1/D2 风扇与温度 | ✅ | 见上；hwmon0/1 mt7530 10G PHY 68/52°C，hwmon2-4 mt7996 三射频 56/50/57°C，hwmon5 nct7802 temp1≈51°C、fan_rpm≈1210。 |
| D3 72h 长稳 | ⚠️ 未测 | 本会话 2 轮 reboot + 多探针后无内核报错；累计运行未达 72h。 |
| E2 版本元数据 | ❌ 旧构建限制 | `DISTRIB_DESCRIPTION='OpenWrt SNAPSHOT r0-3d1645e'` 无档位标识；仓库已在 `scripts/build.sh`/`.github/workflows/build.yml` 注入 `CONFIG_VERSION_DIST`，待下一轮 stock 构建后实机复核。 |
| stock 包集合 | ✅ | ci-69 manifest 206 个包；设备 `apk info` 207 个包，差集仅 `phytool`（本会话安装，用于 B2.1）。 |

## 三、修复/改动

1. **设备 UCI LED 配置修正（本机，针对 ci-69/3d1645e 旧内核）**：`/etc/config/system` 四个 1G LED sysfs 由 `mt7530_dsa-0:*` 改为 `mt7530-0:*`；删除 `led_lan1_green`/`led_lan2_green`（本构建无 10G PHY LED）。`led start` rc=0。
2. **`scripts/device-hw-probe.sh` 增加 DSA 前缀自动探测**：route_b 的 MDIO bus 与 route_c 的 LED sysfs 前缀在 `mt7530_dsa-0`（新内核）与 `mt7530-0`（3d1645e 旧内核）之间自动选择，两代内核均可全绿。
3. **设备安装 phytool**（`apk add phytool`）：B2.1 10G PHY VEND1 判据打通。

## 四、附加观察（非 7.1 stock 判据）

- `scripts/device-wifi-downup-probe.sh`（ROUNDS=5, SETTLE=10）rc=0：每轮 BSS 均 `AP-ENABLED`；10s settle 内客户端重连数少于基线（round3/4 为 0、round5 为 1）。客户端重连受终端扫描节奏影响，建议后续以更长 settle 或客户端侧行为复核，不据此判定 issue #10 复发。

## 五、遗留/待办

- F66：需 U-Boot 侧默认 env 补 `rdinit=/sbin/init` 或换用 9002 OpenWrt U-Boot 后重验（见 FIXES F66）。
- E2：下一轮 stock/experimental 构建后实机确认 `DISTRIB_DESCRIPTION` 含档位标识。
- C2/C3/B2/A3/D3：需物理对端、6G 客户端、10G 对端与长稳条件。
- 10G PHY LED（F53/9033）：ci-69 旧内核无 phy5/phy8 LED；待基于 eb7a45b 且含 9033 的新构建实机验证。
