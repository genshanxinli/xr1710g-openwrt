# Source: Gilly1970/Gemtek-W1700K-6.18 openwrt-patches/748（2026-09-10；内核 v6.18 真实源码验证两 hunk 命中）
# Upstream: n/a（板级体验项）
# Reason: **替换原 9016**。原版无条件删掉 an7581_phy_probe() 的 dev_err+检查——会掩盖真实 pinctrl 配置
#   错误，且不覆盖 mt7988_phy_fix_leds_polarity()。本版：`-ENODEV`（DT 无 pinctrl-0，LED 走 GPIO 或未用）
#   **静默**；其他错误降为 `dev_warn` + `%pe` + "LEDs disabled"（失败非致命，PHY 仍 probe、流量仍通）。
# 变更留痕：原 9016 = vendor/fanboy/08 的 led 段（内层 998-silence-PHY-LED-pinctrl-error.patch，删 3 行）；
#   备份 docs/absorptions/9016-before-replace-20260911.patch.bak。
# 验证：内层对 Linux v6.18 drivers/net/phy/mediatek/mtk-ge-soc.c（47398B）patch -p1 两 hunk 全命中
#   （offset +3 / +10）；本仓构建树内层未实跑。实机判据：无 <3> 级 "Failed to setup PHY LED pinctrl"；
#   若 pinctrl-0 被占用则出现 dev_warn + "LEDs disabled" 且 PHY 正常 probe。
From: Gilly <gilroyscott1970@gmail.com>
Subject: [PATCH] net: phy: mediatek: make PHY LED pinctrl optional

The AN7581 and MT7988 SoC PHY drivers unconditionally call
devm_pinctrl_get_select(&phydev->mdio.dev, "gbe-led") and log an
error at <3> priority when it fails. This is wrong for two reasons:

1. The LED pinctrl is optional. Boards that drive PHY LEDs via GPIO
   intentionally omit any pinctrl-0 reference, and -ENODEV is not an error.
2. Even when pinctrl-0 IS referenced but fails, the failure is non-fatal;
   only the LED routing is unconfigured, so dev_warn is more appropriate.

Signed-off-by: Gilly <gilroyscott1970@gmail.com>
---
diff --git a/target/linux/airoha/patches-6.18/998-net-phy-mediatek-make-LED-pinctrl-optional.patch b/target/linux/airoha/patches-6.18/998-net-phy-mediatek-make-LED-pinctrl-optional.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/998-net-phy-mediatek-make-LED-pinctrl-optional.patch
@@ -0,0 +1,66 @@
+From: Gilly <gilroyscott1970@gmail.com>
+Date: Mon, 25 May 2026 00:00:00 +1000
+Subject: [PATCH] net: phy: mediatek: make PHY LED pinctrl optional
+
+The AN7581 and MT7988 SoC PHY drivers unconditionally call
+devm_pinctrl_get_select(&phydev->mdio.dev, "gbe-led") and log an
+error at <3> priority when it fails. This is wrong for two reasons:
+
+1. The LED pinctrl is optional. Boards that drive PHY
+   LEDs via GPIO (e.g. Gemtek W1700K) intentionally omit any
+   pinctrl-0 reference. In that case devm_pinctrl_get_select()
+   returns -ENODEV which is not an error -- it just means "no LED
+   pinctrl configured." Logging dev_err here is noisy and
+   misleading.
+
+2. Even when a pinctrl-0 IS referenced but fails (e.g. the muxed
+   target pin is already claimed by another driver), the failure
+   is non-fatal -- the PHY still probes and traffic still flows.
+   Only the LED routing is unconfigured. dev_warn is more
+   appropriate than dev_err for this case.
+---
+--- a/drivers/net/phy/mediatek/mtk-ge-soc.c
++++ b/drivers/net/phy/mediatek/mtk-ge-soc.c
+@@ -1310,9 +1310,17 @@ static int mt7988_phy_fix_leds_polariti
+ 
+ 	/* Only now setup pinctrl to avoid bogus blinking */
+ 	pinctrl = devm_pinctrl_get_select(&phydev->mdio.dev, "gbe-led");
+-	if (IS_ERR(pinctrl))
+-		dev_err(&phydev->mdio.bus->dev,
+-			"Failed to setup PHY LED pinctrl\n");
++	if (IS_ERR(pinctrl)) {
++		int err = PTR_ERR(pinctrl);
++
++		/* -ENODEV: no pinctrl-0 in DT, LEDs driven by GPIO or not
++		 * used. Stay silent. Other errors are real misconfiguration.
++		 */
++		if (err != -ENODEV)
++			dev_warn(&phydev->mdio.bus->dev,
++				 "Failed to setup PHY LED pinctrl (%pe), LEDs disabled\n",
++				 pinctrl);
++	}
+ 
+ 	return 0;
+ }
+@@ -1415,9 +1423,17 @@ static int an7581_phy_probe(struct phy_
+ 
+ 	/* Toggle pinctrl to enable PHY LED */
+ 	pinctrl = devm_pinctrl_get_select(&phydev->mdio.dev, "gbe-led");
+-	if (IS_ERR(pinctrl))
+-		dev_err(&phydev->mdio.bus->dev,
+-			"Failed to setup PHY LED pinctrl\n");
++	if (IS_ERR(pinctrl)) {
++		int err = PTR_ERR(pinctrl);
++
++		/* -ENODEV: no pinctrl-0 in DT, LEDs driven by GPIO or not
++		 * used. Stay silent. Other errors are real misconfiguration.
++		 */
++		if (err != -ENODEV)
++			dev_warn(&phydev->mdio.bus->dev,
++				 "Failed to setup PHY LED pinctrl (%pe), LEDs disabled\n",
++				 pinctrl);
++	}
+ 
+ 	priv = devm_kzalloc(&phydev->mdio.dev, sizeof(*priv), GFP_KERNEL);
+ 	if (!priv)
+
