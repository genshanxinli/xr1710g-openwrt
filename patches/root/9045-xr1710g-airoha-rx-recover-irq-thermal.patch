# Source: Gilly1970/Gemtek-W1700K-6.18 openwrt-patches/ 981+980+976+957+960+961（2026-09-10）
# Upstream: n/a（作者自持；与本仓 9042=上游 #24872 不同 hunk，互补不冲突）
# Reason: ① 981 RX 环硬件完成停顿后的恢复（与 9042 的"DMA 越环"是不同故障面：9042 防越界，本条治停顿）；
#   ② 980 处理 RX_NO_CPU_DSCP 中断（不只 RX_DONE）；③ 976 为 RX queue 31 打开 RX_DONE 中断；
#   ④ 957 修共享 QDMA TX 环的 BQL underflow 与 UAF；⑤/⑥ 960/961 thermal 两处 1 行确定性 bug
#   （FIELD_PREP 掩码用错、low-trip 钳位用错变量）。
# 验证：内层对 openwrt main（KB 6.18）为新增补丁文件；本仓 9042 只改 RX_DSCP_NUM 宏与 ring4 尺寸，
#   与本组 hunk 不相交。未运行时验证。
#EXP diff --git a/target/linux/airoha/patches-6.18/981-net-airoha-recover-RX-ring-after-hw-completion-stall.patch b/target/linux/airoha/patches-6.18/981-net-airoha-recover-RX-ring-after-hw-completion-stall.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/981-net-airoha-recover-RX-ring-after-hw-completion-stall.patch
@@ -0,0 +1,216 @@
+From: netdev "[PATCH net v2 2/3] net: airoha: recover RX ring after hw completion stall"
+Date: Tue, 1 Sep 2026 00:00:00 +0000
+Subject: [PATCH] net: airoha: recover RX ring after hw completion stall
+
+Staged 2026-09-01 from a netdev "[PATCH net v2 N/3]" airoha RX series (raw diff
+only; author not recorded). NOT merged in torvalds/linux, stable, or OpenWrt
+master as of 2026-09-01 - re-check on each master sync and drop once it lands.
+Regenerated against this tree's effective airoha_eth source (0 fuzz/offset) and
+cross-compiled clean (no warnings) against a built 6.18.44 on 2026-09-01.
+
+Rebased onto this tree: (1) #include block differs (920-xx/975 extra
+includes), (2) airoha_qdma_cleanup() has the 957-restructured signature
+(struct airoha_qdma *qdma only, no DEV_STATE block) - cancel_work_sync()
+inserted at its top. All other hunks land as upstream.
+
+On every "next descriptor not DONE" break, compare q->tail against the
+hardware's own REG_RX_DMA_IDX. If hw keeps advancing while the strictly
+sequential consumer is stuck for AIROHA_RX_STALL_THRESHOLD (3) polls, a
+per-QDMA work disables that NAPI, pauses RX DMA (poll BUSY <= 50 ms),
+drains + refills the ring, re-enables, and logs
+"qid=%d RX ring recovered after hw stall (RX DMA paused %lld us)".
+disable_work_sync() in stop_napi, cancel_work_sync() in cleanup, INIT_WORK
+for every qdma[] before the init loop (error path tears down all).
+
+Safe with 975 LRO/scatter (DONE is set per descriptor in order; rx_process
+consumes MORE-chained descriptors one at a time). Known-benign race vs
+957: the work may re-set RX_DMA_EN after airoha_qdma_stop() dropped users
+to 0 (GDM ports down, TX DMA off, next open sets both). A FALSE positive
+costs <= 50 ms RX pause on that QDMA + the flush of that ring's queued
+packets, and is visible in dmesg - check after flashing:
+  dmesg | grep -E 'RX ring recovered|RX DMA busy'
+
+---
+diff -ruN s1/drivers/net/ethernet/airoha/airoha_eth.c s2/drivers/net/ethernet/airoha/airoha_eth.c
+--- a/drivers/net/ethernet/airoha/airoha_eth.c
++++ b/drivers/net/ethernet/airoha/airoha_eth.c
+@@ -3,6 +3,7 @@
+  * Copyright (c) 2024 AIROHA Inc
+  * Author: Lorenzo Bianconi <lorenzo@kernel.org>
+  */
++#include <linux/ktime.h>
+ #include <linux/of.h>
+ #include <linux/of_net.h>
+ #include <linux/of_reserved_mem.h>
+@@ -11,6 +12,7 @@
+ #include <linux/if_vlan.h>
+ #include <linux/pcs/pcs.h>
+ #include <linux/u64_stats_sync.h>
++#include <linux/workqueue.h>
+ #include <net/dst_metadata.h>
+ #include <net/ip6_checksum.h>
+ #include <net/page_pool/helpers.h>
+@@ -901,6 +903,34 @@
+ 	return skb;
+ }
+ 
++#define AIROHA_RX_STALL_THRESHOLD	3
++
++/* REG_RX_DMA_IDX is hw's own completion counter, independent of what sw has
++ * posted, so comparing it against q->tail spots the stall without trusting
++ * ring content: if hw keeps advancing while the strictly sequential consumer
++ * does not, it is completing descriptors that consumer can never reach.
++ */
++static void airoha_qdma_rx_check_stall(struct airoha_queue *q)
++{
++	struct airoha_qdma *qdma = q->qdma;
++	int qid = q - &qdma->q_rx[0];
++	u32 dma_idx;
++
++	dma_idx = airoha_qdma_get(qdma, REG_RX_DMA_IDX(qid),
++				  RX_RING_DMA_IDX_MASK);
++
++	if (q->stall_tail == q->tail && dma_idx != q->stall_dma_idx) {
++		if (++q->stall_count >= AIROHA_RX_STALL_THRESHOLD &&
++		    !test_and_set_bit(qid, qdma->rx_recover_mask))
++			schedule_work(&qdma->rx_recover_work);
++	} else {
++		q->stall_count = 0;
++	}
++
++	q->stall_tail = q->tail;
++	q->stall_dma_idx = dma_idx;
++}
++
+ static int airoha_qdma_rx_process(struct airoha_queue *q, int budget)
+ {
+ 	enum dma_data_direction dir = page_pool_get_dma_dir(q->page_pool);
+@@ -917,8 +947,10 @@
+ 		struct page *page;
+ 
+ 		desc_ctrl = le32_to_cpu(READ_ONCE(desc->ctrl));
+-		if (!(desc_ctrl & QDMA_DESC_DONE_MASK))
++		if (!(desc_ctrl & QDMA_DESC_DONE_MASK)) {
++			airoha_qdma_rx_check_stall(q);
+ 			break;
++		}
+ 
+ 		dma_rmb();
+ 
+@@ -1137,6 +1169,56 @@
+ 			FIELD_PREP(RX_RING_DMA_IDX_MASK, q->tail));
+ }
+ 
++static void airoha_qdma_rx_recover_work(struct work_struct *work)
++{
++	struct airoha_qdma *qdma = container_of(work, struct airoha_qdma,
++						rx_recover_work);
++	int qid;
++
++	for_each_set_bit(qid, qdma->rx_recover_mask, AIROHA_NUM_RX_RING) {
++		struct airoha_queue *q = &qdma->q_rx[qid];
++		ktime_t rx_dma_off_ts;
++		s64 rx_dma_off_us;
++		u32 status;
++
++		napi_disable(&q->napi);
++
++		/* per-QDMA, not per-ring: this pauses every RX ring behind
++		 * this instance, hence the measured duration below
++		 */
++		rx_dma_off_ts = ktime_get();
++		airoha_qdma_clear(qdma, REG_QDMA_GLOBAL_CFG,
++				  GLOBAL_CFG_RX_DMA_EN_MASK);
++		if (read_poll_timeout(airoha_qdma_rr, status,
++				      !(status & GLOBAL_CFG_RX_DMA_BUSY_MASK),
++				      USEC_PER_MSEC, 50 * USEC_PER_MSEC, true,
++				      qdma, REG_QDMA_GLOBAL_CFG))
++			dev_warn(qdma->eth->dev,
++				 "qid=%d RX DMA busy timeout during recovery\n",
++				 qid);
++
++		airoha_qdma_cleanup_rx_queue(q);
++		if (q->skb) {
++			dev_kfree_skb(q->skb);
++			q->skb = NULL;
++		}
++		airoha_qdma_fill_rx_queue(q);
++
++		airoha_qdma_set(qdma, REG_QDMA_GLOBAL_CFG,
++				GLOBAL_CFG_RX_DMA_EN_MASK);
++		rx_dma_off_us = ktime_us_delta(ktime_get(), rx_dma_off_ts);
++
++		q->stall_count = 0;
++		napi_enable(&q->napi);
++		napi_schedule(&q->napi);
++
++		dev_warn_ratelimited(qdma->eth->dev,
++				     "qid=%d RX ring recovered after hw stall (RX DMA paused %lld us)\n",
++				     qid, rx_dma_off_us);
++		clear_bit(qid, qdma->rx_recover_mask);
++	}
++}
++
+ static int airoha_qdma_init_rx(struct airoha_qdma *qdma)
+ {
+ 	int i;
+@@ -1787,6 +1869,8 @@
+ {
+ 	int i;
+ 
++	cancel_work_sync(&qdma->rx_recover_work);
++
+ 	for (i = 0; i < ARRAY_SIZE(qdma->q_rx); i++) {
+ 		if (!qdma->q_rx[i].ndesc)
+ 			continue;
+@@ -1842,6 +1926,13 @@
+ 	if (err)
+ 		return err;
+ 
++	/* init every instance up front: the error path below tears down all
++	 * of eth->qdma[], including entries the init loop never reached
++	 */
++	for (i = 0; i < ARRAY_SIZE(eth->qdma); i++)
++		INIT_WORK(&eth->qdma[i].rx_recover_work,
++			  airoha_qdma_rx_recover_work);
++
+ 	for (i = 0; i < ARRAY_SIZE(eth->qdma); i++) {
+ 		err = airoha_qdma_init(pdev, eth, &eth->qdma[i]);
+ 		if (err)
+@@ -1890,6 +1981,11 @@
+ {
+ 	int i;
+ 
++	/* must not run or re-arm past this point: the work calls
++	 * napi_disable() too, and doing that twice spins forever
++	 */
++	disable_work_sync(&qdma->rx_recover_work);
++
+ 	for (i = 0; i < ARRAY_SIZE(qdma->q_tx_irq); i++)
+ 		napi_disable(&qdma->q_tx_irq[i].napi);
+ 
+diff -ruN s1/drivers/net/ethernet/airoha/airoha_eth.h s2/drivers/net/ethernet/airoha/airoha_eth.h
+--- a/drivers/net/ethernet/airoha/airoha_eth.h
++++ b/drivers/net/ethernet/airoha/airoha_eth.h
+@@ -211,6 +211,11 @@
+ 	int buf_size;
+ 	bool txq_stopped;
+ 
++	/* see airoha_qdma_rx_check_stall() */
++	u32 stall_dma_idx;
++	u16 stall_tail;
++	u8 stall_count;
++
+ 	struct napi_struct napi;
+ 	struct page_pool *page_pool;
+ 	struct sk_buff *skb;
+@@ -573,6 +578,9 @@
+ 	struct airoha_queue q_tx[AIROHA_NUM_TX_RING];
+ 	struct airoha_queue q_rx[AIROHA_NUM_RX_RING];
+ 
++	struct work_struct rx_recover_work;
++	DECLARE_BITMAP(rx_recover_mask, AIROHA_NUM_RX_RING);
++
+ 	DECLARE_BITMAP(qos_channel_map, AIROHA_NUM_QOS_CHANNELS);
+ };
+
diff --git a/target/linux/airoha/patches-6.18/980-net-airoha-handle-RX_NO_CPU_DSCP-interrupt-not-just-RX_DONE.patch b/target/linux/airoha/patches-6.18/980-net-airoha-handle-RX_NO_CPU_DSCP-interrupt-not-just-RX_DONE.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/980-net-airoha-handle-RX_NO_CPU_DSCP-interrupt-not-just-RX_DONE.patch
@@ -0,0 +1,82 @@
+From: netdev "[PATCH net v2 1/3] net: airoha: handle RX_NO_CPU_DSCP interrupt, not just RX_DONE"
+Date: Tue, 1 Sep 2026 00:00:00 +0000
+Subject: [PATCH] net: airoha: handle RX_NO_CPU_DSCP interrupt, not just RX_DONE
+
+Staged 2026-09-01 from a netdev "[PATCH net v2 N/3]" airoha RX series (raw diff
+only; author not recorded). NOT merged in torvalds/linux, stable, or OpenWrt
+master as of 2026-09-01 - re-check on each master sync and drop once it lands.
+Regenerated against this tree's effective airoha_eth source (0 fuzz/offset) and
+cross-compiled clean (no warnings) against a built 6.18.44 on 2026-09-01.
+
+The per-ring RX_NO_CPU_DSCP ("no free CPU descriptor") interrupts are
+enabled at init (INT_RX1/2_MASK include them) but airoha_irq_handler()
+masked the status down to RX_DONE only, so they were acked and ignored.
+A ring that fills up without a coincident RX_DONE never wakes its NAPI
+and stays dead until something else kicks it. Route the NO_CPU_DSCP bits
+to napi_schedule() for the owning queue and re-enable both bits in
+napi_poll. Bit layout verified: IDX1 = RX_DONE 0-15 + NO_CPU_DSCP 16-31
+(qid 0-15); IDX2 the same for qid 16-31 - consistent with 976's queue-31
+IRQ bank pin.
+
+W1700K relevance: candidate root cause for the "silent RX death after
+hours" / LAN2-RX-intermittently-dead class.
+
+---
+diff -ruN s0/drivers/net/ethernet/airoha/airoha_eth.c s1/drivers/net/ethernet/airoha/airoha_eth.c
+--- a/drivers/net/ethernet/airoha/airoha_eth.c
++++ b/drivers/net/ethernet/airoha/airoha_eth.c
+@@ -1017,13 +1017,16 @@
+ 		int i, qid = q - &qdma->q_rx[0];
+ 		int intr_reg = qid < RX_DONE_HIGH_OFFSET ? QDMA_INT_REG_IDX1
+ 							 : QDMA_INT_REG_IDX2;
++		u32 irq_id = qid % RX_DONE_HIGH_OFFSET;
++		u32 intr_mask = BIT(irq_id) |
++				BIT(irq_id + RX_NO_CPU_DSCP_LOW_OFFSET);
+ 
+ 		for (i = 0; i < ARRAY_SIZE(qdma->irq_banks); i++) {
+ 			if (!(BIT(qid) & RX_IRQ_BANK_PIN_MASK(i)))
+ 				continue;
+ 
+ 			airoha_qdma_irq_enable(&qdma->irq_banks[i], intr_reg,
+-					       BIT(qid % RX_DONE_HIGH_OFFSET));
++					       intr_mask);
+ 		}
+ 	}
+ 
+@@ -1674,16 +1677,19 @@
+ 	if (!test_bit(DEV_STATE_INITIALIZED, &qdma->eth->state))
+ 		return IRQ_NONE;
+ 
+-	rx_intr1 = intr[1] & RX_DONE_LOW_INT_MASK;
++	rx_intr1 = intr[1] & (RX_DONE_LOW_INT_MASK | RX_NO_CPU_DSCP_LOW_INT_MASK);
+ 	if (rx_intr1) {
+ 		airoha_qdma_irq_disable(irq_bank, QDMA_INT_REG_IDX1, rx_intr1);
+-		rx_intr_mask |= rx_intr1;
++		rx_intr_mask |= (rx_intr1 & RX_DONE_LOW_INT_MASK) |
++				RX_NO_CPU_DSCP_INT_RX1_MASK(rx_intr1);
+ 	}
+ 
+-	rx_intr2 = intr[2] & RX_DONE_HIGH_INT_MASK;
++	rx_intr2 = intr[2] & (RX_DONE_HIGH_INT_MASK | RX_NO_CPU_DSCP_HIGH_INT_MASK);
+ 	if (rx_intr2) {
+ 		airoha_qdma_irq_disable(irq_bank, QDMA_INT_REG_IDX2, rx_intr2);
+-		rx_intr_mask |= (rx_intr2 << 16);
++		rx_intr_mask |= ((rx_intr2 & RX_DONE_HIGH_INT_MASK) <<
++				 RX_DONE_HIGH_OFFSET) |
++				(rx_intr2 & RX_NO_CPU_DSCP_HIGH_INT_MASK);
+ 	}
+ 
+ 	for (i = 0; rx_intr_mask && i < ARRAY_SIZE(qdma->q_rx); i++) {
+diff -ruN s0/drivers/net/ethernet/airoha/airoha_regs.h s1/drivers/net/ethernet/airoha/airoha_regs.h
+--- a/drivers/net/ethernet/airoha/airoha_regs.h
++++ b/drivers/net/ethernet/airoha/airoha_regs.h
+@@ -575,6 +575,8 @@
+ #define INT_RX1_MASK(_n)							\
+ 	((((_n) << RX_NO_CPU_DSCP_LOW_OFFSET) & RX_NO_CPU_DSCP_LOW_INT_MASK) |	\
+ 	 (RX_DONE_LOW_INT_MASK & (_n)))
++#define RX_NO_CPU_DSCP_INT_RX1_MASK(_n)					\
++	(((_n) & RX_NO_CPU_DSCP_LOW_INT_MASK) >> RX_NO_CPU_DSCP_LOW_OFFSET)
+ 
+ /* QDMA_CSR_INT_ENABLE3 */
+ #define RX31_NO_CPU_DSCP_INT_MASK	BIT(31)
+
diff --git a/target/linux/airoha/patches-6.18/976-net-airoha-enable-RX_DONE-interrupt-for-RX-queue-31.patch b/target/linux/airoha/patches-6.18/976-net-airoha-enable-RX_DONE-interrupt-for-RX-queue-31.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/976-net-airoha-enable-RX_DONE-interrupt-for-RX-queue-31.patch
@@ -0,0 +1,27 @@
+From: Lorenzo Bianconi <lorenzo@kernel.org>
+Date: Sun, 31 Aug 2026 00:00:00 +0000
+Subject: [PATCH] net: airoha: enable RX_DONE interrupt for RX queue 31
+
+RX_IRQ1_BANK_PIN_MASK maps rx queues 21-30 to IRQ bank 1 but misses
+BIT(31), so rx queue 31 never has its RX_DONE interrupt routed to an
+IRQ bank. With the HW GRO offload moving the LRO-capable rx queues to
+the high range (24-31), traffic hashed to queue 31 would fill the ring
+and stall with no completion interrupt. Add BIT(31) to the bank 1 pin
+mask.
+
+Companion fix to the net-next v5 "add HW GRO offload support" series;
+in this tree it complements 916-02 + 975 which already implement the
+same LRO queue layout.
+---
+--- a/drivers/net/ethernet/airoha/airoha_eth.h
++++ b/drivers/net/ethernet/airoha/airoha_eth.h
+@@ -538,7 +538,7 @@ struct airoha_wdma_info {
+ 
+ /* RX queue to IRQ mapping: BIT(q) in IRQ(n) */
+ #define RX_IRQ0_BANK_PIN_MASK			0x839f
+-#define RX_IRQ1_BANK_PIN_MASK			0x7fe00000
++#define RX_IRQ1_BANK_PIN_MASK			0xffe00000
+ #define RX_IRQ2_BANK_PIN_MASK			0x20
+ #define RX_IRQ3_BANK_PIN_MASK			0x40
+ #define RX_IRQ_BANK_PIN_MASK(_n)		\
+
diff --git a/target/linux/airoha/patches-6.18/957-net-airoha-fix-BQL-underflow-and-UAF-in-shared-QDMA-TX-ring.patch b/target/linux/airoha/patches-6.18/957-net-airoha-fix-BQL-underflow-and-UAF-in-shared-QDMA-TX-ring.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/957-net-airoha-fix-BQL-underflow-and-UAF-in-shared-QDMA-TX-ring.patch
@@ -0,0 +1,53 @@
+From: Gilly <gilroyscott1970@gmail.com>
+Date: Sat, 20 Jun 2026 00:00:00 +0000
+Subject: [PATCH] net: airoha: fix BQL underflow and UAF in shared QDMA TX ring
+
+Minimal form of Lorenzo Bianconi's BQL-underflow/UAF fix, re-scoped
+2026-06-20 for master @ 2026-06-19. Upstream meanwhile refactored DMA
+enable/disable into airoha_qdma_start()/airoha_qdma_stop() and already
+added the TX/RX DMA drain busy-poll in airoha_qdma_stop() (the part that
+local patch 628 used to add, and that the earlier v2 restructure folded
+into airoha_qdma_cleanup()). So the full v2 lifecycle restructure
+(DMA-at-probe / DEV_STATE_FLUSH / tx_flush) is no longer the right fit and
+would fight upstream's refactor.
+
+Two real bugs remain in the new base, fixed here directly:
+
+1) UAF: airoha_qdma_tx_napi_poll() read e->skb but never cleared it, so the
+   per-stop teardown (airoha_qdma_stop -> airoha_qdma_cleanup_tx_queue)
+   could act on a stale skb pointer. Clear e->skb when consumed.
+
+2) BQL underflow: airoha_qdma_cleanup_tx_queue() freed in-flight skbs
+   without netdev_tx_completed_queue(), underflowing BQL and wedging TX
+   after a stop that had packets in flight on the shared ring. Balance BQL
+   before freeing.
+
+This supersedes local patch 628 (drain-poll now upstream in
+airoha_qdma_stop) — 628 stays retired.
+---
+--- a/drivers/net/ethernet/airoha/airoha_eth.c
++++ b/drivers/net/ethernet/airoha/airoha_eth.c
+@@ -1239,6 +1239,7 @@
+ 
+ 		e = &q->entry[index];
+ 		skb = e->skb;
++		e->skb = NULL;
+ 
+ 		dma_unmap_single(eth->dev, e->dma_addr, e->dma_len,
+ 				 DMA_TO_DEVICE);
+@@ -1399,7 +1400,13 @@
+ 
+ 		dma_unmap_single(eth->dev, e->dma_addr, e->dma_len,
+ 				 DMA_TO_DEVICE);
+-		dev_kfree_skb_any(e->skb);
++		if (e->skb) {
++			struct netdev_queue *txq;
++
++			txq = skb_get_tx_queue(e->skb->dev, e->skb);
++			netdev_tx_completed_queue(txq, 1, e->skb->len);
++			dev_kfree_skb_any(e->skb);
++		}
+ 		e->dma_addr = 0;
+ 		e->skb = NULL;
+ 		list_add_tail(&e->list, &q->tx_list);
+
diff --git a/target/linux/airoha/patches-6.18/960-thermal-airoha-Fix-FIELD_PREP-using-wrong-mask-for-sensor-interval.patch b/target/linux/airoha/patches-6.18/960-thermal-airoha-Fix-FIELD_PREP-using-wrong-mask-for-sensor-interval.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/960-thermal-airoha-Fix-FIELD_PREP-using-wrong-mask-for-sensor-interval.patch
@@ -0,0 +1,12 @@
+--- a/drivers/thermal/airoha_thermal.c
++++ b/drivers/thermal/airoha_thermal.c
+@@ -503,7 +503,7 @@
+ 	 */
+ 	regmap_write(priv->map, EN7581_TEMPMONCTL2,
+ 		     FIELD_PREP(EN7581_FILT_INTERVAL, 1) |
+-		     FIELD_PREP(EN7581_FILT_INTERVAL, 379));
++		     FIELD_PREP(EN7581_SEN_INTERVAL, 379));
+ 
+ 	/* AHB poll is set to 146 * 68.64 = 10.02us */
+ 	regmap_write(priv->map, EN7581_TEMPAHBPOLL,
+
diff --git a/target/linux/airoha/patches-6.18/961-thermal-airoha-Fix-low-trip-clamp-using-wrong-variable.patch b/target/linux/airoha/patches-6.18/961-thermal-airoha-Fix-low-trip-clamp-using-wrong-variable.patch
new file mode 100644
--- /dev/null
+++ b/target/linux/airoha/patches-6.18/961-thermal-airoha-Fix-low-trip-clamp-using-wrong-variable.patch
@@ -0,0 +1,27 @@
+From: Gilly <gilroyscott1970@gmail.com>
+Date: Sat, 20 Jun 2026 00:00:00 +0000
+Subject: [PATCH] thermal: airoha: fix low-trip clamp using wrong variable
+
+In airoha_thermal_set_trips(), the low-trip branch clamps 'high' into
+'low': low = clamp_t(int, high, ...). It must clamp 'low', otherwise the
+low trip threshold is derived from the high temperature.
+
+Scope note (2026-06-20): this used to also fix the TEMPOFFSETL regmap_write
+(TEMP_TO_RAW(priv, high) -> low), but the base 402-01-...regmap-API.patch
+already writes 'low' there (and upstream commit 71d0568 fixes the same line
+for trees that still had 'high'). Bundling both in one hunk caused GNU patch
+to treat the whole hunk as already-applied and silently skip the clamp fix.
+Reduced to the clamp line only so it actually lands.
+---
+--- a/drivers/thermal/airoha_thermal.c
++++ b/drivers/thermal/airoha_thermal.c
+@@ -373,7 +373,7 @@
+ 
+ 	if (low != -INT_MAX) {
+ 		/* Validate low and clamp it to a supported value */
+-		low = clamp_t(int, high, RAW_TO_TEMP(priv, 0),
++		low = clamp_t(int, low, RAW_TO_TEMP(priv, 0),
+ 			      RAW_TO_TEMP(priv, FIELD_MAX(EN7581_DOUT_TADC_MASK)));
+ 
+ 		/* We offset the low temp of 1°C to trigger correct event */
+
