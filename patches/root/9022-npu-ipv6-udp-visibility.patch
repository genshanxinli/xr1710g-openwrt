diff --git a/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense b/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
index 00ef64f..113517f 100755
--- a/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
+++ b/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
@@ -214,7 +214,9 @@ get_ppe_entries() {
 
 	local neigh
 	neigh=$(ip neigh show 2>/dev/null | grep ' lladdr ')
-	local ips0 ips1 ips2
+	local neigh6
+	neigh6=$(ip -6 neigh show 2>/dev/null | grep ' lladdr ')
+	local ips0 ips1 ips2 ips60 ips61 ips62
 	ips0=$(printf '%s\n' "$neigh" | awk -v macs="$macs0" '
 		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
 		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
@@ -224,13 +226,22 @@ get_ppe_entries() {
 	ips2=$(printf '%s\n' "$neigh" | awk -v macs="$macs2" '
 		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
 		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+	ips60=$(printf '%s\n' "$neigh6" | awk -v macs="$macs0" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+	ips61=$(printf '%s\n' "$neigh6" | awk -v macs="$macs1" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+	ips62=$(printf '%s\n' "$neigh6" | awk -v macs="$macs2" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
 
 	# Per-band BND counts: match each BND row to a band by station MAC (eth=) or
 	# station IPv4 (orig/new tuple). For routed client->WAN flows eth= carries
 	# bridge/next-hop MACs and never the station MAC, so IP matching is required.
 	local band_bnd="[0,0,0]"
 	if [ -f "$bind_file" ]; then
-		band_bnd=$(awk -v m0="$macs0" -v m1="$macs1" -v m2="$macs2" -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" '
+		band_bnd=$(awk -v m0="$macs0" -v m1="$macs1" -v m2="$macs2" -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" -v j0="$ips60" -v j1="$ips61" -v j2="$ips62" '
 		function has_mac(macs, eth,    n,arr,i,low) {
 			if (macs == "") return 0
 			low = tolower(eth)
@@ -244,14 +255,43 @@ get_ppe_entries() {
 			for (i=1; i<=n; i++) if (arr[i] != "" && index(tuple, arr[i] ":") > 0) return 1
 			return 0
 		}
+		function canon6(ip,    out,head,tail,nh,nt,i,g,h,t) {
+			if (index(ip, "::") > 0) {
+				split(ip, parts, "::")
+				head = parts[1]; tail = parts[2]
+				nh = split(head, h, ":"); if (head == "") nh = 0
+				nt = split(tail, t, ":"); if (tail == "") nt = 0
+				out = ""
+				for (i=1; i<=nh; i++) out = out ":" h[i]
+				for (i=nh+1; i<=8-nt; i++) out = out ":0"
+				for (i=1; i<=nt; i++) out = out ":" t[i]
+				out = substr(out, 2)
+			} else {
+				out = ip
+			}
+			n = split(out, a, ":")
+			out = ""
+			for (i=1; i<=8; i++) {
+				g = a[i]; if (g == "") g = "0"
+				while (length(g) < 4) g = "0" g
+				out = out (i==1 ? "" : ":") tolower(g)
+			}
+			return out
+		}
+		function has_ip6(ips6, tuple,    n,arr,i) {
+			if (ips6 == "") return 0
+			n = split(ips6, arr, "|")
+			for (i=1; i<=n; i++) if (arr[i] != "" && index(tuple, canon6(arr[i])) > 0) return 1
+			return 0
+		}
 		/^[0-9a-fA-F]+ BND / {
 			eth=""; match($0,/eth=[^ \t]+/); if (RSTART>0) eth=substr($0,RSTART+4,RLENGTH-4)
 			orig=""; match($0,/orig=[^ \t]+/); if (RSTART>0) orig=substr($0,RSTART+5,RLENGTH-5)
 			new_flow=""; match($0,/new=[^ \t]+/); if (RSTART>0) new_flow=substr($0,RSTART+4,RLENGTH-4)
 			tuple = orig " " new_flow
-			if      (has_mac(m0,eth) || has_ip(i0,tuple)) c0++
-			else if (has_mac(m1,eth) || has_ip(i1,tuple)) c1++
-			else if (has_mac(m2,eth) || has_ip(i2,tuple)) c2++
+			if      (has_mac(m0,eth) || has_ip(i0,tuple) || has_ip6(j0,tuple)) c0++
+			else if (has_mac(m1,eth) || has_ip(i1,tuple) || has_ip6(j1,tuple)) c1++
+			else if (has_mac(m2,eth) || has_ip(i2,tuple) || has_ip6(j2,tuple)) c2++
 		}
 		END { printf "[%d,%d,%d]", c0+0, c1+0, c2+0 }
 		' "$bind_file" 2>/dev/null || printf "[0,0,0]")
@@ -261,24 +301,75 @@ get_ppe_entries() {
 	# Per-band UNB counts: IP-based matching (UNB flows lack eth= MAC info)
 	local band_unb="[0,0,0]"
 	if [ -f "$entries_file" ]; then
-		band_unb=$(awk -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" '
+		band_unb=$(awk -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" -v j0="$ips60" -v j1="$ips61" -v j2="$ips62" '
 		function has(ips, ip,    n,arr,i) {
 			if (ips == "") return 0
 			n = split(ips, arr, "|")
 			for (i=1; i<=n; i++) if (arr[i] == ip) return 1
 			return 0
 		}
+		function canon6(ip,    out,head,tail,nh,nt,i,g,h,t) {
+			if (index(ip, "::") > 0) {
+				split(ip, parts, "::")
+				head = parts[1]; tail = parts[2]
+				nh = split(head, h, ":"); if (head == "") nh = 0
+				nt = split(tail, t, ":"); if (tail == "") nt = 0
+				out = ""
+				for (i=1; i<=nh; i++) out = out ":" h[i]
+				for (i=nh+1; i<=8-nt; i++) out = out ":0"
+				for (i=1; i<=nt; i++) out = out ":" t[i]
+				out = substr(out, 2)
+			} else {
+				out = ip
+			}
+			n = split(out, a, ":")
+			out = ""
+			for (i=1; i<=8; i++) {
+				g = a[i]; if (g == "") g = "0"
+				while (length(g) < 4) g = "0" g
+				out = out (i==1 ? "" : ":") tolower(g)
+			}
+			return out
+		}
+		function has6(ips6, ip6,    n,arr,i,c) {
+			if (ips6 == "") return 0
+			n = split(ips6, arr, "|")
+			c = canon6(ip6)
+			for (i=1; i<=n; i++) if (arr[i] != "" && canon6(arr[i]) == c) return 1
+			return 0
+		}
+		function strip_port(orig,    n,a,i,s) {
+			# orig is an expanded IPv6 address optionally followed by :port;
+			# an expanded address has 8 groups (7 colons), so the 9th field
+			# is the port and must be dropped.
+			n = split(orig, a, ":")
+			if (n >= 9) {
+				s = ""
+				for (i=1; i<=8; i++) s = s (i==1 ? "" : ":") a[i]
+				return s
+			}
+			return orig
+		}
 		/^[0-9a-fA-F]+ UNB / {
 			src=""
 			match($0,/orig=[^ \t]+/)
 			if (RSTART>0) {
 				orig=substr($0,RSTART+5,RLENGTH-5)
-				if (substr(orig,1,1) != "[") { n=split(orig,p,":"); src=p[1] }
+				if (substr(orig,1,1) != "[") {
+					if (index(orig, ".") > 0) { n=split(orig,p,":"); src=p[1] }
+					else { src=strip_port(orig) }
+				}
 			}
 			if (src != "") {
-				if      (has(i0,src)) c0++
-				else if (has(i1,src)) c1++
-				else if (has(i2,src)) c2++
+				if (index(src, ":") > 0) {
+					if      (has6(j0,src)) c0++
+					else if (has6(j1,src)) c1++
+					else if (has6(j2,src)) c2++
+				} else {
+					if      (has(i0,src)) c0++
+					else if (has(i1,src)) c1++
+					else if (has(i2,src)) c2++
+				}
 			}
 		}
 		END { printf "[%d,%d,%d]", c0+0, c1+0, c2+0 }
@@ -331,6 +422,7 @@ get_ppe_entries() {
 		client_bnd=$(
 		{
 			printf '%s\n' "$neigh" | sed 's/^/@NEIGH /'
+			printf '%s\n' "$neigh6" | sed 's/^/@NEIGH6 /'
 			sed 's/^/@LEASE /' /tmp/dhcp.leases 2>/dev/null
 			cat "$bind_file"
 			grep -E '\[HW_OFFLOAD\]|\[OFFLOAD\]' /proc/net/nf_conntrack 2>/dev/null | sed 's/^/@CT /'
@@ -341,7 +433,31 @@ get_ppe_entries() {
 			c=split(m1,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=1; cnt_eth[ntot]=0; cnt_bnd[ntot]=0; cnt_hw[ntot]=0; cnt_sw[ntot]=0 }
 			c=split(m2,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=2; cnt_eth[ntot]=0; cnt_bnd[ntot]=0; cnt_hw[ntot]=0; cnt_sw[ntot]=0 }
 		}
+		function canon6(ip,    out,head,tail,nh,nt,i,g,h,t) {
+			if (index(ip, "::") > 0) {
+				split(ip, parts, "::")
+				head = parts[1]; tail = parts[2]
+				nh = split(head, h, ":"); if (head == "") nh = 0
+				nt = split(tail, t, ":"); if (tail == "") nt = 0
+				out = ""
+				for (i=1; i<=nh; i++) out = out ":" h[i]
+				for (i=nh+1; i<=8-nt; i++) out = out ":0"
+				for (i=1; i<=nt; i++) out = out ":" t[i]
+				out = substr(out, 2)
+			} else {
+				out = ip
+			}
+			n = split(out, a, ":")
+			out = ""
+			for (i=1; i<=8; i++) {
+				g = a[i]; if (g == "") g = "0"
+				while (length(g) < 4) g = "0" g
+				out = out (i==1 ? "" : ":") tolower(g)
+			}
+			return out
+		}
 		$1=="@NEIGH" { ipx=$2; mcx=tolower($6); if(index(ipx,":")==0 && index(ipx,".")>0 && mcx!="" && !(mcx in mac2ip)) mac2ip[mcx]=ipx; next }
+		$1=="@NEIGH6" { ipx=$2; mcx=tolower($6); if(index(ipx,":")>0 && mcx!="" && !(mcx in mac2ip6)) mac2ip6[mcx]=canon6(ipx); next }
 		$1=="@LEASE" { lm=tolower($3); lh=$5; ipx=$4; gsub(/["\\]/,"",lh); if(lm!="" && lh!="" && lh!="*") mac2host[lm]=lh; if(lm!="" && ipx!="" && !(lm in mac2ip)) mac2ip[lm]=ipx; next }
 		$1=="@CT" {
 			is_hw = index($0,"[HW_OFFLOAD]") > 0
@@ -351,8 +467,9 @@ get_ppe_entries() {
 			match(line,/dst=[^ ]+/); if(RSTART>0) dst=substr(line,RSTART+4,RLENGTH-4)
 			for(i=1;i<=ntot;i++){
 				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
-				if(ip=="" || ip=="0.0.0.0") continue
-				if(src==ip || dst==ip) { if(is_hw) cnt_hw[i]++; else if(is_sw) cnt_sw[i]++ }
+				ip6 = (mac[i] in mac2ip6) ? mac2ip6[mac[i]] : ""
+				if(ip=="" && ip6=="") continue
+				if((ip!="" && ip!="0.0.0.0" && (src==ip || dst==ip)) || (ip6!="" && (index(src,ip6)>0 || index(dst,ip6)>0))) { if(is_hw) cnt_hw[i]++; else if(is_sw) cnt_sw[i]++ }
 			}
 			next
 		}
@@ -363,20 +480,23 @@ get_ppe_entries() {
 			tuple = orig " " new_flow
 			for(i=1;i<=ntot;i++){
 				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
+				ip6 = (mac[i] in mac2ip6) ? mac2ip6[mac[i]] : ""
 				if(eth!="") { if(index(eth,mac[i])>0) cnt_eth[i]++ }
 				if(ip!="") { if(index(tuple, ip ":") > 0) cnt_bnd[i]++ }
+				if(ip6!="") { if(index(tuple, ip6) > 0) cnt_bnd[i]++ }
 			}
 		}
 		END {
 			printf "["
 			for(i=1;i<=ntot;i++){
 				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
+				ip6 = (mac[i] in mac2ip6) ? mac2ip6[mac[i]] : ""
 				host = (mac[i] in mac2host) ? mac2host[mac[i]] : ""
 				bnd = cnt_eth[i]
 				if(cnt_bnd[i] > bnd) bnd = cnt_bnd[i]
 				if(cnt_hw[i] > bnd) bnd = cnt_hw[i]
 				if(i>1)printf ","
-				printf "{\"mac\":\"%s\",\"ip\":\"%s\",\"host\":\"%s\",\"band\":%d,\"bnd\":%d,\"hw_offload\":%d,\"sw_offload\":%d}",mac[i],ip,host,bnd_band[i],bnd,cnt_hw[i],cnt_sw[i]
+				printf "{\"mac\":\"%s\",\"ip\":\"%s\",\"ip6\":\"%s\",\"host\":\"%s\",\"band\":%d,\"bnd\":%d,\"hw_offload\":%d,\"sw_offload\":%d}",mac[i],ip,ip6,host,bnd_band[i],bnd,cnt_hw[i],cnt_sw[i]
 			}
 			printf "]"
 		}
@@ -384,6 +504,15 @@ get_ppe_entries() {
 	fi
 	bnd_json=$(printf '%s' "$bnd_json" | sed 's/}$/,"client_bnd":'"$client_bnd"'}/')
 
+	# UDP hardware-offload visibility: PPE entries keep UDP flows as UNB
+	# (no BND row), so count conntrack [HW_OFFLOAD] UDP flows separately
+	# and expose it to prevent BND-only monitoring from false-negativing UDP.
+	local udp_hw=0
+	if [ -f /proc/net/nf_conntrack ]; then
+		udp_hw=$(grep '\[HW_OFFLOAD\]' /proc/net/nf_conntrack 2>/dev/null | grep -c 'udp' || echo 0)
+	fi
+	unb_json=$(printf '%s' "$unb_json" | sed 's/}$/,"udp_hw":'"$udp_hw"'}/')
+
 	printf '{"bnd":%s,"unb":%s}' "$bnd_json" "$unb_json"
 }
 
