--- a/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
+++ b/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
@@ -465,10 +465,39 @@
 	' "$stats_file" 2>/dev/null || echo '{"bands":[]}'
 }
 
+_flow_offload_hw_enabled() {
+	local fo foh
+	fo=$(uci get firewall.@defaults[0].flow_offloading 2>/dev/null)
+	foh=$(uci get firewall.@defaults[0].flow_offloading_hw 2>/dev/null)
+	[ "$fo" = "1" ] && [ "$foh" = "1" ]
+}
+
+_enable_flow_offload() {
+	uci set firewall.@defaults[0].flow_offloading='1'
+	uci set firewall.@defaults[0].flow_offloading_hw='1'
+	uci commit firewall
+	/etc/init.d/firewall reload >/dev/null 2>&1 &
+}
+
+_pppoe_offload_active() {
+	_flow_offload_hw_enabled || return 1
+	local wan_dev
+	wan_dev=$(ubus call network.interface.wan status 2>/dev/null | jsonfilter -e "@.l3_device" 2>/dev/null)
+	case "$wan_dev" in pppoe-*) ;; *) return 1;; esac
+	nft -j list flowtables 2>/dev/null | grep -q "\"$wan_dev\""
+}
+
 get_vlan_offload() {
 	local val
-	val=$(cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null)
-	printf '{"enabled":%d}' "${val:-0}"
+	if val=$(cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null); then
+		printf '{"enabled":%d}' "${val:-0}"
+		return
+	fi
+	if _flow_offload_hw_enabled; then
+		printf '{"enabled":1}'
+	else
+		printf '{"enabled":0}'
+	fi
 }
 
 set_vlan_offload() {
@@ -483,14 +512,22 @@
 		return
 	fi
 
-	if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null; then
-		echo '{"error":"failed - bridge module may not be loaded"}'
+	if [ -e /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged ]; then
+		if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null; then
+			echo '{"error":"failed - bridge module may not be loaded"}'
+			return
+		fi
+		# Persist across reboots
+		echo "net.bridge.bridge-nf-filter-vlan-tagged=$enabled" > /etc/sysctl.d/14-vlan-offload.conf
+		chmod 644 /etc/sysctl.d/14-vlan-offload.conf
+	elif [ "$enabled" = "1" ]; then
+		# New nft_flow_offload bridge architecture: VLAN offload is enabled by
+		# the hardware flowtable; make sure it is switched on.
+		_enable_flow_offload
+	else
+		echo '{"error":"VLAN offload is controlled by HW Flow Offload in this firmware"}'
 		return
 	fi
-
-	# Persist across reboots
-	echo "net.bridge.bridge-nf-filter-vlan-tagged=$enabled" > /etc/sysctl.d/14-vlan-offload.conf
-	chmod 644 /etc/sysctl.d/14-vlan-offload.conf
 	printf '{"result":"ok","enabled":%d}' "$enabled"
 }
 
@@ -530,8 +567,15 @@
 
 get_pppoe_offload() {
 	local val
-	val=$(cat /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null)
-	printf '{"enabled":%d}' "${val:-0}"
+	if val=$(cat /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null); then
+		printf '{"enabled":%d}' "${val:-0}"
+		return
+	fi
+	if _pppoe_offload_active; then
+		printf '{"enabled":1}'
+	else
+		printf '{"enabled":0}'
+	fi
 }
 
 set_pppoe_offload() {
@@ -545,14 +589,22 @@
 		return
 	fi
 
-	if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null; then
-		echo '{"error":"failed - bridge module may not be loaded"}'
+	if [ -e /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged ]; then
+		if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null; then
+			echo '{"error":"failed - bridge module may not be loaded"}'
+			return
+		fi
+		# Persist across reboots
+		echo "net.bridge.bridge-nf-filter-pppoe-tagged=$enabled" > /etc/sysctl.d/15-pppoe-offload.conf
+		chmod 644 /etc/sysctl.d/15-pppoe-offload.conf
+	elif [ "$enabled" = "1" ]; then
+		# New nft_flow_offload bridge architecture: PPPoE offload is enabled by
+		# the hardware flowtable; make sure it is switched on.
+		_enable_flow_offload
+	else
+		echo '{"error":"PPPoE offload is controlled by HW Flow Offload in this firmware"}'
 		return
 	fi
-
-	# Persist across reboots
-	echo "net.bridge.bridge-nf-filter-pppoe-tagged=$enabled" > /etc/sysctl.d/15-pppoe-offload.conf
-	chmod 644 /etc/sysctl.d/15-pppoe-offload.conf
 	printf '{"result":"ok","enabled":%d}' "$enabled"
 }
 
--- a/package/luci-app-airoha-npu/root/usr/libexec/rpcd/luci.airoha_npu
+++ b/package/luci-app-airoha-npu/root/usr/libexec/rpcd/luci.airoha_npu
@@ -445,10 +445,39 @@
 	printf '"cdm2":{"tx":%d,"rx_cpu":%d,"rx_hwf":%d,"rx_cpu_drop":%d,"rx_hwf_drop":%d}}' "$cdm2_tx" "$cdm2_rxcpu" "$cdm2_rxhwf" "$cdm2_rxcpud" "$cdm2_rxhwfd"
 }
 
+_flow_offload_hw_enabled() {
+	local fo foh
+	fo=$(uci get firewall.@defaults[0].flow_offloading 2>/dev/null)
+	foh=$(uci get firewall.@defaults[0].flow_offloading_hw 2>/dev/null)
+	[ "$fo" = "1" ] && [ "$foh" = "1" ]
+}
+
+_enable_flow_offload() {
+	uci set firewall.@defaults[0].flow_offloading='1'
+	uci set firewall.@defaults[0].flow_offloading_hw='1'
+	uci commit firewall
+	/etc/init.d/firewall reload >/dev/null 2>&1 &
+}
+
+_pppoe_offload_active() {
+	_flow_offload_hw_enabled || return 1
+	local wan_dev
+	wan_dev=$(ubus call network.interface.wan status 2>/dev/null | jsonfilter -e "@.l3_device" 2>/dev/null)
+	case "$wan_dev" in pppoe-*) ;; *) return 1;; esac
+	nft -j list flowtables 2>/dev/null | grep -q "\"$wan_dev\""
+}
+
 get_vlan_offload() {
 	local val
-	val=$(cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null)
-	printf '{"enabled":%d}' "${val:-0}"
+	if val=$(cat /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null); then
+		printf '{"enabled":%d}' "${val:-0}"
+		return
+	fi
+	if _flow_offload_hw_enabled; then
+		printf '{"enabled":1}'
+	else
+		printf '{"enabled":0}'
+	fi
 }
 
 set_vlan_offload() {
@@ -463,27 +492,42 @@
 		return
 	fi
 
-	if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null; then
-		echo '{"error":"failed - bridge module may not be loaded"}'
+	if [ -e /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged ]; then
+		if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-vlan-tagged 2>/dev/null; then
+			echo '{"error":"failed - bridge module may not be loaded"}'
+			return
+		fi
+		# Persist across reboots
+		echo "net.bridge.bridge-nf-filter-vlan-tagged=$enabled" > /etc/sysctl.d/14-vlan-offload.conf
+		chmod 644 /etc/sysctl.d/14-vlan-offload.conf
+	elif [ "$enabled" = "1" ]; then
+		# New nft_flow_offload bridge architecture: VLAN offload is enabled by
+		# the hardware flowtable; make sure it is switched on.
+		_enable_flow_offload
+	else
+		echo '{"error":"VLAN offload is controlled by HW Flow Offload in this firmware"}'
 		return
 	fi
-
-	# Persist across reboots
-	echo "net.bridge.bridge-nf-filter-vlan-tagged=$enabled" > /etc/sysctl.d/14-vlan-offload.conf
 	printf '{"result":"ok","enabled":%d}' "$enabled"
 }
 
 get_pppoe_offload() {
 	local val
-	val=$(cat /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null)
-	printf '{"enabled":%d}' "${val:-0}"
+	if val=$(cat /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null); then
+		printf '{"enabled":%d}' "${val:-0}"
+		return
+	fi
+	if _pppoe_offload_active; then
+		printf '{"enabled":1}'
+	else
+		printf '{"enabled":0}'
+	fi
 }
 
 set_pppoe_offload() {
 	local enabled
 	enabled=$(echo "$1" | jsonfilter -e "@.enabled" 2>/dev/null)
 
-	# jsonfilter returns empty string for integer 0; treat empty as 0
 	[ -z "$enabled" ] && enabled="0"
 
 	if [ "$enabled" != "0" ] && [ "$enabled" != "1" ]; then
@@ -491,13 +535,22 @@
 		return
 	fi
 
-	if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null; then
-		echo '{"error":"failed - bridge module may not be loaded"}'
+	if [ -e /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged ]; then
+		if ! echo "$enabled" > /proc/sys/net/bridge/bridge-nf-filter-pppoe-tagged 2>/dev/null; then
+			echo '{"error":"failed - bridge module may not be loaded"}'
+			return
+		fi
+		# Persist across reboots
+		echo "net.bridge.bridge-nf-filter-pppoe-tagged=$enabled" > /etc/sysctl.d/15-pppoe-offload.conf
+		chmod 644 /etc/sysctl.d/15-pppoe-offload.conf
+	elif [ "$enabled" = "1" ]; then
+		# New nft_flow_offload bridge architecture: PPPoE offload is enabled by
+		# the hardware flowtable; make sure it is switched on.
+		_enable_flow_offload
+	else
+		echo '{"error":"PPPoE offload is controlled by HW Flow Offload in this firmware"}'
 		return
 	fi
-
-	# Persist across reboots
-	echo "net.bridge.bridge-nf-filter-pppoe-tagged=$enabled" > /etc/sysctl.d/15-pppoe-offload.conf
 	printf '{"result":"ok","enabled":%d}' "$enabled"
 }
 
