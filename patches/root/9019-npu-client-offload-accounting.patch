diff --git a/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense b/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
index 2f19d07..32bb219 100755
--- a/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
+++ b/package/luci-app-airoha-flowsense/root/usr/libexec/rpcd/luci.airoha_flowsense
@@ -181,21 +181,46 @@ get_ppe_entries() {
 		iw dev "$iface" station dump 2>/dev/null | awk '/^Station/{print $2}'
 	done | tr '\n' '|' | sed 's/|$//')
 
+	local neigh
+	neigh=$(ip neigh show 2>/dev/null | grep ' lladdr ')
+	local ips0 ips1 ips2
+	ips0=$(printf '%s\n' "$neigh" | awk -v macs="$macs0" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+	ips1=$(printf '%s\n' "$neigh" | awk -v macs="$macs1" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+	ips2=$(printf '%s\n' "$neigh" | awk -v macs="$macs2" '
+		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
+		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
+
+	# Per-band BND counts: match each BND row to a band by station MAC (eth=) or
+	# station IPv4 (orig/new tuple). For routed client->WAN flows eth= carries
+	# bridge/next-hop MACs and never the station MAC, so IP matching is required.
 	local band_bnd="[0,0,0]"
 	if [ -f "$bind_file" ]; then
-		band_bnd=$(awk -v m0="$macs0" -v m1="$macs1" -v m2="$macs2" '
-		function has(macs, eth,    n,arr,i,low) {
+		band_bnd=$(awk -v m0="$macs0" -v m1="$macs1" -v m2="$macs2" -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" '
+		function has_mac(macs, eth,    n,arr,i,low) {
 			if (macs == "") return 0
 			low = tolower(eth)
 			n = split(macs, arr, "|")
 			for (i=1; i<=n; i++) if (arr[i] != "" && index(low, tolower(arr[i])) > 0) return 1
 			return 0
 		}
+		function has_ip(ips, tuple,    n,arr,i) {
+			if (ips == "") return 0
+			n = split(ips, arr, "|")
+			for (i=1; i<=n; i++) if (arr[i] != "" && index(tuple, arr[i] ":") > 0) return 1
+			return 0
+		}
 		/^[0-9a-fA-F]+ BND / {
 			eth=""; match($0,/eth=[^ \t]+/); if (RSTART>0) eth=substr($0,RSTART+4,RLENGTH-4)
-			if (has(m0,eth)) c0++
-			else if (has(m1,eth)) c1++
-			else if (has(m2,eth)) c2++
+			orig=""; match($0,/orig=[^ \t]+/); if (RSTART>0) orig=substr($0,RSTART+5,RLENGTH-5)
+			new_flow=""; match($0,/new=[^ \t]+/); if (RSTART>0) new_flow=substr($0,RSTART+4,RLENGTH-4)
+			tuple = orig " " new_flow
+			if      (has_mac(m0,eth) || has_ip(i0,tuple)) c0++
+			else if (has_mac(m1,eth) || has_ip(i1,tuple)) c1++
+			else if (has_mac(m2,eth) || has_ip(i2,tuple)) c2++
 		}
 		END { printf "[%d,%d,%d]", c0+0, c1+0, c2+0 }
 		' "$bind_file" 2>/dev/null || printf "[0,0,0]")
@@ -203,21 +228,6 @@ get_ppe_entries() {
 	bnd_json=$(printf '%s' "$bnd_json" | sed 's/}$/,"band_bnd":'"$band_bnd"'}/')
 
 	# Per-band UNB counts: IP-based matching (UNB flows lack eth= MAC info)
-	# Cross-reference WiFi client MACs with ARP neighbour table to get per-band IPs,
-	# then match UNB orig= source IP against those sets.
-	local neigh
-	neigh=$(ip neigh show 2>/dev/null | grep ' lladdr ')
-	local ips0 ips1 ips2
-	ips0=$(printf '%s\n' "$neigh" | awk -v macs="$macs0" '
-		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
-		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
-	ips1=$(printf '%s\n' "$neigh" | awk -v macs="$macs1" '
-		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
-		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
-	ips2=$(printf '%s\n' "$neigh" | awk -v macs="$macs2" '
-		BEGIN{n=split(macs,a,"|");for(i=1;i<=n;i++)if(a[i]!="")m[tolower(a[i])]=1}
-		m[tolower($5)]{printf "%s|",$1}' | sed 's/|$//')
-
 	local band_unb="[0,0,0]"
 	if [ -f "$entries_file" ]; then
 		band_unb=$(awk -v i0="$ips0" -v i1="$ips1" -v i2="$ips2" '
@@ -274,15 +284,17 @@ get_ppe_entries() {
 	fi
 	bnd_json=$(printf '%s' "$bnd_json" | sed 's/}$/,"port_bnd":'"$port_bnd"'}/')
 
-	# Per-client BND counts: for each connected WiFi station, how many bound (offloaded)
-	# flows carry its MAC in eth=. Reuses the per-band station MAC lists (macs0/1/2) and
-	# the same eth= match as band_bnd/port_bnd. Counts per station independently (a
-	# client<->client flow can credit both ends; the common client<->WAN flow matches
-	# only the client MAC, so no double count in practice).
-	# Per-client BND counts + IP/hostname. Feeds awk three tagged streams: @NEIGH lines
-	# (ip neigh) -> mac->ipv4, @LEASE lines (/tmp/dhcp.leases) -> mac->hostname, then the
-	# raw bind table for the eth= match. Marker prefixes keep them apart (bind rows start
-	# with a hex index, never @NEIGH/@LEASE). Counts per station; IP/host are best-effort.
+	# Per-client BND counts: for each connected WiFi station, count bound (offloaded)
+	# flows using three signals:
+	#   1) eth= contains the station MAC (works for L2/bridge-forwarded flows),
+	#   2) BND orig/new tuple contains the station IPv4 (routed client->WAN flows
+	#      carry bridge/next-hop MACs in eth=, so MAC-only matching undercounts),
+	#   3) conntrack [HW_OFFLOAD] / [OFFLOAD] fallback for flows that are offloaded
+	#      but not (yet) visible in the PPE bind table.
+	# IP/hostname are best-effort from ip neigh + /tmp/dhcp.leases. The four marker
+	# streams (@NEIGH/@LEASE/raw bind/@CT) are disambiguated by their first field
+	# (bind rows start with a hex index; @CT rows carry the conntrack line after
+	# the marker and contain [HW_OFFLOAD] or [OFFLOAD]).
 	local client_bnd="[]"
 	if [ -f "$bind_file" ]; then
 		client_bnd=$(
@@ -290,27 +302,50 @@ get_ppe_entries() {
 			printf '%s\n' "$neigh" | sed 's/^/@NEIGH /'
 			sed 's/^/@LEASE /' /tmp/dhcp.leases 2>/dev/null
 			cat "$bind_file"
+			grep -E '\[HW_OFFLOAD\]|\[OFFLOAD\]' /proc/net/nf_conntrack 2>/dev/null | sed 's/^/@CT /'
 		} | awk -v m0="$macs0" -v m1="$macs1" -v m2="$macs2" '
 		BEGIN {
 			ntot=0
-			c=split(m0,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=0; cnt[ntot]=0 }
-			c=split(m1,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=1; cnt[ntot]=0 }
-			c=split(m2,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=2; cnt[ntot]=0 }
+			c=split(m0,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=0; cnt_eth[ntot]=0; cnt_bnd[ntot]=0; cnt_hw[ntot]=0; cnt_sw[ntot]=0 }
+			c=split(m1,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=1; cnt_eth[ntot]=0; cnt_bnd[ntot]=0; cnt_hw[ntot]=0; cnt_sw[ntot]=0 }
+			c=split(m2,a,"|"); for(i=1;i<=c;i++) if(a[i]!=""){ mac[++ntot]=tolower(a[i]); bnd_band[ntot]=2; cnt_eth[ntot]=0; cnt_bnd[ntot]=0; cnt_hw[ntot]=0; cnt_sw[ntot]=0 }
 		}
 		$1=="@NEIGH" { ipx=$2; mcx=tolower($6); if(index(ipx,":")==0 && index(ipx,".")>0 && mcx!="" && !(mcx in mac2ip)) mac2ip[mcx]=ipx; next }
-		$1=="@LEASE" { lm=tolower($3); lh=$5; gsub(/["\\]/,"",lh); if(lm!="" && lh!="" && lh!="*") mac2host[lm]=lh; next }
+		$1=="@LEASE" { lm=tolower($3); lh=$5; ipx=$4; gsub(/["\\]/,"",lh); if(lm!="" && lh!="" && lh!="*") mac2host[lm]=lh; if(lm!="" && ipx!="" && !(lm in mac2ip)) mac2ip[lm]=ipx; next }
+		$1=="@CT" {
+			is_hw = index($0,"[HW_OFFLOAD]") > 0
+			is_sw = (is_hw == 0 && index($0,"[OFFLOAD]") > 0)
+			line=$0; src=""; dst=""
+			match(line,/src=[^ ]+/); if(RSTART>0) src=substr(line,RSTART+4,RLENGTH-4)
+			match(line,/dst=[^ ]+/); if(RSTART>0) dst=substr(line,RSTART+4,RLENGTH-4)
+			for(i=1;i<=ntot;i++){
+				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
+				if(ip=="" || ip=="0.0.0.0") continue
+				if(src==ip || dst==ip) { if(is_hw) cnt_hw[i]++; else if(is_sw) cnt_sw[i]++ }
+			}
+			next
+		}
 		/^[0-9a-fA-F]+ BND / {
 			eth=""; match($0,/eth=[^ \t]+/); if(RSTART>0) eth=tolower(substr($0,RSTART+4,RLENGTH-4))
-			if(eth=="") next
-			for(i=1;i<=ntot;i++) if(index(eth,mac[i])>0) cnt[i]++
+			orig=""; match($0,/orig=[^ \t]+/); if(RSTART>0) orig=substr($0,RSTART+5,RLENGTH-5)
+			new_flow=""; match($0,/new=[^ \t]+/); if(RSTART>0) new_flow=substr($0,RSTART+4,RLENGTH-4)
+			tuple = orig " " new_flow
+			for(i=1;i<=ntot;i++){
+				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
+				if(eth!="") { if(index(eth,mac[i])>0) cnt_eth[i]++ }
+				if(ip!="") { if(index(tuple, ip ":") > 0) cnt_bnd[i]++ }
+			}
 		}
 		END {
 			printf "["
 			for(i=1;i<=ntot;i++){
 				ip = (mac[i] in mac2ip) ? mac2ip[mac[i]] : ""
 				host = (mac[i] in mac2host) ? mac2host[mac[i]] : ""
+				bnd = cnt_eth[i]
+				if(cnt_bnd[i] > bnd) bnd = cnt_bnd[i]
+				if(cnt_hw[i] > bnd) bnd = cnt_hw[i]
 				if(i>1)printf ","
-				printf "{\"mac\":\"%s\",\"ip\":\"%s\",\"host\":\"%s\",\"band\":%d,\"bnd\":%d}",mac[i],ip,host,bnd_band[i],cnt[i]
+				printf "{\"mac\":\"%s\",\"ip\":\"%s\",\"host\":\"%s\",\"band\":%d,\"bnd\":%d,\"hw_offload\":%d,\"sw_offload\":%d}",mac[i],ip,host,bnd_band[i],bnd,cnt_hw[i],cnt_sw[i]
 			}
 			printf "]"
 		}
