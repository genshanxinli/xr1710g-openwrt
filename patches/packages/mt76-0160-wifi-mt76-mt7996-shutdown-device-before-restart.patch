# Source: YYH2913/openwrt mt76/0160（Yanghan Ye, 2026-08-07）—— 2026-09-12 P3-B 吸收，#EXP
# Upstream: n/a
# Reason: MT7990 在 host 重启期间保持供电 → PCIe host 拉 PERST 时 firmware/NPU/WED/WFDMA
#   仍在发事务 → 下次启动一个或两个 HIF 枚举失败。修法：新增 mt7996_shutdown_device()
#   （停两个 WED、mt76_npu_disable_irqs()、mt7996_irq_disable(dev, ~0)、
#   mt7996_dma_disable(dev, true)、is_mt7990() 时 mt7996_wfsys_reset()）与
#   mt7996_pci_shutdown()/mt7996_hif_shutdown()（pci_clear_master），挂到 pci_driver.shutdown；
#   mt7996_dma_disable() 由 static 改导出。作者说明 normal remove 不适合 reboot 回调
#   （NPU mailbox stop 握手在 mac80211 teardown 后不可靠；shutdown 期释放 WED page pool/NAPI 会告警）。
# 注意：本机是 MT7996，is_mt7990() 分支不触发；与已并入 mt76-0014 的 Gilly 047（reset_work 路径）
#   走不同路径，需在 0014 在位前提下重测 reboot 序列。
# 实证：对 be5ce791 + 本仓 mt76 内层全序按序 fuzz=0。
diff --git a/mt7996/dma.c b/mt7996/dma.c
index 3d935381..20ddf1ae 100644
--- a/mt7996/dma.c
+++ b/mt7996/dma.c
@@ -258,7 +258,7 @@ void mt7996_dma_prefetch(struct mt7996_dev *dev)
 		__mt7996_dma_prefetch(dev, MT_WFDMA0_PCIE1(0) - MT_WFDMA0(0));
 }
 
-static void mt7996_dma_disable(struct mt7996_dev *dev, bool reset)
+void mt7996_dma_disable(struct mt7996_dev *dev, bool reset)
 {
 	u32 hif1_ofs = 0;
 
diff --git a/mt7996/init.c b/mt7996/init.c
index bb486a56..3418193d 100644
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -1865,3 +1865,21 @@ void mt7996_unregister_device(struct mt7996_dev *dev)
 
 	mt76_free_device(&dev->mt76);
 }
+
+void mt7996_shutdown_device(struct mt7996_dev *dev)
+{
+	if (mtk_wed_device_active(&dev->mt76.mmio.wed_hif2))
+		mtk_wed_device_stop(&dev->mt76.mmio.wed_hif2);
+
+	if (mtk_wed_device_active(&dev->mt76.mmio.wed))
+		mtk_wed_device_stop(&dev->mt76.mmio.wed);
+
+	mt76_npu_disable_irqs(&dev->mt76);
+	mt7996_irq_disable(dev, ~0);
+	mt7996_dma_disable(dev, true);
+
+	if (is_mt7990(&dev->mt76)) {
+		dev->recovery.hw_full_reset = true;
+		mt7996_wfsys_reset(dev);
+	}
+}
diff --git a/mt7996/mt7996.h b/mt7996/mt7996.h
index 571dc0da..d020d377 100644
--- a/mt7996/mt7996.h
+++ b/mt7996/mt7996.h
@@ -728,6 +728,7 @@ irqreturn_t mt7996_irq_handler(int irq, void *dev_instance);
 u64 __mt7996_get_tsf(struct ieee80211_hw *hw, struct mt7996_vif_link *link);
 int mt7996_register_device(struct mt7996_dev *dev);
 void mt7996_unregister_device(struct mt7996_dev *dev);
+void mt7996_shutdown_device(struct mt7996_dev *dev);
 int mt7996_vif_link_add(struct mt76_phy *mphy, struct ieee80211_vif *vif,
 			struct ieee80211_bss_conf *link_conf,
 			struct mt76_vif_link *mlink);
@@ -743,6 +744,7 @@ bool mt7996_eeprom_has_background_radar(struct mt7996_dev *dev);
 int mt7996_dma_init(struct mt7996_dev *dev);
 void mt7996_dma_reset(struct mt7996_dev *dev, bool force);
 void mt7996_dma_prefetch(struct mt7996_dev *dev);
+void mt7996_dma_disable(struct mt7996_dev *dev, bool reset);
 void mt7996_dma_cleanup(struct mt7996_dev *dev);
 void mt7996_dma_start(struct mt7996_dev *dev, bool reset, bool wed_reset);
 int mt7996_init_tx_queues(struct mt7996_phy *phy, int idx,
diff --git a/mt7996/pci.c b/mt7996/pci.c
index cf4f7858..ae9e3e60 100644
--- a/mt7996/pci.c
+++ b/mt7996/pci.c
@@ -234,6 +234,11 @@ static void mt7996_hif_remove(struct pci_dev *pdev)
 	list_del(&hif->list);
 }
 
+static void mt7996_hif_shutdown(struct pci_dev *pdev)
+{
+	pci_clear_master(pdev);
+}
+
 static void mt7996_pci_remove(struct pci_dev *pdev)
 {
 	struct mt76_dev *mdev;
@@ -245,11 +250,32 @@ static void mt7996_pci_remove(struct pci_dev *pdev)
 	mt7996_unregister_device(dev);
 }
 
+static void mt7996_pci_shutdown(struct pci_dev *pdev)
+{
+	struct mt76_dev *mdev = pci_get_drvdata(pdev);
+	struct mt7996_dev *dev;
+	struct pci_dev *hif2_dev = NULL;
+
+	dev = container_of(mdev, struct mt7996_dev, mt76);
+	if (dev->hif2)
+		hif2_dev = pci_dev_get(to_pci_dev(dev->hif2->dev));
+
+	mt7996_put_hif2(dev->hif2);
+	mt7996_shutdown_device(dev);
+	pci_clear_master(pdev);
+
+	if (hif2_dev) {
+		pci_clear_master(hif2_dev);
+		pci_dev_put(hif2_dev);
+	}
+}
+
 struct pci_driver mt7996_hif_driver = {
 	.name		= KBUILD_MODNAME "_hif",
 	.id_table	= mt7996_hif_device_table,
 	.probe		= mt7996_pci_probe,
 	.remove		= mt7996_hif_remove,
+	.shutdown	= mt7996_hif_shutdown,
 };
 
 struct pci_driver mt7996_pci_driver = {
@@ -257,6 +283,7 @@ struct pci_driver mt7996_pci_driver = {
 	.id_table	= mt7996_pci_device_table,
 	.probe		= mt7996_pci_probe,
 	.remove		= mt7996_pci_remove,
+	.shutdown	= mt7996_pci_shutdown,
 };
 
 MODULE_DEVICE_TABLE(pci, mt7996_pci_device_table);
