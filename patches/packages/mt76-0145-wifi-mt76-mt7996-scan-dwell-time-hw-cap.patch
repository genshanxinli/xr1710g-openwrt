# Source: YYH2913/openwrt mt76/0145（MTK）—— 2026-09-12 P3-C 吸收，#EXP
# Upstream: n/a（能力位广告；低风险）
# Reason: 给 MT7996 广告 `NL80211_EXT_FEATURE_SET_SCAN_DWELL`，允许用户态设置扫描驻留时间
#   （漫游/扫描体验相关）。1 行。本机三频扫描参数可按需调整。
# 实证：对 be5ce791 + 本仓 mt76 内层全序 fuzz=0。
diff --git a/mt7996/init.c b/mt7996/init.c
index ca1c1017..c6deb8dd 100644
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -540,6 +540,7 @@ mt7996_init_wiphy(struct ieee80211_hw *hw, struct mtk_wed_device *wed)
 	wiphy_ext_feature_set(wiphy, NL80211_EXT_FEATURE_ACK_SIGNAL_SUPPORT);
 	wiphy_ext_feature_set(wiphy, NL80211_EXT_FEATURE_CAN_REPLACE_PTK0);
 	wiphy_ext_feature_set(wiphy, NL80211_EXT_FEATURE_MU_MIMO_AIR_SNIFFER);
+	wiphy_ext_feature_set(wiphy, NL80211_EXT_FEATURE_SET_SCAN_DWELL);
 
 	if (mt7996_eeprom_has_background_radar(dev) &&
 	    (!mdev->dev->of_node ||
