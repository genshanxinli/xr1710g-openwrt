# 自建 kmods / 包 feed（D5 供给链）——plan/00 §4.4、docs/adr/0006 §4、FIXES F92
#
# 为什么需要这个文件（而不是直接用官方 kmods）：
#   1) 官方 kmods 目录发布的是 …/kmods/6.18.44-1-<官方 config hash>/，
#      与本仓库构建的 hash **不同**；而 aarch64 的 vermagic = "6.18.44 SMP mod_unload aarch64"
#      **不含 config hash** ⇒ apk 依赖解析**不会**发现不匹配（静默 ABI 风险，装上去可能 insmod 才炸）。
#   2) 设备 distfeeds.list 只挂 …/targets/airoha/an7581/packages/，**没有 kmods 这一项**
#      ⇒ `apk add kmod-wireguard` 只会得到 "no such package"（F92 实机取证）。
#   ⇒ 结论：能力走"随固件预装"（config/seed-config.diff），**增量** kmod/包走本 feed。
#
# 用法：把 CI Release 附件 kmodsfeed-<profile>.tar.gz 解包到设备某处，然后取消下面一行的注释
#       并把路径改成实际位置。apk 的 repositories 支持 file:// 与 http(s)://。
#
# 示例（把附件解到 /root/feeds/ 后）：
# file:///root/feeds/kmodsfeed-stock/packages.adb
#
# 铁律：内核跳版（uname -r 变 / config hash 变）⇒ 必须换成**同一次构建**产出的附件，
#       否则就是上面说的静默 ABI 错配。换之前先 `apk update` 确认索引可见。
